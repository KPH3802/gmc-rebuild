# Daily Monitoring Report: 2026-06-15 UTC (P6-08 planning / enumeration docs/governance-only packet — sibling Mode B for the forthcoming P6-08 planning PR)

## Report Metadata

**Environment**: Local (governance-only repository; no runtime, no broker, no live or paper trading wired to a real broker, no market data ingestion).
**Operator**: Backup AI authors the packet text under ADR-008 Mode B (Continuous Governance Monitor) per `AI_WORKFLOW.md` §1.4; the default builder commits per `AI_WORKFLOW.md` §6 rule 1 ("One builder at a time"); Perplexity Computer verifies and oversees per `AI_WORKFLOW.md` §1.2 (Supervisor / Verifier / Status Keeper) — Perplexity Computer is not the Mode B author. Per the GOV-02 execution-environment workflow rule (merged via PR #132 / reconciled via PR #134), default-builder work on `gmc-rebuild` is by default carried out via Kevin's local Claude Code / Claude Max subscription; this packet is built in compliance with that rule.
**Report Window**: 2026-06-15T00:00:00Z to 2026-06-15T23:59:59Z (third 2026-06-15 monitoring packet, following `monitoring/daily/2026-06-15_pr-p6-07-planning.md` for the merged P6-07 planning PR #179 with sibling Mode B PR #178 first, and `monitoring/daily/2026-06-15_pr-p6-07-implementation.md` for the merged P6-07 implementation PR #181 with sibling Mode B PR #180 first).
**Authored**: approx. 2026-06-15T (authored timestamp; not a completed-at timestamp).
**Overall Status**: Green at packet authoring (the P6-08 planning PR is **not yet opened**; this packet is required by ADR-008 §D3 / §D5 to be committed and merged to `main` **before** the P6-08 planning PR merges; `main` head at time of authoring is `88fa3bf`, the merged P6-07 deterministic read-only operator view implementation, PR #181).
**Monitor Mode**: ADR-008 Mode B — Continuous Governance Monitor.
**Trigger**: ADR-008 §D3 / §D5. A P6-08 planning / enumeration PR (working title `docs: add P6-08 planning packet`, candidate branch `docs/p6-08-planning-2026-06-15`) is being prepared against `main` at `88fa3bf`, making 2026-06-15 a third active-workday event under ADR-008 §D3.

**Naming note (ADR-008 §D4 / §D5 + established sibling-Mode-B practice).** This is the third Mode B sibling packet for 2026-06-15 (the first two are the merged `monitoring/daily/2026-06-15_pr-p6-07-planning.md` for PR #178 and the merged `monitoring/daily/2026-06-15_pr-p6-07-implementation.md` for PR #180). Two naming conventions interact here:

- ADR-008 §D4 says the *first* packet on a UTC date is named `YYYY-MM-DD.md` (no slug). In practice, the established sibling-Mode-B precedent (e.g. `monitoring/daily/2026-05-23_pr-p6-06-planning.md`, the merged `monitoring/daily/2026-06-15_pr-p6-07-planning.md`) uses the slugged `YYYY-MM-DD_pr-<id>-<slug>.md` form even for subsequent sibling packets, so the file plainly names the PR it monitors. This packet follows that established sibling practice.
- The P6-08 planning PR **number is not yet allocated** (the PR has not been opened). The repo has an accepted **`pr-<task-id>` placeholder** convention for exactly this case: the merged `monitoring/daily/2026-06-15_pr-p6-07-planning.md` packet uses `pr-p6-07` (task-id, no number). This packet therefore uses the placeholder filename `monitoring/daily/2026-06-15_pr-p6-08-planning.md`. At PR-open time the maintainer may either keep this placeholder name (consistent with the `pr-p6-07` precedent) or rename it to the numeric form `monitoring/daily/2026-06-15_pr-<NNN>-p6-08-planning.md` once the planning PR number is known; the body below refers to the planning PR by description rather than a guessed number.

Per ADR-008 §D5, the PR that commits this packet must merge to `main` **before** the P6-08 planning PR merges.

---

## Activity Summary

UTC date 2026-06-15 has a third active-workday event under ADR-008 §D3: a pull request (working title `docs: add P6-08 planning packet`, candidate branch `docs/p6-08-planning-2026-06-15`, base `main` at `88fa3bf`) is being prepared against `main` on 2026-06-15 by the maintainer to record the candidate shape for the **eighth** Phase 6 implementation task — a **tripwire-only** test packet, modeled on the merged P5-03 / P5-04 / P5-05 / P5-06 / P5-07 precedent, that consolidates and pins the failure-handling and exception-typing discipline already enforced by the merged P6-01..P6-07 surface (explicit named exception types raised at each layer boundary, no swallowed exceptions inside the merged code, and the structured `audit_event` record contract for every refusal / skip / kill-switch trip / reconciliation mismatch / unhandled error already carried by the merged P6-06 `lifecycle.daily_report` surface) — at the **planning level only** per Kevin's verbatim written planning directive on 2026-06-15. The forthcoming planning PR adds exactly one new file (`governance/authorizations/2026-06-15_p6-08-planning.md`); that authorization is the planning / enumeration analogue to the merged P6-07 planning / enumeration packet (PR #179 at `416f550`), the merged P6-06 planning / enumeration packet (PR #175 at `c4d2d4a`), the merged P6-05 planning / enumeration packet (PR #162 at `5d1c743`), and the earlier P6-NN / P5-NN / P4-NN planning packets. At the time this monitoring packet is authored, **the P6-08 planning PR has not yet been opened and has not merged**; this packet must be committed and merged to `main` before that planning PR merges per ADR-008 §D5.

**Directive-confirmation evidence (recorded as monitoring evidence, not new authorization).** Kevin's directive asked the builder to confirm three facts before drafting; all three are confirmed against the working tree at `main` head `88fa3bf`:

- **P6-07 (deterministic read-only operator view of dry-run engine state) is the last merged Phase 6 task.** Confirmed — implementation commit `db4530d`, merged via PR #181 at `88fa3bf`, with sibling Mode B PR #180 merged first at `04443f6`; `src/gmc_rebuild/operator_view/` exists on `main`. No P6-08 implementation, authorization, or planning artifact exists on `main`.
- **`plan/phase6_entry_plan.md` §4 lines ~154-161 (item 7) records the failure-handling / exception-typing consolidation candidate.** Confirmed — the merged §4 item 7 enumerates the candidate as: tripwire-only tests modeled on the P5-03 / P5-04 / P5-05 / P5-06 / P5-07 precedent; new test file (or files) under the existing `tests/` tree; no new `src/**` directory; no `src/**` modification; preserves the allowlist unchanged; does not authorize any new production behaviour or any change to existing exception classes. Per Kevin's 2026-06-15 P6-07 re-aim (recorded in the merged P6-07 planning packet), the P6-07 label was re-aimed at the operator view and the failure-handling / exception-typing consolidation candidate slides to P6-08. This planning packet honors that re-aim.
- **Plan line ~161 ("Further candidate tasks beyond P6-07 (P6-08, P6-09, …) are deliberately not enumerated in this plan").** Confirmed — P6-08 is currently **not yet enumerated** in §4; Kevin's directive asks for the planning packet (the forthcoming planning PR) **plus** a future documentation-only update to the plan before any implementation. This monitoring packet records the sequencing constraint; the §4 update is a separate status-keeper workstream per `AI_WORKFLOW.md` §1.2 / §6 rule 2 and is not bundled into this monitoring PR or the planning PR.

**Local-checkout note (operational, recorded for audit).** At session start the local working tree was current with `origin/main` (head `88fa3bf` matches origin). No fast-forward was required for this session.

**P6-08 planning PR metadata (forthcoming).**

- **URL:** to be assigned at PR-open time (not yet opened).
- **Title (working):** `docs: add P6-08 planning packet`
- **Branch (candidate):** `docs/p6-08-planning-2026-06-15`
- **Base:** `main` at `88fa3bf`
- **State:** not yet opened (this sibling Mode B packet is being prepared first, per ADR-008 §D5 sequencing).
- **Classification:** Bounded docs/governance-only **P6-08 planning / enumeration** packet authorized by Kevin in writing on 2026-06-15 (verbatim planning directive reproduced in `governance/authorizations/2026-06-15_p6-08-planning.md` §Authorization). **The forthcoming planning PR adds exactly one new file (`governance/authorizations/2026-06-15_p6-08-planning.md`), modifies no other file, changes no observable runtime behavior, changes no test logic, changes no public API, modifies no `src/**` file, modifies no `tests/**` file, modifies no `MASTER_STATUS.md` file, modifies no `README.md`, modifies no `RECOVERY.md`, modifies no `plan/**` file, modifies no other `governance/authorizations/*` file, modifies no `monitoring/**` file (other than this new monitoring packet on the monitoring branch), changes no existing exception class or raise site, opens no P6-08 implementation, opens no successor P6 implementation task, opens no P6-09 or any later P6-0N enumeration, and changes no runtime / broker / paper-trading / live-trading / market-data / order-routing / strategy / scheduler / daemon / persistence / filesystem-snapshot / external-sink / deployment / env / secrets / network / allowlist / quality-gate / tag / release surface.**

**Authorization of record for the P6-08 planning PR.** Kevin's verbatim written planning directive on 2026-06-15 is reproduced in full in `governance/authorizations/2026-06-15_p6-08-planning.md` §Authorization per `AI_WORKFLOW.md` §7's requirement that "the verbatim block in this file is the authorization of record". The authorization is **planning / enumeration only**: it authorizes a durable governance planning artifact under `governance/authorizations/` for P6-08 recording the candidate failure-handling / exception-typing tripwire shape; it does **not** authorize P6-08 implementation, any `tests/**` change (or `src/**` change), any runtime activation, any real account data, any broker, any market data, any persistence, any I/O / external sink, any network call, any scheduler, any daemon, any `__main__`, any env-var read, any secrets, any tag or release, any change to existing exception classes, any new `audit_event` emission, or any P6-09+ packet.

**P6-08 planning PR scope (forthcoming).**

| File | Change | Notes |
|---|---|---|
| `governance/authorizations/2026-06-15_p6-08-planning.md` | new | Durable governance planning artifact for the P6-08 failure-handling / exception-typing consolidation tripwire-only test packet. Contents: title and metadata header (date, authorizer, scope); §Draft Status; §Authorization (Kevin's verbatim planning directive reproduced in full per `AI_WORKFLOW.md` §7) with a §Directive Confirmation subsection (P6-07 is last merged; §4 item-7 → P6-08 slide; plan line ~161 deliberately-not-enumerated note) and the exact P6-07 / P5-03..P5-07 precedents; §Current State (Post-P6-07 — planning context only); §Scope (narrowly limited to the artifact itself, mirroring the merged P6-07 / P6-06 / P6-05 planning packets' one-file scope — no `MASTER_STATUS.md` reflection, no `README.md` cross-reference, no `plan/phase6_entry_plan.md` update); §Companion-Docs Decision; §Relationship to the merged `plan/phase6_entry_plan.md` §4 item-7 / item-8 failure-handling candidate (flags the §4 reconciliation transparently); §Candidate P6-08 Shape (Planning Level Only — Future / Not Authorized) with subsections for candidate objective, candidate test location (`tests/p6_08_failure_handling/` or per-layer files), relationship to the existing P6-01..P6-07 exception surface (plain `TypeError` / `ValueError` / `FrozenInstanceError` at P6 boundaries; typed `SimulationBoundaryError` / `RuntimeShellError` / etc. at P4 / P5 / P2-04 boundaries; structured `audit_event` contract for the merged P6-06 `lifecycle.daily_report`), typed input/output boundaries (closed inputs = merged P6-01..P6-07 / P5 / P4 / P3 / P2-04 public surfaces only; closed outputs = pytest pass/fail signals only, no new public symbol), deterministic / no-clock-read / read-only contract, §8 allowlist impact (preserved unchanged — no new `src/**` directory), proposed test matrix (12 invariant categories), and safety / non-goals; §Expected Files; §Expected Proof Bundle; §Allowed Planning Topics; §Stop Conditions; §Explicitly Not Authorized; §Required Validation; §Required Sequencing (records the Mode B sibling requirement per ADR-008 §D5); §Required Implementation Authorization (Future); §Review Basis. No `src/**` change; no `tests/**` change; no other file modified. |

One file added; total **diff is purely additive** (one new markdown file under `governance/authorizations/`). No file deleted. `MASTER_STATUS.md`, `README.md`, `RECOVERY.md`, `plan/phase4_entry_plan.md`, `plan/phase5_entry_plan.md`, `plan/phase6_entry_plan.md`, every file under `src/**`, every file under `tests/**`, every file under `monitoring/daily/` (other than the new monitoring packet on this monitoring branch), `pyproject.toml`, `.pre-commit-config.yaml`, `.secrets.baseline`, and `.gitignore` are all unchanged by the forthcoming planning PR. The planning PR is **bounded docs/governance-only P6-08 planning / enumeration**. It explicitly does **not**:

- Modify any file under `src/**`. The planning PR's `src/**` diff is empty.
- Modify any file under `tests/**`. The planning PR's `tests/**` diff is empty.
- Modify `MASTER_STATUS.md`. The planning PR's `MASTER_STATUS.md` diff is empty. The §1 implementation status reflections, the §8 step 4a `allowed_p2_infra` allowlist (preserved exactly at the **sixteen** entries after the merged P6-07 implementation), the §8 step 4 always-forbidden scan, the §8 step 4c forbidden-token bash gate, and the §8 step 8 canonical-doc staleness check tripwire grep block are all preserved verbatim.
- Modify `README.md`, `RECOVERY.md`, `plan/phase4_entry_plan.md`, `plan/phase5_entry_plan.md`, or `plan/phase6_entry_plan.md`. The planning PR's diff on these files is empty. **The merged `plan/phase6_entry_plan.md` §4 item 7 currently records the failure-handling / exception-typing consolidation candidate under the P6-07 label; per Kevin's 2026-06-15 P6-07 re-aim, that candidate slides to P6-08, and the planning packet flags this discrepancy transparently (recording that re-aiming P6-07 at the operator view and re-numbering the failure-handling consolidation candidate to a §4 item 8 P6-08 entry is a separate status-keeper workstream) — it does not edit §4.**
- Modify any other `governance/authorizations/*` file. The planning PR adds exactly one new file under `governance/authorizations/`; no existing authorization artifact is modified. The merged P6-07 implementation authorization (`governance/authorizations/2026-06-15_p6-07.md`), the merged P6-07 planning packet (`governance/authorizations/2026-06-15_p6-07-planning.md`), the merged P6-06 implementation authorization (`governance/authorizations/2026-05-23_p6-06.md`), the merged P6-06 planning packet (`governance/authorizations/2026-05-23_p6-06-planning.md`), and every earlier authorization artifact are preserved unchanged.
- Choose the candidate test layout, file count, assertion style, exact substring-match strings, or exception-type taxonomy. The planning packet records each as a planning-level candidate without pre-committing; the choices are reserved for the future P6-08 implementation authorization, which the packet requires to name in writing.
- Open or authorize any P6-08 implementation. The planning PR does not pre-authorize, nor does it open, any P6-08 implementation work. Any future P6-08 implementation PR requires its own separate written authorization from Kevin under `governance/authorizations/<YYYY-MM-DD>_p6-08.md`, with its own §Authorization section reproducing Kevin's verbatim implementation-authorization block.
- Open any successor P6 task beyond P6-08. P6-09 remains future / not authorized. Each future P6-0N packet continues to require its own separate written authorization from Kevin per `AI_WORKFLOW.md` §7.
- Change any existing exception class. `SimulationBoundaryError`, `RuntimeShellError`, `RiskControlError`, `AuditEventError`, `NaiveDatetimeError`, and the plain `TypeError` / `ValueError` / `FrozenInstanceError` raise sites in P6-01..P6-07 are not renamed, re-typed, moved, extended, or introduced. The planning packet records this constraint as one of the §Explicitly Not Authorized items (Kevin's directive explicitly forbids it).
- Modify the `MASTER_STATUS.md` §8 step 4a `allowed_p2_infra` bash gate (preserved exactly at the sixteen entries after the merged P6-07 implementation: `src`, `src/gmc_rebuild/config`, `src/gmc_rebuild/time`, `src/gmc_rebuild/logging`, `src/gmc_rebuild/risk`, `src/gmc_rebuild/heartbeat`, `src/gmc_rebuild/kill_switch`, `src/gmc_rebuild/reconciliation`, `src/gmc_rebuild/runtime`, `src/gmc_rebuild/simulation`, `src/gmc_rebuild/signal_intake`, `src/gmc_rebuild/eligibility`, `src/gmc_rebuild/decision`, `src/gmc_rebuild/portfolio_state`, `src/gmc_rebuild/reporting`, `src/gmc_rebuild/operator_view`). The candidate P6-08 implementation introduces no new `src/**` directory, so no future allowlist update is even contemplated.
- Modify the `MASTER_STATUS.md` §8 step 4 always-forbidden scan, §8 step 4c forbidden-token bash gate, or §8 step 8 canonical-doc staleness check. The planning packet records (in its candidate-shape sections) that the candidate test directory name `p6_08_failure_handling` does **not** collide with the forbidden-token set (`strategy strategies signal signals scanner scanners model models portfolio backtest backtests broker brokers execution executions live paper daemon daemons market_data order orders secret secrets`), unlike the merged `src/gmc_rebuild/signal_intake/` (token `signal`) and `src/gmc_rebuild/portfolio_state/` (token `portfolio`). If the implementation authorization elects a per-layer file layout with a name like `test_signal_intake_failure_modes.py`, that filename tokenizes to the forbidden `signal` token — an expected, authorized flag per the merged P6-01 precedent; the planning packet records the consideration and pre-commits to no specific filename. The planning packet makes no §8 forbidden-set change.
- Modify the merged P6-01..P6-07 / P5-01..P5-07 / P4-06..P4-08 / P3-03..P3-05 / P2-04 / P2-05 surfaces. The planning packet records that the candidate P6-08 tests are pure read-only *consumers* of the merged Phase 2 / 3 / 4 / 5 / 6 public surfaces, emit no `audit_event`, and exercise the merged P3 fakes only inside pytest (construction-only — not a runtime activation, exactly as in the merged P5-03..P5-07 precedent).
- Introduce any I/O, external log sink (syslog, journald, cloud, HTTP, message broker), new `audit_event` emission, on-disk view artifact, database, filesystem write, any `open(` builtin for write, any `pickle`, any `shelve`, or any other persistence surface beyond `pathlib.Path.read_text` on the new test module's own source file for the AST inertness self-check.
- Introduce any real account data, real position book, real account identifier, real holdings, real balance, real P&L, broker-confirmed position, broker integration (real or paper), market-data feed, live data, order placement / routing, execution adapter, FIX / REST / WebSocket client, or any other connectivity surface.
- Introduce any secrets, credentials, API keys, `.env` files, recovery codes, or any other credential storage; any `os.environ` / `os.getenv` read; any network call (`socket`, `urllib`, `requests`, `http`, `ssl`, `smtplib`, `ftplib`, DNS).
- Introduce any scheduler, daemon, background thread, long-running service, `__main__` entry point, `if __name__` block, `time.sleep`, `asyncio.sleep`, cron job, launchd plist, `logging.basicConfig`, or any handler installation; or any wall-clock read (`now_utc()` / `time.*` / `datetime.now()`) — the tripwire tests read no clock and any `datetime` value is a fixed inline literal.
- Introduce any strategy, scanner, model, portfolio-management strategy, position-sizing algorithm, backtest, live trading, paper trading wired to a real broker, or production execution implementation.
- Authorize any change to the merged P5-01 / P5-02 / P6-04 simulation surface, the merged P6-05 `portfolio_state` surface, the merged P6-06 `reporting` surface, the merged P6-01 / P6-02 / P6-03 surfaces, the merged P6-07 `operator_view` surface, or the merged P4-06 / P4-07 / P4-08 safety surface (no new enum member or dataclass field; no change to `accept_signal_intent`, `check_eligibility`, `compose_position_decision`, `propose` / `propose_order`, `derive_simulated_order_intent_id`, `apply_simulated_order_intent`, `build_daily_report`, `render_daily_report_event`, `build_dry_run_operator_view`, `DryRunOperatorView.render`, `SafetyVerdict.clear`, `OperatorSafetyView`, `format_safety_verdict`, or the `BLOCKER_*` set; no re-export of any of their symbols from `src/gmc_rebuild/__init__.py`).
- Authorize any runtime activation of any merged Phase 3 fixture (`InMemoryHeartbeat`, `InMemoryKillSwitch`, `InMemoryReconciliation`) from a `__main__` / daemon / scheduler / re-export / runtime path. (Construction inside pytest is not a runtime activation, exactly as in the merged P5-03..P5-07 precedent.)
- Authorize any ops execution work (X10 Layer 5 promotion / OPS-05, backup-monitoring automation, DR drill execution / OPS-07) or any Backblaze / Time Machine / drive / FileVault / sleep / power / USB / network / macOS setting change.
- Reinterpret or relax the GOV-02 execution-environment workflow rule (merged via PR #132 / reconciled via PR #134). Local Claude Code / Claude Max remains the default execution environment for `gmc-rebuild` repo edits; Perplexity Computer's `AI_WORKFLOW.md` §1.2 Supervisor / Verifier / Status Keeper role and read-only `gh` / `git` metadata-check role are preserved unchanged.
- Modify any ADR, `AI_WORKFLOW.md`, `pyproject.toml`, `.pre-commit-config.yaml`, `.secrets.baseline`, `.gitignore`, `EXTERNAL_REVIEW_BRIEF.md`, or any other `monitoring/**` file beyond this new packet itself.
- Relax any quality gate, hook, mypy strictness, ruff rule, or `detect-secrets` baseline; expand any allowlist; create any git tag, GitHub release, or version bump.
- Reintroduce or extend any stale `**pending merge**` language. The planning PR does not modify `MASTER_STATUS.md`.
- Touch, stage, modify, or include the untracked local working-tree files `.claude/`, `Claude_Transfes/`, and `governance/authorizations/2026-06-15_p6-08-planning.md`. **The P6-08 planning authorization draft is added by the separate forthcoming planning PR — not by this monitoring PR — and `.claude/` / `Claude_Transfes/` are never staged or committed by either PR.**

---

## Mode A Context (P6-08 planning PR)

The forthcoming P6-08 planning PR is a **bounded docs/governance-only P6-08 planning / enumeration packet** that adds no production behaviour, no `src/**` change, no `tests/**` change, no `MASTER_STATUS.md` change, no `README.md` change, no `plan/**` change, no new directory, and opens no implementation task. Per `AI_WORKFLOW.md` §4's routine-exclusion sentence and the precedent set by the merged Phase 3 / Phase 4 / Phase 5 / Phase 6 entry-planning and P4-NN / P5-NN / P6-NN planning authorizations, Mode A adversarial review is **not independently required** for this planning / enumeration authorization. The maintainer retains discretion to require Mode A as PR-review text on the planning PR; if delivered, it is recorded as PR-review text (not committed as a file) per `AI_WORKFLOW.md` §6 rule 5.

**Important: This monitoring PR does not itself authorize any new Phase 6 work, open any successor packet, open any new authorization artifact, change the authorization or status of the forthcoming planning PR beyond serving as monitoring evidence, or change any phase-boundary control.** It records that the forthcoming planning PR is a docs/governance-only **planning / enumeration** packet adding **no production behaviour**, with the merged P6-07 deterministic read-only operator view (`db4530d`), the merged P6-06 deterministic daily dry-run report (`03cafae`), the merged P6-05 implementation (`da19cfa`, ruff cleanup `ae0668f`), the merged P6-04 Direction A implementation (`cea8553`), the merged P6-03 decision composer (`8d98a41`), the merged P6-02 eligibility-check pure functions (`4785e24`), the merged P6-01 typed signal-intake boundary (`d52551a`), the simulation surface (P5-01..P5-07), the safety foundation (P4-06 / P4-07 / P4-08), the in-memory fakes (P3-03 / P3-04 / P3-05), the operations records (OPS-01..OPS-04B / OPS-06), the canonical allowlists, the merged GOV-01 / GOV-02 governance packets, the merged Phase 6 entry plan, and all merged status reflections preserved unchanged.

---

## Risks Considered (P6-08 planning PR)

1. **Risk: the planning packet drifts into pre-authorizing P6-08 implementation.** Mitigation: the planning packet names P6-08 only as **future / not authorized** in every section where the candidate shape is documented; the candidate-shape sketch carries multiple "planning level only" / "future / not authorized" / "candidate only" qualifiers. The §Explicitly Not Authorized list enumerates every category that requires Kevin's separate written authorization, and a dedicated §Required Implementation Authorization (Future) section records that "any P6-08 implementation requires its own separate written authorization from Kevin" with the structural template the future implementation authorization must follow. A §Draft Status section restates that merging the planning packet authorizes no implementation.
2. **Risk: the planning packet pre-commits the test layout, file count, or assertion text.** Mitigation: the packet records each as a planning-level candidate and pre-commits to none; the §Explicitly Not Authorized list forbids the choice and requires the implementation authorization to name the chosen models in writing.
3. **Risk: the planning packet drifts into authorizing a change to existing exception classes.** Mitigation: the planning packet explicitly records the "pin existing types, no new typed exceptions" default as the lower-risk planning posture and forbids any change to `SimulationBoundaryError`, `RuntimeShellError`, `RiskControlError`, `AuditEventError`, `NaiveDatetimeError`, or the plain `TypeError` / `ValueError` / `FrozenInstanceError` raise sites in P6-01..P6-07 (Kevin's directive explicitly forbids this). A dedicated stop condition (§8) fires if a P6-08 PR proposes to add, remove, rename, or re-type any existing exception class.
4. **Risk: the planning packet drifts into authorizing a new `audit_event` emission on any P6-01..P6-05 / P6-07 surface.** Mitigation: the planning packet records that the merged P6-06 `lifecycle.daily_report` is the only P6 audit emission and that the directive's "structured audit_event records for every refusal / skip / kill-switch trip / reconciliation mismatch / unhandled error" is interpreted as the P6-06 daily report's closed-field record carrying those categories end-to-end (the would-skip counts, the reconciliation status preserving `UNAVAILABLE` vs `FAILED`, the tripped-invariant tuple) — not as a new per-event emission. A dedicated stop condition (§9) fires if a P6-08 PR proposes any new emission.
5. **Risk: the P6-08 directive contradicts the merged `plan/phase6_entry_plan.md` §4 item 7 (which currently records the failure-handling consolidation candidate under the P6-07 label) and the discrepancy is buried.** Mitigation: the planning packet surfaces this transparently in a dedicated §Relationship section — recording that the §4 item-7 candidate is the same candidate now sliding to P6-08 per Kevin's 2026-06-15 P6-07 re-aim, and that reconciling §4 (re-numbering the failure-handling consolidation candidate to a §4 item 8 P6-08 entry, with the §4 item 7 entry rewritten to record the merged operator view) is a separate status-keeper workstream — and asks Kevin to confirm the re-aim at implementation-authorization time. The planning packet does **not** edit §4.
6. **Risk: the planning packet accidentally extends the §8 step 4a `allowed_p2_infra` allowlist or otherwise modifies a quality gate.** Mitigation: `git diff main --name-status` for the planning PR returns exactly one new file (`governance/authorizations/2026-06-15_p6-08-planning.md`) with no `src/**` directory introduction; the §8 step 4a allowlist (in `MASTER_STATUS.md`) preserves the sixteen entries after the merged P6-07 implementation — `MASTER_STATUS.md` is not modified by the planning PR. Per the packet, no new `src/**` directory is even contemplated by the implementation candidate.
7. **Risk: a candidate P6-08 test-file name silently collides with the §8 forbidden-token scan and the collision is missed at implementation time.** Mitigation: the planning packet records the verbatim forbidden-token set and confirms the candidate directory name `p6_08_failure_handling` (tokens `p6`, `08`, `failure`, `handling`) does **not** collide with it (a positive contrast to `src/gmc_rebuild/signal_intake/` → `signal` and `src/gmc_rebuild/portfolio_state/` → `portfolio`), so no §8 reconciliation note is required for the directory itself; the planning packet also flags that a per-layer filename like `test_signal_intake_failure_modes.py` would trip the forbidden `signal` token at §8 step 4c — an expected, authorized flag per the merged P6-01 precedent — and suggests a non-tokenized alternative (`test_intake_failure_modes.py`) at the implementation authorization's discretion. The planning packet makes no §8 forbidden-set change.
8. **Risk: the read-only / no-side-effect contract is overlooked and the future tripwire tests perform I/O, read the clock, or emit an `audit_event`.** Mitigation: the planning packet's §3.7 / §3.10 pin the read-only contract (no I/O beyond `pathlib.Path.read_text` on the new test module's own source for the inertness self-check, no `audit_event`, no `logging.basicConfig`/handler, no clock read — any `datetime` is a fixed inline literal), and §3.9 includes a no-clock-read / no-side-effect self-check, an inertness AST self-check, and a substring scan for `audit_event(`, `open(` for write, `time.sleep(`, handler installation, etc.
9. **Risk: the candidate-shape sketch is mistaken for a binding commitment that constrains a future P6-08 implementation authorization.** Mitigation: the packet states that the future P6-08 implementation authorization (when and if Kevin records one in writing) is free to replace any element of the candidate shape recorded here, including the candidate location, test layout, assertion style, exact substring-match strings, and test matrix.
10. **Risk: the planning packet reintroduces stale "pending merge" current-state language.** Mitigation: the planning packet adds no new `MASTER_STATUS.md` §1 reflection paragraph (out of scope per the narrow one-file scope), adds no `README.md` cross-reference, and adds no `plan/phase6_entry_plan.md` update. The targeted "pending merge in this open" grep returns no matches in the canonical doc set.
11. **Risk: confusion about whether the planning PR introduces a runtime / test / broker / market-data / order / strategy / scheduler / persistence / external-sink surface.** Mitigation: the planning PR adds exactly one new markdown file under `governance/authorizations/` and modifies no other file. The `src/**` and `tests/**` diffs are empty. The 696-test pytest suite passes unchanged because no test logic is altered.
12. **Risk: the untracked P6-08 planning draft, `.claude/`, or `Claude_Transfes/` is accidentally swept into this monitoring PR.** Mitigation: this monitoring PR stages **only** the single new monitoring file `monitoring/daily/2026-06-15_pr-p6-08-planning.md`; the planning authorization draft (`governance/authorizations/2026-06-15_p6-08-planning.md`), `.claude/`, and `Claude_Transfes/` remain untracked and are neither staged nor committed by this monitoring PR. The planning draft is added by the **separate** forthcoming planning PR, which merges *after* this sibling per ADR-008 §D5.
13. **Risk: the GOV-02 execution-environment workflow rule is silently reinterpreted by this packet.** Mitigation: the planning packet and this monitoring packet preserve GOV-02 unchanged; both are built in compliance with the rule (local Claude Code / Claude Max session as the default builder).
14. **Risk: monitoring-packet drift if this Mode B sibling does not merge before the planning PR.** Mitigation: ADR-008 §D5 / §D3 and the established precedents all require merge of this packet first; the maintainer is responsible for sequencing the two merges accordingly.

---

## Conditions to be Confirmed Before the P6-08 Planning PR Merges

1. **The narrow diff on the planning PR** — `git diff main --name-only` returns exactly one file (`governance/authorizations/2026-06-15_p6-08-planning.md`), with `src/**`, `tests/**`, `MASTER_STATUS.md`, `README.md`, `RECOVERY.md`, `plan/**`, `docs/**`, other `governance/authorizations/*` files, `pyproject.toml`, `.pre-commit-config.yaml`, `.secrets.baseline`, `.gitignore`, and all other `monitoring/**` files unchanged; `.claude/` and `Claude_Transfes/` not staged or committed.
2. **Mode A critique (if required by the maintainer)** recorded against the planning PR itself in PR-review text — per ADR-008 §D7 and `AI_WORKFLOW.md` §4 / §6 rule 5, the critique (if delivered) is recorded as PR-review text, **not committed as a file**. Per `AI_WORKFLOW.md` §4's routine-exclusion sentence and the planning / enumeration precedents, Mode A is **not** independently required for this planning / enumeration authorization.
3. **Mode B monitoring packet (this packet)** authored under ADR-008 Mode B, filed in a separate monitoring PR per ADR-008 §D5, and merged to `main` **before** the planning PR merges.
4. **Planning PR validation** as reported on the planning PR branch: `.venv/bin/python -m pytest -q` returns **696 passed** (the post-P6-07 baseline preserved; the planning packet adds and modifies no tests); `.venv/bin/pre-commit run --all-files` exits 0 with every hook passing and no second-pass `files were modified by this hook` message; `git diff --name-status main` shows exactly one new file (status A); targeted stale-phrase grep returns no matches in the canonical doc set.

---

## Diff Scope Verification (this monitoring PR)

This monitoring PR adds exactly **one** new file: `monitoring/daily/2026-06-15_pr-p6-08-planning.md`. **No other file is modified, added, or deleted by this monitoring PR; this monitoring PR changes no code and no canonical docs on the monitoring branch — only this single monitoring file. It does not stage, commit, modify, or include the untracked `governance/authorizations/2026-06-15_p6-08-planning.md` planning draft, and it does not touch `.claude/` or `Claude_Transfes/`.** The monitoring branch (candidate `monitoring/2026-06-15-pr-p6-08-planning`) is based on **current `main` head `88fa3bf`**, not on the planning PR's branch. The §8 step 4a `allowed_p2_infra` allowlist on `main` is preserved exactly at the sixteen entries after the merged P6-07 implementation. This monitoring PR introduces no new `src/**` directory, so §8 step 4b's same-PR allowlist-update rule does not trigger.

---

## Non-Goals (this monitoring PR and the P6-08 planning PR)

Neither this monitoring PR nor the forthcoming P6-08 planning PR does any of the following:

- Open any new packet authorization beyond the planning artifact the planning PR itself introduces (which is governed by its own verbatim Kevin authorization quoted in `governance/authorizations/2026-06-15_p6-08-planning.md` §Authorization). Any P6-08 implementation; any P6-09 or later P6-0N task; any P6-01 / P6-02 / P6-03 / P6-04 / P6-05 / P6-06 / P6-07 expansion; any simulation expansion; any change to the merged P2-04 `audit_event` surface; any change to any existing exception class; any runtime activation; or any ops execution work continues to require its own separate written authorization from Kevin per `AI_WORKFLOW.md` §7.
- Choose the P6-08 candidate test layout, file count, assertion style, exact substring-match strings, or per-layer file naming. All choices are reserved for the future P6-08 implementation authorization.
- Add or authorize any real account data, real position book, real account identifier, real holdings, real balance, real P&L, broker-confirmed position, external sink, on-disk artifact, persistence, or filesystem snapshot.
- Add or authorize any change to the merged P5-01 / P5-02 / P6-04 simulation surface, the merged P6-05 `portfolio_state` surface, the merged P6-06 `reporting` surface, the merged P6-01 / P6-02 / P6-03 / P6-07 surfaces, the merged P2-04 logging surface, or the merged P4-06 / P4-07 / P4-08 safety surface.
- Add or authorize any change to any existing exception class.
- Authorize any runtime activation of any merged Phase 3 fixture (`InMemoryHeartbeat`, `InMemoryKillSwitch`, `InMemoryReconciliation`).
- Authorize live or paper execution, broker integration (real or paper), market data, live data, order placement / routing, execution adapter, network / API call, scheduler / daemon, `__main__`, `time.sleep`, secrets / env credential loading, any I/O / external sink (beyond `pathlib.Path.read_text` for the inertness self-check), any new `audit_event` emission, or any strategy / scanner / model / portfolio-management / backtest implementation.
- Reinterpret or relax the GOV-02 execution-environment workflow rule.
- Modify any file under `src/**` or `tests/**`; modify `MASTER_STATUS.md`, `README.md`, `RECOVERY.md`, or any `plan/**` file; modify any other `governance/authorizations/*` file; modify the §8 gates or the §8 step 8 staleness check.
- Promote X10 Layer 5, automate backup-monitoring, execute any DR drill, open OPS-05 / OPS-07, or touch any backup / recovery / Time Machine / Backblaze / X10 / FileVault / drive / power / USB / macOS system setting.
- Modify any ADR, `AI_WORKFLOW.md`, `pyproject.toml`, `.pre-commit-config.yaml`, `.secrets.baseline`, `.gitignore`, or any other `monitoring/**` file beyond this new packet itself.
- Relax any quality gate, hook, mypy strictness, ruff rule, or `detect-secrets` baseline; create any git tag, GitHub release, or version bump.
- Reintroduce or extend stale `**pending merge**` language for any already-merged packet.
- Substitute for any Mode A adversarial review of the planning PR the maintainer elects to require — Mode A and Mode B are independent dual artifacts per ADR-008 §D7.

---

## P6-08 Phase Status (Explicit)

**P6-08 planning remains docs-only, and P6-08 implementation remains NOT authorized.** The forthcoming planning PR records a candidate shape for P6-08 (the failure-handling / exception-typing consolidation tripwire-only test packet) at the planning level only and authorizes no implementation. P6-08 implementation is authorized **only** by a separate, future, written implementation authorization from Kevin under `governance/authorizations/<YYYY-MM-DD>_p6-08.md`, per `MASTER_STATUS.md` §7 and `AI_WORKFLOW.md` §6 rule 3 / §7. Until then, no `tests/**` change may be opened under the P6-08 label. P6-09 and all later successor packets likewise remain future / not authorized.

---

## Required Merge Order

Per ADR-008 §D3 / §D5 and the established sibling-Mode-B precedent: **this monitoring PR must merge to `main` before the P6-08 planning PR merges.** The maintainer is responsible for sequencing the two merges accordingly; this packet does not itself authorize any merge.

---

## Closing

This packet records the Mode B governance-monitor evidence for the forthcoming **P6-08 planning / enumeration** PR (a bounded docs/governance-only packet that will add exactly one new authorization artifact at `governance/authorizations/2026-06-15_p6-08-planning.md` recording the candidate shape for the eighth Phase 6 implementation task — a tripwire-only test packet, modeled on the merged P5-03 / P5-04 / P5-05 / P5-06 / P5-07 precedent, that consolidates and pins the failure-handling and exception-typing discipline already enforced by the merged P6-01..P6-07 surface — under Kevin's verbatim written planning directive on 2026-06-15 reproduced in full in the artifact's §Authorization section). The planning PR opens no implementation task, does not choose the candidate test layout / assertion style / substring-match text, does not change any existing exception class, does not authorize any new `audit_event` emission, does not extend the §8 step 4a allowlist, does not change the §8 forbidden-token set, does not modify the merged P6-01..P6-07 / P5 / P4 / P3 / P2-04 surfaces, does not authorize any I/O / external sink / persistence / real account data / broker / market data / runtime activation / ops execution work, does not reinterpret or relax the GOV-02 execution-environment workflow rule, intentionally adopts the narrower one-file scope, transparently flags the `plan/phase6_entry_plan.md` §4 item-7 / item-8 reconciliation without editing §4, and preserves the canonical reconciliations through the merged P6-07 implementation in full. This monitoring PR stages only the single monitoring file and does **not** include the untracked planning draft, `.claude/`, or `Claude_Transfes/`. The merged P6-07 deterministic read-only operator view (`db4530d`), the merged P6-06 deterministic daily dry-run report (`03cafae`), the merged P6-05 implementation (`da19cfa`), the merged P6-04 Direction A implementation (`cea8553`), the merged P6-03 decision composer (`8d98a41`), the merged P6-02 eligibility-check pure functions (`4785e24`), the merged P6-01 typed signal-intake boundary (`d52551a`), the merged Phase 6 entry plan, and all earlier merged surfaces are preserved unchanged. **P6-08 planning remains docs-only; P6-08 implementation remains not authorized.** Any P6-08 implementation requires its own separate written authorization from Kevin per `AI_WORKFLOW.md` §7. Per ADR-008 §D5, this packet must merge to `main` **before** the P6-08 planning PR merges.

## Sign-off

**Completed At (UTC)**: 2026-06-15 (authoring; pending maintainer commit and merge-sequencing)
**Prepared By**: Backup AI (Mode B author) under ADR-008 Mode B; committed by the default builder (local Claude Code / Claude Max) under `AI_WORKFLOW.md` §1.4 / §6 rule 1.
**Kevin Decision**: Pending — Accepted | Needs Follow-up | Blocked
