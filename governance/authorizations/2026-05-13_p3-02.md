# P3-02 — Phase 3 Entry Decision (Governance/Documentation-Only, Phase-Gate PR)

Date: 2026-05-13
Authorizer: Kevin (written authorization quoted verbatim below).
Status: Authorization recorded for the P3-02 entry PR. Phase 3 is **formally opened by the merge of this PR**, governance/documentation only. **No Phase 3 implementation is authorized.** P3-03 is **not** authorized. No file under `src/**` or `tests/**` is added or modified by this PR. No runtime / broker / market-data / order / strategy / scheduler / persistence / deployment / env-var / secrets / concrete-risk / automation / notification / CI-gate / tag / release change is authorized by this PR. The `MASTER_STATUS.md` §8 step 4a `allowed_p2_infra` allowlist is **not** extended by this PR.
Scope: P3-02 **entry only** — Phase 3 entry decision (the phase-gate PR named as candidate task 2 in `plan/phase3_entry_plan.md` §4). Governance/documentation only.

## Verbatim User Authorization

The user authorization for this P3-02 entry PR (Kevin, chat, 2026-05-13) is recorded verbatim below:

> "Authorize P3-02 entry only: prepare the governance/documentation-only Phase 3 entry PR, using the existing P3-02 preparation artifact as the basis. This may formally open Phase 3 but authorizes no implementation. No P3-03 work, no src/tests changes, no runtime/broker/scheduler/persistence/deployment changes, no tags/releases. Any allowlist change must be limited to what is required for the Phase 3 entry gate and must be reviewed explicitly. Use Mode A review and separate Mode B packet sequencing before merge."

This authorization is recorded under `AI_WORKFLOW.md` §7 ("Durable Authorization Artifacts"). It is a **phase-opening** authorization: it formally opens Phase 3 as a governance state. It is **not** a Phase 3 implementation authorization. It authorizes only the governance/documentation-only contents of this PR, exactly as enumerated in the "Scope" section below.

The preparation prose for this entry decision is recorded at `governance/authorizations/2026-05-13_p3-02-preparation.md` (PR #36, merged at `d2e4b3c`) and forms the basis for this artifact per the user's authorization above. That preparation artifact remains the longer-form draft; this entry artifact is the actual authorization of record for the Phase 3 entry decision.

## Authorization Status

This artifact is the **P3-02 entry authorization of record** for the Phase 3 entry decision. It is distinct from, and supersedes for the purpose of recording the Phase 3 entry decision, the preparation artifact at `governance/authorizations/2026-05-13_p3-02-preparation.md`. The preparation artifact remains the in-tree draft prose for the entry decision; this artifact carries the actual phase-opening authorization. Both artifacts coexist on `main`; neither is rescinded by the other.

Per `AI_WORKFLOW.md` §4(1), the Phase 3 entry decision is a **phase gate** by definition and requires Mode A adversarial review. Per ADR-008 §D3 / §D5 (still in force in the governance phase per ADR-009 D7's bootstrap-avoidance clause until runtime exists on `main`), a Mode B monitoring packet for the active workday on which this PR is open / merged is required, authored by the Backup AI per ADR-008 §D1 and committed by Codex per ADR-008 §D4 in a **separate** monitoring PR that **merges to `main` before this entry PR merges** per ADR-008 §D5. This entry PR does **not** bundle the Mode B packet — it is a separate sibling PR, exactly as for PR #30 / PR #32 / PR #34 / PR #36.

This entry PR may merge only after:

1. Mode A adversarial review of this entry PR is recorded (as PR-review text, not committed to the repository).
2. The Mode B monitoring packet for the active workday on which this PR is open / merged has been authored, committed to `monitoring/daily/`, and merged to `main` in a separate sibling PR.
3. All gates listed in "Exit Criteria" below pass.
4. Kevin merges this PR.

## Scope

The work authorized by this entry artifact is restricted to:

- **This entry authorization artifact** at `governance/authorizations/2026-05-13_p3-02.md` (this file). The artifact records the verbatim Kevin authorization above and the exact scope/exclusions in this section.
- **A narrow governance/status reflection in `MASTER_STATUS.md`** §1 and §9 recording that Phase 3 is **formally opened** by the merge of this PR as a governance state, that **no Phase 3 implementation is authorized** by this opening, that P3-03 remains **future / not authorized**, that Phase 2 remains formally closed, and that the §8 step 4a `allowed_p2_infra` allowlist is **unchanged** (the five P2-01..P2-05 entries are preserved exactly). Per `AI_WORKFLOW.md` §1.2 / §6 rule 2 ("One status keeper"), this `MASTER_STATUS.md` edit is conservative and remains subject to Perplexity Computer's verification before being treated as the canonical status.
- **A `README.md` "Current Phase" cross-reference update** naming this entry artifact alongside the existing Phase 3 entry planning, P3-01 drafting, P3-01 acceptance, and P3-02 preparation references, and recording that Phase 3 is opened in governance state only with no implementation authorized.
- **A narrow `plan/phase3_entry_plan.md` §1 "Current Status" update** recording that P3-02 entry has merged (Phase 3 is opened as a governance state), restating that no Phase 3 implementation is authorized, that P3-03 remains future / not authorized, and that the next allowed work is a future P3-03 written authorization (subject to its own separate Kevin-written authorization, sibling artifact, Mode A / Mode B review, and §8 step 4a allowlist update in the same PR that introduces any new directory).
- **No other file is modified.** In particular: no file under `src/**`, no file under `tests/**`, no ADR text, no `.pre-commit-config.yaml`, no `pyproject.toml`, no `.gitignore`, no `.secrets.baseline`, no template, no test, no `monitoring/daily/` packet (any required Mode B packet for the workday on which this entry PR is open / merged is a separate sibling PR per ADR-008 §D5).

### What This PR Does

- **Formally opens Phase 3 as a governance state** (the phase-opening event for Phase 3). The opening is governance/documentation-only.
- Records Kevin's verbatim written authorization above in a durable in-tree artifact under `governance/authorizations/` per `AI_WORKFLOW.md` §7.
- Updates `MASTER_STATUS.md` §1 / §9, `README.md` "Current Phase", and `plan/phase3_entry_plan.md` §1 to reflect that Phase 3 is opened and that no Phase 3 implementation is authorized.

### What This PR Does Not Do

Per the verbatim authorization above, this PR does **not** authorize any of the following. Each remains forbidden until Kevin records a separate written authorization per `MASTER_STATUS.md` §7 and `AI_WORKFLOW.md` §6 rule 3 ("One approver") / rule 7 ("No phase drift"), with a sibling artifact under `governance/authorizations/` and (where it introduces a new directory) a corresponding update to the §8 step 4a allowlist in the same PR that introduces the directory:

- **P3-03 or any task beyond P3-02 entry.** P3-03 (the candidate test-fixture implementation of one risk-control protocol named in `plan/phase3_entry_plan.md` §4 item 3) remains **future / not authorized**. Opening P3-03 requires a separate written authorization from Kevin, a sibling artifact under `governance/authorizations/`, an §8 step 4a allowlist update in the same PR that introduces any new directory, Mode A adversarial review per `AI_WORKFLOW.md` §4(2) (high-risk architecture decision — first concrete behaviour behind a control surface), and a Mode B monitoring packet per ADR-008 §D3 / §D5 (governance phase) or, once runtime exists on `main`, per ADR-009 D3 / D5 (runtime phase).
- **Any Phase 3 implementation of any kind.** This PR adds no runtime, no concrete implementation of `KillSwitchProtocol`, `ReconciliationProtocol`, or `HeartbeatProtocol`, no test fixture exercising those protocols, no `__main__` entry point, no daemon, no scheduler, no background worker, no long-running service, no live execution loop.
- **Source / test changes.** No file under `src/**` or `tests/**` is added or modified by this PR.
- **Runtime work of any kind.** No `__main__` entry point, no daemon, no scheduler, no background worker, no long-running service, no live execution loop.
- **Broker integration.** No broker SDK, no broker connector, no broker reconciliation runtime, no broker authentication, no broker credentials.
- **Market-data integration.** No market-data ingestion code, no market-data pipeline, no committed dataset, no live or historical data feed.
- **Order management.** No order objects, no order placement, no position management, no fills, no trade reports.
- **Strategy logic.** No trading signals, no scanners, no models, no portfolio rules, no backtests.
- **Scheduling.** No cron-style scheduler, no APScheduler, no background-job framework, no operator-heartbeat daemon, no kill-switch runtime, no reconciliation runtime, no monitoring daemon, no notification daemon, and no automation that watches the cadence rule ADR-009 defines.
- **Persistence.** No SQLite, no on-disk database, no on-disk reconciliation snapshot, no on-disk heartbeat state, no log sink, no file artifact written by runtime code.
- **Deployment.** No deployment workflow, no rollout, no rollback runtime, no CI/CD pipeline beyond the existing local pre-commit and pytest gates.
- **Secrets / env-var loading.** No real secrets, no real credentials, no `.env` files, no `os.environ` / `os.getenv` reads, no real account identifiers, no real broker / venue identifiers, no real endpoints embedded anywhere in the repository under this PR. ADR-001's secrets-management discipline is not relaxed.
- **Concrete risk implementations.** No concrete implementation of `KillSwitchProtocol`, `ReconciliationProtocol`, or `HeartbeatProtocol` lands inside the runtime package under this PR. The P2-05 boundary (`src/gmc_rebuild/risk/` is types and abstract `typing.Protocol` definitions only) is preserved.
- **New trading behavior.** No change that introduces, simulates, or wires up trading behavior — in any module, under any name, anywhere in the tree.
- **Tooling relaxation.** No weakening of pre-commit, Ruff, mypy strict mode, `detect-secrets`, `.gitignore`, `.secrets.baseline`, the `MASTER_STATUS.md` §8 step 4 always-forbidden scan, the §8 step 4a per-PR Phase 2 infrastructure allowlist scan, or the §8 step 4c recursive forbidden-token scan.
- **§8 step 4a allowlist expansion.** The `allowed_p2_infra` allowlist is **not** extended by this PR. Per the user authorization above ("Any allowlist change must be limited to what is required for the Phase 3 entry gate and must be reviewed explicitly"), the allowlist change required for the Phase 3 entry gate is **zero entries**: this PR opens Phase 3 as a governance state only, does not introduce any new directory, and does not authorize any Phase 3 implementation. The allowlist therefore remains exactly the five P2-01..P2-05 entries (`src`, `src/gmc_rebuild/config`, `src/gmc_rebuild/time`, `src/gmc_rebuild/logging`, `src/gmc_rebuild/risk`). Any future P3-03 (or later) implementation PR that introduces a new directory must add it to the allowlist in the same PR that introduces the directory, per the standing rule in `MASTER_STATUS.md` §8 step 4b, and must be authorized in writing for the specific directory by Kevin.
- **New tags or releases.** No git tag, no GitHub release, no version bump.
- **Live-trading entry gate triggering.** ADR-009 D5 records the implementation evidence / deferred-with-citation evidence requirement for any future PR that introduces a live-trading entry point. This PR does **not** introduce a live-trading entry point; the §3.4 evidence requirement applies to future PRs, not to this one.

The always-forbidden categories in `MASTER_STATUS.md` §6 and `plan/phase2_entry_plan.md` §2 — strategy code, broker execution code, live or paper trading wired to a real broker, runtime daemons affecting accounts, real market data ingestion, order placement, and real secrets — remain forbidden in all modes. This entry does not relax them.

## Phase 3 Opening Effect (Governance State Only)

Merging this PR formally **opens Phase 3 as a governance state**. The opening is governance/documentation-only and has the following effect:

- **Phase 3 is open** as a governance state. Phase 2 remains formally closed (governance-only) per `governance/authorizations/2026-05-12_phase-2-closure.md`. Phase 1 baseline `1f101fc` remains the accepted Phase 1 baseline per `MASTER_STATUS.md` §3.
- **No Phase 3 implementation is authorized.** Opening Phase 3 as a governance state does not by itself authorize any Phase 3 implementation PR. Each future Phase 3 implementation PR (P3-03 and beyond) requires its own separate written authorization from Kevin, a sibling artifact under `governance/authorizations/`, an §8 step 4a allowlist update in the same PR that introduces any new directory, and Mode A / Mode B review as applicable.
- **P3-03 remains future / not authorized.** The candidate task P3-03 named in `plan/phase3_entry_plan.md` §4 item 3 (test-fixture implementation of one risk-control protocol) is not authorized by this opening. The specific protocol P3-03 would target is not named here; Kevin names the specific protocol in writing at the time of P3-03 authorization.
- **The §8 step 4a allowlist is unchanged.** The five P2-01..P2-05 entries (`src`, `src/gmc_rebuild/config`, `src/gmc_rebuild/time`, `src/gmc_rebuild/logging`, `src/gmc_rebuild/risk`) remain exactly as they were before this PR.
- **All quality gates remain in force.** `pre-commit`, Ruff, mypy strict mode, `detect-secrets`, `.gitignore`, `.secrets.baseline`, the `MASTER_STATUS.md` §8 step 4 always-forbidden scan, the §8 step 4a per-PR Phase 2 infrastructure allowlist scan, and the §8 step 4c recursive forbidden-token scan are all unchanged.
- **All always-forbidden categories remain forbidden.** Strategy code, broker execution code, live or paper trading wired to a real broker, runtime daemons affecting accounts, real market data ingestion, order placement, and real secrets remain forbidden regardless of phase.

## Intended First Phase 3 Implementation Task Scope (Planning Level Only)

This section restates `plan/phase3_entry_plan.md` §4 item 3 verbatim (the candidate P3-03 task) at the planning level only, to satisfy the `MASTER_STATUS.md` §7.4 pattern (mirrored from `plan/phase2_entry_plan.md` §5) that any phase-opening authorization "must also name the first PR's scope". This restatement is for review convenience and **does not narrow Kevin's later P3-03 choice**. It does **not** authorize implementation, does **not** name the specific protocol, does **not** authorize P3-03 itself, and does **not** preclude Kevin from authorizing a different first Phase 3 implementation task scope (or a different protocol within P3-03) in writing at the time of P3-03 authorization.

> **PR P3-03 — (future / not authorized) Test-fixture implementation of one risk-control protocol.** A first, narrowly scoped test-fixture implementation of exactly one of the P2-05 protocols (the specific protocol is to be named by Kevin in writing at the time of authorization). Restricted to a single protocol; restricted to test-fixture code that runs under `pytest`; no broker SDK, no network, no real account identifiers, no real venue identifiers, no real endpoints, no real secrets, no scheduler, no daemon, no `__main__` entry point, no order objects, no market-data ingestion.

Intended scope properties, at the planning level only:

- **One protocol, not all three.** The first Phase 3 implementation task targets exactly one of `KillSwitchProtocol`, `ReconciliationProtocol`, or `HeartbeatProtocol`. Kevin names the specific protocol in writing at the time of P3-03 authorization.
- **Test-fixture only.** The implementation is a test-fixture artifact under `tests/**` (or a sandboxed helper under a future authorized `src/gmc_rebuild/<protocol>/` subdirectory specifically scoped to test-fixture support, named in writing by Kevin at the time of P3-03 authorization), exercised under `pytest`. No `__main__` entry point. No daemon. No scheduler. No background worker. No network. No broker SDK. No real account / venue / endpoint identifier. No real secret. No real or historical market data feed. No order object. No order placement.
- **One control surface at a time.** Even within the chosen protocol, the test-fixture exercise is narrow enough that a single review can verify it does not cross into trading strategy, broker execution, live or paper trading wired to a real broker, real market data ingestion, real secrets, order placement, scheduler / daemon, persistence outside the test sandbox, or runtime-daemon territory — i.e. the always-forbidden categories in `MASTER_STATUS.md` §6 and the stop conditions in `plan/phase3_entry_plan.md` §8.
- **Allowlist update in the same PR.** Any new directory introduced by the future P3-03 implementation must be added to the `MASTER_STATUS.md` §8 step 4a `allowed_p2_infra` allowlist in the same PR that introduces the directory, per the standing rule in `MASTER_STATUS.md` §8 step 4b. This entry PR does **not** extend the allowlist.

## Required Gates Before This Entry PR May Merge

Per Kevin's verbatim authorization ("Use Mode A review and separate Mode B packet sequencing before merge"), the following gates must be satisfied before this PR may merge. None is satisfied by the authorization itself:

1. **Mode A adversarial review** of this entry PR per `AI_WORKFLOW.md` §4(1) (phase gate by definition — opening Phase 3 is a phase gate). The Mode A critique is delivered as PR-review text against this entry PR and is **not** committed to the repository (per `AI_WORKFLOW.md` §6 rule 5 / ADR-008 §D7 dual-artifact rule). The Mode A review of the preparation artifact (PR #36) does **not** substitute for the Mode A review of this entry PR.
2. **Mode B monitoring packet** for the active workday on which this entry PR is open / merged, authored by the Backup AI per ADR-008 §D1 and committed by Codex under `monitoring/daily/` per ADR-008 §D4 in a **separate** monitoring PR, **merged to `main` before this entry PR merges** per ADR-008 §D5. ADR-009 D3 / D5 do not govern this entry PR because ADR-009's bootstrap-avoidance clause keeps ADR-008 §D3 / §D5 in force until runtime exists on `main`; this entry PR does not introduce runtime and therefore continues to fall under ADR-008's governance-phase cadence.
3. **§8 step 4a allowlist update in the same PR for any new authorized directory the entry PR introduces.** This entry PR does **not** introduce any new directory, and therefore the allowlist is **unchanged**. If, on review, a reviewer determines that the Phase 3 entry gate requires an allowlist change, that change must be limited to what is required for the Phase 3 entry gate and must be reviewed explicitly per Kevin's authorization above. As authored, this PR contains no allowlist change.
4. **No silent expansion of scope.** Per `AI_WORKFLOW.md` §6 rule 6 / rule 7, any PR that grows beyond its stated scope during implementation is split or stopped, not merged. The scope of this PR is exactly what is enumerated in the "Scope" section above.
5. **All `MASTER_STATUS.md` §8 startup verification commands** (steps 1 through 7) pass on the entry branch, including:
   - Step 3 baseline ancestry from `1f101fc`.
   - Step 4 always-forbidden category scan.
   - Step 4a per-PR Phase 2 infrastructure allowlist scan (allowlist unchanged).
   - Step 4c recursive forbidden-token scan (subshell exit `0`).
   - Step 6 `pre-commit run --all-files`.
   - Step 7 `pytest`.

## Required Monitoring (Mode B)

Per ADR-008 §D3 / §D5, the entry PR is an active-workday event on its open / merge UTC date. As of 2026-05-13, six 2026-05-13 packets already exist on `main`:

- `monitoring/daily/2026-05-13.md` — first packet, scoped to PR #26.
- `monitoring/daily/2026-05-13_pr28.md` — second packet, scoped to PR #28.
- `monitoring/daily/2026-05-13_p3-01.md` — third packet, scoped to PR #30 (P3-01 drafting).
- `monitoring/daily/2026-05-13_p3-01-revision.md` — fourth packet, scoped to PR #32 (P3-01 revision).
- `monitoring/daily/2026-05-13_adr-009-acceptance.md` — fifth packet, scoped to PR #34 (ADR-009 acceptance).
- `monitoring/daily/2026-05-13_p3-02-preparation.md` — sixth packet, scoped to PR #36 (P3-02 preparation).

If this entry PR opens on 2026-05-13 UTC, a **seventh slugged 2026-05-13 packet** is required (suggested name `monitoring/daily/2026-05-13_p3-02.md` or `monitoring/daily/2026-05-13_pr<N>.md` where `<N>` is the entry PR number). The packet must be **committed and merged to `main` in a separate monitoring PR before the entry PR merges**, per ADR-008 §D5 and the established 2026-05-13 precedents (PR #27 → PR #26, PR #29 → PR #28, PR #31 → PR #30, PR #33 → PR #32, PR #35 → PR #34, PR #37 → PR #36). This entry PR does **not** bundle the monitoring packet.

If the entry PR opens on a later UTC date, the packet is the first or a slugged subsequent packet for that UTC date, per ADR-008 §D4 same-day naming.

Perplexity Computer confirms this sequencing before verifying the entry PR for Kevin's review. The Mode B packet must reflect that the entry PR's content is restricted to (a) this entry authorization artifact, (b) the conservative `MASTER_STATUS.md` §1 / §9 reflection of Phase 3 opening, (c) the `README.md` "Current Phase" cross-reference update, and (d) the narrow `plan/phase3_entry_plan.md` §1 status note — and must reflect that no Phase 3 implementation is authorized, that P3-03 remains future / not authorized, and that the §8 step 4a allowlist is unchanged.

## Mode A Status

This entry PR is **pending Mode A adversarial review** per `AI_WORKFLOW.md` §4(1). Opening Phase 3 is a phase gate by definition.

- The Mode A critique is delivered as PR-review text against this entry PR. Per `AI_WORKFLOW.md` §6 rule 5 / ADR-008 §D7, the Mode A critique is **not** committed to the repository.
- The Mode A review here targets the prose of this entry artifact and the conservative governance/status reflection: scope-boundary preservation (Phase 3 opened as a governance state only; no Phase 3 implementation authorized; P3-03 remains future / not authorized; §8 step 4a allowlist unchanged; no `src/**` or `tests/**` change; no quality-gate change; no runtime / broker / market-data / order / strategy / scheduler / persistence / deployment / env-var / secrets / concrete-risk / automation / notification / CI-gate / tag / release change), accuracy of cross-references to ADR-008 / ADR-009 / `MASTER_STATUS.md` / `AI_WORKFLOW.md` / `plan/phase3_entry_plan.md`, accuracy of the verbatim-authorization quotation, and clarity of the "Intended First Phase 3 Implementation Task Scope" framing as planning-level only.
- The Mode A review of the P3-02 preparation artifact (PR #36) does **not** satisfy this Mode A requirement. The Phase 3 entry decision is a separate phase-gate review against this entry PR.

## Exit Criteria

This entry PR may merge only after the full required check suite passes on the entry branch:

- `pre-commit run --all-files` (clean, or every failure documented).
- `pytest` (clean).
- `MASTER_STATUS.md` §8 step 3 (baseline ancestry from `1f101fc`).
- `MASTER_STATUS.md` §8 step 4 (always-forbidden scan).
- `MASTER_STATUS.md` §8 step 4a (per-PR Phase 2 infrastructure allowlist scan; allowlist is unchanged).
- `MASTER_STATUS.md` §8 step 4c (recursive forbidden-token scan; subshell exit `0`).
- A slugged Mode B monitoring packet for the entry PR's active workday has been authored under ADR-008 Mode B and committed and merged to `main` in a separate monitoring PR, **before** the entry PR merges.
- A Mode A critique against this entry PR has been recorded in PR-review text (not committed as a file).

After this entry authorization merges, the next allowed decisions are those listed in `MASTER_STATUS.md` §9, plus (a) any future P3-03 (or later) implementation PR authorized in writing by Kevin per `MASTER_STATUS.md` §7 with a sibling artifact under `governance/authorizations/`, Mode A / Mode B review as applicable, and §8 step 4a allowlist update in the same PR that introduces any new directory. Phase 3 is open as a governance state; Phase 2 remains formally closed; no Phase 3 implementation is authorized; no P3-03 work has begun.

## Preserved Gates for Any Future Work

Any work beyond this entry — opening any further Phase 3 task (P3-03, P3-04, …) or opening any runtime / broker / market-data / order / strategy / scheduler / persistence / deployment / monitoring-daemon work — requires **all** of the following, none of which is satisfied by this entry:

1. **Separate written authorization from Kevin** per `MASTER_STATUS.md` §7 and `AI_WORKFLOW.md` §6 rule 3 / rule 7, with a sibling artifact under `governance/authorizations/`.
2. **Mode A adversarial review** per `AI_WORKFLOW.md` §4. The first concrete behaviour behind any risk-control surface is a high-risk architecture decision (§4(2)).
3. **Mode B monitoring packet** per ADR-008 §D3 / §D5 (governance phase) or, once runtime exists on `main`, per ADR-009 D3 / D5 (runtime phase). Before-merge sequencing per ADR-008 §D5 (or ADR-009 D5 once in force) applies.
4. **§8 step 4a allowlist updates in the same PR** for any new authorized directory, per the standing rule in `MASTER_STATUS.md` §8 step 4b.
5. **Live-trading entry gate** per ADR-009 D5 for any PR that introduces a live-trading entry point: either named implementation evidence + named test path in the PR's diff, or named deferred-with-citation evidence pointing to an already-merged authorization artifact on `main`.

## Supporting Evidence (Non-Authoritative)

The following are pointers to the surrounding governance record. They are evidence, not the authorization itself; this file is the authorization of record for the Phase 3 entry decision.

- Phase 1 accepted baseline: `1f101fc` (`MASTER_STATUS.md` §3).
- Phase 2 closure authorization: `governance/authorizations/2026-05-12_phase-2-closure.md`.
- Phase 3 entry planning authorization: `governance/authorizations/2026-05-13_phase-3-entry-planning.md`.
- P3-01 drafting authorization: `governance/authorizations/2026-05-13_p3-01.md`.
- P3-01 acceptance authorization: `governance/authorizations/2026-05-13_p3-01-acceptance.md`.
- P3-02 preparation authorization (basis for this entry artifact): `governance/authorizations/2026-05-13_p3-02-preparation.md` (PR #36, merged at `d2e4b3c`).
- Phase 3 entry plan (criterion 5 references ADR-009 acceptance; §4 item 2 names P3-02 as the phase-gate decision): `plan/phase3_entry_plan.md` §4 item 2, §5 criterion 5, §6, §7, §8.
- ADR accepted under P3-01: `docs/decisions/ADR-009_runtime_monitoring_cadence.md`.
- ADR whose §D6 follow-up is closed by P3-01: `docs/decisions/ADR-008_monitoring_cadence_and_ai_monitor_role.md` §D6.
- UTC discipline applicable to any Phase 3 work: `docs/decisions/ADR-004_utc_discipline.md`.
- Runtime-phase control surfaces a future Phase 3 implementation PR would coordinate with: `docs/decisions/ADR-002_kill_switch.md`, `docs/decisions/ADR-003_reconciliation.md`, `docs/decisions/ADR-005_heartbeat.md`.
- Pattern reference (prior governance-only authorization slices): `governance/authorizations/2026-05-12_p2-03.md`, `governance/authorizations/2026-05-12_p2-04.md`, `governance/authorizations/2026-05-12_p2-05.md`.
- Monitoring cadence rule governing the sibling Mode B packet for this PR: `docs/decisions/ADR-008_monitoring_cadence_and_ai_monitor_role.md` §D3 / §D4 / §D5 (governs the entry PR per ADR-009 D7's bootstrap-avoidance clause until runtime exists on `main`).
- Mode B monitoring packets for the surrounding 2026-05-13 PR cycle: `monitoring/daily/2026-05-13.md` (PR #26), `monitoring/daily/2026-05-13_pr28.md` (PR #28), `monitoring/daily/2026-05-13_p3-01.md` (PR #30), `monitoring/daily/2026-05-13_p3-01-revision.md` (PR #32), `monitoring/daily/2026-05-13_adr-009-acceptance.md` (PR #34), `monitoring/daily/2026-05-13_p3-02-preparation.md` (PR #36).
