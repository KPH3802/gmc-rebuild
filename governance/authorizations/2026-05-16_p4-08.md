# P4-08 Implementation Authorization — Safety Policy Hardening and Safety Foundation Closure

Date: 2026-05-16
Authorizer: Kevin Heaney
Scope: Phase 4 / P4-08 implementation — safety policy hardening and safety foundation closure. Makes explicit, in tests and/or the smallest appropriate runtime safety-policy surface, that the merged P4-06 `SafetyVerdict` is `clear` only when heartbeat is FRESH for every required component, kill switch is ARMED, and reconciliation is CLEAN; every other observed state remains blocked or advisory-blocked and visible to the operator view. Adds focused tests proving the policy cannot be weakened accidentally. Adds this governance authorization artifact. Updates `MASTER_STATUS.md` and `plan/phase4_entry_plan.md` conservatively to reflect safety foundation closure only if validation passes. Defines the next stage as local/paper simulation boundary planning, not live execution. Updates allowlists only if narrowly required by existing repository tests.

## Authorization

Kevin authorized this packet on 2026-05-16 (Saturday, May 16, 2026 10:36 AM CDT). The verbatim written authorization follows:

> P4-08 Authorization — safety policy hardening and safety foundation closure — 2026-05-16
>
> I, Kevin Heaney, authorize one bounded P4-08 implementation packet for the gmc-rebuild repository to harden the safety policy and close the safety foundation if validation passes.
>
> Authorized scope:
> - Make explicit, in tests and/or the smallest appropriate runtime safety-policy surface, that the safety verdict is clear only when heartbeat is FRESH, kill switch is ARMED, and reconciliation is CLEAN.
> - Ensure all other heartbeat, kill-switch, and reconciliation states remain blocked or advisory-blocked and visible to the operator view.
> - Add focused tests proving the policy cannot be weakened accidentally.
> - Add the required governance authorization artifact.
> - Update MASTER_STATUS.md and plan/phase4_entry_plan.md conservatively to reflect safety foundation closure only if tests and full validation pass.
> - Define the next stage as local/paper simulation boundary planning, not live execution.
> - Update allowlists only if narrowly required by existing repository tests.
>
> Safety constraints:
> - No broker integration.
> - No market data integration.
> - No order submission.
> - No strategy, scanner, model, portfolio, backtest, live trading, or paper-trading implementation.
> - No secrets or environment-variable credential loading.
> - No network calls.
> - No scheduler or daemon.
> - No persistence or database.
> - No time.sleep.
> - No runtime activation beyond inert safety-policy evaluation and read-only reporting.
> - No weakening of P4-06 runtime shell or P4-07 operator-view safety gates.
>
> Validation required before PR:
> - Targeted tests for safety-policy hardening and operator visibility.
> - python3 -m pre_commit run --all-files.
>
> This authorization is only for the bounded P4-08 packet above and does not authorize any successor packet.

This authorization is the authorization of record per `AI_WORKFLOW.md` §7.

## Allowed Scope

The implementation PR may include exactly:

- A new pure-Python, in-memory, pytest-only test module `tests/runtime/test_safety_policy_hardening.py` (located in the existing `tests/runtime/` directory introduced by P4-06) containing focused tests proving:
  - `SafetyVerdict.clear` is `True` if and only if every required heartbeat component is `FRESH`, the kill switch is `ARMED`, and reconciliation is `CLEAN`.
  - Every non-`FRESH` heartbeat status (`STALE`) produces a blocking verdict.
  - Every non-`ARMED` kill-switch state (`TRIPPED`) produces a blocking verdict.
  - Every non-`CLEAN` reconciliation status (`FAILED`, `UNAVAILABLE`, `WARNING`) produces a blocking or advisory-blocking verdict, with `WARNING` distinctly reported as advisory.
  - The blocked or advisory-blocked state remains visible to the read-only operator view (`OperatorSafetyView` / `format_safety_verdict`) introduced by P4-07: no non-`CLEAN` / non-`FRESH` / non-`ARMED` combination can be rendered as `CLEAR`.
  - The policy cannot be weakened by widening the blocker-code set, by mutating the verdict, or by formatting alone.
- This authorization artifact at `governance/authorizations/2026-05-16_p4-08.md`.
- Conservative status reflections in `MASTER_STATUS.md` and `plan/phase4_entry_plan.md` recording P4-08 as a narrow safety-policy-hardening task that lives entirely within the existing `tests/runtime/` directory and does not extend the §8 step 4a `allowed_p2_infra` allowlist. The status reflections define the next stage as **local/paper simulation boundary planning**, not live execution; they do not open or authorize that next stage.

The new test module may:

- Import the already-merged P4-06 `RuntimeShell` / `SafetyVerdict` / `BLOCKER_*` symbols, the already-merged P4-07 `OperatorSafetyView` / `format_safety_verdict` / `VERDICT_CLEAR` / `VERDICT_BLOCKED` symbols, the merged P2-05 risk-control enums (`HeartbeatStatus`, `KillSwitchState`, `ReconciliationStatus`), and the merged P3-03 / P3-04 / P3-05 in-memory fakes (`InMemoryHeartbeat`, `InMemoryKillSwitch`, `InMemoryReconciliation`).
- Use only standard-library inert primitives (`__future__`, `datetime`, `itertools`).

## Explicitly Not Authorized

This authorization does not permit:

- Any `__main__`, daemon, scheduler, background thread, long-running service, or runtime activation beyond inert pytest-only evaluation of the existing P4-06 `RuntimeShell.evaluate()` read-only path and the existing P4-07 read-only formatter.
- Any broker integration, broker SDK, order objects, order placement, fills, trade reports, positions, executions, or order book.
- Any market-data ingestion, quotes, bars, or symbol universe.
- Any persistent storage, SQLite, snapshots, or file I/O beyond ordinary Python module import.
- Any network call.
- Any env-var or secrets reading.
- Any `time.sleep` or `asyncio.sleep`.
- Any concrete implementation of `HeartbeatProtocol`, `KillSwitchProtocol`, or `ReconciliationProtocol` outside the merged P3-03 / P3-04 / P3-05 fakes.
- Any modification to the merged P4-06 `RuntimeShell` / `SafetyVerdict` types or the merged P4-07 `OperatorSafetyView` / `format_safety_verdict` formatter, beyond what is needed for read-only testing. The P4-06 and P4-07 safety gates must remain unchanged.
- Any extension of the §8 step 4a `allowed_p2_infra` allowlist beyond what was already merged for P4-06. The new test module lives inside the existing `tests/runtime/` directory.
- Any modification of existing `src/**` files.
- Any quality-gate relaxation, tag, or release.
- Any successor packet (P4-09, paper-trading entry, live-trading entry, …). P4-09 and later remain future / not authorized. **Safety foundation closure recorded by this packet does not open the next stage**; the next stage (local/paper simulation boundary planning) requires its own separate written authorization from Kevin.

Phase 2 remains formally closed. Phase 3 remains formally closed. Phase 4 remains open as a governance state. P4-08 is a bounded safety-policy-hardening slice and does not cross into broker, market-data, order, strategy, scheduler, persistence, or live-trading work. The safety foundation closure recorded by this packet is governance-only and does not authorize any successor implementation.

## Required Validation

Before merge, the implementation PR must pass:

- `python3 -m pre_commit run --all-files`
- Targeted pytest for `tests/runtime/test_safety_policy_hardening.py`
- Diff-scope verification showing changes confined to the new `tests/runtime/test_safety_policy_hardening.py` file, this authorization artifact, and the conservative status reflections in `MASTER_STATUS.md` and `plan/phase4_entry_plan.md`.

Mode A / Mode B review requirements remain governed by `AI_WORKFLOW.md`, ADR-008, ADR-009, and the current `MASTER_STATUS.md`.
