# P6-01 Authorization — Typed Signal-Intake Boundary

Date: 2026-05-19
Authorizer: Kevin
Scope: Bounded, narrowly scoped P6-01 implementation packet for `gmc_rebuild` adding the **first Phase 6 implementation task** — a new subpackage `src/gmc_rebuild/signal_intake/` containing the closed `SignalSide` `StrEnum` (`BUY` / `SELL` only), the frozen, slotted `SignalIntent` five-field dataclass (`intent_id`, `symbol`, `side`, `quantity`, `rationale`) with strict per-field validation, and the pure `accept_signal_intent` typed acceptance function; one new test file at `tests/signal_intake/test_signal_intake.py` covering construction, frozen / slotted / closed-shape invariants, the closed two-member `SignalSide` enumeration, per-field validation (including the `bool`-rejected-as-quantity edge case), equality / hashability, an `ast.walk` inertness self-check, and assertions that the root `gmc_rebuild` package does not re-export the new surface; an extension of `tests/test_package_skeleton.py` by exactly one entry (`signal_intake`) added to the authorized phase-package set with the matching docstring entry naming PR P6-01 and this authorization artifact; an extension of the `MASTER_STATUS.md` §8 step 4a `allowed_p2_infra` bash gate by exactly one entry (`src/gmc_rebuild/signal_intake`) with the matching comment block referencing PR P6-01 and this authorization artifact; a conservative `MASTER_STATUS.md` §1 status reflection paragraph describing the P6-01 surface and explicitly listing the categories the packet does **not** open; and this authorization artifact at `governance/authorizations/2026-05-19_p6-01.md`. P6-01 opens Phase 6 work at the typed signal-intake boundary scope authorized here; it does **not** open, enumerate, or pre-commit any successor P6 packet (P6-02 and beyond remain **future / not authorized** per `plan/phase6_entry_plan.md` §4). No modification of any other `src/**` file. No modification of any other existing `tests/**` file beyond the narrowly scoped one-entry extension to `tests/test_package_skeleton.py` per the authorized narrow scope adjustment. The merged P4-06 / P4-07 / P4-08 safety foundation, the merged P3-03 / P3-04 / P3-05 in-memory fakes, the merged P2-01..P2-05 packages, the merged OPS-01..OPS-04B / OPS-06 operations records, the merged P5-01..P5-07 simulation surface and tripwire tests, the merged GOV-01 governance reconciliation, the merged GOV-02 execution-environment workflow correction, the foundation-complete confirmation report and its 1-FU-A / 1-FU-B follow-ups, and the merged Phase 6 entry plan are all preserved unchanged.

## Authorization

Kevin authorized this packet on 2026-05-19. The verbatim written authorization follows:

> Kevin authorizes P6-01 as a narrow implementation packet:
> - Create a typed signal-intake boundary.
> - Add the required authorization artifact.
> - Add the required tests.
> - Add the required MASTER_STATUS.md §8 step 4a allowlist entry for the new directory.
> - Do not authorize any later P6 task.
>
> Authorized files: `governance/authorizations/2026-05-19_p6-01.md`; `src/gmc_rebuild/signal_intake/__init__.py`; `src/gmc_rebuild/signal_intake/_intent.py` or similarly named single internal module; `tests/signal_intake/test_signal_intake.py` or similarly named single test file; `tests/test_package_skeleton.py` only to add the `signal_intake` entry and matching explanatory prose; `MASTER_STATUS.md` only for the narrow §8 step 4a allowlist update and conservative status reflection if repo convention requires it.
>
> Still forbidden: any other existing test; any other `src/**` file; `README.md`; `docs/**`; `plan/**`; `pyproject.toml`; `.pre-commit-config.yaml`; opening P6-02 or later; any strategy, broker, market data, scheduler, persistence, network, env-var, or runtime activation.
>
> Functional intent: typed signal-intake boundary that accepts a candidate trade idea as a structured object. This is not strategy code. It does not decide what to trade. It does not fetch data. It does not connect to anything.
>
> Implementation expectations: frozen, slotted `SignalIntent` dataclass with minimal generic fields (`intent_id: str`, `symbol: str`, `side: str` or closed enum, `quantity: int`, `rationale: str`); closed `BUY` / `SELL` if enum; optional typed acceptance function; validate only basic shape/sanity; no market-hours / portfolio / risk / eligibility / strategy / scanner / model / market-data / broker SDK / order placement / network / filesystem / env-var / persistence / scheduler / daemon / `__main__` logic.
>
> Tests: deterministic; successful construction; frozen / immutable; validation failures for invalid fields; equality / hashability; inertness import scan; root package does not re-export the new type unless explicitly justified.
>
> Allowlist: if `src/gmc_rebuild/signal_intake/` is created, update `MASTER_STATUS.md` §8 step 4a `allowed_p2_infra` to include exactly `src/gmc_rebuild/signal_intake` and no other entry.
>
> Authorized by Kevin on 2026-05-19. Implement using the GOV-02 workflow: local Claude Code / Claude Max does repo edits. Do not commit until Kevin reviews.

This authorization is the authorization of record per `AI_WORKFLOW.md` §7. The packet is built per the GOV-02 execution-environment workflow rule (local Claude Code / Claude Max acting as the default builder for `gmc-rebuild` repo edits; merged via PR #132 / reconciled via PR #134) and preserves the canonical reconciliations through PR #114, PR #118, PR #122, PR #126, PR #130, PR #134, and PR #138 verbatim.

## Allowed Scope

The implementation PR may include exactly:

- A new subpackage at `src/gmc_rebuild/signal_intake/` containing exactly two Python files:
  - `__init__.py` — module docstring, an explicit `from gmc_rebuild.signal_intake._intent import (SignalIntent, SignalSide, accept_signal_intent)`, and `__all__ = ["SignalIntent", "SignalSide", "accept_signal_intent"]`. Imports only `__future__` and the internal `_intent` submodule. No runtime behaviour beyond the re-export.
  - `_intent.py` — the closed `SignalSide` `StrEnum` (`BUY = "BUY"`, `SELL = "SELL"` only), the frozen, slotted `SignalIntent` dataclass with exactly five fields (`intent_id: str`, `symbol: str`, `side: SignalSide`, `quantity: int`, `rationale: str`) and strict per-field validation enforced in `__post_init__` (non-empty `intent_id`, non-empty `symbol`, `SignalSide`-typed `side`, positive integer `quantity` rejecting `bool` and `float`, non-empty `rationale`; mismatched types raise `TypeError`, invalid values raise `ValueError`), and the pure `accept_signal_intent` function that returns the supplied intent by identity on success and raises `TypeError` on a non-`SignalIntent` argument. Imports only `__future__`, `dataclasses` (stdlib), and `enum` (stdlib). No `os`, no `socket`, no `requests`, no `urllib`, no `http`, no `threading`, no `asyncio`, no `subprocess`, no `sqlite3`, no `time`, no `pickle`, no `shelve`, no `ssl`, no `smtplib`, no `ftplib`. No `__main__` block, no `time.sleep`, no `os.environ`, no `os.getenv`, no `open(` builtin, no broker SDK, no third-party dependency.
- A new test file at `tests/signal_intake/test_signal_intake.py` containing focused pytest functions that exercise the merged P6-01 surface. The file:
  - Imports only `__future__`, `ast`, `importlib`, `dataclasses.FrozenInstanceError`, `pathlib.Path` (stdlib), and the already-merged in-repo module `gmc_rebuild.signal_intake` (for `SignalIntent`, `SignalSide`, `accept_signal_intent`). No `pytest.raises`. No fixture-typed parameters. No third-party dependency. The mypy-strict pre-commit hook does not need a pytest-stub dependency, matching the pattern established by `tests/runtime/test_operator_view.py`, `tests/runtime/test_runtime_shell.py`, `tests/runtime/test_operator_view_composed_safety_foundation.py`, `tests/runtime/test_operator_view_determinism.py`, `tests/simulation/test_simulation_boundary.py`, `tests/simulation/test_simulated_order_intent.py`, `tests/simulation/test_simulated_order_intent_invariants.py`, `tests/simulation/test_composed_safety_foundation.py`, and `tests/simulation/test_propose_symmetry.py`.
  - Asserts the following invariant categories: (1) **construction with valid fields succeeds** — every public field is preserved on the constructed instance; (2) **`accept_signal_intent` returns by identity** — a successfully-constructed `SignalIntent` is returned unchanged; (3) **`accept_signal_intent` rejects non-`SignalIntent`** — `str` and `dict` arguments raise `TypeError` with the type name in the message; (4) **frozen / slotted / closed-shape** — assignment to a field after construction raises `FrozenInstanceError`, the instance has no `__dict__`, `SignalIntent.__slots__` exists, the tuple of declared fields equals exactly `("intent_id", "symbol", "side", "quantity", "rationale")` in order, and the declared types match the documented shape; (5) **closed two-member `SignalSide` enumeration** — `set(SignalSide) == {SignalSide.BUY, SignalSide.SELL}`, `.value` is `"BUY"` / `"SELL"`, and `SignalSide` is a `str` subclass per `StrEnum`; (6) **per-field validation: `intent_id`** — non-string `intent_id` raises `TypeError`, empty string raises `ValueError`; (7) **per-field validation: `symbol`** — non-string `symbol` raises `TypeError`, empty string raises `ValueError`; (8) **per-field validation: `side`** — a `str` value matching a `SignalSide` value still raises `TypeError` (the field is `SignalSide`-typed, not `str`-typed), and `None` raises `TypeError`; the second enum member (`SignalSide.SELL`) is also accepted; (9) **per-field validation: `quantity`** — `float`, `True`, and `False` each raise `TypeError`, `0` and `-3` each raise `ValueError`; (10) **per-field validation: `rationale`** — non-string `rationale` raises `TypeError`, empty string raises `ValueError`; (11) **equality and hashability** — two `SignalIntent` instances with structurally-equal fields compare `==` and have equal hashes, instances differing in any one field do not compare equal, and a `set` of two equal instances collapses to a single member; (12) an **inertness self-check using `ast.walk`** confirming that every Python file under `src/gmc_rebuild/signal_intake/` imports only from the authorized prefixes (`__future__`, `dataclasses`, `enum`, `gmc_rebuild.signal_intake`) and that no forbidden runtime root (`os`, `socket`, `requests`, `urllib`, `http`, `threading`, `asyncio`, `subprocess`, `sqlite3`, `pickle`, `shelve`, `ssl`, `smtplib`, `ftplib`, `time`) is imported; (13) a **belt-and-suspenders substring scan** for `__main__` blocks, `time.sleep(`, `socket.`, `urllib`, `requests.`, and the `open(` builtin — `os.environ` and `os.getenv` are deliberately not substring-checked because the docstrings legitimately use those tokens in backticked reassurance prose, and the AST import-graph scan already proves `os` is not imported so neither attribute can be referenced from code; (14) **root package non-re-export** — `gmc_rebuild` does not expose `SignalIntent` / `SignalSide` / `accept_signal_intent` as attributes, and `gmc_rebuild.__all__` does not include them.
  - Implements small local helpers private to the new module (`_VALID_KWARGS`, `_build`, `_collect_imported_modules_from_subpackage_source`); no helper is exported.
- A narrowly scoped extension of `tests/test_package_skeleton.py` by exactly one entry — `signal_intake` — added to the authorized phase-package set with the matching docstring entry naming PR P6-01 and this authorization artifact. The test's purpose as a phase-expansion guard is preserved; the assertion is not weakened; the hardcoded set is not converted into a looser dynamic allowlist.
- A narrowly scoped extension of the `MASTER_STATUS.md` §8 step 4a `allowed_p2_infra` bash gate by exactly one entry — `src/gmc_rebuild/signal_intake` — added in the same PR that introduces the directory. The `for path in ...` loop is extended by the same entry, a `src/gmc_rebuild/signal_intake) pr_tag="PR P6-01" ;;` case is added, the comment block above the bash gate is extended with the matching PR P6-01 entry referencing this authorization artifact, and the closing OK echo lists `P6-01` in the conformance message. No other §8 step 4a allowlist entry is added.
- A conservative `MASTER_STATUS.md` §1 status reflection paragraph titled "P6-01 implementation status (governance/documentation reflection, 2026-05-19 UTC — pending merge in this open P6-01 implementation PR)" describing the new signal-intake subpackage, the new test file, the package-skeleton-test extension, and the §8 step 4a allowlist extension, and explicitly listing the categories the packet does **not** open. The conservative wording is scoped to this open PR only; the merged P5-01..P5-07 / P5-07 planning / P5-06 planning / Phase 5 entry planning / GOV-01 / GOV-02 / OPS-01..OPS-04B / OPS-06 status reflections continue to read **merged on `main`** and are not reverted to stale `**pending merge**` language by this packet; the canonical reconciliations merged via PR #114, PR #118, PR #122, PR #126, PR #130, PR #134, and PR #138 are preserved unchanged.
- This authorization artifact at `governance/authorizations/2026-05-19_p6-01.md`.

No file under `src/**` other than the new `src/gmc_rebuild/signal_intake/` subpackage is added or modified. No existing test file under `tests/**` other than the narrowly scoped one-entry extension to `tests/test_package_skeleton.py` is modified. No file under `monitoring/**`, `docs/**`, `plan/**`, `governance/authorizations/` other than this new P6-01 artifact, `pyproject.toml`, `.pre-commit-config.yaml`, `.secrets.baseline`, `.gitignore`, `README.md`, `RECOVERY.md`, `AI_WORKFLOW.md`, any ADR under `docs/decisions/`, `EXTERNAL_REVIEW_BRIEF.md`, or any other governance / planning document is added or modified. The merged P4-06 / P4-07 / P4-08 safety foundation, P3-03 / P3-04 / P3-05 in-memory fakes, P2-01..P2-05 packages, OPS-01..OPS-04B / OPS-06 operations records, P5-01..P5-07 simulation surface and tripwire tests, GOV-01 governance reconciliation, GOV-02 execution-environment workflow correction, the foundation-complete confirmation report (`a92568e`) and its 1-FU-A (`ce010cb`) / 1-FU-B (`8dc7a2d`) follow-ups, and the merged Phase 6 entry plan (`cd1f78b`) are all preserved unchanged.

## Explicitly Not Authorized

This authorization does not permit:

- Any `src/**` change other than the new `src/gmc_rebuild/signal_intake/` subpackage with its two files (`__init__.py` and `_intent.py`); no existing `src/**` file is modified.
- Any modification of any existing test file under `tests/**` other than the narrowly scoped one-entry extension to `tests/test_package_skeleton.py` per the authorized narrow scope adjustment. The test's purpose as a phase-expansion guard is preserved; the assertion is not weakened; the hardcoded set is not converted into a looser dynamic allowlist.
- Any new public symbol on `gmc_rebuild` (the package root); no re-export from `src/gmc_rebuild/__init__.py`. The new `SignalIntent` / `SignalSide` / `accept_signal_intent` surface is reachable only via `from gmc_rebuild.signal_intake import ...`.
- Any expansion of the closed two-member `SignalSide` enumeration. No `SELL_SHORT`, `BUY_TO_COVER`, `EXIT`, `FLATTEN`, or any other member.
- Any expansion of the closed five-field `SignalIntent` dataclass. No sixth field, no removal or rename of any of the five named fields, no field type change.
- Any new method on `SignalIntent` beyond the auto-generated dataclass methods. No `validate()` helper, no `to_dict()`, no `from_dict()`, no `with_quantity()`, no `with_side()`, no `replace()` shim — callers wanting a value-replace operation use `dataclasses.replace` from stdlib directly.
- Any new function in the `gmc_rebuild.signal_intake` subpackage beyond `accept_signal_intent`. No factory function, no validator function exposed as a separate entry point, no helper.
- Any strategy logic. The boundary accepts an already-structured candidate trade idea; it does not generate signals, does not scan markets, does not produce trade ideas, does not score, rank, or filter candidates.
- Any scanner, model, portfolio, backtest, alpha factor, parameter sweep, regime classifier, market-microstructure analysis, or other algorithmic-trading logic.
- Any broker integration (real or paper), broker SDK, broker account identifier, broker session, broker handshake, broker credential, or broker reference of any kind.
- Any live, delayed, or paper market data, market-data feed, vendor SDK, real symbol universe, real venue identifier, or normalization to any vendor symbol format. The `symbol` field is a free-form `str` for callers to populate; no validation against any external universe is performed.
- Any real or paper order placement, order routing, order management, order book, fill, execution, position, P&L, trade report, OMS, EMS, routing instruction, time-in-force, or execution adapter. A `SignalIntent` is upstream of any order.
- Any FIX session, REST client, WebSocket client, or any other connectivity surface.
- Any venue, account, broker credential, routing field, exchange field, or any other order-execution field on `SignalIntent` (the closed-five-field tripwire is preserved).
- Any secrets, credentials, API keys, broker keys, session tokens, OAuth tokens, SSH keys, `.env` files containing live credentials, recovery codes, PEKs, or any other credential storage.
- Any external API call, network call, `socket`, `urllib`, `requests`, `http`, `ssl`, `smtplib`, `ftplib`, DNS lookup, or any other outbound or inbound network surface.
- Any scheduler, daemon, background thread, long-running service, `__main__` entry point, `if __name__` block, `time.sleep`, `asyncio.sleep`, cron job, launchd plist, or any other timed / background execution mechanism.
- Any persistence, database, SQLite, pickle, shelve, filesystem write, caching to disk, append-only ledger, log file, audit trail, or any other persistence surface. The `accept_signal_intent` function does not log, emit, or record acceptance.
- Any runtime activation of any merged Phase 3 fixture (`InMemoryHeartbeat`, `InMemoryKillSwitch`, `InMemoryReconciliation`), any P4-06 / P4-07 / P4-08 safety surface (`RuntimeShell`, `SafetyVerdict`, `OperatorSafetyView`, `format_safety_verdict`), or any P5-01 / P5-02 simulation surface (`SimulationBoundary`, `SimulatedIntent`, `SimulatedOrderIntent`). The P6-01 surface stands alone and does not import any other `gmc_rebuild` subpackage.
- Any concrete implementation of `HeartbeatProtocol`, `KillSwitchProtocol`, or `ReconciliationProtocol` outside the merged P3-03 / P3-04 / P3-05 in-memory fakes.
- Any modification of the merged P4-06 / P4-07 / P4-08 safety foundation. P6-01 does not consume, compose, or wrap any safety surface; that integration is a future P6-03 / P6-0N concern.
- Any modification of the merged P3-03 / P3-04 / P3-05 in-memory fakes. P6-01 does not import them.
- Any modification of the merged P5-01..P5-07 simulation surface and tripwire tests. P6-01 does not import the simulation surface.
- Any modification of backup / recovery docs (`RECOVERY.md`, the OPS-01..OPS-04B / OPS-06 authorization artifacts), `AI_WORKFLOW.md`, any ADR under `docs/decisions/`, any other `governance/authorizations/*.md` file, `pyproject.toml`, `.pre-commit-config.yaml`, `.secrets.baseline`, `.gitignore`, `README.md`, `EXTERNAL_REVIEW_BRIEF.md`, any file under `docs/**` (including the GMC Project Map, the Foundation-to-Dry-Run Roadmap, and the Foundation-Complete Confirmation Report), or any file under `plan/**` (including the Phase 6 entry plan merged at commit `cd1f78b`).
- Any modification of any existing `monitoring/daily/*` packet.
- Any extension of the `MASTER_STATUS.md` §8 step 4a `allowed_p2_infra` bash gate beyond the exactly one new entry `src/gmc_rebuild/signal_intake`. No other allowlist entry is added.
- Any extension of the hard-coded `entries == {...}` set in `tests/test_package_skeleton.py` beyond the exactly one new entry `signal_intake`. The set is closed; any future P6-0N packet that introduces a new `src/gmc_rebuild/` subdirectory must extend it by exactly one entry in the PR that introduces the directory, per the precedent established by P3-03 / P3-04 / P3-05 / P4-06 / P5-01 / P6-01.
- Any quality-gate relaxation, hook removal, hook reconfiguration, mypy strictness reduction, ruff rule relaxation, or `detect-secrets` baseline change.
- Any X10 Layer 5 promotion, backup-monitoring automation, DR drill execution, or other ops execution work.
- Any tag or release.
- Any successor P6 packet. P6-02 (eligibility-check pure functions), P6-03 (composed position / risk decision), P6-04 (simulated-order-intent extension), P6-05 (in-memory simulated portfolio state), P6-06 (deterministic daily report), P6-07 (failure-handling tripwires), and any later P6-0N task remain **future / not authorized** per `plan/phase6_entry_plan.md` §4. Each requires its own separate written authorization from Kevin per `AI_WORKFLOW.md` §7.
- Any reintroduction of stale `**pending merge**` language for any already-merged P5-01 / P5-02 / P5-03 / P5-04 / P5-05 / P5-06 / P5-07 / P5-06 planning / P5-07 planning / Phase 5 entry planning / GOV-01 / GOV-02 / OPS-01..OPS-04B / OPS-06 packet, or for the merged foundation-complete confirmation report and its 1-FU-A / 1-FU-B follow-ups, or for the merged Phase 6 entry plan. The canonical reconciliations merged via PR #114, PR #118, PR #122, PR #126, PR #130, PR #134, and PR #138 are preserved by this packet; any "pending merge" wording in the PR is scoped exclusively to this open P6-01 implementation PR itself.
- Any reinterpretation or relaxation of the GOV-02 execution-environment workflow rule (merged via PR #132 / reconciled via PR #134). Local Claude Code / Claude Max remains the default builder for `gmc-rebuild` repo edits; Perplexity-managed codebase subagents that consume Perplexity credits must not perform repo edits absent Kevin's explicit written authorization for the specific packet; Perplexity Computer's `AI_WORKFLOW.md` §1.2 Supervisor / Verifier / Status Keeper role and read-only `gh` / `git` metadata-check role are preserved unchanged.

The always-forbidden categories in `MASTER_STATUS.md` §6, `plan/phase4_entry_plan.md` §10, `plan/phase5_entry_plan.md` §10, and `plan/phase6_entry_plan.md` §10 — strategy code, broker execution code, paper-trading or live-trading wired to a real broker, runtime daemons affecting accounts, real market data ingestion, order placement, and real secrets — remain forbidden in all modes. This packet does not relax them and does not record any relaxation.

## Required Validation

Before merge, the implementation PR must pass:

- `.venv/bin/python -m pytest -q` against the full test suite (must pass; the post-P5-07 / post-foundation-complete baseline of 411 tests must be preserved, plus the 32 new P6-01 signal-intake tests, for a total of 443 passing tests deterministically).
- `.venv/bin/pre-commit run --all-files` (must exit 0 with every hook passing and no "files were modified by this hook" second-pass message).
- The `MASTER_STATUS.md` §8 startup verification commands relevant to the §8 step 4a allowlist must pass: the new `src/gmc_rebuild/signal_intake` path must report `OK: Phase 2/3/4/5/6 infrastructure present and authorized: src/gmc_rebuild/signal_intake (PR P6-01)`, and the closing OK echo must read `OK: Phase 2/3/4/5/6 infrastructure paths conform to P2-01/P2-02/P2-03/P2-04/P2-05/P3-03/P3-04/P3-05/P4-06/P5-01/P6-01 allowlist`.
- `git diff --name-status main` showing changes confined to: this authorization artifact, the new `src/gmc_rebuild/signal_intake/__init__.py`, the new `src/gmc_rebuild/signal_intake/_intent.py`, the new `tests/signal_intake/test_signal_intake.py`, the narrowly scoped one-entry extension of `tests/test_package_skeleton.py`, and the narrowly scoped `MASTER_STATUS.md` updates (§8 step 4a allowlist extension and §1 conservative status reflection paragraph). No file under any other `src/**` path is added or modified; no existing `tests/**` file other than `tests/test_package_skeleton.py` is modified; no file under `docs/**`, `plan/**`, `monitoring/**`, `governance/authorizations/` other than this new P6-01 artifact, `pyproject.toml`, `.pre-commit-config.yaml`, `.secrets.baseline`, `.gitignore`, `README.md`, `RECOVERY.md`, `AI_WORKFLOW.md`, `EXTERNAL_REVIEW_BRIEF.md`, or any other governance / planning document is added or modified.
- Stale-phrase grep (`grep -nE "pending merge" MASTER_STATUS.md README.md RECOVERY.md plan/phase4_entry_plan.md plan/phase5_entry_plan.md plan/phase6_entry_plan.md`) showing that any remaining `pending merge` hits are either intentional historical / self-check / anti-pattern residuals or scoped to the open P6-01 implementation PR itself only, with no current-state stale claim on any merged work.

Mode A / Mode B review requirements remain governed by `AI_WORKFLOW.md`, ADR-008, ADR-009, and the current `MASTER_STATUS.md`. P6-01 implementation introduces a new `src/**` subpackage and is therefore a new control-surface decision; Mode A adversarial review is **independently required** per `AI_WORKFLOW.md` §4(2) and ADR-008 §D7, mirroring the P5-01 first-Phase-5-implementation precedent, and must be delivered as PR-review text on the P6-01 implementation PR only, not committed to the repository (per `AI_WORKFLOW.md` §6 rule 5). Mode B sibling monitoring follows ADR-008 §D3 / §D5 (and ADR-009 D3 / D5 once in force per ADR-009 D7's bootstrap-avoidance clause); per ADR-008 §D5, the Mode B sibling monitoring packet for the active workday must be authored from `main` and merged to `main` **before** this implementation PR per the verbatim authorization above.

Phase 2 remains formally closed. Phase 3 remains formally closed. Phase 4 remains open as a governance state with the safety foundation closure recorded by P4-08 preserved. Phase 5 is closed at the merged P5-01..P5-07 scope. Phase 6 is opened at the P6-01 typed signal-intake boundary scope by this authorization. The foundation-complete confirmation report and its 1-FU-A / 1-FU-B follow-ups are preserved. The merged Phase 6 entry plan is preserved. The GOV-02 execution-environment workflow rule is in force. P6-02 (and any later P6-0N task) remains **future / not authorized**.
