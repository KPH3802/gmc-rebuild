# P4-02 Enumeration Planning — Documentation-Only Update to `plan/phase4_entry_plan.md` §4 To Enumerate The Next Candidate Phase 4 Task

Date: 2026-05-14
Authorizer: Kevin
Scope: Documentation-only planning update to `plan/phase4_entry_plan.md` to **enumerate** the next candidate Phase 4 task (`P4-02`) as **future / not authorized** — governance / documentation scope (**does not open P4-02, does not authorize any Phase 4 implementation, does not authorize any runtime activation of any merged Phase 3 fixture, does not extend the `MASTER_STATUS.md` §8 step 4a `allowed_p2_infra` allowlist, does not add or modify any file under `src/**` or `tests/**`, does not authorize any broker / market-data / order / strategy / scheduler / persistence / deployment / env-var / secrets / network change, does not relax any quality gate, and does not create any tag or release**).

## Verbatim Written Authorization From Kevin (Authorization of Record)

The text in quotation marks immediately below is the verbatim written authorization Kevin provided on 2026-05-14 to author this documentation-only planning update. It is reproduced verbatim in this artifact, in its entirety, as the durable in-tree authorization of record per `AI_WORKFLOW.md` §7 ("durable authorization artifacts") and `MASTER_STATUS.md` §7 ("Phase 2 Boundary" / written-authorization requirement extended by precedent to every later phase-scoped authorization). Nothing in this authorization artifact may be read as relaxing, broadening, or substituting any constraint stated in the verbatim text below:

> "I, Kevin Heaney, authorize a planning-only update to plan/phase4_entry_plan.md to enumerate the next candidate Phase 4 task, P4-02, without authorizing implementation. This authorization does not permit src/** or tests/** changes, runtime activation, broker, market-data, order, strategy, scheduler, persistence, deployment, secrets, network, env-var, allowlist expansion, or quality-gate relaxation. A sibling Mode B packet is required before merge."

The verbatim authorization above is the authoritative source for the scope, the explicit non-goals, and the required monitoring protocol for this PR. Where this artifact restates that scope or those non-goals in prose, the restatement is for audit clarity and is **not** a re-authorization, an expansion, or a relaxation of any constraint stated in the verbatim text. If a conflict appears between the verbatim text and any restatement in this artifact, the verbatim text wins.

## Authorization (Restated)

This artifact, anchored by the verbatim authorization above, authorizes a **documentation-only planning update** to `plan/phase4_entry_plan.md` that **enumerates** the next candidate Phase 4 task (`P4-02`) as **future / not authorized**, in the same shape that `plan/phase4_entry_plan.md` §4 item 1 already enumerated `P4-01` as a candidate task before `P4-01` was authorized under `governance/authorizations/2026-05-14_p4-01.md`. The enumeration is the **planning-only follow-up** that the Phase 4 entry planning authorization at `governance/authorizations/2026-05-14_phase-4-entry-planning.md` "Allowed Planning Topics" item 2 ("Proposed Phase 4 task sequence (`P4-01`, `P4-02`, …) named only as **future** PRs that would each require their own separate written authorization slice and sibling artifact under `governance/authorizations/`. Naming a task in the plan does not authorize it.") explicitly contemplates, and that `plan/phase4_entry_plan.md` §4 itself anticipates ("Any later enumeration of P4-02 / P4-03 / … must be authorized as a documentation-only update to this plan and must not authorize implementation.").

This authorization is intentionally **narrower** than the Phase 4 entry gate authorization at `governance/authorizations/2026-05-14_p4-01.md`. It does **not** open Phase 4 (already opened as a governance state by P4-01 at PR #64), it does **not** authorize `P4-02` itself, it does **not** authorize any Phase 4 implementation, it does **not** authorize any runtime activation of any merged Phase 3 fixture, it does **not** extend the `MASTER_STATUS.md` §8 step 4a `allowed_p2_infra` allowlist, and it does **not** add any directory or path to the working tree under `src/**` or `tests/**`.

This authorization is the **planning-enumeration analogue** to (and is intentionally narrower than) the prior planning slices used at earlier stages of Phase 3:

- `governance/authorizations/2026-05-13_phase-3-entry-planning.md` (Phase 3 entry planning).
- `governance/authorizations/2026-05-13_p3-02-preparation.md` (preparation prose for the Phase 3 entry decision).
- `governance/authorizations/2026-05-13_p3-03-planning.md` (planning prose for the first Phase 3 implementation slice).
- `governance/authorizations/2026-05-14_phase-4-entry-planning.md` (Phase 4 entry planning, opening this very workstream).

It is **not** itself the P4-02 entry / P4-02 implementation authorization. The future P4-02 authorization — whenever, if ever, Kevin chooses to write it — will be a separate sibling artifact under `governance/authorizations/` and is **not** satisfied by this enumeration.

## Scope

The work authorized by this artifact is restricted to:

- **This authorization artifact** at `governance/authorizations/2026-05-14_p4-02-enumeration-planning.md`. The artifact records the verbatim Kevin authorization above and the exact scope / exclusions in this section.
- **A narrow, planning-only enumeration update inside `plan/phase4_entry_plan.md` §4** ("Candidate Future Phase 4 Task Sequence (Future / Not Authorized)") adding `P4-02` as the **next** candidate task in the same "(future / not authorized)" shape that the existing `P4-01` entry used before P4-01 was authorized. The enumeration:
  - Names `P4-02` only as a **future / not-authorized** candidate task. It does **not** name a specific protocol, a specific directory, or any `src/**` / `tests/**` path.
  - Preserves the §4 sequencing rules verbatim: tasks land one at a time, each PR is small enough that the proof bundle fits a single review, scope-creep is split or stopped, P4-01 must land before any later Phase 4 task (already satisfied — P4-01 merged at `9f8bd92` per PR #64).
  - Preserves the §4 "§8 step 4a allowlist note" verbatim. The allowlist remains exactly eight entries.
  - Preserves the §4 stopping clause ("This plan therefore stops at naming the entry-decision PR (P4-01) and explicitly does **not** pre-commit any successor task.") by adjusting only its scope (from "stops at P4-01" to "stops at P4-02") and explicitly noting that the enumeration of P4-02 does **not** pre-commit P4-03 or any later successor task.
- **A narrow `plan/phase4_entry_plan.md` §1 "Current Status" status note** recording that the P4-02 candidate task has been enumerated under this artifact, restating that P4-02 implementation remains **future / not authorized**, restating that the `MASTER_STATUS.md` §8 step 4a allowlist is unchanged at exactly eight entries, and preserving every other §1 status bullet verbatim.
- **A narrow `MASTER_STATUS.md` §1 / §9 conservative governance/status reflection** recording that P4-02 has been enumerated as a candidate task in `plan/phase4_entry_plan.md` §4 under this artifact and that P4-02 implementation remains future / not authorized. Per `AI_WORKFLOW.md` §1.2 and §6 rule 2 ("One status keeper"), this `MASTER_STATUS.md` edit is conservative and remains subject to Perplexity Computer's verification before being treated as the canonical status; the canonical post-merge status reconciliation may be conducted as a separate post-merge PR on the established pattern (e.g. PR #58 / PR #62 / PR #66) if Kevin or Perplexity Computer judges it useful, but is not authorized by this artifact.
- **A narrow `README.md` "Current Phase" cross-reference update** naming this enumeration artifact alongside the existing Phase 4 entry planning, P4-01 entry gate, and Phase 3 references. The update records that P4-02 has been enumerated as a candidate task and that P4-02 implementation remains future / not authorized. No quality-gate or policy text is changed.
- **No other file is modified.** In particular: no file under `src/**`, no file under `tests/**`, no ADR text, no `AI_WORKFLOW.md`, no `.pre-commit-config.yaml`, no `pyproject.toml`, no `.gitignore`, no `.secrets.baseline`, no template, no test, no other `governance/authorizations/*` file, no `monitoring/daily/` packet (the required sibling Mode B packet for the active workday on which this PR is open / merged is a separate sibling PR per ADR-008 §D5 — this PR does **not** bundle it).

The enumeration artifact, by content, must:

1. Be explicitly **planning-only** and explicitly **not authorize implementation**. It is **not** the P4-02 entry / P4-02 implementation PR. It is the written draft of the candidate-task-sequence entry that any future P4-02 authorization would have to descend from.
2. Preserve the existing phase boundary unchanged. Phase 4 remains **open as a governance state only** (per the P4-01 entry gate merged at `9f8bd92`); no Phase 4 implementation is authorized. The §8 step 4a `allowed_p2_infra` allowlist remains exactly the eight P2-01..P2-05 + P3-03 + P3-04 + P3-05 entries (`src`, `src/gmc_rebuild/config`, `src/gmc_rebuild/time`, `src/gmc_rebuild/logging`, `src/gmc_rebuild/risk`, `src/gmc_rebuild/heartbeat`, `src/gmc_rebuild/kill_switch`, `src/gmc_rebuild/reconciliation`).
3. Enumerate `P4-02` only as **future / not-authorized**. The enumeration may name P4-02 **at the candidate-task level**, restating that the **shape** of any specific P4-02 task — whether it is the first composed-fixture / runtime-bootstrapping / ADR-driven follow-up task, or a different shape entirely — remains a future, separate written authorization decision per `plan/phase4_entry_plan.md` §4. The enumeration does **not** name a specific protocol, a specific directory, a specific scope inside `src/**` / `tests/**`, or a specific implementation pattern; per the existing §4 stopping clause, the shape of Phase 4 is itself a decision Kevin must make in writing at the time of the Phase 4 implementation authorization.
4. Make explicit that any future P4-02 PR is a **separate** PR, with a **separate** written Kevin authorization, a **separate** Mode A critique (Mode A is independently mandated by `AI_WORKFLOW.md` §4(2) for the first concrete Phase 4 behaviour behind a P2-05 control surface, and may additionally be required under §4(1) / §4(3) depending on what P4-02 is when authorized), and a **separate** sibling Mode B monitoring packet per ADR-008 §D3 / §D5 (or ADR-009 D3 / D5 once in force per ADR-009 D7's bootstrap-avoidance clause) committed and merged to `main` before the P4-02 PR merges per ADR-008 §D5 (or ADR-009 D5 once in force).

The enumeration artifact must **not**, by content:

- Authorize P4-02 implementation.
- Authorize any Phase 4 implementation.
- Authorize any task beyond enumerating P4-02 as a candidate task.
- Authorize runtime work, broker integration, market-data integration, order management, strategy logic, scheduling, persistence, deployment, env-var loading, any concrete implementation of `KillSwitchProtocol` / `ReconciliationProtocol` / `HeartbeatProtocol`, or any runtime activation of any merged Phase 3 fixture.
- Extend the `MASTER_STATUS.md` §8 step 4a allowlist.
- Modify any quality gate, secrets baseline, or always-forbidden category.
- Create any git tag, GitHub release, or version bump.
- Embed real secrets, real credentials, real account identifiers, real broker / venue identifiers, or real endpoints.
- Add or modify any file under `src/**` or `tests/**`.
- Bundle the sibling Mode B monitoring packet for this PR's active workday (any required Mode B packet is a separate sibling PR per ADR-008 §D5).

## Explicitly Not Authorized

The authorization above does not permit, by implication or otherwise, any of the following. Each remains forbidden until Kevin records a separate written authorization per `MASTER_STATUS.md` §7 and `AI_WORKFLOW.md` §6 rule 3 ("One approver") / rule 7 ("No phase drift"), with a sibling artifact under `governance/authorizations/` and (where it introduces a new directory) a corresponding update to the §8 step 4a allowlist in the same PR that introduces the directory. The mirroring below is intentional and is **not** a re-authorization of any category: those categories remain forbidden by default, and naming them here records that Kevin did not silently relax them when authorizing this planning-only enumeration:

- **P4-02 implementation or any task beyond P4-02 enumeration.** P4-02 implementation remains **future / not authorized**. Opening P4-02 requires a separate written authorization from Kevin, a sibling artifact under `governance/authorizations/`, an §8 step 4a allowlist update in the same PR that introduces any new directory, Mode A adversarial review per `AI_WORKFLOW.md` §4(2) (high-risk architecture decision — first concrete Phase 4 behaviour behind a P2-05 control surface, if applicable) and / or §4(1) / §4(3) as applicable, and a Mode B monitoring packet per ADR-008 §D3 / §D5 (governance phase) or, once runtime exists on `main`, per ADR-009 D3 / D5 (runtime phase). The verbatim authorization above explicitly excludes "implementation."
- **P4-03 or any task beyond P4-02 in the candidate-task sequence.** This artifact enumerates **only** P4-02. It does **not** enumerate `P4-03`, `P4-04`, or any later candidate task. Any later enumeration beyond P4-02 must itself be authorized as a separate documentation-only update to `plan/phase4_entry_plan.md` under a separate sibling authorization artifact.
- **Any Phase 4 implementation of any kind.** This PR adds no runtime, no concrete implementation of `KillSwitchProtocol`, `ReconciliationProtocol`, or `HeartbeatProtocol`, no test fixture exercising those protocols beyond what is already merged under P3-03 / P3-04 / P3-05, no `__main__` entry point, no daemon, no scheduler, no background worker, no long-running service, no live execution loop.
- **Runtime activation of any merged Phase 3 fixture.** The `InMemoryHeartbeat` (P3-03), `InMemoryKillSwitch` (P3-04), and `InMemoryReconciliation` (P3-05) fakes remain test-fixture infrastructure only. No re-export from `src/gmc_rebuild/__init__.py`, no consumption from a `__main__`, no consumption from a daemon, no consumption from a scheduler, no consumption from any runtime path is authorized. The verbatim authorization above explicitly excludes "runtime activation."
- **Source / test changes.** No file under `src/**` or `tests/**` is added or modified by this PR. The verbatim authorization above explicitly excludes "src/**" and "tests/**" changes.
- **Runtime work of any kind.** No `__main__` entry point, no daemon, no scheduler, no background worker, no long-running service, no live execution loop. The verbatim authorization above explicitly excludes "runtime activation" and "scheduler."
- **Broker integration.** No broker SDK, no broker connector, no broker reconciliation runtime, no broker authentication, no broker credentials. The verbatim authorization above explicitly excludes "broker."
- **Market-data integration.** No market-data ingestion code, no market-data pipeline, no committed dataset, no live or historical data feed. The verbatim authorization above explicitly excludes "market-data."
- **Order management.** No order objects, no order placement, no position management, no fills, no trade reports, no executions, no order book. The verbatim authorization above explicitly excludes "order."
- **Strategy logic.** No trading signals, no scanners, no models, no portfolio rules, no backtests. The verbatim authorization above explicitly excludes "strategy."
- **Scheduling.** No cron-style scheduler, no APScheduler, no background-job framework, no operator-heartbeat daemon, no kill-switch runtime, no reconciliation runtime, no monitoring daemon, no notification daemon. The verbatim authorization above explicitly excludes "scheduler."
- **Persistence.** No SQLite, no on-disk database, no on-disk reconciliation snapshot, no on-disk heartbeat state, no log sink, no file artifact written by runtime code. The verbatim authorization above explicitly excludes "persistence."
- **Deployment.** No deployment workflow, no rollout, no rollback runtime, no CI/CD pipeline beyond the existing local pre-commit and pytest gates. The verbatim authorization above explicitly excludes "deployment."
- **Secrets / env-var loading.** No real secrets, no real credentials, no `.env` files, no `os.environ` / `os.getenv` reads, no real account identifiers, no real broker / venue identifiers, no real endpoints embedded anywhere in the repository under this PR. ADR-001's secrets-management discipline is not relaxed. The verbatim authorization above explicitly excludes "secrets" and "env-var."
- **Network.** No `socket`, no `urllib`, no `requests`, no HTTP client, no outbound or inbound network code. The verbatim authorization above explicitly excludes "network."
- **Concrete risk implementations inside the runtime package.** No concrete implementation of `KillSwitchProtocol` / `ReconciliationProtocol` / `HeartbeatProtocol` lands inside `src/gmc_rebuild/risk/` under this PR. The P2-05 boundary (`src/gmc_rebuild/risk/` is types and abstract `typing.Protocol` definitions only) is preserved, and the three merged P3-03 / P3-04 / P3-05 fakes remain test-fixture-only outside `src/gmc_rebuild/risk/`.
- **New trading behavior.** No change that introduces, simulates, or wires up trading behavior — in any module, under any name, anywhere in the tree.
- **Tooling relaxation.** No weakening of pre-commit, Ruff, mypy strict mode, `detect-secrets`, `.gitignore`, `.secrets.baseline`, the `MASTER_STATUS.md` §8 step 4 always-forbidden scan, the §8 step 4a per-PR Phase 2 / Phase 3 infrastructure allowlist scan, or the §8 step 4c recursive forbidden-token scan. The verbatim authorization above explicitly excludes "quality-gate relaxation."
- **§8 step 4a allowlist expansion.** The `allowed_p2_infra` allowlist is **not** extended by this PR. It continues to contain exactly the eight entries currently on `main` after the merged P2-01..P2-05 + P3-03 + P3-04 + P3-05 sequence (`src`, `src/gmc_rebuild/config`, `src/gmc_rebuild/time`, `src/gmc_rebuild/logging`, `src/gmc_rebuild/risk`, `src/gmc_rebuild/heartbeat`, `src/gmc_rebuild/kill_switch`, `src/gmc_rebuild/reconciliation`). Any Phase 4 directory introduced by any future P4-02 PR requires a separate written authorization that introduces the directory in the same PR that adds it to the allowlist (per the standing rule in `MASTER_STATUS.md` §8 step 4b). The verbatim authorization above explicitly excludes "allowlist expansion."
- **New tags or releases.** No git tag, no GitHub release, no version bump.
- **Live-trading entry gate triggering.** ADR-009 D5 records the implementation evidence / deferred-with-citation evidence requirement for any future PR that introduces a live-trading entry point. This planning PR does **not** introduce a live-trading entry point, and the future P4-02 PR is explicitly scoped (above) so it cannot introduce one without a separate, distinct written authorization.
- **Mode A / Mode B substitution.** This authorization is governance prose, not a control-surface decision; it does not substitute for adversarial review of any future P4-02 PR or of any future runtime / broker / market-data / order / strategy / scheduler / persistence / deployment PR. Any such future PR must independently satisfy `AI_WORKFLOW.md` §4 (Mode A) and ADR-008 §D3 / §D5 (or ADR-009 D3 / D5 once in force) (Mode B) as applicable.
- **Mode B monitoring packet bundling.** Any required Mode B packet for the workday on which this planning PR is open / merged is a separate sibling PR per ADR-008 §D5. This PR does **not** bundle it. The verbatim authorization above explicitly requires that "a sibling Mode B packet is required before merge."

The always-forbidden categories in `MASTER_STATUS.md` §6 and `plan/phase4_entry_plan.md` §10 — strategy code, broker execution code, live or paper trading wired to a real broker, runtime daemons affecting accounts, real market data ingestion, order placement, and real secrets — remain forbidden in all modes. This planning artifact does not relax them.

## Mode A Status For This Authorization

This authorizing PR is a **planning-only documentation update** to `plan/phase4_entry_plan.md` §4 that enumerates `P4-02` as **future / not authorized**. Per `AI_WORKFLOW.md` §4 and the precedent set by `governance/authorizations/2026-05-14_phase-4-entry-planning.md` (Phase 4 entry planning, which was treated under `AI_WORKFLOW.md` §4's routine-exclusion sentence and merged in PR #60 without independently mandated Mode A) and by `plan/phase4_entry_plan.md` §4 itself ("Any later enumeration of P4-02 / P4-03 / … must be authorized as a documentation-only update to this plan and must not authorize implementation."), Mode A adversarial review is **not independently mandated** for this planning-only enumeration:

- Per `AI_WORKFLOW.md` §4(1) — this is **not** a phase gate. Phase 4 is already opened as a governance state by P4-01 at `9f8bd92`; this PR does not open a phase, does not close a phase, and does not flip Phase 4 from "governance state" to any other state.
- Per `AI_WORKFLOW.md` §4(2) — this is **not** a high-risk architecture decision. The enumeration adds a candidate-task label to the plan; it does not introduce a control surface, a trust boundary, or a non-reversible decision. The Mode A review of the future P4-02 PR itself remains a separate, future requirement.
- Per `AI_WORKFLOW.md` §4(3) — this is **not** a decision that, if wrong, causes real-world loss. The PR adds prose to a planning document; reverting it is a single `git revert` on the merge commit.
- Per `AI_WORKFLOW.md` §4's routine-exclusion sentence — the PR is in the same routine documentation / planning class as `governance/authorizations/2026-05-14_phase-4-entry-planning.md` (PR #60), `governance/authorizations/2026-05-14_phase-3-closure.md` Mode B sibling pattern, and the planning prose in `plan/phase4_entry_plan.md` itself.

Because this authorization is **phase-gate-adjacent** (it shapes the candidate-task list from which any future P4-02 entry decision will descend), Kevin may **elect** to invoke Mode A on this PR. If Kevin elects to do so, the Mode A critique is delivered as PR-review text and is **not** committed to the repository, per `AI_WORKFLOW.md` §6 rule 5 / ADR-008 §D7. The Mode A status for this PR is therefore: **maintainer-elected only; not independently mandated.** Mode A on the future P4-02 PR remains a **separate, independent** requirement and is **not** satisfied by any Mode A review (elected or otherwise) on this enumeration PR.

## Required Monitoring (Mode B)

Per ADR-008 §D3 / §D5, this enumeration PR is itself an active-workday event on 2026-05-14 (a pull request open / updated / merged on that UTC date). Per ADR-008 §D4 "same-day naming", multiple slugged packets already exist on `main` for 2026-05-14 (covering PR #48 / PR #50 / PR #52 / PR #54 / PR #56 / PR #58 / PR #60 / PR #62 / PR #64 / PR #66). The authorizing PR for this enumeration is therefore a further distinct active-workday event on the same UTC date and requires a **slugged additional packet** at `monitoring/daily/2026-05-14_pr<N>.md` where `<N>` is this PR number (or a similar audit-visible slug naming this PR / P4-02 enumeration — e.g. `monitoring/daily/2026-05-14_p4-02-enumeration-planning.md`).

Per ADR-008 §D5 the slugged additional packet must be **committed and merged to `main` before** this enumeration PR merges. The packet is filed as a **separate sibling monitoring PR** per the established precedent (PR #57 → PR #56 for Phase 3 closure; PR #61 → PR #60 for Phase 4 entry planning; PR #65 → PR #64 for P4-01 entry gate; PR #67 → PR #66 for the post-merge reconciliation); it is **not** bundled into the authorizing PR. Perplexity Computer confirms this sequencing before verifying the authorizing PR for Kevin's review.

The Mode B packet must reflect that this PR's content is restricted to: (a) this authorization artifact at `governance/authorizations/2026-05-14_p4-02-enumeration-planning.md`, (b) the narrow `plan/phase4_entry_plan.md` §4 enumeration update adding `P4-02` as a candidate task (future / not authorized) and the matching §1 status note, (c) the conservative `MASTER_STATUS.md` §1 / §9 reflection of P4-02 enumeration in progress, and (d) the narrow `README.md` "Current Phase" cross-reference update. It must reflect that Phase 4 remains open as a governance state only (no Phase 4 implementation authorized), that P4-02 implementation remains future / not authorized, that the §8 step 4a allowlist is unchanged at exactly eight entries, that no `src/**` or `tests/**` file is modified, and that the always-forbidden categories in `MASTER_STATUS.md` §6 remain absent.

The verbatim authorization above explicitly acknowledges the sibling Mode B requirement: "A sibling Mode B packet is required before merge."

## Preserved Gates for Any Future P4-02 / Phase 4 Implementation Work

Any work beyond this enumeration — opening P4-02 itself, opening any further Phase 4 implementation task, any runtime activation of any merged Phase 3 fixture, or any runtime / broker / market-data / order / strategy / scheduler / persistence / deployment work — requires **all** of the following, none of which is satisfied by this enumeration:

1. **Separate written authorization from Kevin** per `MASTER_STATUS.md` §7 and `AI_WORKFLOW.md` §6 rule 3 / rule 7, with a sibling artifact under `governance/authorizations/`.
2. **Mode A adversarial review** per `AI_WORKFLOW.md` §4. Any first concrete behaviour behind a P2-05 control surface is a high-risk architecture decision under §4(2); any first runtime activation of any merged Phase 3 fixture is a new control surface under §4(2); any decision that, if wrong, would cause real-world loss is under §4(3) and is also out of Phase 4 scope per `plan/phase4_entry_plan.md` §2.
3. **Mode B monitoring packet** per ADR-008 §D3 / §D5 (governance phase) or, once runtime exists on `main`, per ADR-009 D3 / D5 (runtime phase). Before-merge sequencing per ADR-008 §D5 (or ADR-009 D5 once in force) applies.
4. **§8 step 4a allowlist updates in the same PR** for any new authorized directory, per the standing rule in `MASTER_STATUS.md` §8 step 4b.
5. **Live-trading entry gate** per ADR-009 D5 for any PR that introduces a live-trading entry point: either named implementation evidence + named test path in the PR's diff, or named deferred-with-citation evidence pointing to an already-merged authorization artifact on `main`.

## Exit Criteria

This enumeration PR may merge only after the full required check suite passes on the planning branch:

- `pre-commit run --all-files` (clean, or every failure documented).
- `pytest` (clean).
- `MASTER_STATUS.md` §8 step 3 (baseline ancestry from `1f101fc`).
- `MASTER_STATUS.md` §8 step 4 (always-forbidden scan).
- `MASTER_STATUS.md` §8 step 4a (per-PR Phase 2 / Phase 3 infrastructure allowlist scan; allowlist is unchanged at exactly eight entries).
- `MASTER_STATUS.md` §8 step 4c (recursive forbidden-token scan; subshell exit `0`).
- A slugged Mode B monitoring packet for the enumeration PR's active workday has been authored under ADR-008 Mode B by the Backup AI per ADR-008 §D1 and committed by Codex under `monitoring/daily/` per ADR-008 §D4 in a separate monitoring PR, **merged to `main` before** this enumeration PR merges per ADR-008 §D5.
- If Mode A is elected by the maintainer, a Mode A critique against this enumeration PR has been recorded in PR-review text (not committed as a file). Mode A is not independently mandated; see "Mode A Status For This Authorization" above.

After this authorization merges, the next allowed decisions remain those listed in `MASTER_STATUS.md` §9, augmented by the narrow planning workstream described in `governance/authorizations/2026-05-14_phase-4-entry-planning.md` "Allowed Planning Topics" and now augmented by the in-tree enumeration of `P4-02` as a candidate task. **Phase 4 remains open as a governance state only**; **Phase 3 remains formally closed**; **Phase 2 remains formally closed**; **no new Phase 2 task beyond P2-05 is opened**; **no new Phase 3 task beyond P3-05 is opened**; **no Phase 4 implementation task is opened**; **no runtime activation of any merged Phase 3 fixture is authorized**. Any subsequent step — including the eventual P4-02 entry / implementation decision PR — requires its own separate written authorization per the preserved gates above.

## Supporting Evidence (Non-Authoritative)

The following are pointers to the surrounding governance record. They are evidence, not the authorization itself; this file is the authorization of record for the **planning-only enumeration** of `P4-02` as a candidate Phase 4 task.

- Phase 1 accepted baseline: `1f101fc` (`MASTER_STATUS.md` §3).
- Phase 2 closure authorization: `governance/authorizations/2026-05-12_phase-2-closure.md`.
- Phase 3 entry planning authorization (pattern reference): `governance/authorizations/2026-05-13_phase-3-entry-planning.md`.
- Phase 3 entry decision: `governance/authorizations/2026-05-13_p3-02.md` (PR #38 at `08fe563`).
- Phase 3 closure authorization: `governance/authorizations/2026-05-14_phase-3-closure.md` — merged on `main` via PR #56 at `3131a69`, with sibling Mode B packet PR #57 merged first at `302dff6`, and post-merge status reconciliation PR #58 at `0a91261` with sibling Mode B packet PR #59 merged first at `c910c9a`.
- Phase 4 entry planning authorization (the governance/documentation-only authorization that opened the workstream this enumeration belongs to): `governance/authorizations/2026-05-14_phase-4-entry-planning.md` — merged on `main` via PR #60 at `e1dd6c0`, with sibling Mode B packet PR #61 merged first at `8e5b420`, and post-merge status reconciliation PR #62 at `bb838e0` with sibling Mode B packet PR #63 merged first at `230124c`.
- Phase 4 entry gate authorization (P4-01): `governance/authorizations/2026-05-14_p4-01.md` — merged on `main` via PR #64 at `9f8bd92`, with sibling Mode B packet PR #65 merged first at `c34d0dc`. P4-01 post-merge status reconciliation: PR #66 at `bf295a0` with sibling Mode B packet PR #67 merged first at `beee4e4` (current `main` head: `bf295a0`).
- Phase 4 entry plan (the document this enumeration updates): `plan/phase4_entry_plan.md` §1, §3, §4, §5, §10.
- Pattern reference (prior planning / preparation / authorization slices): `governance/authorizations/2026-05-13_phase-3-entry-planning.md`, `governance/authorizations/2026-05-13_p3-02-preparation.md`, `governance/authorizations/2026-05-13_p3-03-planning.md`, `governance/authorizations/2026-05-14_phase-4-entry-planning.md`.
- ADR-008 (governance-phase monitoring cadence): `docs/decisions/ADR-008_monitoring_cadence_and_ai_monitor_role.md` §D3 / §D4 / §D5 (governs this enumeration PR per ADR-009 D7's bootstrap-avoidance clause until runtime exists on `main`).
- ADR-009 (runtime-phase monitoring cadence): `docs/decisions/ADR-009_runtime_monitoring_cadence.md` — Accepted on 2026-05-13 per `governance/authorizations/2026-05-13_p3-01-acceptance.md`; D7 bootstrap-avoidance clause keeps ADR-008 §D3 / §D5 in force until runtime exists on `main`.
- Workflow separation of duties: `AI_WORKFLOW.md` §1 (roles), §2 (standard workflow), §3 (required proof), §4 (when to use the Backup AI), §6 (anti-chaos rules), §7 (durable authorization artifacts).
