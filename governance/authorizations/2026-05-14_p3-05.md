# P3-05 Implementation Authorization — ReconciliationProtocol In-Memory Fixture (Third Single-Protocol Test-Fixture)

Date: 2026-05-14
Authorizer: Kevin
Scope: Phase 3 / P3-05 implementation — **third single-protocol test fixture only**, with `ReconciliationProtocol` (ADR-003) selected as the protocol.

## Authorization

Kevin's written authorization for this work (chat, 2026-05-14 08:38 CDT) is:

> "Authorize one next narrow Phase 3 ReconciliationProtocol in-memory fixture workflow. Pure-Python in-memory fake and tests only; no runtime activation; no broker/market-data/order/strategy/scheduler/persistence/deployment/env/secrets/network; no __main__; no daemon; no live trading; no tags/releases. Include the required §8 step 4a allowlist update for only the new fixture directory introduced by this PR. Use Mode A review and a separate Mode B packet sequenced before merge."

This authorization opens **P3-05 implementation** as the third concrete Phase 3 implementation slice, restricted to a single-protocol test fixture for `ReconciliationProtocol` (ADR-003). It is the structural analogue of `governance/authorizations/2026-05-13_p3-03.md` (P3-03 HeartbeatProtocol implementation, merged via PR #42 with sibling Mode B PR #43) and `governance/authorizations/2026-05-14_p3-04.md` (P3-04 KillSwitchProtocol implementation, merged via PR #48 with sibling Mode B PR #49). It operationalizes — for `ReconciliationProtocol` only — the planning-level scope, expected paths, allowlist-update reference, expected quality gates, and expected review risks that the P3-03 planning artifact (`governance/authorizations/2026-05-13_p3-03-planning.md`, PR #40) drafted as a hypothetical template for any one of the three P2-05 protocols.

This authorization is **narrower** than the P3-03 planning artifact described as a hypothetical: Kevin has named exactly **one** protocol (`ReconciliationProtocol`), has explicitly restricted the implementation to a **pure-Python in-memory fixture and tests only**, has explicitly excluded runtime activation and the entire always-forbidden category set, and has explicitly required that the §8 step 4a allowlist update be limited to the **new fixture directory introduced by this PR** and nothing else. Mode A adversarial review of the implementation PR per `AI_WORKFLOW.md` §4(2) and a separate sibling Mode B monitoring packet PR (per ADR-008 §D5, or ADR-009 D5 once in force per ADR-009 D7) merged to `main` **before** this PR merges are required.

**Phase 2 remains formally closed.** Phase 3 remains open as a governance state per `governance/authorizations/2026-05-13_p3-02.md`. P3-03 implementation (the HeartbeatProtocol in-memory fixture) merged on 2026-05-13. P3-04 implementation (the KillSwitchProtocol in-memory fixture) merged on 2026-05-14. P3-05 implementation as authorized here is the third concrete Phase 3 implementation slice; it does **not** authorize implementation of any further protocol fixture (the P2-05 set is exhausted by P3-03 / P3-04 / P3-05), any runtime activation, or any task beyond the ReconciliationProtocol in-memory fixture described below. Each of those remains future / not authorized and would require its own separate written authorization.

## Allowed Scope

The P3-05 implementation PR, under this authorization, may include exactly:

- A new pure-Python `src/gmc_rebuild/reconciliation/` package containing:
  - `__init__.py` — package marker, exporting only the in-memory fake fixture type. No runtime side effects on import.
  - `_fake.py` — a sandboxed in-memory fake implementation of `ReconciliationProtocol` whose sole purpose is to be exercised by tests under `tests/reconciliation/`. Pure Python, deterministic, no I/O, no network, no `time.sleep`, no `os.environ` / `os.getenv`, no filesystem, no broker SDK, no scheduler, no background thread, no daemon, no `__main__` entry point, no secrets, no real account / venue / endpoint identifier, no order placement, no broker activity, no market data, no positions / fills / executions / order book, no persistence (no SQLite, no snapshots, no file I/O).
- A new `tests/reconciliation/` package containing:
  - `__init__.py` — test package marker.
  - `test_reconciliation_fixture.py` — focused, deterministic tests for the in-memory fixture, asserting `ReconciliationProtocol` shape conformance via `isinstance` against the `runtime_checkable` Protocol, plus a small number of behavioural invariants for the fixture: default construction returns `ReconciliationStatus.UNAVAILABLE` (not `FAILED`); the `UNAVAILABLE` (no upstream data) / `FAILED` (confirmed material mismatch) distinction is preserved end-to-end; staged `CLEAN` / `WARNING` / `FAILED` outcomes are reported in FIFO order via in-memory test helpers and the queue drains to `UNAVAILABLE` when empty; `reconcile()` does not raise in the steady-state case; ADR-004 UTC discipline is preserved at the boundary (naive datetimes rejected; timestamps are Z-suffixed UTC strings); the fixture has no broker / network / filesystem coupling.
- A `MASTER_STATUS.md` §8 step 4a allowlist extension that adds **exactly** `src/gmc_rebuild/reconciliation` to `allowed_p2_infra`, with a matching comment block citing this authorization artifact and the P3-05 implementation PR. **No other path is added to the allowlist.** The new `tests/reconciliation/` directory does **not** require an allowlist entry because the §8 step 4a allowlist is scoped to **Phase 2 / Phase 3 infrastructure paths under `src/**`** only (the §8 step 4a scan in `MASTER_STATUS.md` only iterates over candidate `src/**` infrastructure paths; `tests/**` is governed by `pyproject.toml` `testpaths` and the always-forbidden token scan in §8 step 4c, not by the step 4a allowlist).
- Governance prose updates limited to: `MASTER_STATUS.md` §1, §5 (P3-05 implementation entry), §6 (authorized Phase 3 task list), §7 (decision record), §8 (P3-05 implementation references and the `src/gmc_rebuild/reconciliation` allowlist entry), §9 (next allowed decisions), `README.md` "Current Phase" cross-reference, `plan/phase3_entry_plan.md` §1 "Current Status", and the package-skeleton test `tests/test_package_skeleton.py` (which enumerates the authorized direct children of `src/gmc_rebuild/`) — narrowly recording that P3-05 implementation has been authorized for the ReconciliationProtocol fixture only and is open / pending merge, and that no other Phase 3 implementation beyond P3-05 is authorized.
- This authorization artifact at `governance/authorizations/2026-05-14_p3-05.md`.

The implementation must, by content:

1. Preserve the P2-05 boundary unchanged. `src/gmc_rebuild/risk/` continues to contain only the abstract `typing.Protocol` definitions and supporting frozen dataclasses / enums. The in-memory fixture lives under a separate `src/gmc_rebuild/reconciliation/` package; it does **not** modify any file under `src/gmc_rebuild/risk/`.
2. Be a structural fixture only — the in-memory fake stores at most a FIFO queue of staged outcomes against a fixed in-memory clock and conforms structurally to `ReconciliationProtocol` so `isinstance(..., ReconciliationProtocol)` is `True`. Calls to `reconcile()` do not raise in the steady-state case, preserving ADR-003's runtime contract. **A freshly constructed fake returns `ReconciliationStatus.UNAVAILABLE`** — not `FAILED` — preserving the ADR-003 requirement that "no upstream data" is explicitly distinct from a "confirmed material mismatch". The `UNAVAILABLE` (no upstream data) / `FAILED` (confirmed material mismatch) distinction is preserved end-to-end.
3. Public behaviour is the minimum sufficient surface: `reconcile()` is the only protocol method, plus controlled in-memory test helpers (`advance(seconds)` for the clock; `enqueue(...)` / `set_next(...)` for staging outcomes). No upstream fetchers, no callbacks, no I/O, no wall-clock reads.
4. Avoid any runtime activation. The package is not imported by any `__main__` entry point, by any daemon, by any scheduler, by any broker integration, by any real-runtime path, and is not re-exported by `src/gmc_rebuild/__init__.py`. Its only consumers are the test fixtures themselves.
5. Preserve the P3-03 HeartbeatProtocol fixture and the P3-04 KillSwitchProtocol fixture unchanged. No file under `src/gmc_rebuild/heartbeat/`, `tests/heartbeat/`, `src/gmc_rebuild/kill_switch/`, or `tests/kill_switch/` is modified.
6. Be reversible by a single `git revert` on the merge commit.

## Explicitly Not Authorized

The authorization above does not permit, by implication or otherwise, any of the following. Each remains forbidden until Kevin records a separate written authorization per `MASTER_STATUS.md` §7 and `AI_WORKFLOW.md` §6 rule 3 ("One approver") / rule 7 ("No phase drift"):

- **Runtime activation of any kind.** No `__main__` entry point anywhere in the tree; no scheduler; no background thread; no daemon; no long-running service; no live execution loop; no reconciliation runtime daemon; no wiring of the fixture to any real-runtime path.
- **Implementation of any further protocol fixture or any further Phase 3 implementation slice beyond P3-05.** The existing `HeartbeatProtocol` fixture (P3-03) and `KillSwitchProtocol` fixture (P3-04) are preserved as-is.
- **Broker integration.** No broker SDK, no broker connector, no broker reconciliation runtime, no broker authentication, no broker credentials, no broker / venue / account / endpoint identifier embedded anywhere. No `account_id`, no `broker_id`, no `venue_id`, no endpoint URL.
- **Market-data integration.** No market-data ingestion code, no market-data pipeline, no committed dataset, no live or historical data feed, no quotes / bars / symbol universe (real or fake).
- **Order management.** No order objects, no `order_id`, no order placement, no position management, no `position_qty`, no fills, no trade reports, no executions, no `exec_id`, no order book.
- **Strategy logic.** No trading signals, no scanners, no models, no portfolio rules, no backtests.
- **Scheduling / persistence / deployment.** No cron-style scheduler, no APScheduler, no background-job framework, no SQLite, no on-disk database, no snapshots, no file I/O, no on-disk reconciliation state written by runtime code, no log sink, no file artifact written by runtime code, no deployment workflow, no rollout, no rollback runtime, no CI/CD pipeline beyond the existing local pre-commit and pytest gates.
- **Secrets / env-var loading / network.** No real secrets, no real credentials, no API-key fields, no `.env` files, no `os.environ` / `os.getenv` reads, no `socket` / `requests` / `urllib` / `http` / `ssl` calls, no `pickle` / `shelve`, no `time.sleep`, no real account identifier, no real broker / venue identifier, no real endpoint embedded anywhere in the repository.
- **Concrete risk implementations inside `src/gmc_rebuild/risk/`.** The P2-05 boundary (`src/gmc_rebuild/risk/` is types and abstract `typing.Protocol` definitions only) is preserved. The in-memory fixture lives at `src/gmc_rebuild/reconciliation/`, not inside `src/gmc_rebuild/risk/`.
- **Tooling relaxation.** No weakening of pre-commit, Ruff, mypy strict mode, `detect-secrets`, `.gitignore`, `.secrets.baseline`, the `MASTER_STATUS.md` §8 step 4 always-forbidden scan, the §8 step 4a per-PR Phase 2 infrastructure allowlist scan, or the §8 step 4c recursive forbidden-token scan.
- **§8 step 4a allowlist expansion beyond `src/gmc_rebuild/reconciliation`.** Only the new fixture directory introduced by this PR is added to the allowlist. The allowlist is **not** broadened to anticipate any further Phase 3 implementation slice.
- **Mode A / Mode B bundling.** Mode A adversarial review of the implementation PR is delivered as PR-review text and is **not** committed to the repository (per `AI_WORKFLOW.md` §6 rule 5 / ADR-008 §D7 / ADR-009 D7 dual-artifact rule). The Mode B monitoring packet for the active workday is a **separate sibling PR** under `monitoring/daily/`, **merged to `main` before** this implementation PR merges (ADR-008 §D5, or ADR-009 D5 once in force per ADR-009 D7). This implementation PR does **not** bundle the Mode B packet. The Mode B branch is based on current `main`, **not** on this implementation branch.
- **New tags or releases.** No git tag, no GitHub release, no version bump.
- **Live-trading entry gate triggering.** ADR-009 D5 records the implementation evidence / deferred-with-citation evidence requirement for any future PR that introduces a live-trading entry point. This PR does **not** introduce a live-trading entry point.

The always-forbidden categories in `MASTER_STATUS.md` §6 and `plan/phase2_entry_plan.md` §2 — strategy code, broker execution code, live or paper trading wired to a real broker, runtime daemons affecting accounts, real market data ingestion, order placement, and real secrets — remain forbidden in all modes. This authorization does not relax them.

## Allowlist Update (Operative, This PR)

The `MASTER_STATUS.md` §8 step 4a `allowed_p2_infra` allowlist is extended in this PR by adding **exactly one** directory:

```
allowed_p2_infra="src src/gmc_rebuild/config src/gmc_rebuild/time src/gmc_rebuild/logging src/gmc_rebuild/risk src/gmc_rebuild/heartbeat src/gmc_rebuild/kill_switch src/gmc_rebuild/reconciliation"
```

A matching comment block in `MASTER_STATUS.md` §8 step 4a names `src/gmc_rebuild/reconciliation` alongside this authorization artifact and the P3-05 implementation PR. The directory-to-PR-tag mapping inside the verification script's `case "$path"` block gains the entry `src/gmc_rebuild/reconciliation) pr_tag="PR P3-05" ;;`.

**No other path is added to the allowlist.** The new `tests/reconciliation/` directory does not require an allowlist entry — the §8 step 4a scan only walks the explicit list of candidate `src/**` Phase 2 / Phase 3 infrastructure paths (see `MASTER_STATUS.md` §8 step 4a). The `tests/` tree is governed by `pyproject.toml` `testpaths = ["tests"]` and by the §8 step 4c recursive forbidden-token scan, which the new `tests/reconciliation/` path satisfies (`reconciliation` tokenizes to `[reconciliation]`; the token is not in the forbidden set).

The allowlist update is **operative in this PR only**. It is the §8 step 4b "in the same PR that introduces the directory" rule discharged for `src/gmc_rebuild/reconciliation`. Any further Phase 3 implementation work would require its own separate Kevin-written authorization and its own separate allowlist entry in the PR that introduces that directory.

## Required Quality Gates

The implementation PR must satisfy:

1. `MASTER_STATUS.md` §8 startup verification commands 1–7, including:
   - Step 4 (always-forbidden category scan) — no `strategy`, `signals`, `broker`, `live`, `paper`, `daemons`, `market_data`, `orders`, `secrets`, etc. at any depth.
   - Step 4a (Phase 2 / Phase 3 infrastructure allowlist scan) — `src/gmc_rebuild/reconciliation` is present and allowlisted; no other new infrastructure path is present.
   - Step 4c (recursive forbidden-token scan) — exits 0; `reconciliation` is not a forbidden token.
   - Step 6 (`pre-commit run --all-files`) — clean, or every failure documented.
   - Step 7 (`pytest`) — clean, including the new `tests/reconciliation/test_reconciliation_fixture.py` module.
2. Mode A adversarial review per `AI_WORKFLOW.md` §4(2) (high-risk architecture decision — concrete behaviour behind a P2-05 control surface, third protocol fixture). Delivered as PR-review text, not committed to the repository.
3. Mode B monitoring packet per ADR-008 §D3 / §D5 (or ADR-009 D3 / D5 once in force) for the active workday on which this PR is open / merged, in a separate sibling PR **based on current `main` (not on the implementation branch)**, merged to `main` before this PR merges.
4. Reversibility — single `git revert` on the merge commit removes all changes.

## Sequencing

1. This implementation PR is opened against `main` from a fresh branch (`implementation/p3-05-reconciliation-fixture`).
2. Mode A review is delivered as PR-review text against this PR.
3. A separate sibling Mode B monitoring packet PR is opened from a branch based on **current `main`** (not on the implementation branch), reviewed, and **merged to `main` before** this PR merges, per ADR-008 §D5.
4. This PR merges only after Mode A has cleared and the Mode B packet has merged.

The Mode A reviews of the P3-03 implementation PR (PR #42) and the P3-04 implementation PR (PR #48) do **not** satisfy the Mode A requirement for this implementation PR; they are independent reviews against independent artifacts.

## Future Work (Not Authorized Here)

After this PR merges, the following remain future / not authorized:

- Any runtime activation of the reconciliation fixture (or of any control surface — P3-03 HeartbeatProtocol or P3-04 KillSwitchProtocol included).
- Any Phase 3 implementation slice beyond this single-protocol fixture set (P3-03 / P3-04 / P3-05).
- Any broker / market-data / order / strategy / scheduler / persistence / deployment work.

Phase 3 remains open as a governance state. P3-05 implementation as authorized here is narrow by design.
