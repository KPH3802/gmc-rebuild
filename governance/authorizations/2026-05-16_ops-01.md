# OPS-01 Authorization — Project Resilience Checkpoint (Backup / Recovery / Contingency)

Date: 2026-05-16
Authorizer: Kevin Heaney
Scope: Bounded, docs-only operations checkpoint for `gmc-rebuild` documenting a professional, no-single-point-of-failure backup, recovery, and contingency plan before any P5 simulation work. Adds a top-level `RECOVERY.md` capturing the source-of-truth model, the 3-2-1-1-0 style redundancy layers, the recovery drill, the Mac Studio sync procedure, the local-only forbidden-artifact discipline, the role of each storage device, and unresolved risks observed at this checkpoint. Adds this governance authorization artifact. Makes only conservative status reflections in `MASTER_STATUS.md` and `plan/phase4_entry_plan.md` recording OPS-01 as a docs-only resilience checkpoint that does not open or authorize P5.

## Authorization

Kevin authorized this packet on 2026-05-16 (Saturday, May 16, 2026 12:11 PM CDT). The verbatim written authorization follows:

> OPS-01 Authorization — project resilience checkpoint — 2026-05-16
>
> I, Kevin Heaney, authorize one bounded docs-only OPS-01 packet for the gmc-rebuild repository to document backup, recovery, and contingency operations before P5 simulation work.
>
> Authorized scope:
> - Add or update a recovery/backup policy document, expected as RECOVERY.md or a narrowly named operations document.
> - Document the no-single-point-of-failure standard for the project using a 3-2-1-1-0 style model: GitHub source of truth, local Mac Studio working copy, LaCie Time Machine/local recovery, Backblaze offsite backup, and X10 Pro offline/rotating copy only after health is verified.
> - Document current observed local facts from the read-only Mac Studio check: Mac checkout is behind current GitHub main; Time Machine is active on LaCie; X10 Pro is mounted and selected in Backblaze; Backblaze is installed but not yet proven healthy; Mac DNS/GitHub reachability failed during the check.
> - Document recovery drill steps: fresh clone, virtualenv/dependency setup, validation, expected clean baseline, and local-only artifact restrictions.
> - Document safe next actions for LaCie, X10 Pro, Backblaze, and GitHub reachability.
> - Add the required governance authorization artifact if repository conventions require one for this packet.
> - Make only conservative status/doc updates if needed.
>
> Hard constraints:
> - Docs-only. No src/** changes.
> - No test behavior changes unless an existing repository governance test requires listing the new doc.
> - No backup execution, drive repair, disk formatting, Time Machine changes, Backblaze setting changes, cloud mirror creation, branch deletion, or GitHub destructive actions.
> - No secrets, credentials, broker data, market data, orders, or trading runtime changes.
> - No successor P5 work is authorized by this packet.
>
> Validation required before PR:
> - python3 -m pre_commit run --all-files.
>
> This authorization is only for the bounded OPS-01 project resilience checkpoint and does not authorize any successor packet.

This authorization is the authorization of record per `AI_WORKFLOW.md` §7.

## Allowed Scope

The implementation PR may include exactly:

- A new top-level documentation file at `RECOVERY.md` describing:
  - The single source of truth (the `KPH3802/gmc-rebuild` GitHub repository, `main` branch).
  - The 3-2-1-1-0 style redundancy model adapted to this project: GitHub (authoritative remote), local Mac Studio working copy (working tree), LaCie Time Machine (local recovery), Backblaze (offsite cloud backup), Seagate X10 Pro (offline / rotating cold copy, only after health is verified).
  - Observed local facts as of 2026-05-16 from the read-only Mac Studio check (Mac checkout behind current GitHub `main`; LaCie Time Machine active; X10 Pro mounted and selected in Backblaze; Backblaze installed but not yet proven healthy; Mac DNS / GitHub reachability failed during the check).
  - The recovery drill steps (fresh clone, virtualenv / dependency setup, validation against `python3 -m pre_commit run --all-files`, expected clean baseline, and local-only artifact restrictions).
  - Safe next actions for each device and for GitHub reachability, scoped as future / not-authorized by this packet.
  - The role of each storage layer and the unresolved risks at this checkpoint.
- This authorization artifact at `governance/authorizations/2026-05-16_ops-01.md`.
- Conservative status reflections in `MASTER_STATUS.md` and `plan/phase4_entry_plan.md` recording OPS-01 as a bounded, docs-only operations resilience checkpoint that does **not** open or authorize any successor (P4-09, P5-01, local/paper simulation boundary planning, …). The status reflections do not extend the §8 step 4a `allowed_p2_infra` allowlist and do not change the safety foundation closure recorded by P4-08.

The new `RECOVERY.md` file is documentation only. It contains no executable code, no secrets, no credentials, no broker / market-data / order / trading runtime content, and no scheduler / daemon / network instructions. Any device-side actions named in `RECOVERY.md` are described as **operator-side actions** to be performed by Kevin on his own hardware outside this repository, not as actions the repository or any agent performs.

## Explicitly Not Authorized

This authorization does not permit:

- Any change under `src/**`.
- Any change to existing test behavior, except a narrowly required listing update if (and only if) an existing repository governance test forces the new `RECOVERY.md` to be listed as a required artifact. No existing test under `tests/test_phase1_governance.py`, `tests/test_config_schema.py`, or `tests/test_package_skeleton.py` requires this; therefore no test file is modified by this packet.
- Any backup execution, drive repair, disk formatting, Time Machine setting change, Backblaze setting change, cloud-mirror creation, branch deletion, force-push, or any other GitHub destructive action.
- Any secrets, credentials, broker data, market data, order, fill, position, execution, scheduler, daemon, persistence, network call, `time.sleep`, `asyncio.sleep`, or runtime activation.
- Any concrete implementation of `HeartbeatProtocol`, `KillSwitchProtocol`, or `ReconciliationProtocol` outside the merged P3-03 / P3-04 / P3-05 in-memory fakes.
- Any modification of the merged P4-06 `RuntimeShell` / `SafetyVerdict` types or the merged P4-07 `OperatorSafetyView` / `format_safety_verdict` formatter, or the P4-08 safety-policy-hardening tests.
- Any extension of the §8 step 4a `allowed_p2_infra` allowlist.
- Any quality-gate relaxation, tag, or release.
- Any successor packet (P4-09, P5-01, local/paper simulation boundary planning, …). The safety foundation closure recorded by P4-08 is preserved unchanged. The next stage (local/paper simulation boundary planning) continues to require its own separate written authorization from Kevin.

Phase 2 remains formally closed. Phase 3 remains formally closed. Phase 4 remains open as a governance state. The safety foundation closure recorded by P4-08 is preserved. OPS-01 is an out-of-band, docs-only operations checkpoint that documents redundancy and recovery operations; it is **not** a Phase 4 implementation task, **not** a Phase 5 task, and **not** an enumeration or opening of any successor task.

## Required Validation

Before merge, the implementation PR must pass:

- `python3 -m pre_commit run --all-files`
- Diff-scope verification showing changes confined to the new `RECOVERY.md`, this authorization artifact, and the conservative status reflections in `MASTER_STATUS.md` and `plan/phase4_entry_plan.md`.

Mode A / Mode B review requirements remain governed by `AI_WORKFLOW.md`, ADR-008, ADR-009, and the current `MASTER_STATUS.md`. This packet is a routine documentation edit per `AI_WORKFLOW.md` §4's routine-exclusion sentence and does not independently mandate Mode A; maintainer-elected Mode A, if conducted, is recorded as PR-review text and not committed per `AI_WORKFLOW.md` §6 rule 5.
