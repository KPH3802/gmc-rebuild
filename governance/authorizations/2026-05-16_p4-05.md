# P4-05 Implementation Authorization — Composed Failure-Mode Coverage Test Only

Date: 2026-05-16
Authorizer: Kevin Heaney
Scope: Phase 4 / P4-05 implementation — narrow pytest-only composed failure-mode coverage under the existing `tests/p4_02_composed/` directory.

## Authorization

Kevin authorized continuing the safety foundation without shortcuts on 2026-05-16. This artifact records the bounded implementation scope for the next safety-foundation packet:

> P4-05 is authorized only as a narrow safety-foundation implementation task that adds one pytest-only composed failure-mode coverage file under the existing `tests/p4_02_composed/` directory, expected path `tests/p4_02_composed/test_composed_failure_modes.py`. The task may exercise the already-merged in-memory heartbeat, kill-switch, and reconciliation fakes together against their existing abstract Protocol boundaries. It may add conservative governance/status reflections needed to record the authorization and status. It does not authorize runtime activation, broker integration, market-data ingestion, order placement, strategy logic, scheduler or daemon work, persistence, deployment, env-var or secrets handling, network access, `time.sleep`, new concrete protocol implementations, any `src/**` change, any allowlist expansion, any quality-gate relaxation, tags, or releases.

This authorization opens P4-05 as a **safety-foundation test-only** task. It is a successor to the merged P4-02 composed-fixture test, P4-03 composed-invariants test, and P4-04 composed-edge-cases test. It does not authorize P4-06 or any later task.

## Allowed Scope

The implementation PR may include exactly:

- One new test file at `tests/p4_02_composed/test_composed_failure_modes.py`.
- This authorization artifact at `governance/authorizations/2026-05-16_p4-05.md`.
- Conservative status reflections in `MASTER_STATUS.md` and `plan/phase4_entry_plan.md` limited to recording P4-05 as a narrow pytest-only composed failure-mode coverage task.

The new test file may assert composed failure-mode behavior such as:

- A stale heartbeat remains a blocking composed safety signal even when the kill switch is armed and reconciliation is clean.
- `ReconciliationStatus.UNAVAILABLE` remains blocking until an explicit clean report is observed.
- `ReconciliationStatus.FAILED` remains visible before and after an explicit operator kill-switch trip.
- Simultaneous blockers remain visible after independent in-memory clock advances.
- The composed observation is clear only when heartbeat is fresh, kill switch is armed, and reconciliation is clean together.

## Explicitly Not Authorized

This authorization does not permit:

- Any `src/**` addition, modification, deletion, or re-export.
- Any modification of existing test files.
- Any new test directory or `conftest.py`.
- Any new fake or helper under `src/**`.
- Runtime activation of any merged Phase 3 fixture.
- Any `__main__`, daemon, scheduler, background service, broker integration, market-data ingestion, order placement, strategy logic, persistence, deployment, env-var or secrets handling, network access, `time.sleep`, or `asyncio.sleep`.
- Any concrete implementation of `HeartbeatProtocol`, `KillSwitchProtocol`, or `ReconciliationProtocol`.
- Any `MASTER_STATUS.md` §8 step 4a allowlist expansion.
- Any quality-gate relaxation, tag, or release.

Phase 2 remains formally closed. Phase 3 remains formally closed. Phase 4 remains open as a governance state only. P4-05 is test-only safety-foundation work and does not cross into runtime.

## Required Validation

Before merge, the implementation PR must pass:

- `python -m pre_commit run --all-files`
- Targeted pytest for `tests/p4_02_composed/test_composed_failure_modes.py`
- Diff-scope verification showing no `src/**` changes and no existing test-file modifications

Mode A / Mode B review requirements remain governed by `AI_WORKFLOW.md`, ADR-008, ADR-009, and the current `MASTER_STATUS.md`.
