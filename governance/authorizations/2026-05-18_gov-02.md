# GOV-02 Authorization — Execution-Environment Workflow Correction (Local Claude Code / Claude Max as Default Builder for `gmc-rebuild`)

Date: 2026-05-18
Authorizer: Kevin Heaney
Scope: Bounded, docs/governance-only GOV-02 packet for `gmc-rebuild` recording Kevin's 2026-05-18 written workflow correction. The packet (a) adds a new execution-environment rule to `MASTER_STATUS.md` §1 stating that repo-editing work for `gmc-rebuild` is by default carried out via Kevin's local Claude Code / Claude Max subscription rather than via Perplexity-managed codebase subagents that consume Perplexity credits; (b) records the corresponding role-clarification rule that Perplexity Computer continues to act as the §1.2 Supervisor / Verifier / Status Keeper and as merge-sequencing assistant (including direct `gh` / `git` metadata checks against the public repository, which remain allowed for verification and merge sequencing), but Perplexity-managed codebase subagents must not be used for repo edits unless Kevin explicitly authorizes that credit spend; (c) extends `AI_WORKFLOW.md` with a matching execution-environment clause under §1 so the rule lives next to the existing role definitions; (d) records this authorization at `governance/authorizations/2026-05-18_gov-02.md` per `AI_WORKFLOW.md` §7. The packet is **bounded, docs/governance-only**, makes **no** change under `src/**` or `tests/**`, opens **no** runtime activation, opens **no** successor packet, and does **not** authorize P5-07 or any later Phase 5 implementation work.

## Authorization

Kevin authorized this packet on 2026-05-18. The verbatim written authorization follows:

> GOV-02 Authorization — Execution-Environment Workflow Correction — 2026-05-18
>
> Repo-editing work for `gmc-rebuild` was supposed to run through my local Claude Code / Claude Max subscription, not through Perplexity-managed codebase subagents that consume Perplexity credits. Record that workflow rule so future agents follow it.
>
> Authorized scope:
> - Add an explicit workflow rule to `MASTER_STATUS.md` stating that for `gmc-rebuild` repository edits, local Claude Code / Claude Max is the default execution environment.
> - Record that Perplexity Computer continues to act as governance / status controller, verifier, reviewer, and merge-sequencing assistant.
> - Record that Perplexity-managed codebase subagents must not be used for repo edits unless I explicitly authorize that credit spend.
> - Record that direct `gh` / `git` metadata checks by Perplexity remain allowed for verification and merge sequencing.
> - Extend `AI_WORKFLOW.md` with the same execution-environment rule if it fits the document's existing structure.
> - Add a governance authorization artifact under `governance/authorizations/` recording this 2026-05-18 correction.
> - Add the required Mode B sibling monitoring PR from `main`.
>
> Hard constraints:
> - Docs / governance only.
> - No `src/**` changes.
> - No `tests/**` changes.
> - No runtime activation.
> - No broker, paper-trading, live-trading, market-data, order-routing, strategy, scheduler, daemon, persistence, deployment, env-var, secrets, or network changes.
> - No allowlist extension.
> - No quality-gate relaxation.
> - No tag or release.
> - No P5-07 implementation authorization.
> - No successor packet is authorized.
>
> Validation required before PR:
> - `.venv/bin/python -m pytest -q`
> - `grep -nE "pending merge" MASTER_STATUS.md README.md RECOVERY.md plan/phase4_entry_plan.md plan/phase5_entry_plan.md || true`
>
> This authorization is only for the bounded GOV-02 execution-environment workflow correction packet and does not authorize any successor packet.

This authorization is the authorization of record per `AI_WORKFLOW.md` §7.

## Allowed Scope

The implementation PR may include exactly:

- An update to `MASTER_STATUS.md` §1 appending a new "Execution-environment workflow rule (GOV-02, 2026-05-18 UTC — pending merge in this open PR)" paragraph at the end of §1's current-work-packet stack. The new paragraph:
  - States that for `gmc-rebuild` repository edits, **local Claude Code / Claude Max is the default execution environment**.
  - States that Perplexity Computer continues to act in its `AI_WORKFLOW.md` §1.2 role as Supervisor / Verifier / Status Keeper and merge-sequencing assistant.
  - States that Perplexity-managed codebase subagents that consume Perplexity credits must **not** be used to perform repo edits on `gmc-rebuild` unless Kevin explicitly authorizes that credit spend in writing for the specific packet.
  - States that direct `gh` / `git` metadata checks against the public `gmc-rebuild` repository by Perplexity (e.g. `gh pr list`, `gh pr view`, `git log`, `git show`) remain allowed for verification and merge sequencing and are **not** considered "repo edits" because they are read-only against the public remote.
  - States that the rule does not relax any existing `AI_WORKFLOW.md` role, ADR-008 / ADR-009 monitoring cadence, §6 rule, or §7 authorization-of-record requirement; in particular, Codex (or whoever is acting as Codex on the task) remains the only role that writes to the repository per §6 rule 1 ("One builder at a time").
  - Names the authorization-of-record (`governance/authorizations/2026-05-18_gov-02.md`).
  - Carries a `pending merge in this open PR` self-scoped label scoped exclusively to the open GOV-02 implementation PR; the prior P5-01 / P5-02 / P5-03 / P5-04 / P5-05 / P5-06 / Phase 5 entry planning / P5-06 planning status reflection paragraphs continue to read **merged on `main`** and are **not** reverted to stale `**pending merge**` language by this packet.
- An update to `AI_WORKFLOW.md` §1 appending a new §1.5 "Execution environment for `gmc-rebuild` repo edits" subsection that mirrors the same rule in the role-definitions location. The subsection:
  - Names the default builder environment (local Claude Code / Claude Max running on Kevin's machine).
  - Reiterates that Perplexity Computer continues to act in its §1.2 role and may verify, audit, status-keep, sequence merges, and run read-only `gh` / `git` metadata checks against the public remote without authorization.
  - Explicitly states that Perplexity-managed codebase subagents (credit-consuming agents that perform edits, file authoring, or commits inside the repository via Perplexity's environment) must not be used for `gmc-rebuild` repo edits absent Kevin's explicit written authorization for the specific packet.
  - Cross-references `governance/authorizations/2026-05-18_gov-02.md` as the authorization of record.
  - Notes that the §6 rule 1 ("One builder at a time") constraint is preserved unchanged: only Codex (or whoever is acting as Codex on the task per §1.4's Mode B exception for monitoring packet text authorship) writes to the repository, regardless of which AI tool is providing the underlying text.
- This authorization artifact at `governance/authorizations/2026-05-18_gov-02.md`.

No file under `src/**` is added or modified. No file under `tests/**` is added or modified. No file under `monitoring/**`, `docs/decisions/`, or `governance/authorizations/` other than this new GOV-02 artifact is added or modified. The merged P4-06 `RuntimeShell` / `SafetyVerdict`, P4-07 `OperatorSafetyView` / `format_safety_verdict`, P4-08 safety-policy-hardening tests, P3-03 / P3-04 / P3-05 in-memory fakes, P2-01..P2-05 packages, OPS-01 / OPS-02 / OPS-03 / OPS-04 / OPS-04B operations records, OPS-06 backup monitoring plan, P5-01 inert local simulation boundary skeleton, P5-02 simulated order intent model, P5-03 simulated-order-intent invariants tripwire tests, P5-04 composed safety-foundation × simulation integration tripwire tests, P5-05 operator-view × composed safety-foundation tripwire tests, P5-06 propose / propose_order symmetry tripwire tests, the canonical status reconciliation merged via PR #114, the Phase 5 entry planning reconciliation merged via PR #118, the P5-05 implementation post-merge reconciliation merged via PR #122, the P5-06 planning / enumeration packet merged via PR #124, the P5-06 planning post-merge reconciliation merged via PR #126, the P5-06 implementation packet merged via PR #129, the P5-06 implementation post-merge reconciliation merged via PR #130, and the GOV-01 governance reconciliation are all preserved exactly. The `MASTER_STATUS.md` §8 step 4a `allowed_p2_infra` bash gate (synced to ten entries by GOV-01) is preserved exactly; no new `src/gmc_rebuild/` subpackage directory is created.

## Explicitly Not Authorized

This authorization does not permit:

- Any addition or modification of any file under `src/**`. The merged subpackages are preserved exactly.
- Any addition or modification of any file under `tests/**`. The hard-coded allowlist in `tests/test_package_skeleton.py` is unchanged.
- Any P5-07 implementation, any new Phase 5 implementation packet, any new `SimulationLane` member, any new `SimulatedOrderSide` member, any new `SimulatedOrderType` member, any new field on `SimulatedIntent` / `SimulatedOrderIntent`, any new method on `SimulationBoundary`, any new placeholder or order record class, any new public symbol on any subpackage, or any new re-export from `gmc_rebuild` package root.
- Any new runtime behavior, runtime activation, broker integration (real or paper), broker SDK, broker account identifier, broker session, broker handshake, live or paper market data, market-data feed, vendor SDK, real symbol universe, real venue identifier, real or paper order placement, order routing, order management, order book, fill, execution, position, P&L, trade report, OMS, EMS, routing instruction, execution adapter, broker adapter, paper-broker adapter, mock-broker adapter, exchange adapter, FIX session, REST client, WebSocket client, or any other connectivity surface.
- Any secrets, credentials, API keys, broker keys, session tokens, OAuth tokens, SSH keys, `.env` files containing live credentials, recovery codes, PEKs, or any other credential storage.
- Any external API call, network call, `socket`, `urllib`, `requests`, `http`, `ssl`, `smtplib`, `ftplib`, DNS lookup, or any other outbound or inbound network surface introduced by `src/**` or `tests/**`.
- Any scheduler, daemon, background thread, long-running service, `__main__` entry point, `if __name__` block, `time.sleep`, `asyncio.sleep`, cron job, launchd plist, or any other timed / background execution mechanism.
- Any persistence, database, SQLite, pickle, shelve, filesystem write under any path other than this PR's own commits to the named docs/governance files.
- Any drive action, Backblaze setting change, Time Machine setting change, network configuration change, system setting change, or hardware change.
- Any monitoring automation, alerting system, notification mechanism, desktop notification, email, SMS, push notification, webhook, third-party paging service, chat-bot, Slack / Discord / Telegram / Signal integration, or any other notification-side surface.
- Any modification of the merged P4-06 `RuntimeShell` / `SafetyVerdict`, P4-07 `OperatorSafetyView` / `format_safety_verdict`, P4-08 safety-policy-hardening tests, P3-03 / P3-04 / P3-05 in-memory fakes, P2-01..P2-05 packages, P5-01 inert local simulation boundary, P5-02 simulated order intent model, P5-03 invariants tripwires, P5-04 composed integration tripwires, P5-05 operator-view × composed safety-foundation tripwires, P5-06 propose / propose_order symmetry tripwires, OPS-01..OPS-04B / OPS-06 operations records, GOV-01 governance reconciliation, the PR #114 canonical-status reconciliation, the PR #116 Phase 5 entry planning packet, the PR #118 Phase 5 entry planning post-merge reconciliation, the PR #122 P5-05 implementation post-merge reconciliation, the PR #124 P5-06 planning / enumeration packet, the PR #126 P5-06 planning / enumeration post-merge reconciliation, the PR #129 P5-06 implementation packet, or the PR #130 P5-06 implementation post-merge reconciliation. All are preserved unchanged.
- Any modification of any ADR under `docs/decisions/`, any other `governance/authorizations/*.md` file, `pyproject.toml`, `.pre-commit-config.yaml`, `.secrets.baseline`, `.gitignore`, `README.md`, `RECOVERY.md`, `EXTERNAL_REVIEW_BRIEF.md`, `plan/phase4_entry_plan.md`, or `plan/phase5_entry_plan.md`. The execution-environment rule lives in `MASTER_STATUS.md` §1 and `AI_WORKFLOW.md` §1 only.
- Any modification of any existing `monitoring/daily/*` packet (the new Mode B sibling monitoring packet is added by the separate sibling PR per ADR-008 §D5, not by this GOV-02 implementation PR).
- Any extension of the `MASTER_STATUS.md` §8 step 4a `allowed_p2_infra` bash gate, §8 step 4c forbidden-token bash gate, or §8 step 8 canonical-doc staleness check.
- Any extension of the hard-coded `entries == {...}` set in `tests/test_package_skeleton.py`. The set is unchanged because no new subpackage directory is added.
- Any quality-gate relaxation, hook removal, hook reconfiguration, mypy strictness reduction, ruff rule relaxation, or `detect-secrets` baseline change.
- Any X10 Layer 5 promotion, backup-monitoring automation, DR drill execution, or other ops execution work.
- Any tag or release.
- Any reinterpretation or relaxation of `AI_WORKFLOW.md` §6 rule 1 ("One builder at a time"). The new §1.5 subsection is a **clarification of which AI tool may act as Codex** for `gmc-rebuild` repo edits by default; it does **not** introduce a second builder, it does **not** authorize parallel writers, and it does **not** relax the rule that only one builder writes to the repository per task. Whichever AI tool authors the change, the commit still goes through a single feature branch under §6 rule 4 ("One branch per task") and is still verified by Perplexity Computer per §1.2 and §3.
- Any reinterpretation of `AI_WORKFLOW.md` §1.2's Perplexity Computer role. Perplexity Computer continues to be the Supervisor / Auditor / Verifier / Status Keeper and continues to own `MASTER_STATUS.md`. The execution-environment rule restricts only Perplexity-managed **codebase subagents that perform edits inside the repo and consume Perplexity credits**; it does not restrict Perplexity Computer's read-only verification, read-only `gh` / `git` metadata checks, or its `MASTER_STATUS.md` status-keeping role.
- Any successor packet (P5-07, OPS-05 execution, OPS-07 execution, monitoring automation, alerting, second-review policy enforcement, etc.). Each future packet continues to require its own separate written authorization from Kevin per `AI_WORKFLOW.md` §7.

The always-forbidden categories in `MASTER_STATUS.md` §6, `plan/phase4_entry_plan.md` §10, and `plan/phase5_entry_plan.md` §10 — strategy code, broker execution code, paper-trading or live-trading wired to a real broker, runtime daemons affecting accounts, real market data ingestion, order placement, and real secrets — remain forbidden in all modes. This packet does not relax them and does not record any relaxation.

## Required Validation

Before merge, the implementation PR must pass:

- `git switch main && git pull --ff-only` before branching (to confirm the work is built on current `main` at `25972b0`).
- `.venv/bin/python -m pytest -q` against the existing test suite (must continue to pass at the baseline of 393 tests with no new tests added by this packet).
- `git diff --name-status main` showing changes confined to this authorization artifact, the `MASTER_STATUS.md` §1 append, and the `AI_WORKFLOW.md` §1 append. No file under `src/**` is added or modified; no file under `tests/**` is added or modified; no file under `monitoring/**`, `docs/decisions/`, or `governance/authorizations/` other than this new GOV-02 artifact is added or modified.
- Stale-phrase grep (`grep -nE "pending merge" MASTER_STATUS.md README.md RECOVERY.md plan/phase4_entry_plan.md plan/phase5_entry_plan.md || true`) showing that any new `pending merge in this open PR` occurrence is scoped exclusively to the open GOV-02 implementation PR itself, with no current-state stale claim on any merged work. Remaining wider `pending merge` hits are intentional historical / self-check / anti-pattern residuals.

Mode A / Mode B review requirements remain governed by `AI_WORKFLOW.md`, ADR-008, ADR-009, and the current `MASTER_STATUS.md`. GOV-02 is a routine docs/governance edit per `AI_WORKFLOW.md` §4's routine-exclusion sentence; it is not independently mandated to trigger Mode A. Maintainer-elected Mode A, if conducted, is recorded as PR-review text and not committed per `AI_WORKFLOW.md` §6 rule 5. Mode B sibling monitoring follows ADR-008 §D3 / §D5 (and ADR-009 D3 / D5 once in force per ADR-009 D7's bootstrap-avoidance clause) and must be authored from `main` and merged to `main` **before** this implementation PR per the verbatim authorization above.

Phase 2 remains formally closed. Phase 3 remains formally closed. Phase 4 remains open as a governance state. P5 is open at the P5-01 inert local simulation boundary skeleton scope, the P5-02 simulated order intent model scope, the P5-03 simulated-order-intent invariants tripwire scope, the P5-04 composed safety-foundation × simulation integration tripwire scope, the P5-05 operator-view × composed safety-foundation tripwire scope, the P5-06 planning / enumeration scope, and the P5-06 implementation scope. The safety foundation closure recorded by P4-08 is preserved. P5-07 remains **future / not authorized**.
