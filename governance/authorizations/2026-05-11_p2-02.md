# Authorization Record — PR P2-02 (Config Schema, Minimal and Safe)

**Date authorized:** 2026-05-11 UTC
**Approver:** Kevin Heaney (GitHub: `KPH3802`) — the sole approver per `AI_WORKFLOW.md` §1.3 and §6 rule 3.
**Phase opened by this authorization:** Phase 2 implementation, narrowly expanded from P2-01 to add P2-02 (config schema with fake/example values only).
**PR authorized:** GitHub PR — `P2-02: minimal safe config schema` (this PR; URL recorded in §4 once opened).
**Baseline at time of authorization:** Accepted Phase 1 baseline `1f101fc` (per `MASTER_STATUS.md` §3); current `main` head at authorization is `f7d30af` (PR #10 merge).

---

## 1. Purpose of This Record

This file is the durable, repo-committed copy of Kevin's written authorization for PR P2-02. It satisfies `AI_WORKFLOW.md` §7 ("Durable Authorization Artifacts"), which requires every phase-opening or phase-expanding authorization to be captured **both** in PR history **and** in a sibling in-tree artifact under `governance/authorizations/` before or as part of the authorized PR.

P2-02 is a **phase-expanding** authorization in the sense of `AI_WORKFLOW.md` §7: it adds an authorized task and a new authorized infrastructure path (`src/gmc_rebuild/config/`) inside the already-open Phase 2 implementation phase. It does **not** open a new phase and does **not** extend authorization to P2-03..P2-05 or to any always-forbidden category.

This record is also the audit-visible answer to the ADR-008 §D5 / `AI_WORKFLOW.md` §6 rule 9 question "where is the written authorization for this PR captured": here, in the working tree, alongside `2026-05-11_p2-01.md`.

---

## 2. Authorized Scope

Kevin authorized **exactly and only** the following work under PR P2-02:

- Creation of a minimal config submodule at `src/gmc_rebuild/config/` consisting of:
  - `__init__.py` — re-exports the public, safe-by-default config object.
  - A single module defining an **immutable** (frozen dataclass) project/environment metadata object with safe defaults. Defaults are local-only, disabled where they could imply runtime behavior, and contain no real broker name, account identifier, credential, endpoint, market symbol, strategy parameter, or live/paper mode toggle.
- Matching tests under `tests/` that exercise:
  - the config object imports,
  - its defaults are safe / disabled / local-only,
  - the config submodule introduces no forbidden runtime submodules and no forbidden fields,
  - the `MASTER_STATUS.md` §8 phase-boundary categories continue to be absent from the working tree.
- An update to `MASTER_STATUS.md` §8 step 4a allowlist to extend `allowed_p2_infra` to include exactly the P2-02 directory `src/gmc_rebuild/config` (in addition to the existing `src`), with a comment block citing this PR and this authorization artifact. No other path is added to the allowlist.
- Governance prose updates limited to: `MASTER_STATUS.md` §1, §5, §6, §7, §8 (P2-02 references and allowlist), `README.md`, and `plan/phase2_entry_plan.md` §4 P2-02 entry. The updates describe P2-02 as the only newly-authorized task. P2-03..P2-05 explicitly remain closed.
- A same-day append to `monitoring/daily/2026-05-11.md` documenting PR P2-02's open/merge under ADR-008 §D3, per ADR-008 §D4 (same-day naming convention) and `AI_WORKFLOW.md` §1.4 / §6 rule 1. The append is committed by Codex; no separate slugged packet is created.

The §8 step 4a allowlist after this PR therefore contains exactly two entries: `src` (PR P2-01) and `src/gmc_rebuild/config` (PR P2-02). Both are governance-bounded infrastructure paths and neither is a forbidden category under §8 step 4 / step 4c.

---

## 3. Explicitly Not Authorized

The authorization in §2 does not extend, by implication or otherwise, to any of the following. Each remains forbidden until Kevin records a separate written authorization per `MASTER_STATUS.md` §7 and `AI_WORKFLOW.md` §6 rule 3:

- **P2-03, P2-04, and P2-05** in `plan/phase2_entry_plan.md` §4 (UTC/time utility module, structured logging and audit event conventions, risk-control interfaces). Each requires its own written authorization and its own audit artifact under `governance/authorizations/`.
- Trading strategy code: signals, scanners, models, portfolio rules, backtests.
- Broker code: broker SDK integration, broker execution, order placement, position management.
- Live trading workflows.
- Paper trading workflows wired to a real broker.
- Runtime daemons, schedulers, long-running services, or background workers that touch accounts, markets, or money.
- Market data ingestion code, real data pipelines, or stored datasets.
- Real secrets, private keys, certificates, `.env` files, real account identifiers, real endpoints, real broker names, real venue names.
- Loading configuration from environment variables in a way that could blur the boundary between secrets and runtime behavior. If env-var support is later needed, it must be reauthorized in a separate PR; P2-02 does not introduce it.
- Any submodule under `src/gmc_rebuild/` beyond the `config/` directory named in §2.
- Any relaxation of pre-commit, Ruff, mypy strict mode, detect-secrets, `.gitignore`, `.secrets.baseline`, the `MASTER_STATUS.md` §8 step 4 always-forbidden scan, the §8 step 4c recursive forbidden-token scan, or any other quality gate.
- Any new top-level directory.
- Any `if __name__ == "__main__"` entry point, scheduler, or daemon.

This list mirrors the always-forbidden categories in `MASTER_STATUS.md` §6 and `plan/phase2_entry_plan.md` §2. The mirroring is intentional and not a re-authorization: those categories remain forbidden by default, and naming them here records that Kevin did not silently relax them when authorizing P2-02.

---

## 4. Supporting Evidence (Non-Authoritative)

The following are pointers to the GitHub-side record. They are evidence, not the authorization itself; this file is the authorization of record.

- PR: to be filled in by the PR description once opened (this PR).
- Phase 1 accepted baseline (from which P2-02 descends): `1f101fc`.
- `main` head at authorization: `f7d30af` (PR #10 merge — second monitoring packet).
- Sibling prior authorization: `governance/authorizations/2026-05-11_p2-01.md`.
- Phase 2 entry plan: `plan/phase2_entry_plan.md` §4 (P2-02 entry).

Authorization was given by Kevin in chat on 2026-05-11 UTC after being briefed on the risks and the safe scope. Per `AI_WORKFLOW.md` §7, chat authorization on its own is not sufficient; this in-tree artifact lands as part of the authorized PR and is the audit-visible record of authorization going forward. PR-comment history remains supporting evidence.

---

## 5. How to Use This Record

- `MASTER_STATUS.md` §7 and §8 reference this file as the durable authorization record for P2-02.
- The §8 step 4a allowlist is updated in this PR to include `src/gmc_rebuild/config` alongside `src`, citing this artifact.
- `AI_WORKFLOW.md` §7 continues to require every future phase-opening or phase-expanding authorization (P2-03, P2-04, P2-05, and any later phase) to be captured both in PR history and in a sibling file under `governance/authorizations/` before or as part of the authorized PR. One approver remains: Kevin.
- A reviewer auditing whether P2-02 was authorized should look here first, then cross-reference the named PR on GitHub.
