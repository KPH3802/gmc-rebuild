# P6-08 Planning and Enumeration Authorization (Draft) — Failure-Handling / Exception-Typing Consolidation Across the Phase 6 Surface

Date: 2026-06-15
Authorizer: Kevin (draft pending Kevin's written review / approval — see §Draft Status below)
Scope: Bounded docs/governance-only **P6-08 planning and enumeration** packet for `gmc-rebuild` — records the candidate shape for the eighth Phase 6 implementation task (a **tripwire-only** test packet, modeled on the merged P5-03 / P5-04 / P5-05 / P5-06 / P5-07 precedent, that pins the failure-handling and exception-typing discipline already enforced by the merged P6-01..P6-07 surface: explicit named exception types raised at each layer boundary, no swallowed exceptions inside the merged code, and the structured `audit_event` record contract for every refusal / skip / kill-switch trip / reconciliation mismatch / unhandled error already carried by the merged P6-06 `lifecycle.daily_report` surface) at the **planning level only**, mirroring the pattern established by `governance/authorizations/2026-06-15_p6-07-planning.md`, `governance/authorizations/2026-05-23_p6-06-planning.md`, `governance/authorizations/2026-05-22_p6-05-planning.md`, `governance/authorizations/2026-05-20_p6-04-planning.md`, `governance/authorizations/2026-05-19_p6-03-planning.md`, `governance/authorizations/2026-05-18_p5-07-planning.md`, `governance/authorizations/2026-05-18_p5-06-planning.md`, `governance/authorizations/2026-05-15_p4-04-enumeration-planning.md`, `governance/authorizations/2026-05-14_p4-03-enumeration-planning.md`, and `governance/authorizations/2026-05-14_p4-02-enumeration-planning.md`. **Does not open P6-08, does not authorize any P6-08 implementation, does not authorize any runtime code, does not add or modify any file under `src/**` or `tests/**`, does not change any existing exception class or raise site, does not authorize any new production behaviour, does not authorize any runtime activation, does not authorize any real account data / broker / paper-trading / live-trading / market-data / order-routing / strategy / scheduler / daemon / persistence / filesystem snapshot / external sink / deployment / env-var / secrets / network change, does not extend the `MASTER_STATUS.md` §8 step 4a `allowed_p2_infra` allowlist, does not modify the merged P6-01..P6-07 / P4-06..P4-08 / P2-04 surfaces, does not relax any quality gate, does not promote X10 Layer 5, does not automate backup-monitoring, does not execute any DR drill, does not reinterpret or relax the GOV-02 execution-environment workflow rule, does not create any tag or release, and does not authorize P6-09 or any successor Phase 6 packet.**

## Draft Status

This artifact is a **draft** prepared by the default builder (local Claude Code / Claude Max under the GOV-02 execution-environment workflow rule) at Kevin's written direction on 2026-06-15. It is presented for Kevin's review. Consistent with `AI_WORKFLOW.md` §1.3 ("One approver") and §6 rule 3, **this draft does not by itself constitute approval of anything**, is not committed or merged by the builder on its own authority, and authorizes no implementation. The §Authorization block below reproduces Kevin's written planning directive verbatim and becomes the planning-packet authorization of record once Kevin accepts this draft for merge as a planning-only PR. Per the §Required Implementation Authorization (Future) section, **any P6-08 implementation remains a separate, future, separately-authorized workstream** regardless of whether this planning draft is merged.

## Authorization

The user (Kevin) planning directive for this packet is reproduced below verbatim. Per `AI_WORKFLOW.md` §7 the verbatim block in this file is the planning-packet authorization of record (once accepted per §Draft Status). The operational session-setup / read-order and post-edit instructions from the directive are summarized in §Required Validation rather than reproduced as authorization content.

> We are starting P6-08 PLANNING. Read MASTER_STATUS.md, AI_WORKFLOW.md, ADR-008,
> and plan/phase6_entry_plan.md (esp. the §4 item-7/item-8 failure-handling
> candidate, lines ~154-161). Confirm main is current through the merged P6-07
> operator view.
>
> Per plan line ~161, P6-08 is NOT yet enumerated and requires a planning packet
> plus a documentation-only update to the plan before any implementation. Produce
> PLANNING / ENUMERATION ONLY (docs/governance only — no src/** or tests/**),
> following the exact precedent of governance/authorizations/2026-06-15_p6-07-planning.md:
>
> - Define P6-08 as the failure-handling / exception-typing consolidation across the
>   Phase 6 surface (P6-01..P6-07), per the plan's item-7/8 objective:
>   tripwire-only tests that pin explicit named exception types at each layer
>   boundary, no-swallow invariants, and structured audit_event records for every
>   refusal / skip / kill-switch trip / reconciliation mismatch / unhandled error.
> - Enumerate candidate test location (proposed tests/<...>), closed scope, and the
>   expected invariants (closed-set exception types, structured-record contract,
>   no-swallow, composed-pipeline coverage, inertness self-check).
> - Explicitly mark NOT authorized: any new production behaviour, any change to
>   existing exception classes, any runtime activation, broker, market data,
>   persistence, scheduler, or implementation code.
> - Create governance/authorizations/2026-06-15_p6-08-planning.md.
> - Per ADR-008 §D5, also create the sibling Mode B monitoring packet (merges FIRST).
>
> Then STOP. Open both PRs in required order (Mode B first), do NOT merge, and show
> me both for review.

This authorization is the **planning / enumeration analogue** to `governance/authorizations/2026-06-15_p6-07-planning.md` (merged P6-07 planning / enumeration, PR #179 at `416f550` with sibling Mode B monitoring PR #178 first at `214c456`) and the earlier P6-NN / P5-NN / P4-NN enumeration-planning artifacts. It is intentionally narrower than the merged P6-01..P6-07 implementation authorizations: it does **not** open P6-08, it does **not** add or modify any file under `src/**` or `tests/**`, it does **not** extend the `MASTER_STATUS.md` §8 step 4a `allowed_p2_infra` allowlist (preserved exactly at the sixteen entries on `main` after the merged P6-07 implementation), it does **not** modify any merged Phase 6 / Phase 4 / Phase 2 surface, it does **not** change any existing exception class or raise site (which is itself one of the categories the directive explicitly forbids), and it does **not** open or pre-approve any future P6-09 / OPS / runtime activation / simulation expansion / order semantics change / ops execution work.

### Directive Confirmation (item 1 — what Kevin asked to confirm)

Kevin's directive asked the builder to confirm, before drafting, two facts about the current `main` state. Both are confirmed against the working tree at `main` head `88fa3bf` and recorded here as planning context (this is verification evidence, not a new authorization):

1. **P6-07 (deterministic read-only operator view of dry-run engine state) is the last merged Phase 6 task.** Confirmed. The merged Phase 6 implementation surface on `main` is P6-01 (`signal_intake`), P6-02 (`eligibility`), P6-03 (`decision`), P6-04 (Direction A `simulation` extension), P6-05 (`portfolio_state`), P6-06 (`reporting`, the deterministic daily dry-run report — implementation commit `03cafae` merged via PR #177 at `ed2e962`), and P6-07 (`operator_view`, the deterministic read-only operator view of dry-run engine state — implementation commit `db4530d` merged via PR #181 at `88fa3bf`, with sibling Mode B PR #180 merged first at `04443f6`, and the merged P6-07 planning packet via PR #179 at `416f550` with sibling Mode B PR #178 merged first at `214c456`). No P6-08 implementation, authorization, or planning artifact exists on `main`.

2. **`plan/phase6_entry_plan.md` §4 lines ~154-161 (item 7) records the failure-handling / exception-typing consolidation candidate.** Confirmed. The merged §4 item 7 reads (verbatim): *"PR P6-07 — candidate seventh Phase 6 implementation task (future / not authorized). Objective (planning level): Consolidate the failure-handling and exception-typing discipline across the Phase 6 surface introduced by P6-01..P6-06. Tripwire-only tests modeled on the P5-03 / P5-04 / P5-05 / P5-06 / P5-07 precedent that pin: explicit named exception types at each layer boundary, no swallowed exceptions, structured `audit_event` records for every refusal / skip / kill-switch trip / reconciliation mismatch / unhandled error. Touched directories (candidate): New test file (or files) under the existing `tests/` tree. No new `src/**` directory. No `src/**` modification. Docs-only or implementation: Tripwire-only test packet, no production behavior change. Expected tests (planning level): Closed-set exception-type invariants, structured-record contract assertions against `audit_event` output, no-swallow invariants, composed-pipeline coverage, inertness self-check. §8 step 4a allowlist implication: Preserves the allowlist unchanged. Explicit non-authorizations: Does not authorize any new production behaviour. Does not authorize any change to existing exception classes. Does not authorize any runtime activation, any broker integration, any ops execution work, or any later P6-0N task."* Per Kevin's verbatim 2026-06-15 P6-07 re-aim directive (recorded in the merged P6-07 planning packet at `governance/authorizations/2026-06-15_p6-07-planning.md` §Authorization and §Relationship), P6-07 was re-aimed at the operator view and the failure-handling / exception-typing consolidation candidate slides to P6-08. This planning packet honors that re-aim: the §4 item-7 objective text and constraints are adopted here as the candidate shape for P6-08, and reconciling §4 (renumbering the failure-handling-consolidation candidate to a §4 item 8 P6-08 entry) is recorded as a separate status-keeper workstream (see §Relationship below; **this packet does not edit §4**).

3. **Plan line ~161 ("Further candidate tasks beyond P6-07 (P6-08, P6-09, …) are deliberately not enumerated in this plan").** Confirmed and honored. P6-08 is currently **not yet enumerated** in §4; Kevin's directive asks for a P6-08 planning packet (this artifact) **plus a future documentation-only update to the plan** before any implementation. This packet supplies the planning packet only; the §4 update is the separate status-keeper workstream noted above (`AI_WORKFLOW.md` §1.2 / §6 rule 2). Any further P6-09+ enumeration likewise remains a separate future workstream.

The GOV-02 execution-environment workflow rule (merged on `main` via PR #132 / reconciled via PR #134) governs the build of this packet: repo-editing work for `gmc-rebuild` is by default carried out via Kevin's local Claude Code / Claude Max subscription rather than via Perplexity-managed codebase subagents that consume Perplexity credits. Perplexity Computer continues to act in its `AI_WORKFLOW.md` §1.2 role as Supervisor / Verifier / Status Keeper and as merge-sequencing assistant. The GOV-02 rule does not relax `AI_WORKFLOW.md` §6 rule 1 ("One builder at a time") or any other existing role / cadence / authorization-of-record requirement; this packet is built in compliance with it.

### Exact governance/authorization precedent for P6-07 (the structural model this draft mirrors)

The P6-07 governance trail — the precedent this P6-08 planning draft mirrors — consists of **two** durable in-tree authorization artifacts plus their sibling Mode B monitoring packets:

1. **P6-07 planning / enumeration authorization** — `governance/authorizations/2026-06-15_p6-07-planning.md`, merged via **PR #179** at `416f550`, with sibling Mode B monitoring **PR #178** merged first at `214c456` per ADR-008 §D5. This is the **direct structural precedent for this P6-08 planning draft.** It is a one-file docs-only packet that recorded the candidate P6-07 shape at the planning level only and explicitly did not authorize implementation.
2. **P6-07 implementation authorization (bundled with implementation)** — `governance/authorizations/2026-06-15_p6-07.md`, merged via **PR #181** at `88fa3bf`, with sibling Mode B monitoring **PR #180** merged first at `04443f6` per ADR-008 §D5. This separate, later artifact reproduced Kevin's verbatim *implementation*-authorization directive, resolved the open choices the planning packet deferred (distinct `DryRunOperatorView` name, exclude `SafetyVerdict` from inputs, frozen ten-field value object + `render()` method, closed input set restricted to P6-03 / P6-04 / P6-05 / P6-06), and was bundled with the implementation itself in a single PR.

The two-step "planning packet → separate implementation authorization (bundled with implementation)" cadence is the precedent P6-08 is expected to follow: **this draft is the first step (planning) only.** The P5-03 / P5-04 / P5-05 / P5-06 / P5-07 packets — themselves tripwire-only test packets without a separate planning packet, since the candidate shape was clear from the prior P5-NN surfaces — are the closest structural precedent for the eventual P6-08 *implementation* shape (a single new pytest file or files asserting invariants over the existing merged surfaces with no `src/**` change).

## Current State (Post-P6-07 — for Planning Context Only)

This section records the merged `main` state at packet-authoring time so the candidate shape in §3 references the actual upstream surfaces it would exercise. It records no new authorization.

- `main` head at authoring: `88fa3bf` (PR #181, the P6-07 operator-view implementation; implementation commit `db4530d`).
- Merged Phase 6 surface on `main`:
  - **P6-01** typed signal-intake boundary (`d52551a`; `gmc_rebuild.signal_intake` — `SignalSide`, `SignalIntent`, `accept_signal_intent`). Raises `TypeError` / `ValueError` on construction violations; the existing `tests/signal_intake/test_signal_intake.py` already exercises those raises.
  - **P6-02** eligibility-check pure functions (`4785e24`; `gmc_rebuild.eligibility` — `EligibilityOutcome`, `EligibilityReason`, `EligibilityConfig`, `EligibilityDecision`, `check_eligibility`). Raises `TypeError` / `ValueError` on construction-time and call-site type / value violations.
  - **P6-03** position/risk decision composer (`8d98a41`; `gmc_rebuild.decision` — `PositionDecisionOutcome`, `PositionDecisionReason`, the frozen five-field `PositionDecision`, `compose_position_decision`). Raises `TypeError` / `ValueError` on construction and on `compose_position_decision` argument-type violations.
  - **P6-04** Direction A simulated order intent extension (`cea8553`; inside `gmc_rebuild.simulation` — `SimulatedOrderTimeInForce` `StrEnum`, the ninth `SimulatedOrderIntent` field `time_in_force` defaulting to `DAY`, and `derive_simulated_order_intent_id`). Construction failures raise the typed `SimulationBoundaryError(ValueError)`; the deterministic ID derivation has the same typed-failure surface.
  - **P6-05** deterministic in-memory simulated portfolio state (`da19cfa`, ruff cleanup `ae0668f`; `gmc_rebuild.portfolio_state` — `SimulatedPosition`, `SimulatedPortfolio`, `apply_simulated_order_intent`). Raises `TypeError` / `ValueError` on construction-time and call-site violations.
  - **P6-06** deterministic daily dry-run report (`03cafae`, merged via PR #177 at `ed2e962`; `gmc_rebuild.reporting` — `DailyReport`, `build_daily_report`, `render_daily_report_event`). Raises `TypeError` / `ValueError` on construction-time and call-site violations; `render_daily_report_event` rejects `None` timestamps with `ValueError` to forbid the implicit clock fallback.
  - **P6-07** deterministic read-only operator view of dry-run engine state (`db4530d`, merged via PR #181 at `88fa3bf`; `gmc_rebuild.operator_view` — `DryRunOperatorView`, `build_dry_run_operator_view`). Raises `TypeError` / `ValueError` on construction-time and call-site violations; does not import `gmc_rebuild.runtime` and does not compose with the merged P4-07 `OperatorSafetyView`.
- Merged P4-06 / P4-07 / P4-08 safety surface this candidate may reference for composed-pipeline coverage (`gmc_rebuild.runtime`): `RuntimeShell.evaluate()` → `SafetyVerdict` with `BLOCKER_*` codes; `OperatorSafetyView` / `format_safety_verdict`; the typed `RuntimeShellError(ValueError)` failure type.
- Merged P5-01 / P5-02 simulation boundary surface (`gmc_rebuild.simulation`): `SimulationBoundary.propose` / `propose_order` raise the typed `SimulationBoundaryError(ValueError)` on non-clear safety verdicts (the ADR-002 / ADR-003 / ADR-005 "safe default is no trading" invariant); the existing `tests/simulation/test_simulated_order_intent_invariants.py` / `test_propose_symmetry.py` / `test_composed_safety_foundation.py` / `test_p6_04_order_intent_extension.py` are the merged P5-03 / P5-04 / P5-06 / P6-04 tripwire packets that already pin parts of this surface for Phases 4 and 5.
- Merged P2-04 `gmc_rebuild.logging.audit_event` surface: the **closed** `AUDIT_CATEGORIES = {governance, config, lifecycle}`, the `<category>.<action>` event-name rule, the frozen `AuditEvent`, the deterministic `serialize_event` JSON, and the typed `AuditEventError(ValueError)` failure type. The merged P6-06 `render_daily_report_event` is the only P6 surface currently emitting an `audit_event` (event name `lifecycle.daily_report`, fields: `report_date`, `decisions_total`, `would_trade`, `would_skip`, `applied_intent_ids`, `net_positions`, `reconciliation_status`, `tripped_invariants`).
- Merged `ReconciliationStatus` type (`gmc_rebuild.risk`): the closed reconciliation-status enum preserving the `UNAVAILABLE` vs `FAILED` distinction per ADR-003, carried by value inside `DailyReport.reconciliation_status` and echoed into `DryRunOperatorView.reconciliation_status`.
- §8 step 4a `allowed_p2_infra` allowlist on `main`: exactly **sixteen** entries — `src`, `src/gmc_rebuild/config`, `src/gmc_rebuild/time`, `src/gmc_rebuild/logging`, `src/gmc_rebuild/risk`, `src/gmc_rebuild/heartbeat`, `src/gmc_rebuild/kill_switch`, `src/gmc_rebuild/reconciliation`, `src/gmc_rebuild/runtime`, `src/gmc_rebuild/simulation`, `src/gmc_rebuild/signal_intake`, `src/gmc_rebuild/eligibility`, `src/gmc_rebuild/decision`, `src/gmc_rebuild/portfolio_state`, `src/gmc_rebuild/reporting`, `src/gmc_rebuild/operator_view`.
- §8 step 4 / step 4c forbidden-token set on `main`: `strategy strategies signal signals scanner scanners model models portfolio backtest backtests broker brokers execution executions live paper daemon daemons market_data order orders secret secrets`. (Recorded here verbatim because §3.8 below depends on which candidate P6-08 directory / file names collide with it — the candidate test location `tests/p6_08_failure_handling/` carries no forbidden token; see §3.8.)
- Test baseline on `main`: **696** tests collected and passing (`.venv/bin/pytest -q` post the merged P6-07 implementation: 653 prior baseline + 43 new `tests/operator_view/` tests); `.venv/bin/pre-commit run --all-files` exits 0 with every hook passing. This planning packet adds and modifies no tests; the count is recorded for the §Required Validation expectation only.
- `plan/phase6_entry_plan.md` §4 item 7 currently names the failure-handling / exception-typing consolidation candidate under the **P6-07** label (verbatim quote in §Directive Confirmation item 2 above). Per Kevin's 2026-06-15 P6-07 re-aim, that text describes what is now the P6-08 candidate; reconciling §4 (re-numbering the failure-handling consolidation candidate to a §4 item 8 P6-08 entry) is a separate status-keeper workstream — see §Relationship below.

## Relationship to the merged `plan/phase6_entry_plan.md` §4 item-7/item-8 Failure-Handling Candidate (Discrepancy Flagged Transparently)

The merged `plan/phase6_entry_plan.md` §4 currently records the failure-handling / exception-typing consolidation candidate under **item 7** with the verbatim objective and constraints reproduced in §Directive Confirmation item 2 above. Per Kevin's 2026-06-15 P6-07 re-aim directive (recorded verbatim in the merged P6-07 planning packet at `governance/authorizations/2026-06-15_p6-07-planning.md` §Authorization and §Relationship), the P6-07 label was re-aimed at the operator view and the failure-handling / exception-typing consolidation candidate slides to **P6-08**.

This is recorded here transparently rather than silently contradicted, per `AI_WORKFLOW.md` §6 rule 9 ("Every claim has evidence") and the FAA closed-loop / "when in doubt, surface it" discipline in `docs/GMC_PROJECT_MAP.md`:

- **The failure-handling / exception-typing consolidation candidate is itself a named Phase 6 Layer-2 / `plan/phase6_entry_plan.md` capability.** The §4 item-7 text already enumerates the candidate at the planning level: tripwire-only tests modeled on the P5-03..P5-07 precedent that pin explicit named exception types at each layer boundary, no swallowed exceptions, and structured `audit_event` records for every refusal / skip / kill-switch trip / reconciliation mismatch / unhandled error. So adopting it as P6-08 introduces no non–Phase-6 capability; it carries the §4 item-7 candidate forward under a different `P6-0N` number.
- **Kevin (Approver) defines scope.** Per `AI_WORKFLOW.md` §1.3, Kevin defines scope; the builder and verifier execute and verify. The 2026-06-15 P6-07 re-aim is Kevin's prerogative; the §4 candidate shapes are non-binding and an authorization "is free to replace any element of the candidate shape recorded" in the plan.
- **The §4 reconciliation is a separate workstream.** Reconciling `plan/phase6_entry_plan.md` §4 (renumbering the failure-handling-consolidation candidate from item 7 to a new item 8 under the P6-08 label, with the §4 P6-07 item 7 entry rewritten to record the merged operator view, and the closing "Further candidate tasks beyond P6-07" paragraph updated to read "P6-09, …") is a docs-only status-keeper edit owned by Perplexity Computer per §6 rule 2, to be done in a separate PR — not bundled into this one-file planning packet. This packet neither edits §4 nor claims §4 already names the failure-handling consolidation as P6-08.

Kevin should confirm, at implementation-authorization time, that the **failure-handling / exception-typing consolidation** is the intended P6-08 capability. This packet proceeds on Kevin's explicit 2026-06-15 directive that P6-08 is this consolidation.

## Scope

The work authorized by this artifact is restricted to:

- This authorization artifact at `governance/authorizations/2026-06-15_p6-08-planning.md`.

Per Kevin's verbatim authorization above, **the authorized scope mirrors the narrow one-file scope of the merged P6-07 / P6-06 / P6-05 / P6-04 planning packets** (`governance/authorizations/2026-06-15_p6-07-planning.md` §Scope; `governance/authorizations/2026-05-23_p6-06-planning.md` §Scope; `governance/authorizations/2026-05-22_p6-05-planning.md` §Scope): a single new governance artifact under `governance/authorizations/`. Consistent with that precedent, this packet does **not** modify `MASTER_STATUS.md`, does **not** modify `README.md`, and does **not** modify `plan/phase6_entry_plan.md`. See §Companion-Docs Decision for why no companion-docs edit is required for a planning-only packet, and why the established precedent treats any such edit (including reconciling the §4 item 7 / item 8 P6-08 candidate) as a separate workstream.

The §8 step 4a `allowed_p2_infra` allowlist is **not** extended by this authorization or by the planning document opened under it. It continues to contain exactly the sixteen entries on `main` after the merged P6-07 implementation. The candidate P6-08 shape (§3 below) deliberately introduces no new `src/**` directory at all, so no future allowlist update is even contemplated by the implementation candidate.

## Companion-Docs Decision

Kevin's directive does not ask for companion-docs edits, and on the established precedent **no companion-docs edit is required**, and the minimal change is therefore **none**:

- The merged P6-07 planning packet (`2026-06-15_p6-07-planning.md`), the merged P6-06 planning packet (`2026-05-23_p6-06-planning.md`), and the merged P6-05 planning packet (`2026-05-22_p6-05-planning.md`) were each deliberately **one file**. None added a `MASTER_STATUS.md` §1 planning-status reflection paragraph nor edited `plan/phase6_entry_plan.md`; the `MASTER_STATUS.md` §1 reflections and the `plan/phase6_entry_plan.md` §4 reconciliations for those tasks landed only later, in separate status-keeper PRs. The narrow one-file scope is the precedent for a planning-only packet.
- `MASTER_STATUS.md` §1 records the merged Phase 6 surface through P6-07 and lists any P6-08+ work as **not authorized**. This planning draft does not change that; it is consistent with it. (The §1 P6-07 implementation status reflection is itself a separate Perplexity Computer status-keeper workstream per `AI_WORKFLOW.md` §1.2 / §6 rule 2 and is not this packet's responsibility.)

Accordingly, this draft proposes **no** edit to `MASTER_STATUS.md`, `README.md`, or `plan/phase6_entry_plan.md`. Per `AI_WORKFLOW.md` §1.2 and §6 rule 2 ("One status keeper"), any future `MASTER_STATUS.md` §1 P6-08-planning status reflection — and the reconciliation of the `plan/phase6_entry_plan.md` §4 item 7 P6-07 candidate to a §4 item 8 P6-08 entry (see §Relationship above) — is owned by Perplexity Computer and would be a separate, conservatively-worded reconciliation workstream after this planning PR merges, not part of this packet.

## Candidate P6-08 Shape (Planning Level Only — Future / Not Authorized)

This section records, at the planning level only, the candidate shape for a future P6-08 packet. Recording this shape **does not authorize implementation**. Kevin must independently authorize any P6-08 implementation packet in writing, in a separate authorization artifact under `governance/authorizations/`, with its own scope, constraints, and validation requirements; that future authorization is free to replace any element of the candidate shape recorded here, including the candidate location, the candidate test categories, the candidate invariant set, and the candidate assertion style.

### 3.1 Candidate Objective

The candidate P6-08 task introduces a **tripwire-only test packet** that consolidates and pins the failure-handling and exception-typing discipline already enforced by the merged P6-01..P6-07 surface. It is **characterization-style**: it asserts the *current* contract end-to-end so that any future drift (a swallowed exception, a downgraded raise type, a missing field on the structured audit record, a loosened `None`-timestamp guard) fails before reaching `main`. Modeled on the merged P5-03 / P5-04 / P5-05 / P5-06 / P5-07 tripwire precedent (one new pytest file under `tests/`, no `src/**` change, no new public symbol, no expansion of any merged surface), it is the eighth Phase 6 dry-run capability, downstream of P6-01..P6-07.

The candidate **does not introduce, change, or refactor any production behavior** — including any change to existing exception classes (`SimulationBoundaryError`, `RuntimeShellError`, `RiskControlError`, `AuditEventError`, `NaiveDatetimeError`) or any change to the plain `TypeError` / `ValueError` / `FrozenInstanceError` raise sites in the merged P6-01..P6-07 modules. It records what the merged surface already does and freezes it as a contract.

### 3.2 Candidate Test Location

- **Candidate location (per Kevin's directive) — new pytest-only test directory `tests/p6_08_failure_handling/`** (suggested name; the implementation authorization names the exact path) or a single new test file under an existing `tests/<layer>/` directory, mirroring the merged P5-03 (`tests/simulation/test_simulated_order_intent_invariants.py`) / P5-04 (`tests/simulation/test_composed_safety_foundation.py`) / P5-05 (`tests/runtime/test_operator_view_composed_safety_foundation.py`) / P5-06 (`tests/simulation/test_propose_symmetry.py`) / P5-07 (`tests/runtime/test_operator_view_determinism.py`) precedent (one new tripwire pytest file, no existing test modification). The implementation authorization may instead split the tripwires into one file per layer (`tests/p6_08_failure_handling/test_signal_intake_failure_modes.py`, `test_eligibility_failure_modes.py`, `test_decision_failure_modes.py`, `test_portfolio_state_failure_modes.py`, `test_reporting_failure_modes.py`, `test_operator_view_failure_modes.py`, `test_composed_pipeline_failure_modes.py`); the planning packet does not pre-commit either layout.
- The candidate names `p6_08_failure_handling`, `failure_handling`, `failure_modes`, `composed_pipeline`, `signal_intake_failure_modes`, etc. do **not** collide with the §8 forbidden-token set (`strategy strategies signal signals scanner scanners model models portfolio backtest backtests broker brokers execution executions live paper daemon daemons market_data order orders secret secrets`). Note that `signal_intake_failure_modes` tokenizes to `signal`, `intake`, `failure`, `modes` and trips the forbidden `signal` token at §8 step 4c — exactly as the merged `src/gmc_rebuild/signal_intake/` directory does; the implementation authorization may use a non-tokenized form (e.g. `test_intake_failure_modes.py`) or name the test file with a path that does not include the bare `signal` token. The planning packet records the candidate and pre-commits to none of the names.
- **No new `src/**` directory.** No new public symbol. No change to any merged source module. The implementation PR adds exactly the new test file(s), the implementation authorization artifact, this kind of conservative status-doc reflection (per the merged P5-03..P5-07 precedent), and the sibling Mode B monitoring packet.

This planning packet records the candidate location and pre-commits to none of the candidate names; the implementation authorization names the exact directory, file paths, and public symbols (if any test helpers are exposed; per the P5-03..P5-07 precedent, all helpers are private to the new module).

### 3.3 Candidate Tripwire Test Shape — To Be Made Before Implementation

The implementation authorization must choose, in writing, **before any implementation work begins**, the exact tripwire layout and assertion style. This planning packet records the candidate shape without pre-committing:

- **Style: tripwire / characterization tests.** No `pytest.raises` (the merged P5-NN / P6-NN test convention avoids it so the mypy-strict pre-commit hook does not need a pytest-stub dependency). Exception expectations use a small private `_expect_error(exc_type, fn, *args, **kwargs)` helper modeled on the merged `tests/reporting/test_reporting.py` and `tests/operator_view/test_operator_view.py` precedent. All test helpers are private to the new module; no helper is exported.
- **Closed import surface.** The new module imports only `__future__`, `ast`, `dataclasses`, `datetime` (the stdlib timezone-aware `datetime` type, for the rare `render_daily_report_event` timestamp-required call site), `pathlib`, `types.MappingProxyType` (for `SafetyVerdict` construction), and the already-merged in-repo modules required to exercise the existing surfaces (`gmc_rebuild.decision`, `gmc_rebuild.eligibility`, `gmc_rebuild.heartbeat`, `gmc_rebuild.kill_switch`, `gmc_rebuild.logging` for `AuditEvent` / `audit_event` / `AUDIT_CATEGORIES` / `serialize_event` / `AuditEventError`, `gmc_rebuild.operator_view`, `gmc_rebuild.portfolio_state`, `gmc_rebuild.reconciliation`, `gmc_rebuild.reporting`, `gmc_rebuild.risk`, `gmc_rebuild.runtime` for `BLOCKER_*` / `SafetyVerdict` / `RuntimeShell` / `RuntimeShellError`, `gmc_rebuild.signal_intake`, `gmc_rebuild.simulation`). No `os`, `socket`, `requests`, `urllib`, `http`, `threading`, `asyncio`, `subprocess`, `sqlite3`, `pickle`, `shelve`, `ssl`, `smtplib`, `ftplib`, `time`, `uuid`, `random`, or third-party dependency.
- **Closed assertion vocabulary.** Each assertion either (a) asserts that calling an already-merged public function with a specifically-malformed argument raises a specifically-named exception type (`TypeError`, `ValueError`, `FrozenInstanceError`, `SimulationBoundaryError`, `RuntimeShellError`, `AuditEventError`), (b) asserts that the produced exception's message contains a specific substring that names the violated invariant, (c) asserts that the merged `lifecycle.daily_report` `AuditEvent` carries the documented closed field set with the documented types, or (d) asserts a no-swallow invariant (see §3.9 item 3).

### 3.4 Relationship to the Existing P6-01..P6-07 Exception Surface (Read-Only Inventory)

This is the **P6-08 analogue of the P6-07 planning packet's §3.4 naming consideration**: a non-obvious decision the implementation authorization must resolve, flagged at planning time so the eventual tripwires faithfully record the *existing* contract rather than a wished-for one.

The merged P6-01..P6-07 surface uses two layers of failure type:

1. **Plain `TypeError` / `ValueError` / `FrozenInstanceError` at the P6-01..P6-07 boundaries.** Every merged P6-NN module raises plain stdlib `TypeError` for argument-type violations, plain stdlib `ValueError` for argument-value / invariant violations, and inherits `FrozenInstanceError` from the dataclass `frozen=True` discipline on every value-object assignment attempt. The exception *messages* name the violated invariant (e.g. `"decisions must be a tuple, got list"`, `"PositionDecision with outcome=WOULD_TRADE must have empty reasons"`, `"DryRunOperatorView invariant violated: would_trade + would_skip must equal decisions_total"`). The merged tests under `tests/decision/`, `tests/eligibility/`, `tests/portfolio_state/`, `tests/reporting/`, `tests/operator_view/`, and `tests/signal_intake/` already assert this at the **per-layer** level.

2. **Typed `Error` subclasses at upstream / foundation boundaries.** `SimulationBoundaryError(ValueError)` from `gmc_rebuild.simulation`, `RuntimeShellError(ValueError)` from `gmc_rebuild.runtime`, `RiskControlError(ValueError)` from `gmc_rebuild.risk`, `AuditEventError(ValueError)` from `gmc_rebuild.logging`, and `NaiveDatetimeError(ValueError)` from `gmc_rebuild.time`. These are inherited from Phases 4 and 5 and are already pinned by the merged P5-03 / P5-04 / P5-06 / P5-07 / P4-06..P4-08 tripwire packets.

The candidate P6-08 packet must resolve:

1. **Pin the plain-`TypeError` / plain-`ValueError` contract at each P6-01..P6-07 boundary.** The lower-risk planning default is to pin the *existing* plain stdlib types — the implementation does **not** introduce new typed exception classes (Kevin's directive explicitly forbids "any change to existing exception classes"). Each tripwire asserts the exact type and a substring of the message; future drift (e.g. someone changing `raise ValueError("...")` to `raise RuntimeError("...")`) fails the tripwire.
2. **Pin the typed-Error contract at the P4 / P5 / P2-04 surfaces the P6 surface composes with.** Where a P6 path transitively raises `SimulationBoundaryError` / `RuntimeShellError` / `AuditEventError` (e.g. `compose_position_decision(intent, eligibility, blocked_verdict)` does not itself raise; the upstream `RuntimeShell.evaluate()` produces a non-clear `SafetyVerdict` which `compose_position_decision` then records as `WOULD_SKIP`), the tripwire asserts the *contract for the upstream raise site* as exercised through the P6 surface.
3. **Pin the structured `audit_event` record contract for the merged P6-06 `lifecycle.daily_report`.** Assert the category is `lifecycle`, the name is `lifecycle.daily_report`, the fields tuple is the documented closed eight-field set, `serialize_event` is deterministic for identical inputs and the same caller-supplied timestamp, and `render_daily_report_event(None timestamp)` raises `ValueError` with the documented substring. P6-08 does **not** introduce new `audit_event` emissions on other P6 surfaces; the directive's "structured `audit_event` records for every refusal / skip / kill-switch trip / reconciliation mismatch / unhandled error" is interpreted as the P6-06 daily report's closed-field record carrying those categories end-to-end (the would-skip counts, the reconciliation status preserving `UNAVAILABLE` vs `FAILED`, the tripped-invariant tuple) — not as a new per-event emission.

This packet records all three considerations and **pre-commits to none**, flagging the pin-existing-types default and the existing-`audit_event`-surface-only default as the lower-risk planning posture. **This packet authorizes no change to, and no new emission on, the merged exception, P2-04 audit, or P6-06 daily-report surfaces.**

### 3.5 Typed Input Boundary (Planning-Level Candidates — Closed Inputs, Existing P6-01..P6-07 Public Surfaces Only)

The candidate P6-08 tests accept only already-merged public surfaces and the values they themselves produce. No raw configuration slice, no environment variable, no filesystem, no broker session, no market-data snapshot, no real account, **no live data**, and **no implicit clock read** (see §3.7). Per Kevin's directive and the P5-03..P5-07 precedent, the closed inputs are restricted to the existing public surfaces:

- The merged P6-01..P6-07 public surfaces, constructed inline in the test file from constants.
- The merged P3-03 / P3-04 / P3-05 in-memory fakes (`InMemoryHeartbeat`, `InMemoryKillSwitch`, `InMemoryReconciliation`) for composed-pipeline coverage that exercises blocked-verdict paths through `RuntimeShell.evaluate()`. Construction-only; no runtime activation; no `__main__`; no daemon (the P5-03..P5-07 precedent is identical).
- The merged P4-06 `RuntimeShell` / `SafetyVerdict` and the merged P4-07 `OperatorSafetyView` for the safety-verdict composed-pipeline path.
- The merged P5-01 / P5-02 `SimulationBoundary` / `SimulatedOrderIntent` for the upstream `SimulationBoundaryError` paths.
- A small inline fixed timezone-aware UTC `datetime` for the rare `render_daily_report_event` call site (no `now_utc()` / `time.*` / `datetime.now()`; see §3.7).

The implementation authorization picks which of these surfaces a particular tripwire exercises and names the exact test-file layout. It must **not** add any input outside the merged Phase 2 / 3 / 4 / 5 / 6 public surfaces (no broker, no market-data feed, no real account, no env-var, no filesystem read of anything except via stdlib `pathlib` for the inertness self-check). The planning packet does not pre-commit the exact input set.

### 3.6 Typed Output Boundary (Planning-Level Candidates — Closed Outputs)

- **No new public symbol.** No new value type, no new function, no new exception class, no new audit category, no new audit event name. The tripwire tests are pure assertions; they emit only pytest pass/fail signals.
- **Closed assertion surface.** Each assertion either (a) asserts a specific exception type is raised from a specific call (per §3.3), (b) asserts a specific substring is present in the exception message, (c) asserts a specific field of a merged value object equals a specific value, (d) asserts a specific structured-record contract (the `lifecycle.daily_report` event's closed-field set), or (e) asserts a no-swallow invariant via AST inspection (per §3.9 item 3).
- **No new test-helper export.** All helpers are private to the new module; no helper is re-exported, no helper module is added to a package `__init__.py`.

The exact assertion layout and the exact substring-match strings are planning-level candidates; the implementation authorization names them.

### 3.7 Deterministic Construction Contract (Planning Level Only)

All elements are planning-level candidates; the implementation authorization names the exact contract.

- **Determinism.** Each tripwire runs deterministically. Identical inputs and identical fixed timestamps produce the byte-for-byte identical pytest verdict every run. No global state, no module-level mutable, no cache, no monotonic counter, no `random`, no UUID-from-entropy, no set-ordering nondeterminism.
- **No clock read.** The new module is a projection of already-merged values; it takes **no** timestamp of its own and does **not** call `now_utc()` / `time.*` / `datetime.now()`. Any `datetime` value used for the `render_daily_report_event` call site is a fixed inline literal (e.g. `datetime(2026, 6, 15, 12, 0, 0, tzinfo=UTC)`), mirroring the merged `tests/reporting/test_reporting.py` and `tests/operator_view/test_operator_view.py` convention.
- **No mutation of inputs.** The supplied merged-surface values are not modified in place. All produced values are frozen / value-typed.
- **No hidden state and no side effects.** The tests hold no module-level / class-level / instance mutable state, no cache, no counter, no buffer. They do **not** install logging handlers, call `logging.basicConfig`, write to any sink, touch the filesystem (except via `pathlib.Path.read_text` for the inertness self-check on the new module's own source file), read env vars, open a socket, or call `time.sleep` / `asyncio.sleep`.
- **Closed import surface.** As enumerated in §3.3.
- **No re-export from `src/gmc_rebuild/__init__.py`.** Test-only packet; no `src/**` change at all.

### 3.8 §8 Step 4a Allowlist Impact and Directory-Name Consideration

- **Allowlist preserved unchanged.** The candidate P6-08 packet introduces **no new `src/**` directory** at all. The §8 step 4a `allowed_p2_infra` allowlist remains exactly at the sixteen entries on `main`; the implementation PR adds no entry, no `pr_tag` arm, no iteration entry, no comment block, and no OK-echo extension.
- **Test directory name considerations.** Under the §3.2 candidate location, the implementation PR adds a new `tests/<name>/` directory (test-only; not on the §8 step 4a allowlist by design — `tests/` is not subject to the same per-directory allowlist as `src/`). The candidate names recorded in §3.2 (`p6_08_failure_handling`, `failure_handling`, `failure_modes`, etc.) do **not** collide with the §8 forbidden-token set. If the implementation authorization elects to split the tripwires into per-layer test files, any file named `test_signal_intake_failure_modes.py` will trip the forbidden `signal` token at §8 step 4c exactly as the merged `src/gmc_rebuild/signal_intake/` directory does — that is an expected, authorized flag (per the P6-01 precedent, the forbidden-token set is not weakened), and the implementation authorization may instead use a non-tokenized form (`test_intake_failure_modes.py`) to avoid the §8 step 4c flag entirely. The planning packet records the consideration and pre-commits to no specific filename.

This planning packet performs no allowlist change.

### 3.9 Proposed Test Matrix and Invariants

The candidate tests are expected, at the planning level only, to cover the following invariant categories. Each is a planning-level candidate; Kevin's future P6-08 implementation authorization may add, remove, or replace any category.

1. **P6-01 `accept_signal_intent` / `SignalIntent` exception-type contract.** Asserts: `SignalIntent(intent_id=None, ...)` raises `TypeError` with a message naming `intent_id`; `SignalIntent(intent_id="", ...)` raises `ValueError` with a message naming `intent_id`; assigning to any field raises `FrozenInstanceError`; `accept_signal_intent(non_intent)` raises `TypeError`.
2. **P6-02 `check_eligibility` / `EligibilityConfig` / `EligibilityDecision` exception-type contract.** Asserts: each construction-time invariant on `EligibilityConfig` (allowed-symbols type, min/max quantity ordering, min-rationale-length positivity) raises the documented type; `EligibilityDecision` `ELIGIBLE iff reasons == ()` biconditional violations raise `ValueError`; `check_eligibility(non_intent, ...)` raises `TypeError`; `check_eligibility(intent, non_config)` raises `TypeError`.
3. **P6-03 `compose_position_decision` / `PositionDecision` exception-type contract.** Asserts: `compose_position_decision(non_intent, ...)` raises `TypeError`; `compose_position_decision(intent, non_eligibility, ...)` raises `TypeError`; `compose_position_decision(intent, eligibility, non_verdict)` raises `TypeError`; `PositionDecision(outcome=WOULD_TRADE, reasons=(REASON,), ...)` raises `ValueError` (biconditional violation); assigning to any field raises `FrozenInstanceError`.
4. **P6-04 `SimulatedOrderIntent` / `derive_simulated_order_intent_id` typed-Error contract.** Asserts: construction-time field violations raise `SimulationBoundaryError(ValueError)` (the typed subclass — already pinned by the merged P5-03 / P6-04 tripwires; P6-08 records it here for end-to-end coverage); `derive_simulated_order_intent_id(...)` with malformed inputs raises the documented type; the deterministic-ID contract (identical inputs → identical ID) is asserted as a separate determinism tripwire.
5. **P6-05 `SimulatedPosition` / `SimulatedPortfolio` / `apply_simulated_order_intent` exception-type contract.** Asserts: zero-quantity `SimulatedPosition`, unsorted `SimulatedPortfolio.positions`, repeated `applied_intent_ids`, and non-`SimulatedPosition` portfolio members each raise the documented type with the documented substring; `apply_simulated_order_intent(portfolio, decision=skip_decision, ...)` either rejects or no-ops (whichever the merged surface does — recorded faithfully); `apply_simulated_order_intent` is idempotent under repeated application of the same intent ID (the merged P6-05 contract).
6. **P6-06 `build_daily_report` / `render_daily_report_event` / `DailyReport` exception-type contract and structured-record contract.** Asserts: `build_daily_report(decisions=non_tuple, ...)` raises `TypeError`; `build_daily_report(portfolio=non_portfolio, ...)` raises `TypeError`; `DailyReport` field-validation violations (empty `report_date`, count mismatch, unsorted `applied_intent_ids`, zero-quantity `net_positions` entry, non-`ReconciliationStatus`) raise the documented type; `render_daily_report_event(report, timestamp=None)` raises `ValueError` with the documented "explicit caller-supplied timestamp" substring (the forbids-implicit-clock-fallback contract); `render_daily_report_event(non_report, ...)` raises `TypeError`; the produced `AuditEvent` carries `category == "lifecycle"`, `name == "lifecycle.daily_report"`, `category in AUDIT_CATEGORIES`, and the documented closed eight-field `fields` set; `serialize_event` is deterministic for identical inputs and the same caller-supplied timestamp.
7. **P6-07 `build_dry_run_operator_view` / `DryRunOperatorView` / `render` exception-type contract.** Asserts: `build_dry_run_operator_view(decisions=non_tuple, ...)` raises `TypeError`; `build_dry_run_operator_view(order_intents=non_tuple, ...)` raises `TypeError`; `build_dry_run_operator_view(portfolio=non_portfolio, ...)` raises `TypeError`; `build_dry_run_operator_view(report=non_report, ...)` raises `TypeError`; `DryRunOperatorView` field-validation violations (empty `report_date`, count mismatch, `decision_lines` length-mismatch, non-tuple line containers, empty-string line entries, empty `reconciliation_status`) raise the documented type with the documented substring; `DryRunOperatorView.render()` is deterministic and emits no side effect.
8. **No-swallow invariants via AST inspection.** AST-walk every merged Phase 6 source file under `src/gmc_rebuild/signal_intake/`, `src/gmc_rebuild/eligibility/`, `src/gmc_rebuild/decision/`, `src/gmc_rebuild/portfolio_state/`, `src/gmc_rebuild/reporting/`, and `src/gmc_rebuild/operator_view/` and assert: no `try` / `except Exception` / `except BaseException` / bare `except:` block exists (i.e. no swallowed exception); every `try` block is paired with a specific exception type. (The merged P5-03 / P5-04 / P5-07 / P6-06 / P6-07 tripwire packets all use a similar AST self-check on import surfaces; the P6-08 candidate extends the AST self-check to `Try` / `ExceptHandler` nodes for the no-swallow contract.)
9. **Composed-pipeline coverage.** End-to-end tests that construct a P6-01 `SignalIntent` → P6-02 `EligibilityDecision` → P6-03 `PositionDecision` (against both a clear and a blocked `SafetyVerdict` produced by `RuntimeShell.evaluate()` with each of the five `BLOCKER_*` codes driven by the merged P3-03 / P3-04 / P3-05 in-memory fakes) → P5-02 / P6-04 `SimulatedOrderIntent` (via `SimulationBoundary.propose_order` on the clear path, and asserting `SimulationBoundaryError` on each blocked path with the documented blocker codes surfaced in the error message) → P6-05 `SimulatedPortfolio` (via `apply_simulated_order_intent`) → P6-06 `DailyReport` (via `build_daily_report`) → P6-07 `DryRunOperatorView` (via `build_dry_run_operator_view`), asserting (a) the clear-path produces the expected `WOULD_TRADE` / `would_trade == 1` / non-empty `applied_intent_ids` / non-empty `decision_lines` / non-empty `order_intent_lines` flow; (b) each blocked-path produces the expected `WOULD_SKIP` / `would_skip == 1` / empty `applied_intent_ids` / `decision_lines` containing the documented blocker reason / `reconciliation_status` preserving the `UNAVAILABLE` vs `FAILED` distinction; and (c) the daily-report `lifecycle.daily_report` audit event correctly carries the cycle's refusal / skip / kill-switch-trip / reconciliation-mismatch / tripped-invariant codes.
10. **Inertness self-check.** AST-walk every Python file under the new test module(s) and assert imports are drawn only from a closed authorized prefix set (per §3.3) and that no forbidden runtime root (`os`, `socket`, `requests`, `urllib`, `http`, `threading`, `asyncio`, `subprocess`, `sqlite3`, `pickle`, `shelve`, `ssl`, `smtplib`, `ftplib`, `time`, `uuid`, `random`) is imported. (Mirrors the merged P5-03..P5-07 / P6-06 / P6-07 inertness precedent.)
11. **Substring-scan self-check.** Belt-and-suspenders scan over the new test module(s) for `__main__` blocks, `time.sleep(`, `asyncio.sleep(`, `socket.`, `urllib`, `requests.`, the `open(` builtin, `uuid.`, `random.`, `logging.basicConfig`, and any handler-installation call — confirming no sink/handler is installed and no runtime activation exists.
12. **No new public symbol / no re-export.** A test asserts `gmc_rebuild`, `gmc_rebuild.signal_intake`, `gmc_rebuild.eligibility`, `gmc_rebuild.decision`, `gmc_rebuild.portfolio_state`, `gmc_rebuild.reporting`, and `gmc_rebuild.operator_view` all expose exactly the public names already documented; `__all__` is unchanged; no new attribute appears (i.e. P6-08 added nothing to the production surface).
13. **Baseline preservation.** The existing 696-test suite continues to pass unchanged, plus the new P6-08 tripwire tests; the new total is documented in the implementation PR.

The expected new-test count is a planning-level estimate (the merged P5-03 / P5-04 / P5-05 / P5-06 / P5-07 tripwire packets added between ~10 and ~40 tests each); the exact count is a P6-08 implementation responsibility, not a P6-08 planning responsibility.

### 3.10 Safety and Non-Goals (Safety Boundaries)

The candidate P6-08 packet explicitly **does not**, regardless of the chosen tripwire layout:

- **Change any production behavior.** No new public symbol, no new exception class, no signature change, no docstring change on any merged module, no `__all__` change. The merged P6-01..P6-07 modules are byte-for-byte unchanged by this packet.
- **Change any existing exception class.** `SimulationBoundaryError`, `RuntimeShellError`, `RiskControlError`, `AuditEventError`, and `NaiveDatetimeError` are unchanged. The plain `TypeError` / `ValueError` / `FrozenInstanceError` raise sites in P6-01..P6-07 are unchanged. No new typed exception class is introduced. (Kevin's directive explicitly forbids this.)
- **Introduce any new `audit_event` emission.** The merged P6-06 `lifecycle.daily_report` event is the only P6 audit emission; P6-08 asserts its contract end-to-end without adding new emissions on the P6-01..P6-05 / P6-07 surfaces.
- **Perform any I/O** beyond `pathlib.Path.read_text` on the new test module's own source file for the AST self-check. No print, no socket, no HTTP endpoint, no `open(` for write, no `pickle`, no `shelve`, no sink of any kind.
- **Persist** anything. No SQLite, DuckDB, Postgres, on-disk database, on-disk audit trail, render file, or any file artifact written from any runtime path.
- **Activate any runtime.** No `__main__` entry point, no `if __name__` block, no daemon, no scheduler, no background thread, no cron / launchd, no long-running service, no `time.sleep`, no `asyncio.sleep`. No re-export from `src/gmc_rebuild/__init__.py`.
- Read the **wall clock**. No `now_utc()` / `time.time()` / `datetime.now()`; any `datetime` value is a fixed inline literal.
- Touch the **network**. No `socket`, `urllib`, `requests`, `http`, `ssl`, `smtplib`, `ftplib`, or any outbound/inbound network code.
- Use any **live data**. No real-time feed, no market data, no broker session, no account sync.
- Maintain or display any **real account data**, real position book, real account identifier, real balance, real holdings, or real P&L.
- Place any **broker call**, real or paper order, market-data lookup, order routing, or venue selection.
- Hold any hidden state. No module-level / class-level mutable, no cache, no counter, no buffer.
- Modify any merged surface. P6-08 is a pure *consumer* of the merged P6-01..P6-07 / P5 / P4 / P3 / P2-04 surfaces, modifying none of them.
- Implement strategy logic. Tripwiring already-merged tests is bookkeeping, not strategy.
- Load any external configuration. No import of `gmc_rebuild.config`, no `.env` reading, no YAML / TOML / JSON loading from disk.

## Expected Files a Future Implementation Would Likely Touch

Recorded at planning level only; the implementation authorization names the exact files. Under the §3.2 candidate location (one new tripwire test file or a small set under a new `tests/<name>/` directory):

- One or more new test files under `tests/<name>/` (e.g. `tests/p6_08_failure_handling/test_phase_6_failure_modes.py`, or per-layer files as enumerated in §3.2). (New file(s).)
- `governance/authorizations/2026-<YYYY-MM-DD>_p6-08.md` — the durable implementation-authorization artifact reproducing Kevin's verbatim implementation directive at that time. (New file.)
- `MASTER_STATUS.md` §1 — a conservative implementation-status reflection (Perplexity Computer / status-keeper responsibility, may land in or after the implementation PR per the established cadence). **Not** edited by this planning packet.
- `plan/phase6_entry_plan.md` §4 — the reconciliation of the item-7 P6-07 candidate to a §4 item 8 P6-08 entry (status-keeper responsibility; separate workstream per §Relationship above — not necessarily part of the implementation PR).

This packet adds **none** of the above. No `MASTER_STATUS.md` §8 step 4a allowlist update is required by the implementation candidate (no new `src/**` directory).

## Expected Proof Bundle

Any future P6-08 implementation PR must carry the full proof bundle defined in `AI_WORKFLOW.md` §3 and restated in `plan/phase6_entry_plan.md` §6. At minimum:

- **Git state.** `git status` (clean after commit), `git log --oneline -10`, `git rev-parse HEAD`, and `git diff <baseline>..HEAD` against the accepted Phase 1 baseline `1f101fc` (or a later accepted baseline recorded in `MASTER_STATUS.md` §3).
- **Tests and pre-commit.** Full output of `.venv/bin/pre-commit run --all-files` (must pass with no second-pass modifications) and `.venv/bin/python -m pytest -q` (must pass; the 696 baseline preserved plus the documented new P6-08 tripwire tests).
- **Phase-boundary checklist** (`plan/phase6_entry_plan.md` §6.3): no new top-level directories outside the documented set; no forbidden-token paths (the §8 step 4 / 4c scan returns OK — `p6_08_failure_handling` does not collide; the implementation authorization avoids any `signal_intake_failure_modes` filename that would trip the bare `signal` token if simpler alternatives exist); no `__main__` / scheduler / daemon; no secrets / credentials / account identifiers / generated data files; no broker / market-data / scheduler dependency; no runtime activation of any merged Phase 3 fixture (construction-only in pytest is not a runtime activation, exactly as in the merged P5-03..P5-07 precedent); no I/O / external sink / persistence; no quality-gate relaxation; no allowlist change.
- **§8 evidence.** Evidence the §8 step 4a startup gate is unchanged (no new entry needed because no new `src/**` directory is introduced) and the §8 step 4 / 4c token scan returns OK.
- **File-specific evidence** (`AI_WORKFLOW.md` §3.4) for each new file.
- **ADR-008 / ADR-009 cadence status** and **Mode A / Mode B status** named in the verification report (§6.6 / §6.7 of the entry plan).
- **Canonical-doc staleness check** (`MASTER_STATUS.md` §8 step 8): no stale `**pending merge**` language for already-merged packets.

## Allowed Planning Topics

While the P6-08 planning / enumeration workstream is opened by this authorization, the builder (local Claude Code / Claude Max per the GOV-02 execution-environment rule) may draft planning artifacts on the following topics. Planning artifacts are documents, not code, and must be reviewable as documents.

1. **Updated current post-P6-07 state in prose** (with the canonical reconciliations preserved verbatim and no stale `**pending merge**` language reintroduced for any already-merged packet).
2. **Naming P6-08 only as future / not authorized.** Recording a candidate shape does not authorize it; the plan must not pre-commit any specific P6-08 file path, test directory, function, class name, or assertion text as an authorized choice.
3. **Phase 6 entry criteria as they apply to a future P6-08 PR** (baseline, governance, ADRs, ADR-008 §D3 / §D5 cadence during the governance phase and ADR-009 D3 / D5 once runtime exists on `main` per ADR-009 D7's bootstrap-avoidance clause, Mode A / Mode B review per `AI_WORKFLOW.md` §4).
4. **Per-PR proof-bundle expectations** (restated from `AI_WORKFLOW.md` §3 and `plan/phase6_entry_plan.md` §6 — see §Expected Proof Bundle above).
5. **Rollback and stop conditions** (restated from `plan/phase6_entry_plan.md` §8 — see §Stop Conditions below).
6. **Phase-boundary enforcement** — how any future P6-08 PR would prove it did not cross into real account data, a real position book, broker execution, paper/live trading, market-data ingestion, order routing, strategy logic, scheduler / daemon, persistence, any I/O / external sink, deployment, env-var loading, secrets, network, allowlist expansion (none should be needed), quality-gate relaxation, or merged-surface modification.
7. **The §4-item-7 / §4-item-8 relationship** — recording (without editing `plan/phase6_entry_plan.md`) that Kevin's directive re-aims the failure-handling consolidation candidate from §4 item 7 (P6-07 label, now operator view) to a §4 item 8 (P6-08 label, failure-handling consolidation) and that reconciling §4 is a separate status-keeper workstream (see §Relationship above).
8. **Execution-environment compliance (GOV-02).** Confirmation that the planning work is carried out via the default builder environment (local Claude Code / Claude Max). Perplexity Computer's read-only verification, status-keeping, and merge-sequencing role remains unchanged.

Any planning topic that does not fit the list above must be raised with Kevin before a document is written, per `AI_WORKFLOW.md` §6 rule 7 ("No phase drift").

## Stop Conditions

Restated for P6-08 from `plan/phase6_entry_plan.md` §8. If any of the following occurs, P6-08 work pauses and the situation is escalated to Kevin before further changes:

1. A P6-08 PR cannot satisfy the proof bundle without expanding scope.
2. A P6-08 PR is found to cross into strategy, broker, paper/live trading, market-data ingestion, real account data, real secrets, order placement, scheduler / daemon, persistence outside the test sandbox, any I/O / external sink, runtime activation of any merged Phase 3 fixture beyond what is named in its written authorization, simulation expansion beyond what is named, order-semantics change beyond what is named, or ops execution work beyond what is named — at review time or after merge.
3. `pre-commit` or `pytest` fails and the proposed fix is to weaken a hook, loosen mypy strict mode, relax `detect-secrets`, or modify `.secrets.baseline` to silence a real finding.
4. `MASTER_STATUS.md` and a P6-08 PR disagree about the current phase, the accepted baseline, the `allowed_p2_infra` allowlist (which P6-08 does not change), or the allowed next decisions.
5. Two changes attempt to land on `main` simultaneously, violating `AI_WORKFLOW.md` §6 rule 4.
6. The Backup AI (when invoked) identifies a specific invariant violation the verifier did not catch.
7. A required Mode B monitoring packet under ADR-008 §D3 (or ADR-009 D3 once in force) is missing for a prior active workday and no catch-up note per ADR-008 §D5 (or ADR-009 D5) has been committed.
8. A P6-08 PR proposes to add, remove, rename, or re-type any existing exception class (`SimulationBoundaryError`, `RuntimeShellError`, `RiskControlError`, `AuditEventError`, `NaiveDatetimeError`, or any plain `TypeError` / `ValueError` raise site in P6-01..P6-07). Tripwiring the *existing* contract is authorized; *changing* it is not.
9. A P6-08 PR proposes any new `audit_event` emission on any P6 surface beyond the merged P6-06 `lifecycle.daily_report`, contradicting Kevin's directive ("no new production behaviour"). Tripwiring the *existing* P6-06 structured-record contract is authorized; *adding* new emissions is not.
10. A P6-08 PR proposes any change to a merged source module (`src/**`) at all. The candidate is strictly test-only.
11. A P6-08 PR proposes any I/O, handler installation, runtime activation, or external sink from the new test module, contradicting the read-only / no-side-effect contract.
12. A P6-08 PR proposes any reintroduction of stale `**pending merge**` language for already-merged packets.

Per `AI_WORKFLOW.md` §6 rule 10: "When in doubt, stop. A paused task is recoverable. A merged Phase 6 change made by accident is not."

## Explicitly Not Authorized

The authorization above does not permit, by implication or otherwise, any of the following. Each remains forbidden until Kevin records a separate written authorization per `MASTER_STATUS.md` §7 and `AI_WORKFLOW.md` §6 rule 3 / rule 7, with a sibling artifact under `governance/authorizations/` and (where it introduces a new directory under `src/**`) a §8 step 4a allowlist update in the same PR:

- **Any P6-08 implementation.** P6-08 (and any later P6-0N task) is **future / not authorized**. The candidate shape in §3 is planning-level only and is not pre-authorized. The implementation authorization is the authorization of record for any P6-08 `tests/**` change.
- **Any new production behaviour.** No new public symbol, no new function, no new class, no signature change, no docstring change on any merged module. The merged `src/**` tree is byte-for-byte unchanged by the implementation candidate.
- **Any change to existing exception classes.** `SimulationBoundaryError`, `RuntimeShellError`, `RiskControlError`, `AuditEventError`, and `NaiveDatetimeError` are not renamed, re-typed, moved, or extended. The plain `TypeError` / `ValueError` / `FrozenInstanceError` raise sites in P6-01..P6-07 are not changed. No new typed exception class is introduced.
- **Any choice of test layout, file count, or assertion text.** This planning packet does not choose the §3.2 layout (single file vs per-layer files), the §3.3 assertion style, the §3.6 substring-match strings, or the exact §3.5 input set; the implementation authorization names the chosen models.
- **Any new `audit_event` emission.** The merged P6-06 `lifecycle.daily_report` is the only P6 audit emission; no new emission is introduced.
- **Any real account data, real position book, account identifier, balances, P&L, cash ledger, valuation, broker, market data, live data, orders, order routing, or account sync.**
- **Any broker integration** (real or paper), **any market-data integration**, **any order placement or routing**, **any network**, **any scheduler or daemon**, **any env-var read, any secrets**, **any strategy logic**, **any concrete `KillSwitchProtocol` / `ReconciliationProtocol` / `HeartbeatProtocol` implementation** outside the merged P3-03 / P3-04 / P3-05 in-memory fakes.
- **Any I/O, external sink, file artifact, or persistence.** No print/file/socket/HTTP/syslog sink, no on-disk view, no database, no `open(` builtin for write, no `pickle`, no `shelve`, no generated artifact checked into the repository.
- **Any runtime activation.** No `__main__`, no `if __name__` block, no daemon, no scheduler, no background worker, no long-running service, no `time.sleep`, no `asyncio.sleep`, no re-export of any merged Phase 3 fixture from `src/gmc_rebuild/__init__.py` or any runtime path, no `logging.basicConfig` / handler installation inside the test module. (Construction and exercise of merged surfaces within pytest is **not** a runtime activation; the new test module does not run as a daemon, scheduler, background thread, or `__main__` entry point.)
- **Any clock read.** The tests read no wall clock; any `datetime` value is a fixed inline literal.
- **Any modification of any existing test file under `tests/**`.** The implementation PR adds a new test file (or files); it does not modify the merged `tests/decision/`, `tests/eligibility/`, `tests/operator_view/`, `tests/p4_02_composed/`, `tests/portfolio_state/`, `tests/reconciliation/` (or any other `tests/<dir>/`), `tests/reporting/`, `tests/runtime/`, `tests/signal_intake/`, `tests/simulation/`, `tests/test_config_schema.py`, `tests/test_logging_audit.py`, `tests/test_package_skeleton.py`, `tests/test_phase1_governance.py`, `tests/test_risk_interfaces.py`, or `tests/test_time_utc.py`.
- **Any expansion of the merged P6-01 / P6-02 / P6-03 / P6-04 / P6-05 / P6-06 / P6-07 surfaces.** No additional enum member or dataclass field; no change to `accept_signal_intent`, `check_eligibility`, `compose_position_decision`, `propose` / `propose_order`, `derive_simulated_order_intent_id`, `apply_simulated_order_intent`, `build_daily_report`, `render_daily_report_event`, `build_dry_run_operator_view`, or `DryRunOperatorView.render`; no re-export of any of their symbols from `src/gmc_rebuild/__init__.py`.
- **Any expansion of the merged P4-06 / P4-07 / P4-08 safety surface** (`SafetyVerdict.clear`, the `BLOCKER_*` set, `OperatorSafetyView`, `format_safety_verdict`, `RuntimeShell.evaluate()`).
- **Any expansion of the merged P2-04 `audit_event` surface** (including the closed `AUDIT_CATEGORIES`, the `<category>.<action>` rule, redaction, `AuditEvent`, or `serialize_event` determinism).
- **§8 step 4a allowlist expansion.** Not extended by this packet, and not extended by the implementation candidate (no new `src/**` directory).
- **Companion-docs edits.** This packet does not edit `MASTER_STATUS.md`, `README.md`, or `plan/phase6_entry_plan.md` (see §Companion-Docs Decision and §Relationship). Any future companion-docs update — including the §4 item-7 / item-8 P6-08 reconciliation — is a separate authorized workstream.
- **Source / test changes.** No file under `src/**` is modified or created by this packet or by the PR that lands it. No `tests/**` change is authorized by this packet (the implementation candidate authorizes test changes only via the future implementation authorization).
- **Quality-gate relaxation.** No weakening of pre-commit, Ruff, mypy strict mode, `detect-secrets`, `.gitignore`, `.secrets.baseline`, the §8 step 4 / step 4a / step 4c scans, or the §8 step 8 staleness check.
- **Ops execution work.** No X10 Layer 5 promotion, no backup-monitoring automation, no DR drill execution, no OPS-05 / OPS-07 opening, no Backblaze / Time Machine / drive / FileVault / power / USB / network / macOS setting change.
- **New tags or releases.** No git tag, no GitHub release, no version bump.
- **Touching or committing `.claude/`.** The untracked `.claude/` (and `Claude_Transfes/`) working-tree directories are not touched, staged, or committed by this packet.
- **Successor packet authorization.** No P6-08 implementation, no P6-09 enumeration or implementation, no OPS-05 / OPS-07 opening, no monitoring automation. Each future packet requires its own separate written authorization from Kevin per `AI_WORKFLOW.md` §7.
- **GOV-02 reinterpretation.** Local Claude Code / Claude Max remains the default execution environment; Perplexity-managed codebase subagents must not perform repo edits absent Kevin's explicit per-packet written authorization; Perplexity Computer's `AI_WORKFLOW.md` §1.2 role is preserved.
- **Mode A / Mode B substitution.** This packet is governance prose, not a control-surface decision; it does not substitute for adversarial review of any future P6-08 implementation PR.

The always-forbidden categories in `MASTER_STATUS.md` §6, `plan/phase4_entry_plan.md` §10, `plan/phase5_entry_plan.md` §10, and `plan/phase6_entry_plan.md` §10 — strategy code, broker execution code, paper-trading or live-trading wired to a real broker, runtime daemons affecting accounts, real market data ingestion, order placement, and real secrets — remain forbidden in all modes. This packet does not relax them and does not record any relaxation.

## Required Validation

Before merge, the planning PR that lands this artifact must pass (this mirrors the merged P6-07 / P6-06 / P6-05 planning packets' §Required Validation):

- `.venv/bin/python -m pytest -q` against the existing test suite (must continue to pass at the post-P6-07 baseline of **696** tests; the planning packet adds and modifies no tests).
- `.venv/bin/pre-commit run --all-files` (must exit 0 with every hook passing and no `files were modified by this hook` second-pass message; the packet adds only Markdown, so lint / format / type / pytest hooks are no-ops over the changed file aside from any Markdown whitespace fixes).
- `git diff --name-status main` showing the change confined to exactly one file: this authorization artifact. **No `src/**` change, no `tests/**` change, no `MASTER_STATUS.md` change, no `README.md` change, no `plan/**` change, no other `governance/authorizations/*` change, and no `.claude/` change.** The diff scope mirrors the merged P6-07 / P6-06 / P6-05 planning packets' one-file scope.
- A targeted stale-phrase grep (`grep -nE "pending merge in this open" MASTER_STATUS.md README.md RECOVERY.md plan/phase4_entry_plan.md plan/phase5_entry_plan.md plan/phase6_entry_plan.md`) returning **no matches** in the canonical doc set (the only permissible hit is the literal grep-pattern quote inside such a packet, structurally identical to the P6-05 / P6-06 / P6-07 planning precedents).

## Required Sequencing (Mode B)

Mode B sibling monitoring **is required** for the planning PR that lands this artifact, and is also required for any future P6-08 implementation PR. Per ADR-008 §D3 / §D5 and the established 2026-05-13 through 2026-06-15 precedents (every active-workday PR has been preceded by a sibling Mode B monitoring packet PR that merged first, including PR #174 / the planning PR for the P6-06 planning packet and PR #178 / the planning PR for the P6-07 planning packet):

1. **For this P6-08 planning PR:** a separate sibling Mode B monitoring packet under `monitoring/daily/2026-06-15_pr-p6-08-planning.md` (or the numeric `2026-06-15_pr-<NNN>-p6-08-planning.md` form once the PR number is assigned) must be authored from `main` (not from this planning branch), committed by the default builder (Backup AI authors the text; Codex/local Claude Code commits it per `AI_WORKFLOW.md` §1.4), and **merged to `main` before** the planning PR merges. The sibling Mode B packet for this planning PR has been drafted alongside this packet for Kevin's review (it is a separate file on a separate sibling branch/PR; per `AI_WORKFLOW.md` and ADR-008 the planning packet is one file and does **not** bundle the monitoring packet). This draft only records the sequencing constraint; the sibling monitoring packet is a separate workstream and a separate PR.
2. **Mode A** adversarial review is **not** independently required for this docs-only planning / enumeration authorization, per `AI_WORKFLOW.md` §4's routine-exclusion sentence and the precedent set by the prior planning / enumeration packets (`2026-06-15_p6-07-planning.md`, `2026-05-23_p6-06-planning.md`, `2026-05-22_p6-05-planning.md`, `2026-05-20_p6-04-planning.md`, `2026-05-19_p6-03-planning.md`, etc.). The maintainer retains discretion to require Mode A as PR-review text; if delivered, it is recorded as PR-review text and not committed to the repository per `AI_WORKFLOW.md` §6 rule 5.
3. **For the future P6-08 implementation PR:** it is a separate workstream that starts from clean `main`, uses its own feature branch, requires its own sibling Mode B monitoring packet per ADR-008 §D5, and — because it adds new `tests/**` content without any `src/**` change — Mode A adversarial review is recommended as PR-review text per `AI_WORKFLOW.md` §4 only at the maintainer's discretion (the merged P5-03..P5-07 tripwire-only precedent did not independently require Mode A; the change is sandboxed and adds no production behavior).

Kevin remains the only approver for every merge. **Do not merge this planning PR (or its sibling) without Kevin's explicit approval.**

## Required Implementation Authorization (Future) — Implementation Is Not Authorized by This Draft

**Any P6-08 implementation requires its own separate written authorization from Kevin**, recorded as a separate artifact under `governance/authorizations/<YYYY-MM-DD>_p6-08.md`, with its own §Authorization section reproducing Kevin's verbatim implementation-authorization block, its own §Authorized Scope / §Authorized Contract / §Required Tests / §Explicitly Not Authorized / §Required Sequencing / §Required Validation / §Review Basis — mirroring the structure of the merged P6-07 implementation authorization at `governance/authorizations/2026-06-15_p6-07.md` and the merged P5-07 implementation authorization at `governance/authorizations/2026-05-18_p5-07.md`. Until that future implementation authorization is recorded, P6-08 implementation remains **future / not authorized**, and no `tests/**` change may be opened under the P6-08 label.

The future P6-08 implementation authorization must explicitly: confirm the §3.2 location (`tests/<name>/<file(s)>`) and name the exact directory / file layout; choose the §3.3 assertion style (single file vs per-layer files) and the exact substring-match strings; **resolve the §3.4 "pin existing types, no new typed exceptions" decision** (the lower-risk default) by naming explicitly that the implementation pins the existing `TypeError` / `ValueError` / `FrozenInstanceError` / typed `*Error(ValueError)` contract and **does not introduce a new exception class**; name the exact §3.5 input set; name the explicit no-clock-read / no-side-effect / read-only determinism contract (§3.7); confirm the §8 step 4a allowlist is unchanged (no new `src/**` directory); name the Mode A / Mode B review applicability; and name the validation gates. The implementation authorization is free to replace any element of the candidate shape recorded in §3 above.

**To be unambiguous about the planning-only nature of this draft:** merging this planning packet does **not** authorize, begin, or pre-approve any P6-08 implementation. It records a candidate shape and the governance scaffolding for a future decision. Implementation is authorized **only** by a separate, future, written implementation authorization from Kevin per the paragraph above.

Phase 2 remains formally closed. Phase 3 remains formally closed. Phase 4 remains open as a governance state with the P4-08 safety-foundation closure preserved. Phase 5 is closed at the merged P5-01..P5-07 scope. Phase 6 is opened at the merged P6-01 / P6-02 / P6-03 / P6-04 / P6-05 / P6-06 / P6-07 scope. The foundation-complete confirmation report and its 1-FU-A / 1-FU-B follow-ups, the merged Phase 6 entry plan, and the GOV-02 execution-environment workflow rule are preserved. **P6-08 implementation, and any later P6-0N task, remains future / not authorized.**

## Review Basis

This planning decision is based on:

- `governance/authorizations/2026-06-15_p6-07-planning.md` (merged P6-07 planning / enumeration packet, PR #179 at `416f550`) — the direct structural precedent for this draft, including the §Relationship pattern for transparently re-aiming a P6-0N candidate without editing `plan/phase6_entry_plan.md` §4.
- `governance/authorizations/2026-06-15_p6-07.md` (merged P6-07 implementation authorization, PR #181 at `88fa3bf`) — the two-step "planning packet → separate implementation authorization (bundled with implementation)" cadence P6-08 follows.
- `governance/authorizations/2026-05-17_p5-03.md`, `governance/authorizations/2026-05-17_p5-04.md`, `governance/authorizations/2026-05-18_p5-05.md`, `governance/authorizations/2026-05-18_p5-06.md`, `governance/authorizations/2026-05-18_p5-07.md` (merged P5-03 / P5-04 / P5-05 / P5-06 / P5-07 tripwire authorizations) — the closest structural precedent for the eventual P6-08 *implementation* shape: a single new pytest file under `tests/<layer>/`, no `src/**` change, no new public symbol, no expansion of any merged surface.
- `tests/simulation/test_simulated_order_intent_invariants.py` (merged P5-03 tripwires), `tests/simulation/test_composed_safety_foundation.py` (merged P5-04 tripwires), `tests/runtime/test_operator_view_composed_safety_foundation.py` (merged P5-05 tripwires), `tests/simulation/test_propose_symmetry.py` (merged P5-06 tripwires), `tests/runtime/test_operator_view_determinism.py` (merged P5-07 tripwires) — the concrete patterns the P6-08 implementation candidate mirrors.
- `tests/reporting/test_reporting.py` and `tests/operator_view/test_operator_view.py` (merged P6-06 / P6-07 tests) — the `_expect_error` helper and the AST inertness self-check patterns the P6-08 candidate inherits.
- `src/gmc_rebuild/signal_intake/`, `src/gmc_rebuild/eligibility/`, `src/gmc_rebuild/decision/`, `src/gmc_rebuild/portfolio_state/`, `src/gmc_rebuild/reporting/`, `src/gmc_rebuild/operator_view/` (merged P6-01..P6-07 surfaces, head `88fa3bf`) — the exception surface this candidate pins.
- `src/gmc_rebuild/simulation/_boundary.py` (`SimulationBoundaryError`), `src/gmc_rebuild/runtime/_shell.py` (`RuntimeShellError`), `src/gmc_rebuild/risk/interfaces.py` (`RiskControlError`), `src/gmc_rebuild/logging/audit.py` (`AuditEventError`), `src/gmc_rebuild/time/utc.py` (`NaiveDatetimeError`) — the typed-Error subclasses the candidate composed-pipeline tripwires reference.
- `plan/phase6_entry_plan.md` §4 item 7 (the current failure-handling consolidation candidate, originally labeled P6-07; now sliding to P6-08 per Kevin's 2026-06-15 re-aim), §6 (proof bundle), §8 (stop conditions), §10 (always-forbidden categories); `docs/GMC_PROJECT_MAP.md` Layer 2.
- `MASTER_STATUS.md` §6 (always-forbidden categories) and §8 (step 4 / 4a / 4b / 4c gates, the sixteen-entry `allowed_p2_infra` allowlist, and the verbatim forbidden-token set) at `main` head `88fa3bf`.
- `AI_WORKFLOW.md` §1 (roles, incl. §1.2 status keeper and §1.4 / §1.5 GOV-02), §3 (required proof), §4 (Mode A / Mode B), §6 (anti-chaos rules), §7 (durable authorization artifacts).
- `docs/decisions/ADR-008_monitoring_cadence_and_ai_monitor_role.md` §D3 / §D5 and `docs/decisions/ADR-009_runtime_monitoring_cadence.md` D7 (cadence in force during the governance phase).
- Kevin's verbatim planning directive reproduced in the §Authorization section above.

P6-08 may be opened **only in a separate future implementation PR** conforming to a separate future implementation authorization from Kevin. P6-09 and all later successor packets remain future / not authorized.
