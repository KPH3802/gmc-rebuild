# P5-07 Planning and Enumeration Authorization

Date: 2026-05-18
Authorizer: Kevin
Scope: Bounded docs/governance-only **P5-07 planning and enumeration** packet for `gmc-rebuild` — updates Phase 5 planning documentation to reflect that P5-06 has merged and been reconciled and that the GOV-02 execution-environment workflow correction has merged and been reconciled, and evaluates / documents the candidate shape for P5-07 at the planning level only. **Does not open P5-07, does not authorize any P5-07 implementation, does not authorize any simulation expansion, does not authorize any order semantics change, does not authorize any runtime activation, does not authorize any ops execution work, does not extend any allowlist, does not relax any quality gate, does not promote X10 Layer 5, does not automate backup-monitoring, does not execute any DR drill, and creates no tag or release.**

## Authorization

The user authorization for this packet is reproduced below verbatim. Per `AI_WORKFLOW.md` §7 the verbatim block in this file is the authorization of record.

> I authorize a bounded docs/governance-only P5-07 planning and enumeration packet for gmc-rebuild on 2026-05-18.
>
> Scope:
> - Update Phase 5 planning documentation to reflect that P5-06 has merged and been reconciled and that GOV-02 has merged and been reconciled.
> - Evaluate and document the candidate shape for P5-07 at the planning level only.
> - Name P5-07 only as future / not authorized unless this packet explicitly says otherwise.
> - Record that any P5-07 implementation requires separate written authorization.
>
> Constraints:
> - Docs / governance only.
> - No src/** changes.
> - No tests/** changes.
> - No runtime activation.
> - No broker, paper-trading, live-trading, market-data, order-routing, strategy, scheduler, daemon, persistence, deployment, env-var, secrets, or network changes.
> - No allowlist extension.
> - No quality-gate relaxation.
> - No tag or release.
> - No P5-07 implementation authorization.
> - No successor packet is authorized.
>
> Governance:
> - This is planning / enumeration only.
> - The required Mode B sibling monitoring PR must be authored from `main` and merged before the planning PR per ADR-008 §D5.
> - Mode A is not independently required unless maintainer-elected.
> - The packet must preserve the canonical status reconciliations through PR #114, PR #118, PR #122, PR #126, PR #130, and PR #134.
> - The packet must not reintroduce stale `pending merge` language for any merged work; any `pending merge` wording must be scoped only to this open P5-07 planning PR itself.
> - The packet must be built per the GOV-02 execution-environment workflow rule (local Claude Code / Claude Max session as the default Codex-role builder for `gmc-rebuild`).
>
> Validation required before PR:
> - `.venv/bin/python -m pytest -q`
> - `grep -nE "pending merge" MASTER_STATUS.md README.md RECOVERY.md plan/phase4_entry_plan.md plan/phase5_entry_plan.md || true`
>
> Authorized by Kevin on 2026-05-18.

This authorization is the **planning / enumeration analogue** to `governance/authorizations/2026-05-18_phase-5-entry-planning.md` (Phase 5 entry planning) and `governance/authorizations/2026-05-18_p5-06-planning.md` (P5-06 planning / enumeration) and the earlier P4-NN enumeration-planning artifacts (`governance/authorizations/2026-05-14_p4-02-enumeration-planning.md`, `governance/authorizations/2026-05-14_p4-03-enumeration-planning.md`, `governance/authorizations/2026-05-15_p4-04-enumeration-planning.md`). It is intentionally narrower than each Phase-5 implementation authorization: it does **not** open P5-07 or any other successor P5 task, it does **not** extend the `MASTER_STATUS.md` §8 step 4a `allowed_p2_infra` allowlist (preserved exactly at the ten entries synced by GOV-01), it does **not** add or modify any file under `src/**` or `tests/**`, and it does **not** open or pre-approve any future P5-07 / OPS-05 / OPS-07 / runtime activation / simulation expansion / order semantics change / ops execution work.

The canonical reconciliations merged via PR #114 (`a02f17c`), PR #118 (`b5b245c`), PR #122 (`5f317b9`), PR #126 (`5997cb2`), PR #130 (`25972b0`), and PR #134 (`fb13994`) are preserved verbatim by this authorization. The merge sequence on `main` from the canonical-status reconciliation forward is: PR #114 canonical status reconciliation at `a02f17c` with sibling Mode B PR #115 at `75d6f28`; PR #116 Phase 5 entry planning at `3ec7395` with sibling Mode B PR #117 at `1b72903`; PR #118 Phase 5 entry planning post-merge reconciliation at `b5b245c` with sibling Mode B PR #119; PR #120 P5-05 implementation at `8fbd1ed` with sibling Mode B PR #121 at `dd3b72d`; PR #122 P5-05 implementation post-merge reconciliation at `5f317b9` with sibling Mode B PR #123 at `9629e54`; PR #124 P5-06 planning / enumeration at `61e3b34` with sibling Mode B PR #125 at `20eadd8`; PR #126 P5-06 planning post-merge reconciliation at `5997cb2` with sibling Mode B PR #127 at `703b53f`; PR #129 P5-06 implementation at `09c427e` with sibling Mode B PR #128 at `61fadb2`; PR #130 P5-06 implementation post-merge reconciliation at `25972b0` with sibling Mode B PR #131 at `3489b43`; PR #132 GOV-02 execution-environment workflow correction at `94d30bf` with sibling Mode B PR #133 at `27ab3a6`; and PR #134 GOV-02 post-merge reconciliation at `fb13994` with sibling Mode B PR #135 at `26a7dc8`. This planning packet must not reintroduce stale `**pending merge**` language for any of the already-merged P5-01 / P5-02 / P5-03 / P5-04 / P5-05 / P5-06 / GOV-01 / GOV-02 / OPS-06 / Phase 5 entry planning / P5-06 planning packets. The reconciled canonical statuses are factual and not modified by this packet.

The GOV-02 execution-environment workflow rule (merged on `main` via PR #132 / reconciled via PR #134) governs the build of this packet: repo-editing work for `gmc-rebuild` is by default carried out via Kevin's local Claude Code / Claude Max subscription rather than via Perplexity-managed codebase subagents that consume Perplexity credits. Perplexity Computer continues to act in its `AI_WORKFLOW.md` §1.2 role as Supervisor / Verifier / Status Keeper and as merge-sequencing assistant. The GOV-02 rule does not relax `AI_WORKFLOW.md` §6 rule 1 ("One builder at a time") or any other existing role / cadence / authorization-of-record requirement; this packet is built in compliance with it.

## Scope

The work authorized by this artifact is restricted to:

- This authorization artifact at `governance/authorizations/2026-05-18_p5-07-planning.md`.
- A conservative update to the existing Phase 5 entry plan at `plan/phase5_entry_plan.md` that (i) reflects P5-06 implementation as merged on `main` and reconciled (the canonical reconciliations through PR #114, PR #118, PR #122, PR #126, PR #130, and PR #134 are preserved verbatim) and GOV-02 as merged and reconciled, (ii) records P5-07 as **future / not authorized**, (iii) records a single candidate shape for P5-07 — at the **planning level only** — as a conservative recommendation, namely a **tripwire-only tests packet** modeled on the merged P5-03 / P5-04 / P5-05 / P5-06 pattern that exercises the already-merged composed surfaces without adding any production behaviour, without expanding the simulation surface, and without modifying order semantics. The plan continues to require that any P5-07 implementation receive its own separate written authorization from Kevin per `AI_WORKFLOW.md` §7 and that the shape recorded here is **not** pre-authorized; it is a planning-level candidate only and may be replaced by Kevin at the time of P5-07 authorization.
- Conservative governance-prose cross-reference updates to `MASTER_STATUS.md` and `README.md` that record that the P5-07 planning / enumeration workstream has been authorized (planning only; no P5-07 implementation authorized; no runtime activation authorized; no allowlist expansion; no simulation expansion; no order semantics change). The updates name this artifact, do not relax any boundary, do not change the §8 step 4a allowlist, and do not declare P5-07 open. Per `AI_WORKFLOW.md` §1.2 and §6 rule 2 ("One status keeper"), any `MASTER_STATUS.md` or `README.md` edit in this PR is conservative and remains subject to Perplexity Computer's verification before being treated as the canonical status. The edits must not reintroduce stale `**pending merge**` language for any merged packet; any "pending merge" wording must be scoped only to this open P5-07 planning PR itself.

The §8 step 4a `allowed_p2_infra` allowlist is **not** extended by this authorization or by the planning document opened under it. It continues to contain exactly the ten entries on `main` as synced by GOV-01 (`src`, `src/gmc_rebuild/config`, `src/gmc_rebuild/time`, `src/gmc_rebuild/logging`, `src/gmc_rebuild/risk`, `src/gmc_rebuild/heartbeat`, `src/gmc_rebuild/kill_switch`, `src/gmc_rebuild/reconciliation`, `src/gmc_rebuild/runtime`, `src/gmc_rebuild/simulation`). Any future P5-07 / P5-08 / … directory addition would require a separate written authorization that introduces the directory in the same PR that adds it to the allowlist (per the standing rule in `MASTER_STATUS.md` §8 step 4b). The candidate P5-07 shape recommended at the planning level by this packet does **not** require any new directory.

## Candidate P5-07 Shape (Planning Level Only — Future / Not Authorized)

This section records, at the planning level only, the conservative candidate shape for a future P5-07 packet. Recording this shape **does not authorize implementation**. Kevin must independently authorize any P5-07 implementation packet in writing, in a separate authorization artifact under `governance/authorizations/`, with its own scope, constraints, and validation requirements; that future authorization is free to replace any element of the candidate shape recorded here.

**Recommended candidate shape (tripwire-only, no new production behaviour).** A bounded **tests-only tripwire packet**, modeled directly on the merged P5-03 (`tests/simulation/test_simulated_order_intent_invariants.py`), P5-04 (`tests/simulation/test_composed_safety_foundation.py`), P5-05 (`tests/runtime/test_operator_view_composed_safety_foundation.py`), and P5-06 (`tests/simulation/test_propose_symmetry.py`) precedents. It would add exactly one new test file under the existing `tests/` tree, exercising only the **already-merged** P2 / P3 / P4 / P5 public surfaces. It would add no new `src/**` directory, modify no `src/**` file, modify no existing test file, add no new public symbol, add no `SimulationLane` / `SimulatedOrderSide` / `SimulatedOrderType` member, add no field on `SimulatedIntent` or `SimulatedOrderIntent`, add no method on `SimulationBoundary` beyond the merged `propose` (P5-01) and `propose_order` (P5-02), add no placeholder or order record class, and preserve the §8 step 4a `allowed_p2_infra` allowlist exactly at ten entries.

Within that tripwire-only envelope, several plausible directions exist for a future P5-07 authorization to choose from. **None of these is pre-authorized**; they are listed at the planning level only so that a future P5-07 authorization can name the chosen direction explicitly. Directions (b), (c), and (d) here are the three non-chosen planning-level candidate directions recorded in `governance/authorizations/2026-05-18_p5-06-planning.md` (direction (a) — the `propose` ↔ `propose_order` symmetry tripwire — was chosen by Kevin for the merged P5-06 implementation and is therefore no longer a P5-07 candidate); the carry-forward of those three directions reflects continuity with the prior planning packet without pre-authorizing any of them. Direction (e) is a new fifth candidate added by this P5-07 planning packet at the planning level only:

- **Direction (b) — operator-view determinism / idempotence under the real composed pipeline.** A focused test module that asserts that `format_safety_verdict` is byte-identical across repeated calls for the same `SafetyVerdict` (deterministic rendering tripwire), and that repeated calls under the real composed P3 / P4-06 / P4-07 / P5-01 / P5-02 pipeline do not perturb any observable state in the P3 fakes, the safety surface, or the simulation surface. Tightens the P5-05 determinism property under repeated rendering.
- **Direction (c) — closed-enum / closed-field defense-in-depth tripwire.** A focused test module that re-asserts, under the real composed pipeline, the closed-set invariants of `SimulationLane` (one member: `LOCAL_ONLY`), `SimulatedOrderSide` (two members: `BUY`, `SELL`), `SimulatedOrderType` (two members: `MARKET`, `LIMIT`), and the exact eight fields of `SimulatedOrderIntent`. Adds defense-in-depth against future enum-expansion / field-addition drift by checking the closed surface from a composed-pipeline vantage point.
- **Direction (d) — ADR-005 / ADR-002 / ADR-003 composed-policy alignment tripwire.** A focused test module that re-asserts ADR-005 (operator-availability heartbeat safe-default — STALE for unknown components), ADR-002 (kill-switch ARMED biconditional), and ADR-003 (reconciliation CLEAN biconditional) at the composed-pipeline level, ensuring the merged P4-06 `RuntimeShell` continues to honor each ADR's safe-default rule end-to-end under the merged P3-03 / P3-04 / P3-05 fakes.
- **Direction (e) — simulation surface inertness self-check tripwire.** A focused test module that re-asserts the structural inertness of the `gmc_rebuild.simulation` subpackage from a composed-pipeline vantage point: that no `src/gmc_rebuild/simulation/*.py` source file contains a `__main__` block, a `time.sleep` call, an `os.environ` / `os.getenv` read, a `socket` / `urllib` / `requests` / `http` / `ssl` / `smtplib` / `ftplib` import, an `open(` filesystem call, or any other forbidden runtime surface, by AST-walking the merged simulation subpackage and asserting the imports and call-site sets are disjoint from the closed forbidden set. Complements the existing inertness assertions in the merged P5-01 `tests/simulation/test_simulation_boundary.py` glob by walking the simulation tree from a composed-pipeline test that also drives the real P3 / P4-06 / P4-07 pipeline, so any future drift in the simulation source's runtime quietness fails alongside the composed pipeline's expected behavior.

**Out of candidate scope** (forbidden at the always-forbidden category level per `MASTER_STATUS.md` §6 independent of phase, and re-forbidden by this planning packet's constraints):

- Any expansion of `SimulationLane`, `SimulatedOrderSide`, `SimulatedOrderType`, `SimulatedOrderIntent`, or `SimulationBoundary`.
- Any change to order semantics (no change to `propose` / `propose_order` meaning, no change to the `SafetyVerdict.clear` precondition, no change to the identity-return contract, no addition of side effects).
- Any runtime activation of any merged Phase 3 fixture, P4-06 / P4-07 / P4-08 safety surface, or P5-01 / P5-02 simulation surface.
- Any broker integration, paper-trading or live-trading wired to a real broker, market-data integration, order placement / routing, strategy logic, scheduler / daemon, persistence outside the test sandbox, deployment workflow, env-var / secrets loading, or network call.
- Any X10 Layer 5 promotion, backup-monitoring automation, or DR drill execution.
- Any `src/**` change, any new `tests/**` directory, any modification of any existing test file under `tests/**`, any `__main__` entry point, any new tag, any new release.
- Any extension of the `MASTER_STATUS.md` §8 step 4a `allowed_p2_infra` allowlist or relaxation of any quality gate.

This planning-level candidate shape is **not** an authorization. The future P5-07 implementation authorization (when and if Kevin records one in writing) will independently name its chosen direction, its file path, its invariant categories, its Mode A / Mode B requirements, its validation, and its constraints, and that authorization will govern P5-07 regardless of what this section records.

## Allowed Planning Topics

While the P5-07 planning / enumeration workstream is opened by this authorization, Codex (per the GOV-02 execution-environment rule, local Claude Code / Claude Max running as Codex by default) and Perplexity Computer may draft planning artifacts on the following topics. Planning artifacts are documents, not code, and must be reviewable as documents.

1. **Updated current post-P5-06 / post-GOV-02 state in prose.** What has been merged on `main` through PR #134 (the GOV-02 execution-environment workflow correction post-merge reconciliation), with the canonical reconciliations through PR #114, PR #118, PR #122, PR #126, PR #130, and PR #134 preserved verbatim and no stale `**pending merge**` language reintroduced for any merged packet.
2. **Naming P5-07 only as future / not authorized.** Naming the task in the plan and recording a candidate shape at the planning level does **not** authorize it; the shape of any P5-07 task remains a decision Kevin must make in writing at the time of P5-07 authorization. The plan must not pre-commit any specific P5-07 file path, specific module, specific field, specific method, specific simulation lane, specific order side / type member, or specific helper as an authorized choice.
3. **Phase 5 entry criteria as they apply to a future P5-07 PR.** What must be true (baseline, governance, ADRs, monitoring cadence under ADR-008 §D3 / §D5 during the governance phase and ADR-009 D3 / D5 once runtime exists on `main` per ADR-009 D7's bootstrap-avoidance clause, Mode A / Mode B review per `AI_WORKFLOW.md` §4) before any future P5-07 implementation PR could open. Restated from the existing `plan/phase5_entry_plan.md` §5.
4. **Per-PR proof-bundle expectations for any future P5-07 PR.** Restated from `AI_WORKFLOW.md` §3 and `plan/phase5_entry_plan.md` §6.
5. **Rollback and stop conditions for any future P5-07 PR.** Restated from `plan/phase5_entry_plan.md` §8.
6. **Phase-boundary enforcement.** How any future P5-07 PR would prove it did not cross into broker execution, paper-trading wired to a real broker, live-trading wired to a real broker, real market data ingestion, real order routing, real strategy logic, real scheduler / daemon, real persistence, real deployment, real env-var loading, real secrets, real network, allowlist expansion beyond what its own authorization names, quality-gate relaxation, X10 Layer 5 promotion, backup-monitoring automation, or DR drill execution.
7. **ADR follow-ups.** Restated from `plan/phase5_entry_plan.md` §9 for any future P5-07 scope.
8. **Execution-environment compliance (GOV-02).** Confirmation that the planning work is being carried out via the default builder environment (local Claude Code / Claude Max acting as Codex) rather than via a Perplexity-managed codebase subagent. Perplexity Computer's read-only verification, status-keeping, and merge-sequencing role remains unchanged.

Any planning topic that does not fit the list above must be raised with Kevin before a document is written, per `AI_WORKFLOW.md` §6 rule 7 ("No phase drift").

## Explicitly Not Authorized

The authorization above does not permit, by implication or otherwise, any of the following. Each remains forbidden until Kevin records a separate written authorization per `MASTER_STATUS.md` §7 and `AI_WORKFLOW.md` §6 rule 3 ("One approver") / rule 7 ("No phase drift"), with a sibling artifact under `governance/authorizations/` and (where it introduces a new directory) a corresponding update to the §8 step 4a allowlist in the same PR that introduces the directory:

- **Any P5-07 implementation.** P5-07 (and any later P5-0N task) is **future / not authorized**. The shape of P5-07 is a decision Kevin must make in writing at the time of P5-07 authorization; the candidate shape recorded above is planning-level only and is not pre-authorized.
- **Any simulation expansion.** No additional `SimulationLane` member beyond `LOCAL_ONLY`. No additional `SimulatedOrderSide` member beyond `BUY` / `SELL`. No additional `SimulatedOrderType` member beyond `MARKET` / `LIMIT`. No ninth field on `SimulatedOrderIntent`. No additional method on `SimulationBoundary` beyond the merged `propose` (P5-01) and `propose_order` (P5-02). No additional placeholder / order record class. No new public name on `gmc_rebuild.simulation`.
- **Any order semantics change.** No change to the meaning of `propose` or `propose_order`, no change to the `SafetyVerdict.clear` precondition, no change to the identity-return contract, no addition of side effects.
- **Any runtime activation.** No `__main__` entry point, no daemon, no scheduler, no background worker, no long-running service, no live execution loop, no re-export of any merged Phase 3 fixture (`InMemoryHeartbeat`, `InMemoryKillSwitch`, `InMemoryReconciliation`) from `src/gmc_rebuild/__init__.py` or any other runtime path, no consumption of any merged Phase 3 fixture from a `__main__`, a daemon, a scheduler, or any runtime path.
- **Any ops execution work.** No X10 Layer 5 promotion. No backup-monitoring automation (the OPS-06 plan remains rules and thresholds only; the periodic execution remains an operator-side action). No DR drill execution. No OPS-05 / OPS-07 opening. No change to any Backblaze / Time Machine / drive / FileVault / sleep / power / USB / network / macOS setting.
- **Broker integration.** No broker SDK, no broker connector, no broker reconciliation runtime, no broker authentication, no broker credentials.
- **Paper-trading wired to a real broker.** No paper-broker connector, no paper-account identifier, no paper-API surface, no paper-trading execution loop.
- **Live-trading.** No live-broker connector, no live-account identifier, no live-API surface, no live-trading execution loop.
- **Market-data integration.** No market-data ingestion code, no market-data pipeline, no committed dataset, no live / delayed / paper data feed.
- **Order-routing.** No order routing logic, no venue selection, no execution adapter, no FIX session, no REST client, no WebSocket client.
- **Strategy logic.** No trading signals, no scanners, no models, no portfolio rules, no backtests.
- **Scheduling.** No cron-style scheduler, no APScheduler, no background-job framework, no operator-heartbeat daemon, no kill-switch runtime, no reconciliation runtime.
- **Persistence.** No SQLite, no on-disk database, no on-disk reconciliation snapshot, no on-disk heartbeat state, no log sink, no file artifact written by runtime code.
- **Deployment.** No deployment workflow, no rollout, no rollback runtime, no CI/CD pipeline beyond the existing local pre-commit and pytest gates.
- **Secrets / env-var loading.** No real secrets, no real credentials, no `.env` files, no `os.environ` / `os.getenv` reads, no real account identifiers, no real broker / venue identifiers, no real endpoints embedded anywhere in the repository under this plan.
- **Network.** No `socket`, no `urllib`, no `requests`, no `http`, no `ssl`, no `smtplib`, no `ftplib`, no outbound or inbound network code.
- **Concrete risk implementations inside the runtime package.** No concrete implementation of `KillSwitchProtocol` / `ReconciliationProtocol` / `HeartbeatProtocol` lands inside `src/gmc_rebuild/risk/` under this packet. The P2-05 boundary (types and abstract `typing.Protocol` definitions only) is preserved.
- **Source / test changes.** No file under `src/**` or `tests/**` is modified or created by this packet or by the PR that lands it.
- **§8 step 4a allowlist expansion.** The `allowed_p2_infra` allowlist is **not** extended by this packet. Any future addition requires a separate written authorization that names a specific implementation task, a sibling artifact under `governance/authorizations/`, and an allowlist update made in the same PR that introduces the new directory.
- **Quality-gate relaxation.** No weakening of pre-commit, Ruff, mypy strict mode, `detect-secrets`, `.gitignore`, `.secrets.baseline`, the `MASTER_STATUS.md` §8 step 4 always-forbidden scan, the §8 step 4a per-PR infrastructure allowlist scan, the §8 step 4c recursive forbidden-token scan, or the §8 step 8 canonical-doc staleness check introduced by PR #114.
- **New tags or releases.** No git tag, no GitHub release, no version bump.
- **Reintroduction of stale "pending merge" language.** The canonical status reconciliation merged via PR #114, the Phase 5 entry planning post-merge reconciliation merged via PR #118, the P5-05 implementation post-merge status reconciliation merged via PR #122, the P5-06 planning / enumeration post-merge reconciliation merged via PR #126, the P5-06 implementation post-merge reconciliation merged via PR #130, and the GOV-02 post-merge reconciliation merged via PR #134 are preserved by this packet. The PR may, where it carefully scopes its own pending state (the planning PR itself is pending merge before it merges), describe that pending state without reintroducing stale claims about any already-merged P5-01 / P5-02 / P5-03 / P5-04 / P5-05 / P5-06 / GOV-01 / GOV-02 / OPS-06 / Phase 5 entry planning / P5-06 planning packet.
- **Successor packet authorization.** No P5-07 implementation, no P5-08 enumeration, no P5-08 implementation, no OPS-05 / OPS-07 opening, no monitoring automation, no alerting policy enforcement, no second-review policy enforcement. Each future packet continues to require its own separate written authorization from Kevin per `AI_WORKFLOW.md` §7.
- **GOV-02 reinterpretation.** This packet does **not** reinterpret or relax the GOV-02 execution-environment workflow rule (merged via PR #132 / reconciled via PR #134). Local Claude Code / Claude Max remains the default execution environment for `gmc-rebuild` repo edits; Perplexity-managed codebase subagents that consume Perplexity credits must not perform repo edits absent Kevin's explicit written authorization for the specific packet; Perplexity Computer's `AI_WORKFLOW.md` §1.2 Supervisor / Verifier / Status Keeper role and read-only `gh` / `git` metadata-check role are preserved unchanged.
- **Mode A / Mode B substitution.** This packet is governance prose, not a control-surface decision; it does not substitute for adversarial review of any future P5-07 implementation PR or any future runtime / broker / market-data / order / strategy / scheduler / persistence / deployment PR. Any such future PR must independently satisfy `AI_WORKFLOW.md` §4 (Mode A) and ADR-008 / ADR-009 (Mode B) as applicable.

The always-forbidden categories in `MASTER_STATUS.md` §6, `plan/phase4_entry_plan.md` §10, and `plan/phase5_entry_plan.md` §10 — strategy code, broker execution code, paper-trading or live-trading wired to a real broker, runtime daemons affecting accounts, real market data ingestion, order placement, and real secrets — remain forbidden in all modes. This packet does not relax them and does not record any relaxation.

## Required Validation

Before merge, the planning PR must pass:

- `git switch main && git pull --ff-only` before branching (to confirm the work is built on current `main` at `fb13994`).
- `.venv/bin/python -m pytest -q` against the existing test suite (must continue to pass at the post-P5-06 baseline; the planning packet adds and modifies no tests).
- `git diff --name-status main` showing changes confined to this authorization artifact, conservative `plan/phase5_entry_plan.md` updates, and conservative status reflections in `MASTER_STATUS.md` and `README.md`. No file under `src/**` is added or modified; no file under `tests/**` is added or modified.
- Stale-phrase grep (`grep -nE "pending merge" MASTER_STATUS.md README.md RECOVERY.md plan/phase4_entry_plan.md plan/phase5_entry_plan.md || true`) showing that any new `pending merge in this open PR` occurrence is scoped exclusively to the open P5-07 planning PR itself, with no current-state stale claim on any merged work. Remaining wider `pending merge` hits are intentional historical / self-check / anti-pattern residuals.

Mode A / Mode B review requirements remain governed by `AI_WORKFLOW.md`, ADR-008, ADR-009, and the current `MASTER_STATUS.md`. P5-07 planning / enumeration is a **planning-only governance update** under `AI_WORKFLOW.md` §4's routine-exclusion sentence and the precedent set by `governance/authorizations/2026-05-13_phase-3-entry-planning.md`, `governance/authorizations/2026-05-14_phase-4-entry-planning.md`, `governance/authorizations/2026-05-14_p4-02-enumeration-planning.md`, `governance/authorizations/2026-05-14_p4-03-enumeration-planning.md`, `governance/authorizations/2026-05-15_p4-04-enumeration-planning.md`, `governance/authorizations/2026-05-18_phase-5-entry-planning.md`, and `governance/authorizations/2026-05-18_p5-06-planning.md`; Mode A adversarial review is **not** independently required unless maintainer-elected (and, if delivered, is recorded as PR-review text and not committed to the repository per `AI_WORKFLOW.md` §6 rule 5). Mode B sibling monitoring follows ADR-008 §D3 / §D5 (and ADR-009 D3 / D5 once in force per ADR-009 D7's bootstrap-avoidance clause) and must be authored from `main` and merged to `main` **before** this planning PR per the verbatim authorization above.

Phase 2 remains formally closed. Phase 3 remains formally closed. Phase 4 remains open as a governance state. P5 is open at the P5-01 inert local simulation boundary skeleton scope, the P5-02 simulated order intent model scope, the P5-03 simulated-order-intent invariants tripwire scope, the P5-04 composed safety-foundation × simulation integration tripwire scope, the P5-05 operator-view × composed safety-foundation tripwire scope, the P5-06 planning / enumeration scope, and the P5-06 implementation scope. The safety foundation closure recorded by P4-08 is preserved. The GOV-02 execution-environment workflow rule is in force. P5-07 remains **future / not authorized**.
