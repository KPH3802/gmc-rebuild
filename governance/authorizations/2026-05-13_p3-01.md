# P3-01 Authorization — Draft ADR-008 §D6 Follow-up ADR (Governance/Documentation-Only)

Date: 2026-05-13
Authorizer: Kevin
Scope: P3-01 **drafting only** — governance / documentation scope (**does not open Phase 3, does not authorize any Phase 3 implementation, does not extend any allowlist, does not accept the drafted ADR**)

## Authorization

I authorize opening **P3-01: draft the ADR-008 §D6 runtime monitoring cadence and missed-packet severity follow-up ADR** as a governance/documentation-only workstream, per the candidate task description in `plan/phase3_entry_plan.md` §4 item 1.

This authorization is the **drafting analogue** to the governance-only authorization slices used previously for P2-03 (`governance/authorizations/2026-05-12_p2-03.md`), P2-04 (`governance/authorizations/2026-05-12_p2-04.md`), and P2-05 (`governance/authorizations/2026-05-12_p2-05.md`), and to the Phase 3 entry planning authorization at `governance/authorizations/2026-05-13_phase-3-entry-planning.md`. It is intentionally narrower than each of those: it does **not** open Phase 3, does **not** authorize any Phase 3 implementation, does **not** name or open any task beyond P3-01, does **not** extend the `MASTER_STATUS.md` §8 step 4a `allowed_p2_infra` allowlist, does **not** add any path under `src/**` or `tests/**`, and does **not** accept the drafted ADR as a control-surface decision.

The Phase 3 entry planning authorization at `governance/authorizations/2026-05-13_phase-3-entry-planning.md` "Allowed Planning Topics" item 7 contemplates "ADR follow-ups that Phase 3 entry would require — at minimum the runtime-phase monitoring cadence follow-up deferred by ADR-008 §D6". `plan/phase3_entry_plan.md` §4 item 1 names this drafting work as candidate task P3-01 and labels it "future / not-authorized" pending a separate written authorization. This artifact is that separate written authorization, restricted to drafting the follow-up ADR in **Proposed** status.

The closure of Phase 2 at `governance/authorizations/2026-05-12_phase-2-closure.md` and the Phase 3 entry planning authorization at `governance/authorizations/2026-05-13_phase-3-entry-planning.md` both explicitly state that they do **not** open Phase 3. Those statements are unchanged by this authorization. This authorization opens **only** the drafting of one specific ADR.

## Scope

The work authorized by this artifact is restricted to:

- This authorization artifact at `governance/authorizations/2026-05-13_p3-01.md`.
- A new ADR file at `docs/decisions/ADR-009_runtime_monitoring_cadence.md`, drafted in **Proposed** status (per `docs/decisions/ADR-template.md`), defining the runtime-phase monitoring cadence and runtime-phase missed-packet severity that ADR-008 §D6 defers. The ADR is **policy-level only**: it describes the rule, not a runtime implementation of the rule. The ADR's `Status` field is `Proposed`. **Acceptance** of the ADR (changing `Status` to `Accepted`) is a separate, future decision that is **not** authorized by this artifact — see "Explicitly Not Authorized" below.
- Minimal cross-reference updates strictly necessary to record that the ADR draft exists: a single line added to `README.md`'s ADR list naming ADR-009 in its Proposed status, and (optionally) checking off the existing ADR-008 §D6 follow-up checkbox in `docs/decisions/ADR-008_monitoring_cadence_and_ai_monitor_role.md` "Follow-up Actions" with a parenthetical pointer to the Proposed ADR. These edits do not relax any boundary, do not change the `MASTER_STATUS.md` §8 step 4a allowlist, do not declare Phase 3 open, do not accept ADR-009, and do not modify any quality gate. Per `AI_WORKFLOW.md` §1.2 and §6 rule 2 ("One status keeper"), no edit to `MASTER_STATUS.md` is made by the authorizing PR; any later `MASTER_STATUS.md` reflection of the Proposed ADR is reserved for Perplexity Computer.

The ADR draft, by content, must:

1. Define **policy only**, not runtime. The ADR does not create a daemon, scheduler, background worker, notification system, automation, enforcement mechanism, or any code path that watches the clock. It states what the rule is once runtime exists, so that any future PR introducing runtime can be checked against it.
2. Distinguish clearly between **before runtime exists** (current governance-phase rule in ADR-008 §D3 / §D5 continues to apply, unchanged) and **after runtime-adjacent work begins** (the new ADR-009 rule applies, replacing ADR-008 §D3's runtime-phase guidance and ADR-008 §D5's runtime-phase guidance).
3. State its **UTC discipline** consistent with ADR-004 (UTC and Timezone Discipline): all cadence, deadlines, and timestamps are UTC; no local timezone may be used as the cadence reference.
4. Define the terms **"active workday" / "runtime day"** unambiguously for the runtime phase. ADR-008 §D3 defines "active workday" for the governance phase audit-visibly from GitHub-observable events; the runtime-phase definition must be at least as auditable (e.g. trading-day calendar, UTC weekday, or another observable rule that a reviewer can reconstruct without asking the maintainer).
5. State when **Mode B packets are required** in the runtime phase (replacing ADR-008 §D3's runtime-phase guidance).
6. State the **deadline before merges** for any missed runtime-phase packet (replacing or tightening ADR-008 §D5's runtime-phase guidance), including whether a missed packet is an audit issue with a catch-up note, an automatic rollback trigger, or some intermediate severity.
7. State the **severity / rollback / catch-up behavior** for missed runtime-phase packets, including whether a missed packet during live operation halts trading, halts the daemon, requires operator acknowledgement, or only requires a catch-up note.
8. State the **interaction with Mode A** when both Mode A and Mode B triggers fire on the same PR or runtime event (consistent with ADR-008 §D7's dual-artifact rule for the governance phase).
9. State what the ADR does **not** create: no daemon, no scheduler, no automation, no notification implementation, no enforcement code, no kill-switch wiring, no broker-side-effect path. The ADR is a governance decision, not a runtime control surface materialised in code.

The ADR draft must **not**, by content:

- Authorize, plan-into-existence, or pre-approve any Phase 3 task beyond drafting this ADR.
- Open Phase 3.
- Authorize runtime work, broker integration, market-data integration, order management, strategy logic, scheduling, persistence, deployment, env-var loading, or any concrete implementation of `KillSwitchProtocol`, `ReconciliationProtocol`, or `HeartbeatProtocol`.
- Extend the `MASTER_STATUS.md` §8 step 4a allowlist.
- Modify any quality gate, secrets baseline, or always-forbidden category.
- Name a specific trading day calendar or broker as authoritative; calendar references must be illustrative and clearly marked as future / not-authorized.
- Embed real secrets, real credentials, real account identifiers, real broker / venue identifiers, or real endpoints.

## Explicitly Not Authorized

The authorization above does not permit, by implication or otherwise, any of the following. Each remains forbidden until Kevin records a separate written authorization per `MASTER_STATUS.md` §7 and `AI_WORKFLOW.md` §6 rule 3 ("One approver") / rule 7 ("No phase drift"):

- **Accepting ADR-009.** This authorization permits drafting the ADR in **Proposed** status only. Changing the `Status` field to `Accepted` is a high-risk architecture decision under `AI_WORKFLOW.md` §4(2) (it defines a runtime-phase control surface) and requires a separate written authorization from Kevin, a sibling artifact under `governance/authorizations/`, **Mode A** adversarial review per `AI_WORKFLOW.md` §4(2) / `plan/phase3_entry_plan.md` §7, and a **Mode B** monitoring packet per ADR-008 §D3 / §D5.
- **Opening Phase 3.** This authorization opens drafting of one ADR, not Phase 3. The Phase 3 entry decision (candidate task P3-02 in `plan/phase3_entry_plan.md` §4) is itself a phase gate per `AI_WORKFLOW.md` §4(1) and remains a future, separate written authorization. Drafting ADR-009 does **not** authorize opening Phase 3 and does **not** by itself satisfy any Phase 3 entry criterion (`plan/phase3_entry_plan.md` §5 criterion 5 requires the ADR-008 §D6 follow-up ADR to be **authorized, opened, Mode-A reviewed, and merged in Accepted status** before any runtime PR can open; this authorization satisfies the "opened in Proposed status" half only).
- **Any new Phase 2 task beyond P2-05.** The `plan/phase2_entry_plan.md` §4 P2-01..P2-05 sequence is exhausted and Phase 2 is formally closed (`governance/authorizations/2026-05-12_phase-2-closure.md`). No P2-06 or sibling Phase 2 task is opened.
- **Any further Phase 3 candidate task (P3-02, P3-03, …).** Naming a candidate task in `plan/phase3_entry_plan.md` §4 is planning discussion, not authorization. Each requires its own separate written authorization.
- **Runtime work of any kind.** No `__main__` entry point, no daemon, no scheduler, no background worker, no long-running service, no live execution loop. ADR-009 itself is governance prose; it does not materialise runtime.
- **Broker integration.** No broker SDK, no broker connector, no broker reconciliation runtime, no broker authentication, no broker credentials.
- **Market-data integration.** No market-data ingestion code, no market-data pipeline, no committed dataset, no live or historical data feed.
- **Order management.** No order objects, no order placement, no position management, no fills, no trade reports.
- **Strategy logic.** No trading signals, no scanners, no models, no portfolio rules, no backtests.
- **Scheduling.** No cron-style scheduler, no APScheduler, no background-job framework, no operator-heartbeat daemon, no kill-switch runtime, no reconciliation runtime, no monitoring daemon, no notification daemon, and no automation that watches the cadence rule the ADR draft defines.
- **Persistence.** No SQLite, no on-disk database, no on-disk reconciliation snapshot, no on-disk heartbeat state, no log sink, no file artifact written by runtime code.
- **Deployment.** No deployment workflow, no rollout, no rollback runtime, no CI/CD pipeline beyond the existing local pre-commit and pytest gates.
- **Secrets / env-var loading.** No real secrets, no real credentials, no `.env` files, no `os.environ` / `os.getenv` reads, no real account identifiers, no real broker / venue identifiers, no real endpoints embedded anywhere in the repository under this authorization. ADR-001's secrets-management discipline is not relaxed.
- **Concrete risk implementations.** No concrete implementation of `KillSwitchProtocol`, `ReconciliationProtocol`, or `HeartbeatProtocol` lands inside the runtime package under this authorization. The P2-05 boundary (`src/gmc_rebuild/risk/` is types and abstract `typing.Protocol` definitions only) is preserved.
- **New trading behavior.** No change that introduces, simulates, or wires up trading behavior — in any module, under any name, anywhere in the tree.
- **Tooling relaxation.** No weakening of pre-commit, Ruff, mypy strict mode, `detect-secrets`, `.gitignore`, `.secrets.baseline`, the `MASTER_STATUS.md` §8 step 4 always-forbidden scan, the §8 step 4a per-PR Phase 2 infrastructure allowlist scan, or the §8 step 4c recursive forbidden-token scan.
- **§8 step 4a allowlist expansion.** The `allowed_p2_infra` allowlist is **not** extended by this authorization. It continues to contain exactly the five P2-01..P2-05 entries (`src`, `src/gmc_rebuild/config`, `src/gmc_rebuild/time`, `src/gmc_rebuild/logging`, `src/gmc_rebuild/risk`).
- **Source / test changes.** No file under `src/**` or `tests/**` is modified or created by this authorizing PR. ADR-009 lives under `docs/decisions/`; the authorization artifact lives under `governance/authorizations/`.
- **New tags or releases.** No git tag, no GitHub release, no version bump is created by this authorization or its PR.
- **Mode A / Mode B substitution.** This authorization is governance prose, not a control-surface decision; drafting an ADR in Proposed status is not a substitute for the Mode A adversarial review that ADR-009's acceptance will require, nor for the Mode B monitoring packet that the authorizing PR triggers under ADR-008 §D3 (see "Required Monitoring" below).

The always-forbidden categories in `MASTER_STATUS.md` §6 and `plan/phase2_entry_plan.md` §2 — strategy code, broker execution code, live or paper trading wired to a real broker, runtime daemons affecting accounts, real market data ingestion, order placement, and real secrets — remain forbidden in all modes. This authorization does not relax them.

## Required Monitoring (Mode B)

Per ADR-008 §D3 / §D5, the authorizing PR is itself an active-workday event on 2026-05-13 (a pull request open / merged on that UTC date). Per ADR-008 §D4 "same-day naming", the first packet for 2026-05-13 already exists at `monitoring/daily/2026-05-13.md` (PR #26 / merged via PR #27) and a second slugged packet already exists at `monitoring/daily/2026-05-13_pr28.md` (PR #28 / merged via PR #29). The authorizing PR for P3-01 is therefore a **third** distinct active-workday event on the same UTC date and requires a **slugged third packet** (suggested name `monitoring/daily/2026-05-13_p3-01.md` or `monitoring/daily/2026-05-13_pr<N>.md` where `<N>` is this PR number).

Per ADR-008 §D5 the slugged third packet must be **committed and merged to `main` before** the authorizing PR for P3-01 merges. The packet is filed as a **separate** monitoring PR per the established precedent (PR #27 → PR #26; PR #29 → PR #28); it is not bundled into the authorizing PR. Perplexity Computer confirms this sequencing before verifying the authorizing PR for Kevin's review.

The Mode B packet must reflect that the authorizing PR's content is restricted to drafting ADR-009 in Proposed status, that ADR-009 is **not** accepted by the authorizing PR, and that Phase 3 is **not** opened.

## Mode A Status

ADR-009 in **Proposed** status under this authorization is governance documentation: it records a draft of the future rule, without taking effect. Drafting is narrower than acceptance and does not by itself create the runtime-phase control surface; that control surface is created only when ADR-009 is **Accepted**.

Two judgments record explicitly:

1. **Mode A is required for ADR-009 acceptance.** Per `plan/phase3_entry_plan.md` §4 item 1 and §7, accepting the ADR-008 §D6 follow-up ADR is a high-risk architecture decision under `AI_WORKFLOW.md` §4(2) and requires a Mode A written critique before merge. That acceptance is **not** part of this authorization and is reserved for a future, separate written authorization from Kevin.
2. **Mode A applicability to the authorizing PR itself is a judgment call.** The authorizing PR drafts the ADR in Proposed status only and does not accept it; it is governance documentation analogous to PR #26 (Phase 3 entry planning authorization, judged Mode-B-only in `monitoring/daily/2026-05-13.md`) and PR #28 (Phase 3 entry plan, judged Mode-B-only in `monitoring/daily/2026-05-13_pr28.md`). The default expectation in this authorization is **Mode-B-only for the authorizing PR itself**, recorded explicitly in the Mode B monitoring packet for this PR. If Kevin or Perplexity Computer judges that drafting the ADR (even in Proposed status) constitutes a high-risk architecture decision on its own, Mode A applies and is escalated before merge.

This Mode A judgment for the authorizing PR is **not** a waiver of the Mode A requirement for ADR-009 acceptance, for the Phase 3 entry decision (candidate task P3-02), or for any future Phase 3 implementation PR.

## Preserved Gates for Any Future Work

Any work beyond this drafting authorization — accepting ADR-009, opening Phase 3, opening any specific Phase 3 task (`P3-02`, `P3-03`, …), or opening any runtime / broker / market-data / order / strategy / scheduler / persistence / deployment work — requires **all** of the following, none of which is satisfied by this authorization:

1. **Separate written authorization from Kevin** per `MASTER_STATUS.md` §7 and `AI_WORKFLOW.md` §6 rule 3 / rule 7, with a sibling artifact under `governance/authorizations/`.
2. **Mode A adversarial review** per `AI_WORKFLOW.md` §4. ADR-009 acceptance is a high-risk architecture decision (§4(2)); opening Phase 3 is a phase gate (§4(1)).
3. **Mode B monitoring packet** per ADR-008 §D3 / §D5 on the active workday of the authorizing PR; before-merge sequencing per ADR-008 §D5.
4. **§8 step 4a allowlist updates in the same PR** for any new authorized directory, per the standing rule in `MASTER_STATUS.md` §8 step 4b.
5. **Runtime-cadence ADR-008 §D6 follow-up in Accepted status** before any Phase 3 PR that introduces runtime can open, per `plan/phase3_entry_plan.md` §5 criterion 5. Drafting ADR-009 in Proposed status under this authorization is the first half of that criterion; acceptance is the second half and is a separate decision.

## Exit Criteria

This authorizing PR may merge only after the full required check suite passes on the authorizing branch:

- `pre-commit run --all-files` (clean, or every failure documented).
- `pytest` (clean).
- `MASTER_STATUS.md` §8 step 3 (baseline ancestry from `1f101fc`).
- `MASTER_STATUS.md` §8 step 4 (always-forbidden scan).
- `MASTER_STATUS.md` §8 step 4a (per-PR Phase 2 infrastructure allowlist scan; allowlist is unchanged).
- `MASTER_STATUS.md` §8 step 4c (recursive forbidden-token scan; subshell exit `0`).
- A slugged 2026-05-13 Mode B monitoring packet for this PR has been authored under ADR-008 §D3 and committed and merged to `main` per ADR-008 §D5, in a separate monitoring PR, **before** this authorizing PR merges.

After this authorization merges, the next allowed decisions remain those listed in `MASTER_STATUS.md` §9, plus (a) future Mode-A review of, and future separate written authorization for, ADR-009 acceptance, and (b) the existing planning workstream described in `governance/authorizations/2026-05-13_phase-3-entry-planning.md`. Phase 3 is **not** opened; Phase 2 remains formally closed; no Phase 2 task beyond P2-05 is opened; no Phase 3 task beyond P3-01 drafting is opened.

## Supporting Evidence (Non-Authoritative)

The following are pointers to the surrounding governance record. They are evidence, not the authorization itself; this file is the authorization of record for P3-01 drafting.

- Phase 1 accepted baseline: `1f101fc` (`MASTER_STATUS.md` §3).
- Phase 2 closure authorization: `governance/authorizations/2026-05-12_phase-2-closure.md`.
- Phase 3 entry planning authorization (parent governance workstream): `governance/authorizations/2026-05-13_phase-3-entry-planning.md`.
- Phase 3 entry plan (candidate-task definition for P3-01): `plan/phase3_entry_plan.md` §4 item 1, §5 criterion 5, §7, §9.
- ADR being followed up: `docs/decisions/ADR-008_monitoring_cadence_and_ai_monitor_role.md` §D6 (deferred follow-up), §D3 (current governance-phase cadence rule), §D5 (current governance-phase missed-packet rule), §D7 (Mode A + Mode B overlap).
- UTC discipline applicable to ADR-009: `docs/decisions/ADR-004_utc_discipline.md`.
- Runtime-phase control surfaces ADR-009 must coordinate with: `docs/decisions/ADR-002_kill_switch.md`, `docs/decisions/ADR-003_reconciliation.md`, `docs/decisions/ADR-005_heartbeat.md`.
- Pattern reference (prior governance-only authorization slices): `governance/authorizations/2026-05-12_p2-03.md`, `governance/authorizations/2026-05-12_p2-04.md`, `governance/authorizations/2026-05-12_p2-05.md`.
- Monitoring cadence rule governing the sibling Mode B packet for this PR: `docs/decisions/ADR-008_monitoring_cadence_and_ai_monitor_role.md` §D3 / §D4 / §D5.
