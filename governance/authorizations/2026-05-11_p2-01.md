# Authorization Record — PR P2-01 (Package Skeleton and Test Harness)

**Date authorized:** 2026-05-11 UTC
**Approver:** Kevin Heaney (GitHub: `KPH3802`) — the sole approver per `AI_WORKFLOW.md` §1.3 and §6 rule 3.
**Phase opened by this authorization:** Phase 2 implementation, narrowly opened at PR P2-01 only.
**PR authorized:** GitHub PR #6 — `P2-01: package skeleton and test harness`.
**Merged commit:** `e0278c4` (`P2-01: add package skeleton and test harness`).
**Baseline at time of authorization:** Accepted Phase 1 baseline `1f101fc` (per `MASTER_STATUS.md` §3).

---

## 1. Purpose of This Record

This file is the durable, repo-committed copy of Kevin's written authorization for PR P2-01.

Prior to this artifact, the audit-visible record of Kevin's authorization lived entirely in GitHub PR-comment history. PR history remains supporting evidence and is not invalidated by this record, but it is not a substitute for an in-tree governance artifact: PR comments can be edited or deleted, are not part of the working tree, and are not reachable from `git log`. This file closes that gap by recording the authorization inside the repository itself, on the same audit surface as `MASTER_STATUS.md` and `AI_WORKFLOW.md`.

This artifact does not retroactively authorize anything. P2-01 was already authorized in writing by Kevin on PR #6 and merged at `e0278c4`. This file records that authorization audit-visibly.

---

## 2. Authorized Scope

Kevin authorized **exactly and only** the following work under PR P2-01:

- Creation of the importable `src/gmc_rebuild/` package skeleton with `__init__.py`, a `__version__` string, and a `py.typed` marker.
- Matching test harness under `tests/` (`tests/test_package_skeleton.py`) that exercises importability, version exposure, `src/` layout, the `py.typed` marker, and the absence of unexpected submodules.
- Minimal `pyproject.toml` adjustments required for package discovery under the `src/` layout. Tooling strictness (Ruff, mypy strict, pytest, detect-secrets) is unchanged.
- Governance updates to `MASTER_STATUS.md` and `README.md` that reflect P2-01 being authorized while leaving P2-02+ closed.

The `src/` directory is the **only** Phase 2 infrastructure path on the `MASTER_STATUS.md` §8 step 4a allowlist as a result of this authorization.

---

## 3. Explicitly Not Authorized

The authorization in §2 does not extend, by implication or otherwise, to any of the following. Each remains forbidden until Kevin records a separate written authorization per `MASTER_STATUS.md` §7 and `AI_WORKFLOW.md` §6 rule 3:

- **P2-02 and later (P2-02..P2-05)** in `plan/phase2_entry_plan.md` §4. Each requires its own written authorization and its own audit artifact under `governance/authorizations/`.
- Trading strategy code: signals, scanners, models, portfolio rules, backtests.
- Broker code: broker SDK integration, broker execution, order placement, position management.
- Live trading workflows.
- Paper trading workflows wired to a real broker.
- Runtime daemons, schedulers, long-running services, or background workers that touch accounts, markets, or money.
- Market data ingestion code, real data pipelines, or stored datasets.
- Real secrets, private keys, certificates, `.env` files, real account identifiers, real endpoints.
- Any submodule under `src/gmc_rebuild/` beyond the skeleton files named in §2.
- Any relaxation of pre-commit, Ruff, mypy strict mode, detect-secrets, `.gitignore`, `.secrets.baseline`, the forbidden-category startup scan, or any other quality gate.

This list mirrors the always-forbidden categories in `MASTER_STATUS.md` §6 and `plan/phase2_entry_plan.md` §2. The mirroring is intentional and not a re-authorization: those categories remain forbidden by default, and naming them here records that Kevin did not silently relax them when authorizing P2-01.

---

## 4. Supporting Evidence (Non-Authoritative)

The following are pointers to the GitHub-side record. They are evidence, not the authorization itself; this file is the authorization of record.

- PR: https://github.com/KPH3802/gmc-rebuild/pull/6
- Merge commit: `e0278c4`
- Phase 1 accepted baseline (from which P2-01 descends): `1f101fc`
- Phase 2 entry plan: `plan/phase2_entry_plan.md` §4

---

## 5. How to Use This Record

- `MASTER_STATUS.md` §7 and §8 reference this file as the durable authorization record for P2-01.
- `AI_WORKFLOW.md` requires every future phase-opening or phase-expanding authorization (P2-02, P2-03, …, and any later phase) to be captured both in PR history and in a sibling file under `governance/authorizations/` before or as part of the authorized PR. One approver remains: Kevin.
- A reviewer auditing whether a given PR was authorized should look here first, then cross-reference the named PR and merge commit on GitHub.
