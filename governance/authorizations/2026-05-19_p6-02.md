# P6-02 Authorization — Eligibility-Check Pure Functions

Date: 2026-05-19
Authorizer: Kevin
Scope: Bounded, narrowly scoped P6-02 implementation packet for `gmc_rebuild` adding the **second Phase 6 implementation task** — a new subpackage `src/gmc_rebuild/eligibility/` containing the closed two-member `EligibilityOutcome` `StrEnum` (`ELIGIBLE` / `INELIGIBLE`), the closed five-member `EligibilityReason` `StrEnum` (`SYMBOL_NOT_ALLOWED`, `SIDE_NOT_PERMITTED`, `QUANTITY_BELOW_MINIMUM`, `QUANTITY_ABOVE_MAXIMUM`, `RATIONALE_TOO_SHORT`), the frozen, slotted `EligibilityConfig` five-field caller-supplied config slice (`allowed_symbols`, `allowed_sides`, `min_quantity`, `max_quantity`, `min_rationale_length`) with strict per-field validation, the frozen, slotted `EligibilityDecision` two-field result (`outcome`, `reasons`) enforcing the biconditional that `ELIGIBLE iff reasons == ()`, and the pure `check_eligibility(intent, config) -> EligibilityDecision` function that runs every check, returns every failing reason in canonical declaration order, and does not mutate the supplied `SignalIntent` or `EligibilityConfig`; one new test file at `tests/eligibility/test_eligibility.py` covering the eligible happy path, every closed reason code individually, the maximum-simultaneous-reasons cases under the `max_quantity >= min_quantity` config invariant, deterministic repeat calls, input non-mutation, type validation, closed-set enumeration invariants, frozen / slotted / closed-shape invariants on both dataclasses, the `ELIGIBLE iff reasons == ()` biconditional, equality / hashability, an `ast.walk` inertness self-check, and assertions that the root `gmc_rebuild` package does not re-export the new surface; an extension of `tests/test_package_skeleton.py` by exactly one entry (`eligibility`) added to the authorized phase-package set with the matching docstring entry naming PR P6-02 and this authorization artifact; an extension of the `MASTER_STATUS.md` §8 step 4a `allowed_p2_infra` bash gate by exactly one entry (`src/gmc_rebuild/eligibility`) with the matching comment block referencing PR P6-02 and this authorization artifact; a conservative `MASTER_STATUS.md` §1 status reflection paragraph describing the P6-02 surface and explicitly listing the categories the packet does **not** open; and this authorization artifact at `governance/authorizations/2026-05-19_p6-02.md`. P6-02 opens Phase 6 work at the eligibility-check pure-function scope authorized here; it does **not** open, enumerate, or pre-commit any successor P6 packet (P6-03 and beyond remain **future / not authorized** per `plan/phase6_entry_plan.md` §4). No modification of any other `src/**` file. No modification of any other existing `tests/**` file beyond the narrowly scoped one-entry extension to `tests/test_package_skeleton.py` per the authorized narrow scope adjustment. The merged P4-06 / P4-07 / P4-08 safety foundation, the merged P3-03 / P3-04 / P3-05 in-memory fakes, the merged P2-01..P2-05 packages, the merged OPS-01..OPS-04B / OPS-06 operations records, the merged P5-01..P5-07 simulation surface and tripwire tests, the merged GOV-01 governance reconciliation, the merged GOV-02 execution-environment workflow correction, the foundation-complete confirmation report and its 1-FU-A / 1-FU-B follow-ups, the merged Phase 6 entry plan, and the merged P6-01 typed signal-intake boundary (commit `d52551a`) are all preserved unchanged.

## Authorization

Kevin authorized this packet on 2026-05-19. The verbatim written authorization follows:

> Authorize P6-02 only: Eligibility-check pure functions.
>
> Context: P6-01 was completed and pushed as d52551a. This packet corresponds to plan/phase6_entry_plan.md §4 P6-02 / Roadmap §8 Packet 4. This is an implementation packet, but only within the narrow scope below.
>
> Allowed scope:
> 1. Add a new inert subpackage: src/gmc_rebuild/eligibility/__init__.py and src/gmc_rebuild/eligibility/_check.py or similarly minimal internal module.
> 2. Add tests: tests/eligibility/test_eligibility.py.
> 3. Extend exactly one existing package-skeleton test: tests/test_package_skeleton.py — only add one authorized package entry (eligibility) and add only matching explanatory prose if needed.
> 4. Update MASTER_STATUS.md narrowly: add exactly one §8 step 4a allowlist entry for src/gmc_rebuild/eligibility and add a conservative P6-02 status reflection. Do not rewrite unrelated status sections.
> 5. Add authorization artifact: governance/authorizations/2026-05-19_p6-02.md mirroring the P6-01 authorization artifact structure.
>
> Objective: Implement pure-function eligibility checks that accept a P6-01 SignalIntent plus an explicitly supplied project-config slice and return a structured eligibility decision: ELIGIBLE / INELIGIBLE, closed-set reason codes, deterministic, frozen/slotted dataclass shape if introducing decision/config objects, no mutation of the input SignalIntent.
>
> Required tests: eligible happy path; ineligible reason-code paths; exhaustive closed reason-code coverage; deterministic repeat calls; input SignalIntent is not mutated; frozen / slots behavior for new dataclasses if introduced; equality/hashability if dataclasses are introduced; AST inertness self-check; root package does not re-export eligibility symbols; package skeleton includes exactly one new eligibility entry.
>
> Explicitly not authorized: no strategy/scanner/model logic; no market-data ingestion; no broker SDK or broker integration; no order intent, order staging, execution, paper trading, or live trading; no scheduler, daemon, __main__, CLI, background loop, or runtime activation; no network, filesystem persistence, database, secrets, env-var reads, or external config loading; no real external symbol allow/deny list; no expansion of P6-01 SignalIntent shape or SignalSide enum; no re-export from src/gmc_rebuild/__init__.py; no changes to README.md, docs/**, plan/**, pyproject.toml, .pre-commit-config.yaml, RECOVERY.md, AI_WORKFLOW.md, or prior authorization artifacts; do not open P6-03 or later packets.
>
> Validation required before reporting: .venv/bin/python -m pytest -q; .venv/bin/pre-commit run --all-files; confirm no files modified by hooks on final pass; confirm no unintended files changed; confirm .claude/ and Claude_Transfes/ remain untracked.
>
> Stop before commit. Report the diff, files changed, test/pre-commit results, and any concerns.
>
> Authorized by Kevin on 2026-05-19.

This authorization is the authorization of record per `AI_WORKFLOW.md` §7. The packet is built per the GOV-02 execution-environment workflow rule (local Claude Code / Claude Max acting as the default builder for `gmc-rebuild` repo edits; merged via PR #132 / reconciled via PR #134) and preserves the canonical reconciliations through PR #114, PR #118, PR #122, PR #126, PR #130, PR #134, and PR #138 verbatim.

## Allowed Scope

The implementation PR may include exactly:

- A new subpackage at `src/gmc_rebuild/eligibility/` containing exactly two Python files:
  - `__init__.py` — module docstring, an explicit `from gmc_rebuild.eligibility._check import (EligibilityConfig, EligibilityDecision, EligibilityOutcome, EligibilityReason, check_eligibility)`, and `__all__ = ["EligibilityConfig", "EligibilityDecision", "EligibilityOutcome", "EligibilityReason", "check_eligibility"]`. Imports only `__future__` and the internal `_check` submodule. No runtime behaviour beyond the re-export.
  - `_check.py` — the closed `EligibilityOutcome` `StrEnum` (`ELIGIBLE = "ELIGIBLE"`, `INELIGIBLE = "INELIGIBLE"` only); the closed `EligibilityReason` `StrEnum` (`SYMBOL_NOT_ALLOWED = "SYMBOL_NOT_ALLOWED"`, `SIDE_NOT_PERMITTED = "SIDE_NOT_PERMITTED"`, `QUANTITY_BELOW_MINIMUM = "QUANTITY_BELOW_MINIMUM"`, `QUANTITY_ABOVE_MAXIMUM = "QUANTITY_ABOVE_MAXIMUM"`, `RATIONALE_TOO_SHORT = "RATIONALE_TOO_SHORT"` only); a module-level `_REASON_ORDER` tuple in the canonical declaration order used to sort the `reasons` tuple on an ineligible decision; the frozen, slotted `EligibilityConfig` dataclass with exactly five caller-supplied fields (`allowed_symbols: frozenset[str]`, `allowed_sides: frozenset[SignalSide]`, `min_quantity: int`, `max_quantity: int`, `min_rationale_length: int`) and strict per-field validation enforced in `__post_init__` (frozenset-typed `allowed_symbols` containing only non-empty strings; frozenset-typed `allowed_sides` containing only `SignalSide` members; positive integer `min_quantity` rejecting `bool` and `float`; positive integer `max_quantity` rejecting `bool` and `float` and requiring `max_quantity >= min_quantity`; non-negative integer `min_rationale_length` rejecting `bool` and `float`; mismatched types raise `TypeError`, invalid values raise `ValueError`); the frozen, slotted `EligibilityDecision` dataclass with exactly two fields (`outcome: EligibilityOutcome`, `reasons: tuple[EligibilityReason, ...]`) enforcing the biconditional that `outcome == ELIGIBLE iff reasons == ()`; and the pure `check_eligibility(intent, config) -> EligibilityDecision` function that runs every check, returns every failing reason in `_REASON_ORDER` order, and returns the supplied intent / config unchanged by identity-preservation of the underlying objects. Imports only `__future__`, `dataclasses` (stdlib), `enum` (stdlib), and `gmc_rebuild.signal_intake` (for `SignalIntent` and `SignalSide`). No `os`, no `socket`, no `requests`, no `urllib`, no `http`, no `threading`, no `asyncio`, no `subprocess`, no `sqlite3`, no `time`, no `pickle`, no `shelve`, no `ssl`, no `smtplib`, no `ftplib`, no `gmc_rebuild.config` import, no external configuration loader, no real external symbol allow/deny list backed by any data source other than the caller-supplied `EligibilityConfig.allowed_symbols` frozenset. No `__main__` block, no `time.sleep`, no `os.environ`, no `os.getenv`, no `open(` builtin, no broker SDK, no third-party dependency.
- A new test file at `tests/eligibility/test_eligibility.py` containing focused pytest functions that exercise the merged P6-02 surface. The file:
  - Imports only `__future__`, `ast`, `importlib`, `dataclasses.FrozenInstanceError`, `pathlib.Path` (stdlib), and the already-merged in-repo modules `gmc_rebuild.eligibility` (for the new surface) and `gmc_rebuild.signal_intake` (for `SignalIntent` and `SignalSide`). No `pytest.raises`. No fixture-typed parameters. No third-party dependency. The mypy-strict pre-commit hook does not need a pytest-stub dependency.
  - Asserts the following invariant categories: (1) **eligible happy path** plus three boundary cases (at `min_quantity`, at `max_quantity`, at `min_rationale_length`); (2) **each of the five `EligibilityReason` codes individually**; (3) **two four-reason multi-failure cases** (below-min and above-max quantity paths) exercising the maximum number of simultaneously-failing reasons under the `max_quantity >= min_quantity` config invariant, plus a pair test that asserts canonical-order on a two-reason subset, plus a documentation tripwire pinning the impossibility of the all-five-simultaneous case; (4) **exhaustive closed-set coverage** of `EligibilityReason`; (5) **deterministic repeat calls** on both the eligible and ineligible paths; (6) **input non-mutation** for both `SignalIntent` and `EligibilityConfig` snapshots; (7) **type validation** rejecting non-`SignalIntent` and non-`EligibilityConfig` arguments to `check_eligibility`; (8) **closed two-member `EligibilityOutcome`** enumeration with `StrEnum` `str`-subclass invariants; (9) **closed five-member `EligibilityReason`** enumeration with value-equals-name invariants; (10) **`EligibilityConfig` frozen / slotted / closed-five-field shape** plus per-field validation (non-frozenset rejection, non-string allowed-symbol-member rejection, empty-string-in-allowed-symbols rejection, non-frozenset allowed-sides rejection, non-`SignalSide`-member rejection, bool-as-quantity rejection, zero-or-negative quantity rejection, `max_quantity < min_quantity` rejection, negative `min_rationale_length` rejection, bool-as-`min_rationale_length` rejection, zero-`min_rationale_length` acceptance); (11) **`EligibilityDecision` frozen / slotted / closed-two-field shape** plus the `ELIGIBLE iff reasons == ()` biconditional plus type validation on the constructor (non-outcome rejection, non-tuple rejection, non-reason-member rejection); (12) **equality and hashability** for both dataclasses (structurally-equal instances compare `==` and have equal hashes; outcome-differing decisions are not equal); (13) an **inertness self-check using `ast.walk`** confirming that every Python file under `src/gmc_rebuild/eligibility/` imports only from the authorized prefixes (`__future__`, `dataclasses`, `enum`, `gmc_rebuild.eligibility`, `gmc_rebuild.signal_intake`) and that no forbidden runtime root is imported; (14) a **belt-and-suspenders substring scan** for `__main__` blocks, `time.sleep(`, `socket.`, `urllib`, `requests.`, and the `open(` builtin; (15) **root package non-re-export** — `gmc_rebuild` does not expose any of `EligibilityConfig` / `EligibilityDecision` / `EligibilityOutcome` / `EligibilityReason` / `check_eligibility` as attributes, and `gmc_rebuild.__all__` does not include them.
  - Implements small local helpers private to the new module (`_intent`, `_config`, `_snapshot_intent`, `_snapshot_config`, `_collect_imported_modules_from_subpackage_source`); no helper is exported.
- A narrowly scoped extension of `tests/test_package_skeleton.py` by exactly one entry — `eligibility` — added to the authorized phase-package set with the matching docstring entry naming PR P6-02 and this authorization artifact. The test's purpose as a phase-expansion guard is preserved; the assertion is not weakened; the hardcoded set is not converted into a looser dynamic allowlist.
- A narrowly scoped extension of the `MASTER_STATUS.md` §8 step 4a `allowed_p2_infra` bash gate by exactly one entry — `src/gmc_rebuild/eligibility` — added in the same PR that introduces the directory. The `for path in ...` loop is extended by the same entry; a `src/gmc_rebuild/eligibility) pr_tag="PR P6-02" ;;` case is added; the comment block above the bash gate is extended with the matching PR P6-02 entry referencing this authorization artifact; the closing OK echo lists `P6-02` in the conformance message. No other §8 step 4a allowlist entry is added.
- A conservative `MASTER_STATUS.md` §1 status reflection paragraph titled "P6-02 implementation status (governance/documentation reflection, 2026-05-19 UTC — pending merge in this open P6-02 implementation PR)" describing the new eligibility subpackage, the new test file, the package-skeleton-test extension, and the §8 step 4a allowlist extension, and explicitly listing the categories the packet does **not** open. The conservative wording is scoped to this open PR only; the merged P5-01..P5-07 / P5-06 planning / P5-07 planning / Phase 5 entry planning / GOV-01 / GOV-02 / OPS-01..OPS-04B / OPS-06 status reflections continue to read **merged on `main`** and are not reverted to stale `**pending merge**` language by this packet; the canonical reconciliations merged via PR #114, PR #118, PR #122, PR #126, PR #130, PR #134, and PR #138 are preserved unchanged.
- This authorization artifact at `governance/authorizations/2026-05-19_p6-02.md`.

No file under `src/**` other than the new `src/gmc_rebuild/eligibility/` subpackage is added or modified. No existing test file under `tests/**` other than the narrowly scoped one-entry extension to `tests/test_package_skeleton.py` is modified. No file under `monitoring/**`, `docs/**`, `plan/**`, `governance/authorizations/` other than this new P6-02 artifact, `pyproject.toml`, `.pre-commit-config.yaml`, `.secrets.baseline`, `.gitignore`, `README.md`, `RECOVERY.md`, `AI_WORKFLOW.md`, any ADR under `docs/decisions/`, `EXTERNAL_REVIEW_BRIEF.md`, or any other governance / planning document is added or modified. The merged P4-06 / P4-07 / P4-08 safety foundation, P3-03 / P3-04 / P3-05 in-memory fakes, P2-01..P2-05 packages, OPS-01..OPS-04B / OPS-06 operations records, P5-01..P5-07 simulation surface and tripwire tests, GOV-01 governance reconciliation, GOV-02 execution-environment workflow correction, the foundation-complete confirmation report and its 1-FU-A / 1-FU-B follow-ups, the merged Phase 6 entry plan, and the merged P6-01 typed signal-intake boundary (commit `d52551a`) are all preserved unchanged.

## Explicitly Not Authorized

This authorization does not permit:

- Any `src/**` change other than the new `src/gmc_rebuild/eligibility/` subpackage with its two files (`__init__.py` and `_check.py`); no existing `src/**` file is modified, including no modification of `src/gmc_rebuild/signal_intake/`.
- Any modification of any existing test file under `tests/**` other than the narrowly scoped one-entry extension to `tests/test_package_skeleton.py`. The test's purpose as a phase-expansion guard is preserved; the assertion is not weakened; the hardcoded set is not converted into a looser dynamic allowlist.
- Any new public symbol on `gmc_rebuild` (the package root); no re-export from `src/gmc_rebuild/__init__.py`. The new `EligibilityConfig` / `EligibilityDecision` / `EligibilityOutcome` / `EligibilityReason` / `check_eligibility` surface is reachable only via `from gmc_rebuild.eligibility import ...`.
- Any expansion of the closed two-member `EligibilityOutcome` enumeration. No third outcome member.
- Any expansion of the closed five-member `EligibilityReason` enumeration. No sixth reason code; no removal or rename of any of the five named codes.
- Any expansion of the closed five-field `EligibilityConfig` dataclass. No sixth field; no removal or rename of any of the five named fields; no field type change.
- Any expansion of the closed two-field `EligibilityDecision` dataclass. No third field; no removal or rename of `outcome` or `reasons`; no relaxation of the `ELIGIBLE iff reasons == ()` biconditional.
- Any expansion of the P6-01 closed two-member `SignalSide` enumeration or closed five-field `SignalIntent` dataclass.
- Any new method on `EligibilityConfig` or `EligibilityDecision` beyond the auto-generated dataclass methods. No `validate()` helper, no `to_dict()`, no `from_dict()`, no `replace()` shim — callers wanting a value-replace operation use `dataclasses.replace` from stdlib directly.
- Any new function in the `gmc_rebuild.eligibility` subpackage beyond `check_eligibility`. No factory function, no validator function exposed as a separate entry point, no helper.
- Any strategy logic. The eligibility check is a structural pre-filter operating only on the supplied `SignalIntent` plus the supplied `EligibilityConfig`. It does not generate signals, scan markets, fetch data, score candidates, or rank alternatives. The `allowed_symbols` field is a closed frozenset supplied by the caller; this module does not maintain or update it.
- Any scanner, model, portfolio, backtest, alpha factor, parameter sweep, regime classifier, market-microstructure analysis, or other algorithmic-trading logic.
- Any external configuration loader. No import of `gmc_rebuild.config`; no `.env` reading; no YAML / TOML / JSON loading from disk. The caller constructs an `EligibilityConfig` directly and supplies it.
- Any broker integration (real or paper), broker SDK, broker account identifier, broker session, broker handshake, broker credential, or broker reference of any kind.
- Any live, delayed, or paper market data, market-data feed, vendor SDK, real symbol universe, real venue identifier, or normalization to any vendor symbol format.
- Any real or paper order placement, order routing, order management, order book, fill, execution, position, P&L, trade report, OMS, EMS, routing instruction, time-in-force, or execution adapter. The eligibility decision is informational; downstream packets (P6-03 and beyond) decide what to do with it.
- Any FIX session, REST client, WebSocket client, or any other connectivity surface.
- Any venue, account, broker credential, routing field, exchange field, or any other order-execution field on `EligibilityConfig` or `EligibilityDecision`.
- Any secrets, credentials, API keys, broker keys, session tokens, OAuth tokens, SSH keys, `.env` files containing live credentials, recovery codes, PEKs, or any other credential storage.
- Any external API call, network call, `socket`, `urllib`, `requests`, `http`, `ssl`, `smtplib`, `ftplib`, DNS lookup, or any other outbound or inbound network surface.
- Any scheduler, daemon, background thread, long-running service, `__main__` entry point, `if __name__` block, `time.sleep`, `asyncio.sleep`, cron job, launchd plist, or any other timed / background execution mechanism.
- Any persistence, database, SQLite, pickle, shelve, filesystem write, caching to disk, append-only ledger, log file, audit trail, or any other persistence surface. The `check_eligibility` function does not log, emit, or record decisions.
- Any runtime activation of any merged Phase 3 fixture (`InMemoryHeartbeat`, `InMemoryKillSwitch`, `InMemoryReconciliation`), any P4-06 / P4-07 / P4-08 safety surface (`RuntimeShell`, `SafetyVerdict`, `OperatorSafetyView`, `format_safety_verdict`), or any P5-01 / P5-02 simulation surface (`SimulationBoundary`, `SimulatedIntent`, `SimulatedOrderIntent`). The P6-02 surface composes only with the merged P6-01 `SignalIntent` / `SignalSide` types; it does not import any other `gmc_rebuild` subpackage.
- Any concrete implementation of `HeartbeatProtocol`, `KillSwitchProtocol`, or `ReconciliationProtocol` outside the merged P3-03 / P3-04 / P3-05 in-memory fakes.
- Any modification of the merged P4-06 / P4-07 / P4-08 safety foundation. P6-02 does not consume, compose, or wrap any safety surface.
- Any modification of the merged P3-03 / P3-04 / P3-05 in-memory fakes. P6-02 does not import them.
- Any modification of the merged P5-01..P5-07 simulation surface and tripwire tests. P6-02 does not import the simulation surface.
- Any modification of the merged P6-01 signal-intake subpackage (`src/gmc_rebuild/signal_intake/`). P6-02 imports `SignalIntent` and `SignalSide` from it and uses them by value; no `signal_intake` source file is modified.
- Any modification of backup / recovery docs (`RECOVERY.md`, the OPS-01..OPS-04B / OPS-06 authorization artifacts), `AI_WORKFLOW.md`, any ADR under `docs/decisions/`, any other `governance/authorizations/*.md` file (including `governance/authorizations/2026-05-19_p6-01.md` and the Phase 6 entry plan), `pyproject.toml`, `.pre-commit-config.yaml`, `.secrets.baseline`, `.gitignore`, `README.md`, `EXTERNAL_REVIEW_BRIEF.md`, any file under `docs/**`, or any file under `plan/**`.
- Any modification of any existing `monitoring/daily/*` packet.
- Any extension of the `MASTER_STATUS.md` §8 step 4a `allowed_p2_infra` bash gate beyond the exactly one new entry `src/gmc_rebuild/eligibility`. No other allowlist entry is added.
- Any extension of the hard-coded `entries == {...}` set in `tests/test_package_skeleton.py` beyond the exactly one new entry `eligibility`.
- Any quality-gate relaxation, hook removal, hook reconfiguration, mypy strictness reduction, ruff rule relaxation, or `detect-secrets` baseline change.
- Any X10 Layer 5 promotion, backup-monitoring automation, DR drill execution, or other ops execution work.
- Any tag or release.
- Any successor P6 packet. P6-03 (composed position / risk decision), P6-04 (simulated-order-intent extension), P6-05 (in-memory simulated portfolio state), P6-06 (deterministic daily report), P6-07 (failure-handling tripwires), and any later P6-0N task remain **future / not authorized** per `plan/phase6_entry_plan.md` §4. Each requires its own separate written authorization from Kevin per `AI_WORKFLOW.md` §7.
- Any reintroduction of stale `**pending merge**` language for any already-merged packet. Any "pending merge" wording in the PR is scoped exclusively to this open P6-02 implementation PR itself.
- Any reinterpretation or relaxation of the GOV-02 execution-environment workflow rule (merged via PR #132 / reconciled via PR #134).

The always-forbidden categories in `MASTER_STATUS.md` §6, `plan/phase4_entry_plan.md` §10, `plan/phase5_entry_plan.md` §10, and `plan/phase6_entry_plan.md` §10 — strategy code, broker execution code, paper-trading or live-trading wired to a real broker, runtime daemons affecting accounts, real market data ingestion, order placement, and real secrets — remain forbidden in all modes. This packet does not relax them and does not record any relaxation.

## Required Validation

Before merge, the implementation PR must pass:

- `.venv/bin/python -m pytest -q` against the full test suite (must pass; the post-P6-01 baseline of 443 tests must be preserved, plus the 54 new P6-02 eligibility tests, for a total of 497 passing tests deterministically).
- `.venv/bin/pre-commit run --all-files` (must exit 0 with every hook passing and no "files were modified by this hook" second-pass message).
- The `MASTER_STATUS.md` §8 startup verification commands relevant to the §8 step 4a allowlist must pass: the new `src/gmc_rebuild/eligibility` path must report `OK: Phase 2/3/4/5/6 infrastructure present and authorized: src/gmc_rebuild/eligibility (PR P6-02)`, and the closing OK echo must read `OK: Phase 2/3/4/5/6 infrastructure paths conform to P2-01/P2-02/P2-03/P2-04/P2-05/P3-03/P3-04/P3-05/P4-06/P5-01/P6-01/P6-02 allowlist`.
- `git diff --name-status main` showing changes confined to: this authorization artifact, the new `src/gmc_rebuild/eligibility/__init__.py`, the new `src/gmc_rebuild/eligibility/_check.py`, the new `tests/eligibility/test_eligibility.py`, the narrowly scoped one-entry extension of `tests/test_package_skeleton.py`, and the narrowly scoped `MASTER_STATUS.md` updates (§8 step 4a allowlist extension and §1 conservative status reflection paragraph). No file under any other `src/**` path is added or modified; no existing `tests/**` file other than `tests/test_package_skeleton.py` is modified.

Mode A / Mode B review requirements remain governed by `AI_WORKFLOW.md`, ADR-008, ADR-009, and the current `MASTER_STATUS.md`. P6-02 implementation introduces a new `src/**` subpackage and is therefore a new control-surface decision; Mode A adversarial review is **independently required** per `AI_WORKFLOW.md` §4(2) and ADR-008 §D7, mirroring the P5-01 / P6-01 first-implementation precedents, and must be delivered as PR-review text on the P6-02 implementation PR only, not committed to the repository (per `AI_WORKFLOW.md` §6 rule 5). Mode B sibling monitoring follows ADR-008 §D3 / §D5 (and ADR-009 D3 / D5 once in force per ADR-009 D7's bootstrap-avoidance clause); per ADR-008 §D5, the Mode B sibling monitoring packet for the active workday must be authored from `main` and merged to `main` **before** this implementation PR per the standing rule.

Phase 2 remains formally closed. Phase 3 remains formally closed. Phase 4 remains open as a governance state with the safety foundation closure recorded by P4-08 preserved. Phase 5 is closed at the merged P5-01..P5-07 scope. Phase 6 is opened at the P6-01 typed signal-intake boundary scope and now extended to the P6-02 eligibility-check pure-function scope by this authorization. The foundation-complete confirmation report and its 1-FU-A / 1-FU-B follow-ups are preserved. The merged Phase 6 entry plan is preserved. The GOV-02 execution-environment workflow rule is in force. P6-03 (and any later P6-0N task) remains **future / not authorized**.
