# P5-05 Authorization — Operator-View × Composed Safety-Foundation Tripwire Tests

Date: 2026-05-18
Authorizer: Kevin Heaney
Scope: Bounded, tests-only P5-05 implementation packet for `gmc_rebuild` adding the **fifth Phase 5 implementation task** — a single new pytest-only test file at `tests/runtime/test_operator_view_composed_safety_foundation.py` that composes the merged P4-07 read-only operator view (`OperatorSafetyView`, `format_safety_verdict`) with the merged P4-06 / P4-07 / P4-08 safety foundation (`RuntimeShell`, `SafetyVerdict`, the five `BLOCKER_*` constants), the merged P3-03 / P3-04 / P3-05 in-memory fakes (`InMemoryHeartbeat`, `InMemoryKillSwitch`, `InMemoryReconciliation`), and the merged P5-01 / P5-02 `SimulationBoundary` (`propose`, `propose_order`) — exercising the real composed pipeline so that operator-view text remains consistent with `SimulationBoundary.propose` and `SimulationBoundary.propose_order` behavior across blocker outcomes. P5-05 opens Phase 5 work only at the tripwire-test scope authorized here; it does **not** open, enumerate, or pre-commit any successor P5 packet (P5-06 and beyond). No `src/**` change. No new public symbol, re-export, field, method, `SimulationLane` member, `SimulatedOrderSide` member, or `SimulatedOrderType` member is introduced. The merged P4-06 / P4-07 / P4-08 safety foundation, the merged P3-03 / P3-04 / P3-05 in-memory fakes, the merged P2-01..P2-05 packages, the merged OPS-01 / OPS-02 / OPS-03 / OPS-04 / OPS-04B / OPS-06 operations records, the merged P5-01 / P5-02 simulation surface, the merged P5-03 invariants tests, the merged P5-04 composed safety-foundation × simulation integration tripwire tests, the canonical status reconciliation merged via PR #114, the Phase 5 entry planning reconciliation merged via PR #118, and the merged GOV-01 governance reconciliation are all preserved exactly. Adds the new tripwire test file, this governance authorization artifact, and conservative governance/status reflections in `MASTER_STATUS.md` and `plan/phase5_entry_plan.md`. Makes no other changes.

## Authorization

Kevin authorized this packet on 2026-05-18. The verbatim written authorization follows:

> I authorize P5-05 for gmc-rebuild on 2026-05-18 as a bounded tests-only Phase 5 tripwire packet.
>
> Scope:
> - Add exactly one new test file:
>   tests/runtime/test_operator_view_composed_safety_foundation.py
> - The test file may exercise the merged P4-07 read-only operator view, including OperatorSafetyView and format_safety_verdict.
> - The test file may compose that operator-view surface with the already-merged P4-06 / P4-07 / P4-08 safety foundation, the already-merged P3-03 / P3-04 / P3-05 in-memory fakes, and the already-merged P5-01 / P5-02 simulation boundary surfaces.
> - The tests should verify that operator-view text remains consistent with SimulationBoundary.propose and SimulationBoundary.propose_order behavior across blocker outcomes.
> - The packet may add the durable authorization artifact under governance/authorizations/.
> - The packet may update MASTER_STATUS.md and plan/phase5_entry_plan.md only as conservative governance/status reflections for P5-05.
>
> Constraints:
> - No src/** changes.
> - No modification of existing tests.
> - No new runtime behavior.
> - No runtime activation.
> - No new SimulationLane member.
> - No new SimulatedOrderSide or SimulatedOrderType member.
> - No new SimulatedOrderIntent field.
> - No new SimulationBoundary method.
> - No new placeholder or order record class.
> - No broker, paper-trading, live-trading, market-data, order-routing, strategy, scheduler, daemon, persistence, deployment, env-var, secrets, network, tag, or release changes.
> - No X10 Layer 5 promotion.
> - No backup-monitoring automation.
> - No DR drill execution.
> - No allowlist expansion.
> - No quality-gate relaxation.
>
> Governance:
> - Mode B sibling monitoring is required and must be authored from main and merged before the P5-05 implementation PR.
> - Mode A adversarial review is required because this is a composed safety/simulation/operator-view tripwire touching execution-adjacent semantics, but Mode A must be delivered as PR-review text only, not committed.
> - The §8 step 4a allowlist must remain exactly at the current ten entries.
> - The packet must preserve the canonical status reconciliation from PR #114 and the Phase 5 entry planning reconciliation from PR #118.
> - P5-05 does not authorize P5-06 or any successor packet.
>
> Authorized by Kevin on 2026-05-18.

This authorization is the authorization of record per `AI_WORKFLOW.md` §7.

## Allowed Scope

The implementation PR may include exactly:

- A new test file at `tests/runtime/test_operator_view_composed_safety_foundation.py` containing focused pytest functions that compose the real merged P4-07 read-only operator view (`OperatorSafetyView`, `format_safety_verdict`, `VERDICT_CLEAR`, `VERDICT_BLOCKED`) with the real merged P4-06 `RuntimeShell` driven by the real merged P3-03 / P3-04 / P3-05 in-memory fakes (`InMemoryHeartbeat`, `InMemoryKillSwitch`, `InMemoryReconciliation`) and the merged P5-01 / P5-02 `SimulationBoundary` (`propose`, `propose_order`). The file:
  - Imports only `__future__`, `ast`, `dataclasses`, `datetime`, `pathlib` (stdlib), and the already-merged in-repo modules `gmc_rebuild.heartbeat` (for `InMemoryHeartbeat`), `gmc_rebuild.kill_switch` (for `InMemoryKillSwitch`), `gmc_rebuild.reconciliation` (for `InMemoryReconciliation`), `gmc_rebuild.risk` (for `HeartbeatStatus`, `KillSwitchState`, `ReconciliationStatus`), `gmc_rebuild.runtime` (for the five `BLOCKER_*` constants, `OperatorSafetyView`, `RuntimeShell`, `SafetyVerdict`, `VERDICT_BLOCKED`, `VERDICT_CLEAR`, `format_safety_verdict`), and `gmc_rebuild.simulation` (for the seven public names already re-exported by P5-02). No other import is added; in particular no `os`, `socket`, `requests`, `urllib`, `http`, `threading`, `asyncio`, `subprocess`, `sqlite3`, `pickle`, `shelve`, `ssl`, `smtplib`, `ftplib`. No `pytest.raises`. No fixture-typed parameters. No third-party dependency.
  - Avoids `pytest.raises` and fixture-typed parameters so the mypy-strict pre-commit hook does not need a pytest-stub dependency, matching the pattern established by `tests/runtime/test_operator_view.py`, `tests/runtime/test_runtime_shell.py`, `tests/simulation/test_simulation_boundary.py`, `tests/simulation/test_simulated_order_intent.py`, `tests/simulation/test_simulated_order_intent_invariants.py`, and `tests/simulation/test_composed_safety_foundation.py`.
  - Implements small local helpers private to the new module; no helper is exported, and no existing test file is modified.
  - Asserts the following invariant categories: (1) on a real-pipeline-clear `SafetyVerdict`, the operator view rendered by `format_safety_verdict(verdict).render()` carries the `CLEAR` label, contains no blockers section, and the same verdict admits `SimulationBoundary.propose` and `SimulationBoundary.propose_order` (both return the supplied intent / order intent by identity); (2) for each of the five `BLOCKER_*` codes driven from the corresponding P3 fake state, the operator view rendered for the resulting blocked verdict carries the `BLOCKED` label and includes the blocker description for that code, while the same verdict causes both `propose` and `propose_order` to raise `SimulationBoundaryError` with the blocker code present in the message — proving operator-view text remains consistent with `SimulationBoundary.propose` / `propose_order` behavior across blocker outcomes; (3) on the multi-blocker real-pipeline case (heartbeat STALE + kill switch TRIPPED + reconciliation FAILED), every blocker code is surfaced in both the rendered operator-view text and both boundary error messages — tripwiring future "summarize blockers" drift symmetrically across the operator view and the simulation boundary; (4) `format_safety_verdict` is a pure read-only formatter — the rendered view does not mutate the underlying P3 fakes, the `SafetyVerdict`, the `SimulatedIntent`, or the `SimulatedOrderIntent`, and the rendered view is deterministic across repeated calls for the same verdict; (5) the operator view's `status` field is `CLEAR` if and only if `SimulationBoundary.propose` and `SimulationBoundary.propose_order` would each return the supplied intent by identity (i.e., the view's accept/reject label is exactly the boundary's accept/reject decision for the same verdict); (6) an inertness self-check using `ast.walk` to confirm the new module's actual imports are disjoint from `{os, socket, requests, urllib, http, threading, asyncio, subprocess, sqlite3, pickle, shelve, ssl, smtplib, ftplib}` and that every imported module starts with one of the authorized prefixes.
- A conservative `MASTER_STATUS.md` §1 paragraph titled "P5-05 implementation status (governance/documentation reflection, 2026-05-18 UTC — pending merge)" describing the new tripwire file and explicitly listing the categories the packet does **not** open. The conservative wording is scoped to this open PR only; the four merged P5-01 / P5-02 / P5-03 / P5-04 status reflection paragraphs continue to read **merged on `main`** and are not reverted to stale `**pending merge**` language by this packet; the Phase 5 entry planning paragraph from PR #118 is preserved unchanged.
- A conservative `plan/phase5_entry_plan.md` update reflecting the P5-05 authorization at the planning level, scoped only to the open P5-05 PR. The plan continues to record P5-01 / P5-02 / P5-03 / P5-04 as merged on `main` and the Phase 5 entry planning packet as merged via PR #116.
- This authorization artifact at `governance/authorizations/2026-05-18_p5-05.md`.

No file under `src/**` is added or modified. No new test directory is added; the new test file lives in the existing `tests/runtime/` directory. No existing test file under `tests/**` is modified. No file under `monitoring/**`, `docs/decisions/`, or `governance/authorizations/` other than this new P5-05 artifact is added or modified. The merged P4-06 `RuntimeShell` / `SafetyVerdict`, P4-07 `OperatorSafetyView` / `format_safety_verdict`, P4-08 safety-policy-hardening tests, P3-03 / P3-04 / P3-05 in-memory fakes, P2-01..P2-05 packages, OPS-01 / OPS-02 / OPS-03 / OPS-04 / OPS-04B operations records, OPS-06 backup monitoring plan, P5-01 inert local simulation boundary skeleton, P5-02 simulated order intent model, P5-03 simulated-order-intent invariants tripwire tests, P5-04 composed safety-foundation × simulation integration tripwire tests, the canonical status reconciliation merged via PR #114, the Phase 5 entry planning reconciliation merged via PR #118, and the GOV-01 governance reconciliation / risk register are all preserved exactly. The `MASTER_STATUS.md` §8 step 4a `allowed_p2_infra` bash gate (synced to ten entries by GOV-01) is preserved exactly; no new `src/gmc_rebuild/` subpackage directory is created.

## Explicitly Not Authorized

This authorization does not permit:

- Any `src/**` change. The packet adds no module under `src/gmc_rebuild/runtime/`, `src/gmc_rebuild/simulation/`, or anywhere else under `src/**`; no existing `src/**` file is modified.
- Any new public symbol on `gmc_rebuild.runtime`, `gmc_rebuild.simulation`, or any other subpackage; no new re-export from `gmc_rebuild` package root.
- Any modification of any existing test file under `tests/**` (including `tests/runtime/test_operator_view.py`, `tests/runtime/test_runtime_shell.py`, `tests/runtime/test_safety_policy_hardening.py`, `tests/simulation/test_simulation_boundary.py`, `tests/simulation/test_simulated_order_intent.py`, `tests/simulation/test_simulated_order_intent_invariants.py`, and `tests/simulation/test_composed_safety_foundation.py`).
- Any new field on `SimulatedIntent` or `SimulatedOrderIntent`. The eight-field structural tripwires from P5-02 / P5-03 / P5-04 continue to be enforced at the test level and are not weakened.
- Any new method on `SimulationBoundary`. The existing `propose` (P5-01) and `propose_order` (P5-02) are exercised unchanged.
- Any new placeholder or order record class. The existing `SimulatedIntent` (P5-01) and `SimulatedOrderIntent` (P5-02) are exercised unchanged.
- Any addition of a `SimulationLane` member beyond `LOCAL_ONLY`. The closed-enum tripwire from P5-01 is preserved.
- Any addition of a third `SimulatedOrderSide` member (no `SELL_SHORT`, `BUY_TO_COVER`, etc.). The closed-enum tripwire from P5-02 is preserved.
- Any addition of a third `SimulatedOrderType` member (no `STOP`, `STOP_LIMIT`, `MARKET_ON_CLOSE`, `LIMIT_ON_OPEN`, `TRAILING_STOP`, etc.). The closed-enum tripwire from P5-02 is preserved.
- Any new runtime behavior. The packet adds tests only; it does not introduce any runtime behavior, any concrete `HeartbeatProtocol` / `KillSwitchProtocol` / `ReconciliationProtocol` implementation outside the merged P3-03 / P3-04 / P3-05 in-memory fakes, any concrete safety-control implementation outside the merged P4-06 `RuntimeShell`, or any new `format_safety_verdict` / `OperatorSafetyView` capability outside the merged P4-07 surface.
- Any runtime activation of any merged Phase 3 fixture or P4-06 / P4-07 / P4-08 safety surface or P5-01 / P5-02 simulation surface. Construction and exercise of the merged surfaces within pytest is **not** a runtime activation; the new test module does not run as a daemon, scheduler, background thread, or `__main__` entry point.
- Any broker integration (real or paper), broker SDK, broker account identifier, broker session, broker handshake, or broker credential of any kind.
- Any live, delayed, or paper market data, market-data feed, vendor SDK, real symbol universe, real venue identifier, or normalization to any vendor symbol format. The new test file uses inert placeholder symbols only; it does not import any market-data fixture, vendor SDK, or symbol database.
- Any real or paper order placement, order routing, order management, order book, fill, execution, position, P&L, trade report, OMS, EMS, or routing instruction. The tripwire tests assert that `propose` / `propose_order` continues to return the supplied intent unchanged (by identity) on a clear verdict and raises `SimulationBoundaryError` on a blocked verdict; no side effect is exercised or authorized.
- Any execution adapter, broker adapter, paper-broker adapter, mock-broker adapter, exchange adapter, FIX session, REST client, WebSocket client, or any other connectivity surface.
- Any venue, account, broker credential, or routing field on `SimulatedOrderIntent` (the structural-field tripwire is preserved).
- Any secrets, credentials, API keys, broker keys, session tokens, OAuth tokens, SSH keys, `.env` files containing live credentials, recovery codes, PEKs, or any other credential storage.
- Any external API call, network call, `socket`, `urllib`, `requests`, `http`, `ssl`, `smtplib`, `ftplib`, DNS lookup, or any other outbound or inbound network surface.
- Any scheduler, daemon, background thread, long-running service, `__main__` entry point, `if __name__` block, `time.sleep`, `asyncio.sleep`, cron job, launchd plist, or any other timed / background execution mechanism.
- Any persistence, database, SQLite, pickle, shelve, filesystem write, caching to disk, append-only ledger, log file, audit trail, or any other persistence surface.
- Any strategy, scanner, model, portfolio, backtest, live trading, paper trading wired to a real broker, or production execution implementation.
- Any modification of the merged P4-06 `RuntimeShell` / `SafetyVerdict`, P4-07 `OperatorSafetyView` / `format_safety_verdict`, or P4-08 safety-policy-hardening tests. The safety foundation closure recorded by P4-08 is preserved unchanged; P5-05 reuses the existing `SafetyVerdict.clear` invariant and the existing P4-07 read-only operator view via the real composed pipeline without weakening, replacing, or bypassing them.
- Any modification of the merged P3-03 / P3-04 / P3-05 in-memory fakes. The packet imports and exercises the fakes via their already-public read methods and the documented `advance` / `beat` / `trip` / `enqueue` / `set_next` test helpers; it does not modify the fakes' source.
- Any modification of the merged P5-01 or P5-02 simulation source, the merged P5-03 invariants tests, or the merged P5-04 composed safety-foundation × simulation integration tripwire tests. No existing `src/gmc_rebuild/simulation/` file is modified; no existing test under `tests/simulation/` is modified.
- Any concrete implementation of `HeartbeatProtocol`, `KillSwitchProtocol`, or `ReconciliationProtocol` outside the merged P3-03 / P3-04 / P3-05 in-memory fakes.
- Any modification of backup / recovery docs (`RECOVERY.md`, `governance/authorizations/2026-05-16_ops-*.md`, `governance/authorizations/2026-05-17_ops-04b.md`, `governance/authorizations/2026-05-17_ops-06.md`, `governance/authorizations/2026-05-17_gov-01.md`). The OPS-01 / OPS-02 / OPS-03 / OPS-04 / OPS-04B / OPS-06 / GOV-01 records are preserved unchanged.
- Any modification of `AI_WORKFLOW.md`, any ADR under `docs/decisions/`, any other `governance/authorizations/*.md` file, `pyproject.toml`, `.pre-commit-config.yaml`, `.secrets.baseline`, `.gitignore`, `README.md`, or `EXTERNAL_REVIEW_BRIEF.md`.
- Any modification of any existing `monitoring/daily/*` packet.
- Any extension of the `MASTER_STATUS.md` §8 step 4a `allowed_p2_infra` bash gate or §8 step 4c forbidden-token bash gate. No new `src/gmc_rebuild/` subpackage directory is added; the bash gate is preserved exactly at the ten entries synced by GOV-01.
- Any extension of the hard-coded `entries == {...}` set in `tests/test_package_skeleton.py`. The set is unchanged by this packet because no new subpackage directory is added.
- Any quality-gate relaxation, hook removal, hook reconfiguration, mypy strictness reduction, ruff rule relaxation, or `detect-secrets` baseline change.
- Any X10 Layer 5 promotion, backup-monitoring automation, DR drill execution, or other ops execution work.
- Any tag, GitHub release, or version bump.
- Any successor P5 packet (P5-06 and beyond, additional simulation lanes, paper-broker integration, live integration, OMS / EMS work, market-data integration, persistence integration, scheduler / daemon, alerting, monitoring automation, …). Each future packet continues to require its own separate written authorization from Kevin per `AI_WORKFLOW.md` §7.
- Any Mode A or Mode B sequencing exception. Mode A adversarial review (high-risk architecture decision — composed safety/simulation/operator-view tripwire touching execution-adjacent semantics, exercising the real `RuntimeShell` pipeline, the real P4-07 operator view, and the real P5-01 / P5-02 `SimulationBoundary` end-to-end) is delivered as PR-review text and not committed to the repository per `AI_WORKFLOW.md` §6 rule 5. Mode B sibling monitoring follows ADR-008 §D3 / §D5 (and ADR-009 D3 / D5 once in force per ADR-009 D7's bootstrap-avoidance clause) and must be authored from `main` and merged to `main` before the P5-05 implementation PR per the verbatim authorization above.

Phase 2 remains formally closed. Phase 3 remains formally closed. Phase 4 remains open as a governance state. P5 is open at the P5-01 inert local simulation boundary skeleton scope, the P5-02 simulated order intent model scope, the P5-03 simulated-order-intent invariants tripwire scope, the P5-04 composed safety-foundation × simulation integration tripwire scope, and now the P5-05 operator-view × composed safety-foundation tripwire scope. The safety foundation closure recorded by P4-08 is preserved.

## Required Validation

Before merge, the implementation PR must pass:

- `git switch main && git pull --ff-only` before branching (to confirm the work is built on current `main`).
- Targeted `pytest tests/runtime/ tests/simulation/ -v` against the existing operator-view, runtime-shell, safety-policy-hardening, and simulation tests and the new P5-05 operator-view × composed safety-foundation tests.
- `source .venv/bin/activate && python -m pre_commit run --all-files` from the active `.venv` if available.
- Diff-scope verification showing changes confined to `tests/runtime/test_operator_view_composed_safety_foundation.py` (new), conservative status reflections in `MASTER_STATUS.md` §1 and `plan/phase5_entry_plan.md`, and this authorization artifact. No file under `src/**` is added or modified; no other file under `tests/**` is added or modified.

Mode A / Mode B review requirements remain governed by `AI_WORKFLOW.md`, ADR-008, ADR-009, and the current `MASTER_STATUS.md`. P5-05 is a high-risk architecture decision (subsequent concrete Phase 5 behaviour — even at the tripwire-test-only scope — that additionally crosses both the P4 boundary by exercising the real `RuntimeShell` pipeline and the real P4-07 operator view, and the P5 boundary by exercising the real `SimulationBoundary.propose` and `SimulationBoundary.propose_order`) per `AI_WORKFLOW.md` §4(2); Mode A adversarial review is delivered as PR-review text and not committed per `AI_WORKFLOW.md` §6 rule 5. Mode B sibling monitoring follows ADR-008 §D3 / §D5 (and ADR-009 D3 / D5 once in force per ADR-009 D7's bootstrap-avoidance clause) and must be authored from `main` and merged to `main` before the P5-05 implementation PR per the verbatim authorization above.
