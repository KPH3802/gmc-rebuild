# OPS-04B Authorization — Memory-Pressure Measurement Record

Date: 2026-05-17
Authorizer: Kevin Heaney
Scope: Bounded, docs-only operations packet for `gmc-rebuild` recording the completed OPS-04 memory-pressure measurement cycle performed on 2026-05-17. Adds a dated `RECOVERY.md` §5.6 "Recorded Investigations" entry titled "2026-05-17 — Mac Studio (Layer 2), OPS-04 memory-pressure measurement cycle" that records the BEFORE / DURING_PEAK / AFTER snapshots taken per `RECOVERY.md` §14.2, summary statistics from those snapshots (swap stayed at 0 MB across all three samples; compressor occupancy decreased from 8,719 MB to 5,737 MB across the cycle; no new `JetsamEvent`, hang, or panic artifacts appeared in the prior 24 hours), the §14.3 escalation-criteria evaluation (all five criteria not tripped), the resulting `RECOVERY.md` §14.4 P5 gate satisfaction status, and the explicit non-claims about what one measurement cycle does and does not prove. Refines the existing `RECOVERY.md` §10 memory-pressure unresolved-risk bullet to reflect that the baseline is now established as of 2026-05-17. Adds a "Status as of 2026-05-17" satisfaction note to `RECOVERY.md` §14.4. Adds one entry to `RECOVERY.md` §12 References for this authorization artifact. Adds this governance authorization artifact. Makes no other changes.

## Authorization

Kevin authorized this packet on 2026-05-17. The verbatim written authorization follows:

> OPS-04B Authorization — memory-pressure measurement record — 2026-05-17
>
> I, Kevin Heaney, authorize one bounded docs-only OPS-04B packet for the gmc-rebuild repository to record the completed OPS-04 memory-pressure measurement cycle performed on 2026-05-17.
>
> Authorized scope:
> - Add a dated §5.6 Recorded Investigations entry for the 2026-05-17 OPS-04 memory-pressure measurement cycle.
> - Record that BEFORE, DURING_PEAK, and AFTER snapshots were captured.
> - Record that swap stayed at 0 MB and swapins/swapouts stayed 0/0 across all snapshots.
> - Record that no new JetsamEvents, UI hangs, panics, or ungraceful reboots appeared in the last 24h.
> - Record that the compressor decreased from 8,719 MB before workload to 5,737 MB after workload.
> - Record that pre-commit briefly raised CPU load but did not create memory pressure.
> - Record that all OPS-04 §14.3 escalation signals passed.
> - Record that the OPS-04 §14.4 P5 gate is satisfied as of 2026-05-17.
> - Add governance/authorizations/2026-05-17_ops-04b.md if consistent with repo conventions.
> - Update only RECOVERY.md and the authorization artifact unless a repository convention requires otherwise.
>
> Hard constraints:
> - Docs-only.
> - No src/** changes.
> - No tests/source changes.
> - No system changes.
> - No backup, drive, Backblaze, Time Machine, or network changes.
> - No P5 implementation.
> - Do not commit raw snapshots, logs, screenshots, process lists, or measurement artifacts.
> - Do not modify MASTER_STATUS.md or plan/phase4_entry_plan.md unless existing repo conventions clearly require it.
>
> Validation required before PR:
> - git switch main
> - git pull --ff-only
> - source .venv/bin/activate
> - python -m pre_commit run --all-files
>
> This authorization is only for the bounded OPS-04B memory-pressure measurement record packet and does not authorize any successor packet.

This authorization is the authorization of record per `AI_WORKFLOW.md` §7.

## Allowed Scope

The implementation PR may include exactly:

- An update to `RECOVERY.md` adding a new dated entry under the existing §5.6 "Recorded Investigations" subsection (parallel to the existing 2026-05-16 X10 Pro panic-history investigation entry). The new entry is titled "2026-05-17 — Mac Studio (Layer 2), OPS-04 memory-pressure measurement cycle" and documents:
  - The layer and device examined (Mac Studio, Layer 2 in §3.2).
  - The trigger (the OPS-04 §14 procedure authorized by `governance/authorizations/2026-05-16_ops-04.md` requires at least one before-heavy-work snapshot and one during-heavy-work snapshot before §14.4 can be satisfied; the OPS-03 finding earlier in §5.6 had surfaced a stronger evidence pattern of memory pressure plus a UI hang than of X10 Pro storage fault).
  - The read-only method (`uptime`, `memory_pressure`, `vm_stat`, `sysctl vm.swapusage`, `top -l 1 -n 15 -o mem`, `last reboot`, plus a 24-hour incident scan of `/Library/Logs/DiagnosticReports`; the validation workload was `python -m pre_commit run --all-files` from the project's pinned Python 3.12 `.venv`; raw snapshot output, process lists, and screenshots are stored locally on the operator's hardware and explicitly **not** committed to this repository per §14.2.4 and §7).
  - The findings as summary statistics only (continuous boot session since 2026-05-13 19:43 CDT with uptime 3 days 8 hours 38 minutes at BEFORE; swap at 0 MB and zero swap activity across all three samples; compressor occupancy decreased 8,719 → 7,931 → 5,737 MB across BEFORE / DURING_PEAK / AFTER; pages-stored-in-compressor fell from 1,155,124 to 927,610; lifetime compressions rose by ~41,571 pages during the workload and then stopped; lifetime decompressions rose by ~148,928 pages during the AFTER window; physical-memory unused grew from 1,357 MB BEFORE to 5,052 MB DURING_PEAK and 4,953 MB AFTER; CPU idle dropped from 68% to 8.5% during the workload and recovered to 73% after; pre-commit completed in approximately 3 seconds wall-clock against the pre-warmed `~/.cache/pre-commit/` cache with thirteen concurrent `detect-secrets-hook` Python workers; zero new `JetsamEvent`, hang, or panic artifacts in the prior 24 hours).
  - The §14.3 escalation-criteria check (each of the five criteria evaluated against the snapshot evidence and explicitly labelled "not tripped").
  - The conclusion (the Mac Studio handled the validation workload elastically; running pre-commit reduced rather than increased compressor pressure).
  - The resulting state (the OPS-04 §14.4 P5 gate is satisfied as of 2026-05-17; the baseline is a point-in-time record per §14.2.5 and must be re-established after any material change in routinely-running apps, any macOS major upgrade, any new external device connection, or any future ungraceful reboot; satisfying §14.4 alone does not authorize P5).
  - The authorization of record (`governance/authorizations/2026-05-17_ops-04b.md`).
  - The standard "what this entry proves" / "what this entry does not prove" framing consistent with the existing §5.5 drill record and the existing 2026-05-16 §5.6 investigation entry.
- An update to `RECOVERY.md` §10 Unresolved Risks refining the existing "Mac Studio memory-pressure baseline is not yet established" bullet to record that the baseline is now established as of 2026-05-17 and that none of the five §14.3 escalation signals were tripped at the time of the measurement. The bullet preserves the §14.2.5 re-baseline cadence and explicitly states that satisfying §14.4 alone does not authorize P5.
- An update to `RECOVERY.md` §14.4 P5 gate adding a "Status as of 2026-05-17" paragraph that records §14.4's "Completed" condition as satisfied by the new §5.6 entry under this authorization, while preserving the re-baseline clause and the preserved-additive-gate language.
- An update to `RECOVERY.md` §12 References adding one bullet pointing at `governance/authorizations/2026-05-17_ops-04b.md`.
- This authorization artifact at `governance/authorizations/2026-05-17_ops-04b.md`.

No updates to `MASTER_STATUS.md`, `plan/phase4_entry_plan.md`, or any other status document are made by this packet. The header text of `MASTER_STATUS.md` for the current work packet (OPS-01) is preserved unchanged because OPS-04B, like OPS-02 / OPS-03 / OPS-04, is a narrower operations record and does not supersede or restate OPS-01.

The `RECOVERY.md` updates and the new authorization artifact are documentation only. They contain no executable code, no secrets, no credentials, no broker / market-data / order / trading runtime content, and no scheduler / daemon / network instructions. Raw snapshot output from the OPS-04 measurement cycle (full `top` output, full `vm_stat` dumps, full process lists, screenshots, log dumps) is explicitly **not** included in this packet; per §14.2.4 and §7 those artifacts are operator-side records stored locally on the operator's own hardware and have no place in this repository. The §5.6 entry records only the summary statistics necessary to evaluate the §14.3 escalation criteria and the §14.4 P5 gate condition.

## Explicitly Not Authorized

This authorization does not permit:

- Any change under `src/**`.
- Any change to existing test behavior. No existing repository governance test requires listing this measurement record; therefore no test file is modified by this packet.
- Any commit of raw snapshot output, process lists, screenshots, log dumps, or any other operator-side measurement artifact. Only summary statistics necessary to the §14.3 / §14.4 evaluation are recorded; the underlying raw data stays on the operator's hardware per §14.2.4 / §7.
- Any system-setting change, including but not limited to swap configuration, energy / sleep configuration, memory compression configuration, app launch settings, login items, kernel boot arguments, or any other macOS configuration surface.
- Any app killing, force-quit, or process termination.
- Any "memory cleaner" tool, RAM-purge tool, swap-cleaner tool, or any other automated memory-cleanup utility.
- Any OS tuning, performance tuning, scheduler tuning, or thermal-policy change.
- Any hardware decision (replace, upgrade, downgrade, return, repair, RMA) made from a single measurement cycle.
- Any backup execution, drive repair, disk formatting, disk repartitioning, eject / unmount, mount, First Aid, Time Machine setting change, Backblaze setting change (including any change to which volumes are selected, the schedule, the throttle, the threads, the exclusion rules, or the encryption key configuration), cloud-mirror creation, branch deletion, force-push, or any other GitHub destructive action.
- Any Phase 5 implementation work, local/paper simulation boundary planning, broker integration, market-data ingestion, strategy / scanner / model / portfolio / backtest content, persistent storage, deployment, env-var or secrets loading, network call, `time.sleep`, `asyncio.sleep`, or trading runtime activation. Satisfying the §14.4 P5 gate by this packet does **not** authorize P5; it only removes one procedural blocker among many.
- Any concrete implementation of `HeartbeatProtocol`, `KillSwitchProtocol`, or `ReconciliationProtocol` outside the merged P3-03 / P3-04 / P3-05 in-memory fakes.
- Any modification of the merged P4-06 `RuntimeShell` / `SafetyVerdict` types, the merged P4-07 `OperatorSafetyView` / `format_safety_verdict` formatter, or the P4-08 safety-policy-hardening tests.
- Any extension of the `MASTER_STATUS.md` §8 step 4a `allowed_p2_infra` allowlist.
- Any quality-gate relaxation, tag, or release.
- Any successor packet (OPS-05, OPS-06, OPS-07, P4-09, P5-01, local/paper simulation boundary planning, additional OPS-04 measurement cycles or re-baseline records, …). The safety foundation closure recorded by P4-08 is preserved unchanged. The OPS-01 project resilience checkpoint (`governance/authorizations/2026-05-16_ops-01.md` and `RECOVERY.md`), the OPS-02 Backblaze restore-drill record (`governance/authorizations/2026-05-16_ops-02.md`), the OPS-03 X10 Pro panic-investigation record (`governance/authorizations/2026-05-16_ops-03.md`), and the OPS-04 memory-pressure baseline / OPS roadmap (`governance/authorizations/2026-05-16_ops-04.md`) are preserved unchanged in scope. Each future packet (including any future re-baseline measurement cycle triggered by §14.2.5) continues to require its own separate written authorization from Kevin.

Phase 2 remains formally closed. Phase 3 remains formally closed. Phase 4 remains open as a governance state. The safety foundation closure recorded by P4-08 is preserved. OPS-04B is an out-of-band, docs-only operations record that captures one completed OPS-04 measurement cycle and its §14.4 gate satisfaction; it is **not** a Phase 4 implementation task, **not** a Phase 5 task, and **not** an enumeration or opening of any successor task.

## Required Validation

Before merge, the implementation PR must pass:

- `git switch main && git pull --ff-only` before branching (to confirm the work is built on current `main`).
- `source .venv/bin/activate && python -m pre_commit run --all-files` from the active `.venv`.
- Diff-scope verification showing changes confined to `RECOVERY.md` and this authorization artifact, with no changes to `MASTER_STATUS.md`, `plan/phase4_entry_plan.md`, or any file under `src/**` or `tests/**`.

Mode A / Mode B review requirements remain governed by `AI_WORKFLOW.md`, ADR-008, ADR-009, and the current `MASTER_STATUS.md`. This packet is a routine documentation edit per `AI_WORKFLOW.md` §4's routine-exclusion sentence and does not independently mandate Mode A; maintainer-elected Mode A, if conducted, is recorded as PR-review text and not committed per `AI_WORKFLOW.md` §6 rule 5.
