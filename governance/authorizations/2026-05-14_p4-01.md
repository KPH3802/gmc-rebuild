# P4-01 Authorization — Phase 4 Entry Gate (Governance-Only Phase 4 Entry)

Date: 2026-05-14
Authorizer: Kevin
Scope: P4-01 — the **Phase 4 entry gate**, governance-only. **Opens Phase 4 as a governance state only.** Does **not** authorize any Phase 4 implementation, does **not** authorize any runtime activation of any merged Phase 3 fixture (`InMemoryHeartbeat`, `InMemoryKillSwitch`, `InMemoryReconciliation`), does **not** authorize any broker / market-data / order / strategy / scheduler / persistence / deployment / env-var / secrets / network change, does **not** extend the `MASTER_STATUS.md` §8 step 4a `allowed_p2_infra` allowlist, does **not** add or modify any file under `src/**` or `tests/**`, and does **not** create any tag or release.

## Verbatim Written Authorization From Kevin (Authorization of Record)

The text in quotation marks immediately below is the verbatim written authorization Kevin provided on 2026-05-14 to open Phase 4 entry as a formal phase-gate workstream limited to governance-only Phase 4 entry. It is reproduced verbatim in this artifact, in its entirety, as the durable in-tree authorization of record per `AI_WORKFLOW.md` §7 ("durable authorization artifacts") and `MASTER_STATUS.md` §7 ("Phase 2 Boundary" / written-authorization requirement extended by precedent to every later phase gate). Nothing in this authorization artifact may be read as relaxing, broadening, or substituting any constraint stated in the verbatim text below:

> "I, Kevin Heaney, authorize opening Phase 4 entry planning into a formal Phase 4 entry gate workstream. This authorization is limited to governance-only Phase 4 entry. It does not authorize Phase 4 implementation, runtime activation, broker, market-data, order, strategy, scheduler, persistence, deployment, secrets, network, env-var, src/**, or tests/** changes. I understand that Phase 4 entry requires the repository's required review and monitoring protocol, including Mode A where required and a sibling Mode B packet before merge."

The verbatim authorization above is the authoritative source for the scope, the explicit non-goals, and the required review and monitoring protocol for this PR and for the Phase 4 entry gate workstream. Where this artifact restates that scope or those non-goals in prose, the restatement is for audit clarity and is **not** a re-authorization, an expansion, or a relaxation of any constraint stated in the verbatim text. If a conflict appears between the verbatim text and any restatement in this artifact, the verbatim text wins.

## Authorization (Restated)

This artifact, anchored by the verbatim authorization above, opens **P4-01** as the **Phase 4 entry gate** — the phase-gate decision named in `plan/phase4_entry_plan.md` §4 item 1 ("PR P4-01 — (future / not authorized) Phase 4 entry decision (phase-gate PR)") that was identified there as "**Not authorized**" at the time `plan/phase4_entry_plan.md` was merged on 2026-05-14 via PR #60. This artifact is the separate written authorization that promotes P4-01 from "future / not authorized" to "authorized, governance-only" while preserving every non-goal that `governance/authorizations/2026-05-14_phase-4-entry-planning.md` and `plan/phase4_entry_plan.md` set out, and while preserving every always-forbidden category in `MASTER_STATUS.md` §6.

This authorization is the **phase-gate analogue** to the Phase 3 entry decision that was opened under `governance/authorizations/2026-05-13_p3-02.md`. It is intentionally narrower than the broader Phase 3 entry decision: it **opens Phase 4 as a governance state only**, it does **not** name or authorize any Phase 4 implementation task (`P4-02`, `P4-03`, …), it does **not** extend the `MASTER_STATUS.md` §8 step 4a `allowed_p2_infra` allowlist, it does **not** add any directory or path to the working tree under `src/**` or `tests/**`, and it does **not** open or pre-approve any runtime activation of any merged Phase 3 fixture.

The Phase 4 entry planning authorization at `governance/authorizations/2026-05-14_phase-4-entry-planning.md` and the Phase 4 entry plan at `plan/phase4_entry_plan.md`, both merged on `main` on 2026-05-14 via PR #60 at `e1dd6c0` (with sibling Mode B packet PR #61 merged first at `8e5b420` and post-merge status reconciliation PR #62 at `4f03c57` with sibling Mode B packet PR #63 merged first at `230124c`), explicitly stated that Phase 4 entry planning did **not** open Phase 4 and named the eventual Phase 4 entry decision as candidate task P4-01 requiring its own separate written authorization, Mode A adversarial review per `AI_WORKFLOW.md` §4(1), and a sibling Mode B monitoring packet per ADR-008 §D3 / §D5. This artifact is that separate written authorization, restricted to governance-only Phase 4 entry.

## Scope

The work authorized by this artifact is restricted to:

- This authorization artifact at `governance/authorizations/2026-05-14_p4-01.md`.
- Minimal governance-prose cross-reference updates to `MASTER_STATUS.md` and `README.md` that record that the Phase 4 entry gate has been authorized as governance-only Phase 4 entry. The updates name this artifact, do not relax any boundary, do not change the §8 step 4a allowlist, do not declare Phase 4 implementation open, and do not authorize any runtime activation of any merged Phase 3 fixture. Per `AI_WORKFLOW.md` §1.2 and §6 rule 2 ("One status keeper"), any `MASTER_STATUS.md` edit in the authorizing PR is conservative and remains subject to Perplexity Computer's verification before being treated as the canonical status; the canonical post-merge status reconciliation may be conducted as a separate post-merge PR on the same pattern as PR #58 (Phase 3 closure reconciliation) and PR #62 (Phase 4 entry planning reconciliation).
- Minimal cross-reference update to `plan/phase4_entry_plan.md` §1 ("Current Status") and §4 ("Candidate Future Phase 4 Task Sequence") item 1, recording that P4-01 has been authorized as the Phase 4 entry gate under this artifact. The cross-reference does **not** modify §2 (Phase 4 objective), §3 (allowed planning topics), §5 (entry criteria), §6 (proof bundle expectations), §7 (review protocol), §8 (rollback / stop conditions), §9 (ADR follow-ups), §10 (explicitly not authorized), or §11 (supporting references). The candidate-task list in §4 continues to **not enumerate** P4-02 or any successor; per `plan/phase4_entry_plan.md` §4, future enumeration beyond P4-01 must itself be authorized as a documentation-only update.

This artifact does **not** open the planning document at `plan/phase4_entry_plan.md` to authorize any of its enumerated criteria as implementation: §5 (entry criteria) is restated as planning-level only and is not promoted to a binding implementation precondition by this PR; §6 (proof bundle expectations) is restated as planning-level only and is not made operative by this PR; §9 (ADR follow-ups) is restated as planning-level only and authorizes no follow-up by this PR.

## What This Authorization Does Open

This authorization opens **Phase 4 as a governance state only**, on the same pattern that `governance/authorizations/2026-05-13_p3-02.md` opened Phase 3 as a governance state only. Concretely, opening Phase 4 as a governance state means and means only the following:

1. The phase-gate decision recorded in `plan/phase4_entry_plan.md` §4 item 1 has been **made** in writing by Kevin: P4-01 is no longer "future / not authorized" and Phase 4 is no longer in the closed-after-Phase-3-closure state recorded by `governance/authorizations/2026-05-14_phase-3-closure.md`.
2. Future Phase 4 implementation tasks (`P4-02`, `P4-03`, …) may now be **proposed in writing** to Kevin and authorized one at a time on the same per-PR pattern that `plan/phase4_entry_plan.md` §4 names. Naming a future task in any later planning prose still does **not** authorize it.
3. `MASTER_STATUS.md` may record that Phase 4 has been opened as a governance state, with explicit non-goals preserved verbatim.

That is the complete content of "Phase 4 governance state opened." It is exhaustive. Nothing else is changed by this authorization. In particular, opening Phase 4 as a governance state does **not** by itself:

- Authorize any Phase 4 implementation task.
- Authorize any runtime activation of any merged Phase 3 fixture.
- Extend the `MASTER_STATUS.md` §8 step 4a `allowed_p2_infra` allowlist.
- Add any directory or path under `src/**` or `tests/**`.
- Relax any quality gate.
- Authorize any broker, market-data, order, strategy, scheduler, persistence, deployment, env-var, secrets, network, or concrete-risk implementation change.
- Create any git tag, GitHub release, or version bump.

## Explicitly Not Authorized

The authorization above does not permit, by implication or otherwise, any of the following. Each remains forbidden until Kevin records a separate written authorization per `MASTER_STATUS.md` §7 and `AI_WORKFLOW.md` §6 rule 3 ("One approver") / rule 7 ("No phase drift"), with a sibling artifact under `governance/authorizations/` and (where it introduces a new directory) a corresponding update to the §8 step 4a allowlist in the same PR that introduces the directory:

- **Phase 4 implementation of any kind.** No Phase 4 implementation task is opened. No Phase 4 directory is created. No `src/**` or `tests/**` file is added under any Phase 4 name. The verbatim authorization above explicitly excludes "Phase 4 implementation."
- **Runtime activation of any merged Phase 3 fixture.** The `InMemoryHeartbeat` (P3-03), `InMemoryKillSwitch` (P3-04), and `InMemoryReconciliation` (P3-05) fakes remain test-fixture infrastructure only. No re-export from `src/gmc_rebuild/__init__.py`, no consumption from a `__main__`, no consumption from a daemon, no consumption from a scheduler, no consumption from any runtime path is authorized. The verbatim authorization above explicitly excludes "runtime activation."
- **Any specific future Phase 4 task (P4-02, P4-03, …).** Each requires its own separate written authorization, a sibling artifact under `governance/authorizations/`, applicable Mode A / Mode B review per `AI_WORKFLOW.md` §4 and ADR-008 / ADR-009, and (where it introduces a new directory) a §8 step 4a allowlist update in the same PR that introduces the directory.
- **Any new Phase 3 task beyond P3-05.** The three in-memory protocol-conformance test fixtures (P3-03 HeartbeatProtocol, P3-04 KillSwitchProtocol, P3-05 ReconciliationProtocol) are exhausted; Phase 3 is formally closed. No fourth protocol fixture, no P3-06, and no sibling Phase 3 task is opened by this Phase 4 entry gate.
- **Any new Phase 2 task beyond P2-05.** The `plan/phase2_entry_plan.md` §4 P2-01..P2-05 sequence is exhausted and Phase 2 is formally closed.
- **Runtime work of any kind.** No `__main__` entry point, no daemon, no scheduler, no background worker, no long-running service, no live execution loop. The verbatim authorization above explicitly excludes "runtime activation," "scheduler," "persistence," and "deployment."
- **Broker integration.** No broker SDK, no broker connector, no broker reconciliation runtime, no broker authentication, no broker credentials. The verbatim authorization above explicitly excludes "broker."
- **Market-data integration.** No market-data ingestion code, no market-data pipeline, no committed dataset, no live or historical data feed. The verbatim authorization above explicitly excludes "market-data."
- **Order management.** No order objects, no order placement, no position management, no fills, no trade reports, no executions, no order book. The verbatim authorization above explicitly excludes "order."
- **Strategy logic.** No trading signals, no scanners, no models, no portfolio rules, no backtests. The verbatim authorization above explicitly excludes "strategy."
- **Scheduling.** No cron-style scheduler, no APScheduler, no background-job framework, no operator-heartbeat daemon, no kill-switch runtime, no reconciliation runtime. The verbatim authorization above explicitly excludes "scheduler."
- **Persistence.** No SQLite, no on-disk database, no on-disk reconciliation snapshot, no on-disk heartbeat state, no log sink, no file artifact written by runtime code. The verbatim authorization above explicitly excludes "persistence."
- **Deployment.** No deployment workflow, no rollout, no rollback runtime, no CI/CD pipeline beyond the existing local pre-commit and pytest gates. The verbatim authorization above explicitly excludes "deployment."
- **Secrets / env-var loading.** No real secrets, no real credentials, no `.env` files, no `os.environ` / `os.getenv` reads, no real account identifiers, no real broker / venue identifiers, no real endpoints embedded anywhere in the repository under this authorization. ADR-001's secrets-management discipline is not relaxed. The verbatim authorization above explicitly excludes "secrets" and "env-var."
- **Network.** No `socket`, no `urllib`, no `requests`, no HTTP client, no outbound or inbound network code. The verbatim authorization above explicitly excludes "network."
- **Concrete risk implementations inside the runtime package.** No concrete implementation of `KillSwitchProtocol` / `ReconciliationProtocol` / `HeartbeatProtocol` lands inside `src/gmc_rebuild/risk/` under this authorization. The P2-05 boundary (`src/gmc_rebuild/risk/` is types and abstract `typing.Protocol` definitions only) is preserved, and the three merged P3-03 / P3-04 / P3-05 fakes remain test-fixture-only outside `src/gmc_rebuild/risk/`.
- **New trading behavior.** No change that introduces, simulates, or wires up trading behavior — in any module, under any name, anywhere in the tree.
- **Tooling relaxation.** No weakening of pre-commit, Ruff, mypy strict mode, `detect-secrets`, `.gitignore`, `.secrets.baseline`, the `MASTER_STATUS.md` §8 step 4 always-forbidden scan, the §8 step 4a per-PR Phase 2 / Phase 3 infrastructure allowlist scan, or the §8 step 4c recursive forbidden-token scan.
- **§8 step 4a allowlist expansion.** The `allowed_p2_infra` allowlist is **not** extended by this authorization. It continues to contain exactly the eight entries currently on `main` after the merged P2-01..P2-05 + P3-03 + P3-04 + P3-05 sequence (`src`, `src/gmc_rebuild/config`, `src/gmc_rebuild/time`, `src/gmc_rebuild/logging`, `src/gmc_rebuild/risk`, `src/gmc_rebuild/heartbeat`, `src/gmc_rebuild/kill_switch`, `src/gmc_rebuild/reconciliation`). Any Phase 4 directory, including the eventual on-disk location of any Phase 4 module, requires a separate written authorization that introduces the directory in the same PR that adds it to the allowlist (per the standing rule in `MASTER_STATUS.md` §8 step 4b). The verbatim authorization above explicitly excludes "src/**" and "tests/**" changes.
- **Source / test changes.** No file under `src/**` or `tests/**` is modified or created by the authorizing PR for this artifact. The verbatim authorization above explicitly excludes "src/**" and "tests/**" changes.
- **New tags or releases.** No git tag, no GitHub release, no version bump is created by this authorization or its PR.
- **Mode A / Mode B substitution.** This authorization is governance prose, not a control-surface decision in the implementation sense; opening Phase 4 as a governance state does not substitute for adversarial review of any future Phase 4 implementation PR, of any first runtime activation of any merged Phase 3 fixture, or of any future runtime / broker / market-data / order / strategy / scheduler / persistence / deployment PR. Any such future PR must independently satisfy `AI_WORKFLOW.md` §4 (Mode A) and ADR-008 §D3 / §D5 (Mode B) — or ADR-009 D3 / D5 once in force per ADR-009 D7's bootstrap-avoidance clause — as applicable.

The always-forbidden categories in `MASTER_STATUS.md` §6 and `plan/phase3_entry_plan.md` §10 — strategy code, broker execution code, live or paper trading wired to a real broker, runtime daemons affecting accounts, real market data ingestion, order placement, and real secrets — remain forbidden in all modes. This authorization does not relax them and does not record any relaxation. The mirroring above is intentional and is **not** a re-authorization of any category: those categories remain forbidden by default, and naming them here records that Kevin did not silently relax them when authorizing the Phase 4 entry gate.

## Mode A Status For This Authorization

This authorizing PR is the **Phase 4 entry gate** — the decision to open Phase 4 as a governance state. Per `AI_WORKFLOW.md` §4(1), Mode A adversarial review by the Backup AI is **independently mandated** for every phase gate. The verbatim authorization above explicitly acknowledges this requirement: "I understand that Phase 4 entry requires the repository's required review and monitoring protocol, including Mode A where required and a sibling Mode B packet before merge." Mode A is therefore **required** for this PR.

The Mode A critique is delivered as PR-review text by the Backup AI and is **not** committed to the repository, per `AI_WORKFLOW.md` §6 rule 5 ("the Backup AI does not write to the repository"). Codex addresses any blocking findings on the implementation branch; Perplexity Computer re-verifies; Kevin decides. The Mode A status for this PR is therefore: **required and pending; PR may not merge until Mode A has been conducted and any blocking findings are addressed.**

Per `AI_WORKFLOW.md` §6 rule 1 ("One builder at a time"), Codex (the builder of this PR) cannot also perform the adversarial review of this PR; Mode A is owned by the Backup AI as a separate role. The Mode A requirement is not satisfied by any review the builder performs on their own PR; it requires the Backup AI's distinct review.

## Required Monitoring (Mode B)

Per ADR-008 §D3 / §D5, the authorizing PR is itself an active-workday event on 2026-05-14 (a pull request open / updated / merged on that UTC date). Per ADR-008 §D4 "same-day naming", multiple slugged packets already exist on `main` for 2026-05-14 (covering PR #48 / PR #50 / PR #52 / PR #54 / PR #56 / PR #58 / PR #60 / PR #62). The authorizing PR for P4-01 is therefore a further distinct active-workday event on the same UTC date and requires a **slugged additional packet** at `monitoring/daily/2026-05-14_pr<N>.md` where `<N>` is this PR number (or a similar audit-visible slug naming this PR / P4-01).

Per ADR-008 §D5 the slugged additional packet must be **committed and merged to `main` before** the authorizing PR for P4-01 merges. The packet is filed as a **separate sibling monitoring PR** per the established precedent (PR #57 → PR #56 for Phase 3 closure; PR #61 → PR #60 for Phase 4 entry planning); it is not bundled into the authorizing PR. Perplexity Computer confirms this sequencing before verifying the authorizing PR for Kevin's review.

The Mode B packet must reflect that this PR's content is restricted to opening Phase 4 as a governance state, that Phase 4 implementation is **not** authorized by this PR, that no runtime activation of any merged Phase 3 fixture is authorized by this PR, that the §8 step 4a allowlist is unchanged, that no `src/**` or `tests/**` file is modified, and that the always-forbidden categories in `MASTER_STATUS.md` §6 remain absent.

The verbatim authorization above explicitly acknowledges the sibling Mode B requirement: "a sibling Mode B packet before merge."

## Preserved Gates for Any Future Phase 4 Work

Any work beyond this Phase 4 entry gate — any Phase 4 implementation task (`P4-02`, `P4-03`, …), any runtime activation of any merged Phase 3 fixture, any extension of the §8 step 4a allowlist, or any runtime / broker / market-data / order / strategy / scheduler / persistence / deployment / env-var / secrets / network change — requires **all** of the following, none of which is satisfied by this Phase 4 entry gate:

1. **Separate written authorization from Kevin** per `MASTER_STATUS.md` §7 and `AI_WORKFLOW.md` §6 rule 3 / rule 7, with a sibling artifact under `governance/authorizations/`.
2. **Runtime-cadence ADR-009 in Accepted status** is already on `main` per `governance/authorizations/2026-05-13_p3-01-acceptance.md`; ADR-009 D7's bootstrap-avoidance clause keeps ADR-008 §D3 / §D5 in force until runtime exists on `main`. Any future Phase 4 PR that introduces runtime must satisfy the ADR-009 D3 / D5 cadence as it takes effect, plus any further ADR follow-up that surfaces at the time of authorization.
3. **Mode A adversarial review** per `AI_WORKFLOW.md` §4. Any first runtime activation of any merged Phase 3 fixture is a new control surface under §4(2). Any future Phase 4 implementation PR that introduces concrete behaviour behind a P2-05 control surface is a high-risk architecture decision under §4(2). Any future Phase 4 PR that touches the secrets-management discipline, the kill-switch policy, the reconciliation policy, the heartbeat policy, or the always-forbidden categories is a high-risk architecture decision under §4(2).
4. **Mode B monitoring packet** per ADR-008 §D3 / §D5 (or ADR-009 D3 / D5 once in force) on the active workday of the authorizing PR; before-merge sequencing per ADR-008 §D5 (or ADR-009 D5).
5. **§8 step 4a allowlist updates in the same PR.** Any new authorized directory must be added to `allowed_p2_infra` (and the matching comment block) in the same PR that introduces the directory, per the standing rule in `MASTER_STATUS.md` §8 step 4b.
6. **No relaxation of any always-forbidden category in `MASTER_STATUS.md` §6.** The Phase 4 entry gate does not relax any always-forbidden category; future authorizations may not silently relax any either.

## Exit Criteria

This Phase 4 entry gate PR may merge only after the full required check suite passes on the authorizing branch:

- `pre-commit run --all-files` (clean, or every failure documented as pre-existing and environment-only).
- `pytest` (clean, or every failure documented as pre-existing and environment-only).
- `MASTER_STATUS.md` §8 step 3 (baseline ancestry from `1f101fc`).
- `MASTER_STATUS.md` §8 step 4 (always-forbidden scan).
- `MASTER_STATUS.md` §8 step 4a (per-PR Phase 2 / Phase 3 infrastructure allowlist scan; allowlist is unchanged at exactly eight entries).
- `MASTER_STATUS.md` §8 step 4c (recursive forbidden-token scan; subshell exit `0`).
- **Mode A adversarial review** has been conducted by the Backup AI per `AI_WORKFLOW.md` §4(1) (phase gate). Any blocking findings are addressed by Codex on the implementation branch; Perplexity Computer re-verifies. The Mode A critique is delivered as PR-review text and is **not** committed to the repository.
- **Sibling Mode B monitoring packet** for the active workday on which this PR is open / merged has been authored by the Backup AI per ADR-008 §D1, committed by Codex per ADR-008 §D4, and merged to `main` **before** this PR merges per ADR-008 §D5.

After this authorization merges, the next allowed decisions remain those listed in `MASTER_STATUS.md` §9, augmented by the open Phase 4 governance state. Phase 4 implementation is **not** opened; Phase 3 remains formally closed; Phase 2 remains formally closed; no new Phase 2 task beyond P2-05 is opened; no new Phase 3 task beyond P3-05 is opened; no Phase 4 implementation task is opened; no runtime activation of any merged Phase 3 fixture is authorized. Any subsequent step — including any future Phase 4 implementation PR (`P4-02`, `P4-03`, …) — requires its own separate written authorization per the preserved gates above.

## Supporting Evidence (Non-Authoritative)

The following are pointers to the surrounding governance record. They are evidence, not the authorization itself; the verbatim authorization in the "Verbatim Written Authorization From Kevin" section above is the authorization of record, and this artifact (which embeds that verbatim text) is the durable in-tree record per `AI_WORKFLOW.md` §7.

- Phase 1 accepted baseline: `1f101fc` (`MASTER_STATUS.md` §3).
- Phase 2 closure authorization: `governance/authorizations/2026-05-12_phase-2-closure.md`.
- Phase 3 entry decision (pattern reference for this Phase 4 entry gate): `governance/authorizations/2026-05-13_p3-02.md`.
- Phase 3 closure authorization: `governance/authorizations/2026-05-14_phase-3-closure.md` — merged on `main` via PR #56 at `3131a69`, with sibling Mode B packet PR #57 merged first at `302dff6`.
- Phase 3 closure post-merge status reconciliation: PR #58 merged on `main` at `0a91261`, with sibling Mode B packet PR #59 merged first at `c910c9a`.
- Phase 4 entry planning authorization (parent governance workstream): `governance/authorizations/2026-05-14_phase-4-entry-planning.md` — merged on `main` via PR #60 at `e1dd6c0`, with sibling Mode B packet PR #61 merged first at `8e5b420`.
- Phase 4 entry plan (parent planning document; identifies P4-01 as the Phase 4 entry-gate candidate task): `plan/phase4_entry_plan.md` §4 item 1, §5, §6, §7, §9 — merged on `main` via PR #60 on 2026-05-14.
- Phase 4 entry planning post-merge status reconciliation: PR #62 merged on `main` at `4f03c57`, with sibling Mode B packet PR #63 merged first at `230124c`.
- ADR-009 (runtime monitoring cadence): `docs/decisions/ADR-009_runtime_monitoring_cadence.md` — Accepted on 2026-05-13 per `governance/authorizations/2026-05-13_p3-01-acceptance.md`; D7 bootstrap-avoidance clause keeps ADR-008 §D3 / §D5 in force until runtime exists on `main`.
- Monitoring cadence rule that governs the sibling Mode B packet for this PR: `docs/decisions/ADR-008_monitoring_cadence_and_ai_monitor_role.md` §D3 / §D5.
- Workflow separation of duties: `AI_WORKFLOW.md` §1 (roles, including §1.4 Backup AI), §2 (standard workflow), §3 (required proof), §4 (when to use the Backup AI), §6 (anti-chaos rules), §7 (durable authorization artifacts).
