# P5-07 Authorization — Operator-View Determinism / Idempotence Tripwire Tests

Date: 2026-05-18
Authorizer: Kevin
Scope: Bounded, tests-only P5-07 implementation packet for `gmc_rebuild` adding the **seventh Phase 5 implementation task** — a single new pytest-only test file at `tests/runtime/test_operator_view_determinism.py` that pins, in characterization-test form, the **byte-identical determinism / idempotence** of `gmc_rebuild.runtime.format_safety_verdict` across repeated calls for the same `SafetyVerdict` under the real composed P3-03 / P3-04 / P3-05 / P4-06 / P4-07 / P5-01 / P5-02 pipeline, and verifies that repeated rendering does **not** perturb any observable state in the P3 fakes, the safety surface (`SafetyVerdict`, `RuntimeShell`), or the simulation surface (`SimulationBoundary`, `SimulatedIntent`, `SimulatedOrderIntent`). This is **direction (b)** of the four planning-level candidate sketches recorded in `governance/authorizations/2026-05-18_p5-07-planning.md` (merged via PR #136 / reconciled via PR #138); the other three candidate directions — (c) closed-enum / closed-field defense-in-depth; (d) ADR-005 / ADR-002 / ADR-003 composed-policy alignment; (e) simulation surface inertness self-check — remain planning-level candidates only and are **not** authorized by this packet. P5-07 opens Phase 5 work only at the tripwire-test scope authorized here; it does **not** open, enumerate, or pre-commit any successor P5 packet (P5-08 and beyond). No `src/**` change. No new public symbol, re-export, field, method, `SimulationLane` member, `SimulatedOrderSide` member, or `SimulatedOrderType` member is introduced. The merged P4-06 / P4-07 / P4-08 safety foundation, the merged P3-03 / P3-04 / P3-05 in-memory fakes, the merged P2-01..P2-05 packages, the merged OPS-01 / OPS-02 / OPS-03 / OPS-04 / OPS-04B / OPS-06 operations records, the merged P5-01 / P5-02 simulation surface, the merged P5-03 invariants tests, the merged P5-04 composed safety-foundation × simulation integration tripwire tests, the merged P5-05 operator-view × composed safety-foundation tripwire tests, the merged P5-06 `propose` ↔ `propose_order` symmetry tripwire tests, the canonical status reconciliation merged via PR #114, the Phase 5 entry planning reconciliation merged via PR #118, the P5-05 implementation post-merge reconciliation merged via PR #122, the P5-06 planning / enumeration packet merged via PR #124 / reconciled via PR #126, the P5-06 implementation packet merged via PR #129 / reconciled via PR #130, the GOV-02 execution-environment workflow correction merged via PR #132 / reconciled via PR #134, the P5-07 planning / enumeration packet merged via PR #136 / reconciled via PR #138, and the merged GOV-01 governance reconciliation are all preserved exactly. Adds the new tripwire test file, this governance authorization artifact, and conservative governance/status reflections in `MASTER_STATUS.md` and `plan/phase5_entry_plan.md`. Makes no other changes.

## Authorization

Kevin authorized this packet on 2026-05-18. The verbatim written authorization follows:

> Kevin authorizes P5-07 implementation as the seventh Phase 5 implementation task, choosing direction (b) from the merged P5-07 planning packet: a bounded tests-only operator-view determinism / idempotence tripwire. Scope: add exactly one new test file under the existing tests tree, exercising already-merged public surfaces only, to assert byte-identical format_safety_verdict output across repeated calls for the same SafetyVerdict under the real composed P3 / P4-06 / P4-07 / P5-01 / P5-02 pipeline and to verify repeated rendering does not perturb observable state in the P3 fakes, the safety surface, or the simulation surface. No src/** changes, no existing test modification, no production behavior, no runtime activation, no broker/paper/live/market-data/order-routing/strategy/scheduler/daemon/persistence/env-var/secrets/network change, no SimulationLane / SimulatedOrderSide / SimulatedOrderType expansion, no SimulatedIntent or SimulatedOrderIntent field addition, no SimulationBoundary method addition, no placeholder/order record class, no allowlist extension, no ops work, no tag/release.
>
> Implement using the GOV-02 workflow: local Claude Code / Claude Max does repo edits. Create the governance authorization artifact under governance/authorizations/, update MASTER_STATUS.md and any required Phase 5 planning/status docs consistently, create the implementation PR, then create the required Mode B sibling monitoring PR from main in the same pass. Run validation:
> PYTHONPATH=src .venv/bin/python -m pytest -q
> git diff --name-status main
> grep -nE "pending merge" MASTER_STATUS.md README.md RECOVERY.md plan/phase4_entry_plan.md plan/phase5_entry_plan.md || true
> pre-commit on changed files if configured.
>
> Do not merge anything. Report PR numbers, branch names, head SHAs, file list, and validation output. Mode B sibling must merge first per ADR-008 §D5.
>
> Authorized by Kevin on 2026-05-18.

This authorization is the authorization of record per `AI_WORKFLOW.md` §7. The packet is built per the GOV-02 execution-environment workflow rule (local Claude Code / Claude Max acting as Codex by default for `gmc-rebuild` repo edits; merged via PR #132 / reconciled via PR #134) and preserves the canonical reconciliations through PR #114, PR #118, PR #122, PR #126, PR #130, PR #134, and PR #138 verbatim.

## Allowed Scope

The implementation PR may include exactly:

- A new test file at `tests/runtime/test_operator_view_determinism.py` containing focused pytest functions that exercise the byte-identical determinism / idempotence of `gmc_rebuild.runtime.format_safety_verdict` under the real composed P4-06 `RuntimeShell` driven by the real merged P3-03 / P3-04 / P3-05 in-memory fakes (`InMemoryHeartbeat`, `InMemoryKillSwitch`, `InMemoryReconciliation`) and observed from the merged P5-01 / P5-02 `SimulationBoundary` (`propose`, `propose_order`) perspective. The file:
  - Imports only `__future__`, `ast`, `dataclasses`, `datetime`, `pathlib` (stdlib), and the already-merged in-repo modules `gmc_rebuild.heartbeat` (for `InMemoryHeartbeat`), `gmc_rebuild.kill_switch` (for `InMemoryKillSwitch`), `gmc_rebuild.reconciliation` (for `InMemoryReconciliation`), `gmc_rebuild.risk` (for `HeartbeatStatus`, `KillSwitchState`, `ReconciliationStatus`), `gmc_rebuild.runtime` (for the five `BLOCKER_*` constants, `OperatorSafetyView`, `RuntimeShell`, `SafetyVerdict`, `VERDICT_BLOCKED`, `VERDICT_CLEAR`, `format_safety_verdict`), and `gmc_rebuild.simulation` (for the seven public names already re-exported by P5-02). No other import is added; in particular no `os`, `socket`, `requests`, `urllib`, `http`, `threading`, `asyncio`, `subprocess`, `sqlite3`, `pickle`, `shelve`, `ssl`, `smtplib`, `ftplib`. No `pytest.raises`. No fixture-typed parameters. No third-party dependency.
  - Avoids `pytest.raises` and fixture-typed parameters so the mypy-strict pre-commit hook does not need a pytest-stub dependency, matching the pattern established by `tests/runtime/test_operator_view.py`, `tests/runtime/test_runtime_shell.py`, `tests/runtime/test_operator_view_composed_safety_foundation.py`, `tests/simulation/test_simulation_boundary.py`, `tests/simulation/test_simulated_order_intent.py`, `tests/simulation/test_simulated_order_intent_invariants.py`, `tests/simulation/test_composed_safety_foundation.py`, and `tests/simulation/test_propose_symmetry.py`.
  - Implements small local helpers private to the new module; no helper is exported, and no existing test file is modified.
  - Asserts the following invariant categories: (1) **clear-verdict byte-identical determinism** — calling `format_safety_verdict` N≥10 times for the same clear `SafetyVerdict` returns `OperatorSafetyView` instances that compare `==` and whose `.render()` strings compare equal **byte-for-byte** (including a SHA-style identity check across the rendered text); (2) **blocked-verdict byte-identical determinism** — same invariant for a blocked `SafetyVerdict` produced from the real composed pipeline with each of the five `BLOCKER_*` codes driven from the corresponding P3 fake state, asserting byte-identical `.render()` output across repeated calls for each code; (3) **multi-blocker byte-identical determinism** — same invariant for a multi-blocker `SafetyVerdict` (heartbeat STALE + kill switch TRIPPED + reconciliation FAILED simultaneously), asserting byte-identical `.render()` output across repeated calls; (4) **field-level idempotence** — every public field of the returned `OperatorSafetyView` (`status`, `blocker_lines`, `heartbeat_lines`, `kill_switch_line`, `reconciliation_line`, `observed_at`) is identical across repeated calls for the same verdict; (5) **distinct-but-equal verdict idempotence** — `dataclasses.replace(verdict)` produces a non-identical but structurally-equal `SafetyVerdict` whose `format_safety_verdict` output is byte-identical to the original's; (6) **P3-fakes non-perturbation under repeated rendering** — repeated calls to `format_safety_verdict` for the same verdict do not observably perturb the `InMemoryHeartbeat.status(...)` reading for required components, the `InMemoryKillSwitch.current()` decision, or the staged `InMemoryReconciliation` outcome queue; before-vs-after snapshots compare equal; (7) **safety surface non-perturbation under repeated rendering** — repeated rendering does not mutate the `SafetyVerdict` (every public field unchanged), does not mutate the `RuntimeShell` (a subsequent `shell.evaluate()` call still returns a verdict equal to the original), and does not change the boundary's lane (`SimulationBoundary.lane` unchanged); (8) **simulation surface non-perturbation under repeated rendering** — repeated rendering does not mutate the supplied `SimulatedIntent` or `SimulatedOrderIntent` (every field unchanged), and after N repeated rendering calls both `SimulationBoundary.propose(intent=..., verdict=verdict)` and `SimulationBoundary.propose_order(order_intent=..., verdict=verdict)` continue to return the supplied intent **by identity** on the clear path and continue to raise `SimulationBoundaryError` on each blocked path; (9) **render-then-propose / propose-then-render order independence** — rendering before proposing and proposing before rendering produce the same byte-identical view text and the same boundary behavior; (10) **interleaved render / propose idempotence** — interleaving N rendering calls with M propose / propose_order calls does not change the rendered text, does not change the boundary's identity-return on the clear path, and does not change the `SimulationBoundaryError` message on the blocked path; (11) **type-validation determinism** — `format_safety_verdict(non_verdict)` raises `TypeError` deterministically across repeated calls; (12) an **inertness self-check** using `ast.walk` to confirm the new module's actual imports are disjoint from `{os, socket, requests, urllib, http, threading, asyncio, subprocess, sqlite3, pickle, shelve, ssl, smtplib, ftplib}` and that every imported module starts with one of the authorized prefixes.
- A conservative `MASTER_STATUS.md` §1 paragraph titled "P5-07 implementation status (governance/documentation reflection, 2026-05-18 UTC — pending merge in this open P5-07 implementation PR)" describing the new tripwire file and explicitly listing the categories the packet does **not** open. The conservative wording is scoped to this open PR only; the six merged P5-01 / P5-02 / P5-03 / P5-04 / P5-05 / P5-06 status reflection paragraphs continue to read **merged on `main`** and are not reverted to stale `**pending merge**` language by this packet; the Phase 5 entry planning paragraph from PR #118 is preserved unchanged; the P5-06 planning / enumeration paragraph from PR #126 is preserved unchanged; the P5-06 implementation paragraph from PR #130 is preserved unchanged; the GOV-02 paragraph from PR #134 is preserved unchanged; the P5-07 planning / enumeration paragraph from PR #138 is preserved unchanged.
- A conservative `plan/phase5_entry_plan.md` update reflecting the P5-07 authorization at the planning level, scoped only to the open P5-07 implementation PR. The plan continues to record P5-01 / P5-02 / P5-03 / P5-04 / P5-05 / P5-06 as merged on `main`, the Phase 5 entry planning packet as merged via PR #116 / reconciled via PR #118, the P5-06 planning / enumeration packet as merged via PR #124 / reconciled via PR #126, the P5-06 implementation as merged via PR #129 / reconciled via PR #130, the GOV-02 packet as merged via PR #132 / reconciled via PR #134, and the P5-07 planning / enumeration packet as merged via PR #136 / reconciled via PR #138.
- This authorization artifact at `governance/authorizations/2026-05-18_p5-07.md`.

No file under `src/**` is added or modified. No new test directory is added; the new test file lives in the existing `tests/runtime/` directory. No existing test file under `tests/**` is modified. No file under `monitoring/**`, `docs/decisions/`, or `governance/authorizations/` other than this new P5-07 artifact is added or modified. The merged P4-06 `RuntimeShell` / `SafetyVerdict`, P4-07 `OperatorSafetyView` / `format_safety_verdict`, P4-08 safety-policy-hardening tests, P3-03 / P3-04 / P3-05 in-memory fakes, P2-01..P2-05 packages, OPS-01 / OPS-02 / OPS-03 / OPS-04 / OPS-04B operations records, OPS-06 backup monitoring plan, P5-01 inert local simulation boundary skeleton, P5-02 simulated order intent model, P5-03 simulated-order-intent invariants tripwire tests, P5-04 composed safety-foundation × simulation integration tripwire tests, P5-05 operator-view × composed safety-foundation tripwire tests, P5-06 `propose` ↔ `propose_order` symmetry tripwire tests, the canonical status reconciliation merged via PR #114, the Phase 5 entry planning reconciliation merged via PR #118, the P5-05 implementation post-merge reconciliation merged via PR #122, the P5-06 planning / enumeration packet merged via PR #124, the P5-06 planning post-merge reconciliation merged via PR #126, the P5-06 implementation packet merged via PR #129, the P5-06 implementation post-merge reconciliation merged via PR #130, the GOV-02 packet merged via PR #132, the GOV-02 post-merge reconciliation merged via PR #134, the P5-07 planning / enumeration packet merged via PR #136, the P5-07 planning post-merge reconciliation merged via PR #138, and the GOV-01 governance reconciliation / risk register are all preserved exactly. The `MASTER_STATUS.md` §8 step 4a `allowed_p2_infra` bash gate (synced to ten entries by GOV-01) is preserved exactly; no new `src/gmc_rebuild/` subpackage directory is created.

## Explicitly Not Authorized

This authorization does not permit:

- Any `src/**` change. The packet adds no module under `src/gmc_rebuild/runtime/`, `src/gmc_rebuild/simulation/`, or anywhere else under `src/**`; no existing `src/**` file is modified.
- Any new public symbol on `gmc_rebuild.runtime`, `gmc_rebuild.simulation`, or any other subpackage; no new re-export from `gmc_rebuild` package root.
- Any modification of any existing test file under `tests/**` (including `tests/runtime/test_operator_view.py`, `tests/runtime/test_runtime_shell.py`, `tests/runtime/test_safety_policy_hardening.py`, `tests/runtime/test_operator_view_composed_safety_foundation.py`, `tests/simulation/test_simulation_boundary.py`, `tests/simulation/test_simulated_order_intent.py`, `tests/simulation/test_simulated_order_intent_invariants.py`, `tests/simulation/test_composed_safety_foundation.py`, and `tests/simulation/test_propose_symmetry.py`).
- Any new field on `SimulatedIntent` or `SimulatedOrderIntent`. The eight-field structural tripwires from P5-02 / P5-03 / P5-04 continue to be enforced at the test level and are not weakened.
- Any new method on `SimulationBoundary`. The existing `propose` (P5-01) and `propose_order` (P5-02) are exercised unchanged.
- Any new placeholder or order record class. The existing `SimulatedIntent` (P5-01) and `SimulatedOrderIntent` (P5-02) are exercised unchanged.
- Any addition of a `SimulationLane` member beyond `LOCAL_ONLY`. The closed-enum tripwire from P5-01 is preserved.
- Any addition of a third `SimulatedOrderSide` member (no `SELL_SHORT`, `BUY_TO_COVER`, etc.). The closed-enum tripwire from P5-02 is preserved.
- Any addition of a third `SimulatedOrderType` member (no `STOP`, `STOP_LIMIT`, `MARKET_ON_CLOSE`, `LIMIT_ON_OPEN`, `TRAILING_STOP`, etc.). The closed-enum tripwire from P5-02 is preserved.
- Any new runtime behavior. The packet adds tests only; it does not introduce any runtime behavior, any concrete `HeartbeatProtocol` / `KillSwitchProtocol` / `ReconciliationProtocol` implementation outside the merged P3-03 / P3-04 / P3-05 in-memory fakes, any concrete safety-control implementation outside the merged P4-06 `RuntimeShell`, or any new `format_safety_verdict` / `OperatorSafetyView` capability outside the merged P4-07 surface.
- Any runtime activation of any merged Phase 3 fixture or P4-06 / P4-07 / P4-08 safety surface or P5-01 / P5-02 simulation surface. Construction and exercise of the merged surfaces within pytest is **not** a runtime activation; the new test module does not run as a daemon, scheduler, background thread, or `__main__` entry point.
- Any broker integration (real or paper), broker SDK, broker account identifier, broker session, broker handshake, or broker credential of any kind.
- Any live, delayed, or paper market data, market-data feed, vendor SDK, real symbol universe, real venue identifier, or normalization to any vendor symbol format. The new test file uses inert placeholder symbols only; it does not import any market-data fixture, vendor SDK, or symbol database.
- Any real or paper order placement, order routing, order management, order book, fill, execution, position, P&L, trade report, OMS, EMS, or routing instruction. The tripwire tests assert determinism of `format_safety_verdict` and non-perturbation of the P3 / safety / simulation surfaces; no side effect is exercised or authorized.
- Any execution adapter, broker adapter, paper-broker adapter, mock-broker adapter, exchange adapter, FIX session, REST client, WebSocket client, or any other connectivity surface.
- Any venue, account, broker credential, or routing field on `SimulatedOrderIntent` (the structural-field tripwire is preserved).
- Any secrets, credentials, API keys, broker keys, session tokens, OAuth tokens, SSH keys, `.env` files containing live credentials, recovery codes, PEKs, or any other credential storage.
- Any external API call, network call, `socket`, `urllib`, `requests`, `http`, `ssl`, `smtplib`, `ftplib`, DNS lookup, or any other outbound or inbound network surface.
- Any scheduler, daemon, background thread, long-running service, `__main__` entry point, `if __name__` block, `time.sleep`, `asyncio.sleep`, cron job, launchd plist, or any other timed / background execution mechanism.
- Any persistence, database, SQLite, pickle, shelve, filesystem write, caching to disk, append-only ledger, log file, audit trail, or any other persistence surface.
- Any strategy, scanner, model, portfolio, backtest, live trading, paper trading wired to a real broker, or production execution implementation.
- Any modification of the merged P4-06 `RuntimeShell` / `SafetyVerdict`, P4-07 `OperatorSafetyView` / `format_safety_verdict`, or P4-08 safety-policy-hardening tests. The safety foundation closure recorded by P4-08 is preserved unchanged; P5-07 reuses the existing `SafetyVerdict.clear` invariant, the existing P4-06 `RuntimeShell`, and the existing P4-07 read-only operator view via the real composed pipeline without weakening, replacing, or bypassing them.
- Any modification of the merged P3-03 / P3-04 / P3-05 in-memory fakes. The packet imports and exercises the fakes via their already-public read methods (`InMemoryHeartbeat.status`, `InMemoryKillSwitch.current`, `InMemoryReconciliation.reconcile`) and the documented `advance` / `beat` / `trip` / `enqueue` / `set_next` test helpers; it does not modify the fakes' source.
- Any modification of the merged P5-01 or P5-02 simulation source, the merged P5-03 invariants tests, the merged P5-04 composed safety-foundation × simulation integration tripwire tests, the merged P5-05 operator-view × composed safety-foundation tripwire tests, or the merged P5-06 `propose` / `propose_order` symmetry tripwire tests. No existing `src/gmc_rebuild/simulation/` file is modified; no existing test under `tests/simulation/` or `tests/runtime/` is modified.
- Any concrete implementation of `HeartbeatProtocol`, `KillSwitchProtocol`, or `ReconciliationProtocol` outside the merged P3-03 / P3-04 / P3-05 in-memory fakes.
- Any modification of backup / recovery docs (`RECOVERY.md`, `governance/authorizations/2026-05-16_ops-*.md`, `governance/authorizations/2026-05-17_ops-04b.md`, `governance/authorizations/2026-05-17_ops-06.md`, `governance/authorizations/2026-05-17_gov-01.md`). The OPS-01 / OPS-02 / OPS-03 / OPS-04 / OPS-04B / OPS-06 / GOV-01 records are preserved unchanged.
- Any modification of `AI_WORKFLOW.md`, any ADR under `docs/decisions/`, any other `governance/authorizations/*.md` file, `pyproject.toml`, `.pre-commit-config.yaml`, `.secrets.baseline`, `.gitignore`, `README.md`, or `EXTERNAL_REVIEW_BRIEF.md`.
- Any modification of any existing `monitoring/daily/*` packet.
- Any extension of the `MASTER_STATUS.md` §8 step 4a `allowed_p2_infra` bash gate or §8 step 4c forbidden-token bash gate. No new `src/gmc_rebuild/` subpackage directory is added; the bash gate is preserved exactly at the ten entries synced by GOV-01.
- Any extension of the hard-coded `entries == {...}` set in `tests/test_package_skeleton.py`. The set is unchanged by this packet because no new subpackage directory is added.
- Any quality-gate relaxation, hook removal, hook reconfiguration, mypy strictness reduction, ruff rule relaxation, or `detect-secrets` baseline change.
- Any X10 Layer 5 promotion, backup-monitoring automation, DR drill execution, or other ops execution work.
- Any tag or release.
- Any successor P5 packet. P5-08 (and any later P5-0N task) remains **future / not authorized**. This authorization does **not** open P5-08 or any later packet; each requires its own separate written authorization from Kevin per `AI_WORKFLOW.md` §7.
- Any reintroduction of stale `**pending merge**` language for any already-merged P5-01 / P5-02 / P5-03 / P5-04 / P5-05 / P5-06 / GOV-01 / GOV-02 / OPS-06 / Phase 5 entry planning / P5-06 planning / P5-07 planning packet. The canonical reconciliations merged via PR #114, PR #118, PR #122, PR #126, PR #130, PR #134, and PR #138 are preserved by this packet; any "pending merge" wording in the PR is scoped exclusively to this open P5-07 implementation PR itself.
- Authorization of the three non-chosen planning-level candidate directions from `governance/authorizations/2026-05-18_p5-07-planning.md`. Direction (c) (closed-enum / closed-field defense-in-depth tripwire), direction (d) (ADR-005 / ADR-002 / ADR-003 composed-policy alignment tripwire), and direction (e) (simulation surface inertness self-check tripwire) remain planning-level candidates only; if Kevin chooses to authorize any of them later, that authorization is a separate written packet under `governance/authorizations/` distinct from this one.
- Any reinterpretation or relaxation of the GOV-02 execution-environment workflow rule (merged via PR #132 / reconciled via PR #134). Local Claude Code / Claude Max remains the default execution environment for `gmc-rebuild` repo edits; Perplexity-managed codebase subagents that consume Perplexity credits must not perform repo edits absent Kevin's explicit written authorization for the specific packet; Perplexity Computer's `AI_WORKFLOW.md` §1.2 Supervisor / Verifier / Status Keeper role and read-only `gh` / `git` metadata-check role are preserved unchanged.

The always-forbidden categories in `MASTER_STATUS.md` §6, `plan/phase4_entry_plan.md` §10, and `plan/phase5_entry_plan.md` §10 — strategy code, broker execution code, paper-trading or live-trading wired to a real broker, runtime daemons affecting accounts, real market data ingestion, order placement, and real secrets — remain forbidden in all modes. This packet does not relax them and does not record any relaxation.

## Required Validation

Before merge, the implementation PR must pass:

- `git switch main && git pull --ff-only` before branching (to confirm the work is built on current `main` at `9ce4887`).
- `PYTHONPATH=src .venv/bin/python -m pytest -q` against the existing test suite (must continue to pass at the post-P5-07-planning baseline of 393 tests plus the new P5-07 operator-view-determinism tripwire tests).
- `git diff --name-status main` showing changes confined to this authorization artifact, the single new test file, the conservative `plan/phase5_entry_plan.md` update, and the conservative status reflection in `MASTER_STATUS.md`. No file under `src/**` is added or modified; no existing file under `tests/**` is added or modified.
- Stale-phrase grep (`grep -nE "pending merge" MASTER_STATUS.md README.md RECOVERY.md plan/phase4_entry_plan.md plan/phase5_entry_plan.md`) showing that remaining `pending merge` hits are intentional historical / self-check / anti-pattern residuals or scoped to the open P5-07 implementation PR itself only, with no current-state stale claim on any merged work.

Mode A / Mode B review requirements remain governed by `AI_WORKFLOW.md`, ADR-008, ADR-009, and the current `MASTER_STATUS.md`. P5-07 implementation is a composed safety/simulation tripwire packet that touches execution-adjacent semantics (operator-view determinism over the P4-06 / P4-07 / P4-08 / P5-01 / P5-02 boundary); Mode A adversarial review is therefore **independently required** per `AI_WORKFLOW.md` §4 and ADR-008 §D7, mirroring the P5-05 / P5-06 precedents, and (per `AI_WORKFLOW.md` §6 rule 5 and the explicit P5-01 / P5-02 / P5-03 / P5-04 / P5-05 / P5-06 precedent) must be delivered as PR-review text on the P5-07 implementation PR only, not committed to the repository. Mode B sibling monitoring follows ADR-008 §D3 / §D5 (and ADR-009 D3 / D5 once in force per ADR-009 D7's bootstrap-avoidance clause) and must be authored from `main` and merged to `main` **before** this implementation PR per the verbatim authorization above.

Phase 2 remains formally closed. Phase 3 remains formally closed. Phase 4 remains open as a governance state. P5 is open at the P5-01 inert local simulation boundary skeleton scope, the P5-02 simulated order intent model scope, the P5-03 simulated-order-intent invariants tripwire scope, the P5-04 composed safety-foundation × simulation integration tripwire scope, the P5-05 operator-view × composed safety-foundation tripwire scope, the P5-06 planning / enumeration scope, the P5-06 implementation scope, the P5-07 planning / enumeration scope, and this P5-07 implementation scope. The safety foundation closure recorded by P4-08 is preserved. The GOV-02 execution-environment workflow rule is in force. P5-08 remains **future / not authorized**.
