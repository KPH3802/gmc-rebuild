# P4-06 Implementation Authorization — Local Runtime Shell Boundary

Date: 2026-05-16
Authorizer: Kevin Heaney
Scope: Phase 4 / P4-06 implementation — add an inert local runtime shell boundary under `src/gmc_rebuild/runtime/` with matching tests under `tests/runtime/`.

## Authorization

Kevin authorized this packet on 2026-05-16 (Saturday, May 16, 2026 10:06 AM CDT). The verbatim written authorization follows:

> P4-06 Authorization — local runtime shell boundary — 2026-05-16
>
> I, Kevin Heaney, authorize one bounded P4-06 implementation packet for the gmc-rebuild repository to add an inert local runtime shell boundary.
>
> Authorized scope:
> - Add the minimum source path(s) needed for an inert runtime shell boundary, expected to be under src/gmc_rebuild/runtime/.
> - Update the repository phase-boundary allowlist only as narrowly required for that exact runtime-shell path.
> - Add focused tests proving the runtime shell boundary is local, inert, and safety-gated.
> - Add the required governance authorization artifact and conservative status updates in MASTER_STATUS.md and plan/phase4_entry_plan.md.
>
> Safety constraints:
> - No broker integration.
> - No market data integration.
> - No order submission.
> - No strategy, scanner, model, portfolio, backtest, live trading, or paper-trading implementation.
> - No secrets or environment-variable credential loading.
> - No network calls.
> - No scheduler or daemon.
> - No persistence or database.
> - No time.sleep.
> - No runtime activation beyond inert local boundary construction and tests.
>
> Validation required before PR:
> - Targeted tests for the new runtime shell boundary.
> - python3 -m pre_commit run --all-files.
>
> This authorization is only for the bounded P4-06 packet above and does not authorize any successor packet.

This authorization is the authorization of record per `AI_WORKFLOW.md` §7.

## Allowed Scope

The implementation PR may include exactly:

- A new source directory `src/gmc_rebuild/runtime/` containing only the inert local runtime shell boundary as a pure-Python module (no `__main__`, no daemon, no scheduler, no network, no persistence, no broker, no market data, no order placement, no strategy/scanner/model/portfolio/backtest/live/paper trading, no env-var or secrets loading, no `time.sleep`).
- A new test directory `tests/runtime/` with focused tests proving the boundary is local, inert, and safety-gated.
- A narrow update to the `tests/test_package_skeleton.py` allowlist enumeration so the new `runtime/` directory is recognized as the authorized P4-06 entry. No other behavioral test modifications.
- This authorization artifact at `governance/authorizations/2026-05-16_p4-06.md`.
- Conservative status reflections in `MASTER_STATUS.md` and `plan/phase4_entry_plan.md` recording P4-06 as a narrow inert local runtime shell boundary task and extending the §8 step 4a allowlist by exactly one entry: `src/gmc_rebuild/runtime/`.

The new runtime module may:

- Accept already-constructed `HeartbeatProtocol`, `KillSwitchProtocol`, and `ReconciliationProtocol` instances via dependency injection at construction.
- Expose one read-only "would safety controls block progression" method that returns an immutable verdict dataclass.
- Reuse `gmc_rebuild.time` UTC helpers and the existing risk-control record types.

## Explicitly Not Authorized

This authorization does not permit:

- Any `__main__`, daemon, scheduler, background thread, long-running service, or runtime activation beyond inert in-memory boundary construction in tests.
- Any broker integration, broker SDK, order objects, order placement, fills, trade reports, positions, executions, or order book.
- Any market-data ingestion, quotes, bars, or symbol universe.
- Any persistent storage, SQLite, snapshots, or file I/O beyond ordinary Python module import.
- Any network call.
- Any env-var or secrets reading inside `src/gmc_rebuild/runtime/`.
- Any `time.sleep` or `asyncio.sleep`.
- Any concrete implementation of `HeartbeatProtocol`, `KillSwitchProtocol`, or `ReconciliationProtocol` inside `src/gmc_rebuild/runtime/`. The module composes the abstract protocol boundaries only.
- Any modification of existing `src/**` files outside the new `src/gmc_rebuild/runtime/` directory.
- Any allowlist expansion beyond the single `src/gmc_rebuild/runtime/` entry.
- Any quality-gate relaxation, tag, or release.
- Any successor packet (P4-07, …). P4-07 and later remain future / not authorized.

Phase 2 remains formally closed. Phase 3 remains formally closed. Phase 4 remains open as a governance state. P4-06 is a bounded inert-shell implementation slice and does not cross into broker, market-data, order, strategy, scheduler, persistence, or live-trading work.

## Required Validation

Before merge, the implementation PR must pass:

- `python3 -m pre_commit run --all-files`
- Targeted pytest for `tests/runtime/`
- Diff-scope verification showing changes confined to the new `src/gmc_rebuild/runtime/` directory, the new `tests/runtime/` directory, the narrow allowlist update in `tests/test_package_skeleton.py`, this authorization artifact, and the conservative status reflections in `MASTER_STATUS.md` and `plan/phase4_entry_plan.md`.

Mode A / Mode B review requirements remain governed by `AI_WORKFLOW.md`, ADR-008, ADR-009, and the current `MASTER_STATUS.md`.
